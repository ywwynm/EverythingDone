<?xml version="1.0" encoding="utf-8"?>
<!-- A FAB + an optional overlay ImageView so the picker can render the gradient
     random-colour FAB. Material FloatingActionButton rejects setBackground()
     wholesale (no-op + log warning), so for gradient mode we make the FAB itself
     transparent and let the overlay carry the GradientDrawable. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_pick_color"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="center"
        app:fabSize="mini"
        app:borderWidth="0dp"
        app:elevation="0dp"
        app:rippleColor="@color/white_54p"
        app:pressedTranslationZ="0dp"/>

    <ImageView
        android:id="@+id/iv_pick_color_overlay"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        android:clickable="false"
        android:focusable="false"
        android:duplicateParentState="true"
        android:scaleType="center"
        android:visibility="gone"
        tools:ignore="ContentDescription"
        xmlns:tools="http://schemas.android.com/tools"/>

</FrameLayout>