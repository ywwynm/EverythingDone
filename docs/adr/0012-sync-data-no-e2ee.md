# 同步数据不做端到端加密

云同步的数据保护采用"传输 TLS + 服务端静态加密（数据库与 OSS）"，服务端可读用户内容；不做端到端加密。理由：E2EE 会把冲突处理、去重、数据修复全部推到客户端，忘记密码等于数据永久丢失（否则需要恢复码体系），并使将来的 Web 端与客服排障大幅复杂化；而本应用的"私密记事"在本地本就是展示层保护（标题前缀 + 解锁查看）而非内容加密，用户语义中"私密"不承诺密码学保证。主流同类（Evernote、Notion、印象笔记）同为此模型。隐私政策必须明示服务端可读。若将来确有需求，可作为独立的"加密笔记本"能力叠加，而不是改造全量同步协议。
