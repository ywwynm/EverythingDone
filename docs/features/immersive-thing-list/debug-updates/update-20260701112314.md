# 沉浸式记事列表：返回上一级保留沉浸态

## 主要改动
- 进入文件夹再返回上一级（或点面包屑跳回祖先）时，若之前在该层处于沉浸态，返回后仍保持沉浸——沉浸位移现在和滚动位置一起、按投影记忆并一并恢复。仅当恢复到的滚动位置确实已完全折叠时才恢复沉浸，否则维持显示；回顶部/无记忆的情况仍是显示。

## 验证重点
1. 在某层（全部记事或某文件夹）下滑进入沉浸态，打开一个子文件夹，再返回上一级：应恢复到之前的滚动位置且仍处于沉浸态（顶栏收起）。
2. 在某层顶部（非沉浸）打开子文件夹再返回：返回后顶栏正常显示、在顶部。
3. 面包屑跳回祖先层：同样按该层之前的滚动位置与沉浸态恢复。
