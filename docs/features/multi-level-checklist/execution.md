# Multi-level Checklist Items Execution

实现阶段的勾选清单，随实现进度填写。设计权威见 `plan.md` / `decisions.md` / `docs/adr/0010`。
当前状态：已实现并多轮发布到阿里云 debug 渠道，经多轮真机视觉微调与逻辑修复（禁跳空、完成态严格
不变量、拖拽脱手修复）。拖拽的“收束动画 + 角标”按用户决定**不做**（从简）；拖根节点脱手已修复
（整块移动改为只用 `notifyItemMoved`）。

## Implementation Checklist

### 存储与迁移
- [ ] 清单串解析/序列化纳入层级位（`item[1]`，文本 `substring(2)`；控制标记 `2/3/4` 仍单字符）。
- [ ] 新增 DB 版本与迁移：扫描 `isCheckListStr` 的 Thing，给每个真实项插入层级位 `1`（含单测）。
- [ ] `toContentStr` 等导出/解析路径跳过层级位。
- [ ] 排查并修正所有把 `substring(1)` 当文本起点、把 `item[0]` 之后当文本的旧调用。

### 完成状态机（组感知）
- [ ] 重写 `CheckListHelper.toggleChecklistItem`：级联子树 + 按组根整组迁移 + 取消回流 + 强制组根取消
      不横扫兄弟 + 孤儿（理论上已被门控禁止）兜底。
- [ ] 顶/底边界判定改用 `3`/`4` 分隔位，替换所有“第一个 `1`”式逻辑（含 `getFirstFinishedItemIndex`
      相关调用排查）。
- [ ] 详情页适配器完成切换改为复用上面的共享核心，仅保留 UI 事务。

### 详情页编辑交互
- [ ] 右侧删除图标改缩进按钮；左侧新增反缩进槽位与按钮。
- [ ] 缩进门控（上方存在更浅项才启用）、按边界隐藏（一级无反缩进、三级无缩进）、聚焦门控。
- [ ] 逐级左移约 44dp 的缩进几何，子项状态图标对齐父项文本；反缩进按钮置于缩进空档。
- [ ] 新建项层级：回车继承当前行层级、底部添加行给一级、有子项回车按 `pos+1` 插入。
- [ ] 退格成为唯一删除入口；删带子项的项只删单项、子项重算归属。
- [ ] 缩进/完成/拖拽的撤销改为内容串 before/after。
- [ ] 缩进/反缩进在顶/底任意区域均可用。

### 分级排版
- [ ] 三个独立比例常量（字号、未完成透明度、已完成透明度，起步 0.9），应用于详情/卡片/widget。

### 显示界面
- [ ] 卡片 TextView：缩进 + 分级字号/透明度。
- [ ] RemoteViews（单一/列表 widget）：`setViewPadding` 缩进 + `setTextViewTextSize` + 按级颜色。
- [ ] 首页/widget 点击完成走共享状态机（含整组迁移）。
- [ ] 截断维持按项数 + `...`；SIMPLE_FCLI 只折叠分隔线以下的底部已完成区。
- [ ] DoingActivity、大文件夹缩略图、通知的多级显示。

### 单一文本输出
- [ ] `toContentStr` 带标记输出改标准 Markdown 任务列表（`- [ ] / - [x]` + 每级 2 空格）；空前缀用法保持拍平。

### 拖拽
- [x] 子树块移动 + 兄弟带约束（不改 owner/层级、不跨完成边界）。
- [x] 整块移动只用 `notifyItemMoved`，修复拖根节点脱手（不再用 `notifyDataSetChanged`）。
- [~] 收束动画 + 角标：按用户决定**不做**（从简，子项拖拽全程保持可见）。
