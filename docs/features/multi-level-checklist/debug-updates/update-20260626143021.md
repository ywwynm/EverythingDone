# 多级清单项 debug 发布日志

## 2026-06-26 缩进与图标上边距再次精调（0.5dp 级）

在上一版基础上继续微调（L2/L3 = 二/三级缩进，T1/T2/T3 = 各级状态图标上边距）：

- **详情页**：L2、L3 各 +2dp（首段偏移 4→6dp）；T2、T3 各 +0.5dp（图标下移），T1 不变。
- **首页卡片**：L2、L3 各 +1dp（首段偏移 2→3dp）；T2、T3 各 +0.5dp，T1 不变。
- **首页文件夹缩略图**：与卡片同渲染器，自动跟随。
- **记事列表 / 单一记事 widget**：L2、L3 各 +2dp（首段偏移 2→4dp）；T1、T2 各 +0.5dp（图标 +1.5dp）、
  T3 −0.5dp（图标 +0.5dp）。

注：0.5dp 在高密度屏上会按整数像素四舍五入，最终落点可能与精确 0.5dp 差零点几 dp。
