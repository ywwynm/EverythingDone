# 多级清单项 debug 发布日志

## 2026-06-26 缩进与图标上边距继续精调（按级）

在上一版基础上：

- **详情页**：L2、L3 各 +1dp（首段偏移 6→7dp）；T2 +0.5dp、T3 不变（图标微调改为：一/二级 0、三级 −0.5dp），
  T1 不变。
- **首页卡片**：L2、L3 各 +0.5dp（首段偏移 3→3.5dp）；T1/T2/T3 不变。
- **首页文件夹缩略图**：与卡片同渲染器，自动跟随。
- **记事列表 / 单一记事 widget**：L2、L3 各 +2dp（首段偏移 4→6dp）；状态图标上边距改为按级——一级 +2.0dp、
  二级 +1.5dp、三级 +0.5dp（即 T1 +0.5dp、T2/T3 不变）。

图标各级上边距现已改为逐级独立设置，便于后续单独微调。0.5dp 仍按整数像素四舍五入。
