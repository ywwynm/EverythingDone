# 多级清单项 debug 发布日志

## 2026-06-26 移动模式下返回键先退出移动模式

在清单项移动（拖拽重排）模式下按返回键（系统返回键 / 手势返回、以及详情页左上角返回箭头），现在会
**先完成并退出移动模式**，而不是直接退出记事详情界面；再按一次才退出详情。

实现：把进入/退出移动模式抽成 `enterChecklistMoveMode` / `exitChecklistMoveMode`，返回回调与左上角返回
按钮都先判断 `isChecklistMoveMode()`，是则退出移动模式并拦截本次返回。
