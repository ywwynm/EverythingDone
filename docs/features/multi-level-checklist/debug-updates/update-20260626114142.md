# 多级清单项 debug 发布日志

## 2026-06-26 缩进调小、箭头跟随颜色变小、首行对齐

针对第二轮反馈：

1. **缩进调小（所有界面）** —— 详情每级 24dp（原约 38dp）、首页/缩略图卡片每级约图标宽的 0.63 倍
   （原为整个图标宽）、widget 每级 13dp（原 20dp）。
2. **左右箭头跟随该级文字颜色 + 变小** —— 缩进/反缩进箭头改为按该清单项层级的文字颜色着色（含分级
   透明度，不再是纯白），尺寸缩小（控件 24dp、箭头实际约 16dp）。
3. **与第一行居中对齐（所有界面）** —— 状态图标改为和清单项**第一行文字**在 y 方向居中对齐；多行清单项
   也只跟第一行对齐，不再按整段居中。详情页与首页卡片均已改。

仍为对齐 / 尺寸 / 着色层面的调整，数据与完成、拖拽逻辑不变。缩进步长、箭头大小等仍可继续微调。
