# 多级清单项 debug 发布日志

## 2026-06-26 按界面精调缩进与状态图标上边距

按各界面实测值精调（L2/L3 = 二/三级缩进距离，T1/T2/T3 = 一/二/三级状态图标上边距）：

- **详情页**：L2、L3 各 +4dp（首段偏移 +4），T2、T3 各 −1dp（二三级状态图标上移 1dp），T1 不变。
  反缩进箭头的父级列对齐随新缩进同步更新。
- **首页卡片**：L2、L3 各 +2dp，T2、T3 各 −1dp，T1 不变。
- **首页文件夹缩略图**：与首页卡片同一渲染器，自动跟随上面的修改。
- **记事列表 / 单一记事 widget**：L2、L3 各 +2dp，T1、T2、T3 各 +1dp（状态图标整体下移 1dp）。

实现上把缩进抽成 `indentForLevel(level, step, extra)`（缩进层级额外加首段偏移），三级排版/完成/拖拽
逻辑不变。
