# 多级清单项 debug 发布日志

## 2026-06-26 L3 缩进再调，详情/widget 缩进改逐级独立值

- **详情页**：L3 +2dp（L2=35dp、L3=67dp）；T 不变。
- **首页卡片**：L3 +0.5dp（step +0.5 / extra −0.5 的累积），L2 不变；T 不变。
- **首页文件夹缩略图**：与卡片同渲染器，自动跟随。
- **记事列表 / 单一记事 widget**：L3 +1dp（L2=19dp、L3=35dp）；T 不变。

重构：详情与 widget 的缩进改为**每级一个独立 dp 值**（`detailIndentDp(level)` / widget `when(level)`），
不再用 step+extra 换算，后续"L2=X、L3=Y"可直接填数。卡片仍是图标相对步长 + 首段偏移。反缩进箭头父级列
对齐随新缩进同步。
