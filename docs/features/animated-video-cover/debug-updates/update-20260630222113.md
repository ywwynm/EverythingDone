# 动态视频封面：循环时长改为 3 秒（debug 发布）

`DURATION_MS` 2500 → **3000**（单次循环 3s，25fps ≈ 75 帧）。VERSION→9，旧缓存按新时长重新生成。

其余不变：720px、25fps、`STABLE_HIGH_MEMORY`、起点为选定的 Thing Card Video Frame。

帧数 62→75，生成耗时与文件略增（取帧 ~90ms/帧 × 75 ≈ 7s + 原生编码）。升级后看封面循环是否到 3s、生成是否仍顺。
