# 2026-06-25 抽屉头图随内容一起滚动

- 抽屉头图不再固定在顶部，改为跟下方的文件夹、状态/类型筛选面板、入口等一起滚动。
- 实现：`DrawerNavigationView` 原本是「固定头图 + 独立滚动的 RecyclerView」，现用 `ConcatAdapter` 把同一个 `headerView` 作为前置单项接到 RecyclerView 最前，头图成为列表第一项，与下方内容同处一个滚动容器。DrawerAdapter 的列表逻辑、展开/收起动画、DiffUtil、底部 inset 处理均不变；头图实例保持唯一（ThingsActivity 仍持有其子 View 引用与点击监听）。
- 本地已通过 :app:assembleDebug 编译验证；本次未使用 adb。
