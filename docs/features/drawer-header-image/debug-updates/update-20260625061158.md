# 2026-06-25 头图裁切交互修复（三项菜单 + 比例刻度）

- 三项菜单（默认 / 选择图片 / 调整裁切）不再把图标与文字 tint 成 accent 渐变，回退为原本的中性色（与原"默认/更多"两项一致）。
- 修复"调整裁切"那一格过小、只有小图标的问题：新增与另外两项同尺寸（约 100dp）的"调整裁切"图标 `act_adjust_drawer_header`，并让三格布局与原两项完全一致（同样的图标在上、文字在下，占据相同空间）。
- 裁切编辑器的比例滑块改为复用记事卡片外观那套带刻度的滑块：显示 1:2 / 1:1 / 4:3 / 3:2 / 16:9 / 2:1 / 65:24 各档刻度并支持吸附，范围与详情附件一致（[0.5, 65/24]）。
- 本地已通过 :app:assembleDebug 编译验证；本次未使用 adb。
