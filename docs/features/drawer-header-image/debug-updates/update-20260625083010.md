# 2026-06-25 调小三项菜单"调整裁切"图标

- 三项菜单里"调整裁切"图标显得偏大。原因：另两个 PNG 图标虽是 100dp 画布，但实际图形只占中间约 34% 宽，四周是透明留白；而 crop 矢量铺满了整个 100dp。
- 修复：`act_adjust_drawer_header` 保持 100dp 外框（三个 drawable 边界一致、文字对齐不变），用 `<group scale=0.34>` 把 crop 图形缩到约 31dp 居中，与另两个图形的可见大小一致。
- 本地已通过 :app:assembleDebug 编译验证；本次未使用 adb。
