# 私密文件夹外观预览：完整呈现"取消私密后的样子"

把私密文件夹外观编辑时的"大文件夹 / 缩略图"预览改为完整揭示——按它取消私密之后的真实样子渲染：

- 记事缩略图正常显示（之前因为预览数量按"摘要"外观取而被截断，所以看不到记事）。
- 子文件夹按它们**自己**的私密状态显示：只有单独设过私密的子文件夹才上锁，仅因父文件夹私密而连带的不再上锁。

实现：在预览取数链路上加 `revealRoot` 标志，真实外观取数量、子文件夹 `effectivePrivate` 按自身判定。关闭外观面板后，文件夹卡片恢复加锁的私密样式。
