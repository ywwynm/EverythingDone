# 开锁标识调整：方向、配色、只对本身私密项

根据反馈修正上一版的开锁标识：

1. 私密文件夹的开锁现在从锁的**左侧**打开，与私密记事的开锁方向一致（之前是从右侧）。
2. 大文件夹缩略图等处的私密文件夹图标不再用文件夹自身颜色填充，恢复成自适应的偏黑/偏白，与普通文件夹图标一致，只多一把开锁。
3. 开锁标识只对**本身就是私密**的记事/文件夹显示。之前进入已验证的私密文件夹后，里面本不私密的普通记事也被错误地标成了开锁的私密状态；现在普通记事保持正常显示，只有自己就是私密项、且已验证的才显示开锁。
