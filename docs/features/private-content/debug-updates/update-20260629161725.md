# 私密文件夹卡片加锁标识

把文件夹设为私密后，卡片上没有任何"它是私密的"提示——因为设私密会自动认证，卡片直接显示内容，看起来和普通文件夹一样。

- 现在私密文件夹的卡片标题行右侧会显示一个小锁，作为"这是私密文件夹"的持续标识，和进入文件夹时标题前的锁对应。
- 卡片处于上锁状态（未认证、显示居中大锁）时不重复加小锁。
- 单选、长按、多选、混选等任意方式设私密，卡片都会显示这个标识。
