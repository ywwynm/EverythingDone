# 私密判定隐患清零：基类与剩余两处也改用有效私密

把上一批排查时标为"低优先 / latent"的两处隐患也根治：

- 记事适配器基类 `BaseThingsAdapter` 的有效私密判定默认改为"含文件夹私密"（之前只看前缀，靠各界面自己缓解）——以后新界面直接用基类也不会再漏判。
- 文件夹缩略图里点击记事、右滑开始做事这两处的私密判定，也由"只看前缀"改为有效私密。

至此全应用对"记事是否私密"的判定口径完全统一为有效私密。
