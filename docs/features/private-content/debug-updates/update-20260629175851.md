# 复查修复：widget 简单样式泄露 + 两处开锁标识

继续复查并修复 3 处遗漏：

1. 桌面 widget 简单样式：无标题的私密记事不再回退显示内容摘要（之前在私密文件夹内、自身无标题的记事可能在简单样式 widget 上露出内容）。
2. 记事详情页顶部的文件夹路径：已验证的私密文件夹，路径图标改画开锁，与路径文字显示真实名称保持一致（之前文字是真实名、图标却还是闭锁）。
3. 列表小组件配置页的文件夹树：已验证的私密文件夹同样改画开锁。

另复核了抽屉、移动到文件夹对话框的开锁状态，确认已正确显示、无需改动。
