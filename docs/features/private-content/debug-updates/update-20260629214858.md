# 修复拖拽取消验证后列表卡在浅色态（指纹路径根因）

上一版对“拖拽记事到私密文件夹、取消验证后列表卡在变灰不可点”的修复只在图案锁路径生效。真正根因在指纹路径：系统指纹验证框被取消（点取消 / 系统取消）时，应用此前**既不回调“通过”也不回调“取消”**，于是拖拽流程收不到“已取消”通知，列表无法复位，一直停在“移动模式遗留的浅色、不可点”状态。

本次修根因：

- 指纹验证框被取消时正确回调“已取消”，与图案锁取消一致；
- 拖拽提交流程在“取消 / 失败”时立即恢复列表，不再死等落位动画。

指纹与图案锁两种验证方式下，拖拽记事到私密文件夹后取消验证，列表都会完全回到正常状态。
