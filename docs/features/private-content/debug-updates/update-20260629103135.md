# 修复：私密文件夹外观面板现在能看到真实外观

修复上一版"私密文件夹外观编辑预览"未生效的问题——之前打开私密文件夹的外观面板时，列表里那张卡片仍显示为锁、看不到真实的缩略图预览。

根因：真正隐藏私密文件夹预览内容的是卡片绑定里的 `hiddenPrivate` 开关（之前只改了外观模式的 `presentationFor`，没动这个内容遮蔽开关）。现在（验证通过后）正在编辑的那个文件夹会放行内容遮蔽，正确展示真实外观（缩略图/预览）供调整，关闭面板后恢复加锁显示。
