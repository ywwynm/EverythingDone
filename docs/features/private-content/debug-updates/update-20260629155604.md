# 修复：设文件夹私密后，里面的大文件夹缩略图缺记事

把一个文件夹设为私密后，它里面原本正常显示的大文件夹，缩略图变成只显示子文件夹、不显示记事。

- 原因：这种大文件夹自身不是私密、只是因为所在文件夹私密而被连带保护。它的缩略图数据在加载时按"未揭示"预算，直接记事被整条过滤、只剩子文件夹，之后绑定时又复用了这份不完整的缓存。
- 已认证（这里是设私密后自动认证）能看到内容时，改为实时重取该大文件夹的缩略图，记事和子文件夹都按真实情况显示。

普通（非私密）文件夹的缩略图走原有缓存、不受影响、无额外开销。
