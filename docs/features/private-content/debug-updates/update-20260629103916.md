# 私密文件夹外观面板：卡片宽度 / 大小模式现在可调

接上次修复，移除了外观面板里残留的"私密文件夹禁用外观控件"逻辑——之前私密文件夹的"卡片宽度（普通 / 全宽）"和"大小模式（摘要 / 缩略图）"控件是隐藏且点了也无效的（`updateFolderCardAppearanceDraft` 对私密文件夹直接 return）。

现在（验证通过后）这些控件正常显示、可调，实时预览真实外观，确认后保存、取消后恢复。
