# 从列表设私密的文件夹立刻上锁

修正"设为私密"的自动认证逻辑：

- 在列表里长按（或多选/混选）把一个**没打开的**文件夹设为私密时，不再自动认证——卡片立刻变成上锁状态，给出明确的"已变私密"反馈。
- 只有在**已经打开**这个文件夹的情况下（进去后点右上角菜单设为私密），才自动认证、内容保持显示（你人还在里面，内容不该当场消失）。

同时撤掉了上一版给私密文件夹卡片加的小锁标识（那是修错了方向）。
