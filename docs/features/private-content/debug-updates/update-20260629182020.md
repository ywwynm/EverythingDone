# Activity Header 私密文件夹图标接入开锁 + 折叠优化

鉴权进入私密文件夹后的几处图标修正：

1. Activity Header 里，私密文件夹标题左侧的图标改成和卡片/抽屉一致的"文件夹+锁"，并按是否已验证显示开锁/闭锁——进入私密文件夹（含其下属的私密子文件夹）即已验证，显示开锁。
2. 这个图标不再影响 Header 的滑动折叠：滑动到接近 actionbar 时自动隐藏，标题文字的缩小、换行、最终归位到 actionbar 区都不再被它干扰。
3. 把"开锁"的左侧缺口加大了一点，在详情页路径等小图标里更容易看出是开锁状态。
