# 已鉴权的私密文件夹/记事加"开锁"标识

本会话已经验证过（内容已揭示）的私密项，现在有了明确的开锁标识，区别于未验证的上锁状态。

- 私密文件夹：已验证时，卡片左上角的文件夹图标里画一把"开着的锁"（与抽屉里私密文件夹的锁同源）。这个标识在抽屉、移动到文件夹对话框、大文件夹里的子文件夹缩略图都一致生效。未验证的私密文件夹仍是闭锁 / 居中大锁。
- 私密记事：已验证时，标题行左侧加一把开锁图标，与标题第一行对齐；没有标题的私密记事也会单独显示这把开锁。

桌面小部件配置界面是独立会话，按它自己的验证状态显示，不会带入主程序的验证。
