# 开锁形状与缩略图着色再调整

继续按反馈修正开锁标识：

1. 私密文件夹的开锁不再旋转（之前看着歪），改成把锁的左侧锁栓删掉一段、留出缺口，形状端正、开口在左。
2. 大文件夹缩略图的着色再纠正：大文件夹卡片本身的文件夹图标仍按文件夹颜色着色（这是大文件夹一贯的样子）；只有当一个文件夹以"小文件夹形态"出现在别的大文件夹缩略图预览里时，它的图标才用自适应的偏黑/偏白。上一版把两种情况都改成了自适应，这次区分开。
