# Motion Photo 播放 · 网格实况标识加半透明

## 本次更新

- 详情附件网格左下角的**"实况"标识加了一点透明度**（约 76%，与右侧调整外观/删除图标观感一致）；只作用于网格这一处，全屏"实况"徽标不受影响。

## 技术记录

- `attachment_image.xml` 的 `iv_badge_live` 加 `android:alpha="0.76"`（仅此实例；`ic_live_badge` drawable 与全屏 `ll_live_badge` 均不改）。
