# Motion Photo 播放 · 网格实况标识去底色

## 本次更新

- 详情附件网格左下角的**"实况"标识去掉深色底**，直接显示图标（图标相应放大到 22dp）；HDR、GIF 标识的底色保留。

## 技术记录

- `attachment_image.xml` 的 `iv_badge_live` 移除 `background`（`bg_media_badge`）与 `padding`。
