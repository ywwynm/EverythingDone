# 2026-08-17 - 录音会话异步时序与资源收尾清扫

外部审查第七轮 7 项发现（4 项 P2、3 项 P3）全部修复。

## 更新内容

- 修复关闭录音窗口的瞬间可能破坏会话收尾的问题：此前窗口关闭的两步生命周期会互相抢占，留下"假活"的会话状态，系统音频模式下还可能残留屏幕授权、通知与后台服务；现在收尾期间不再叠加多余操作，关闭后服务完整退出（真机实证零残留）。
- 修复配置超时后麦克风可能被后台"无主占用"的问题：超时报错后若原任务迟迟才返回并启动了采集，现在会被随后的清理任务自动停掉，不再出现"界面显示失败但麦克风仍在采集"。
- 预览期间麦克风硬故障后返回窗口，现在自动完成重建，不再先复用失效通道再报错；系统类来源因需重新授权，仍由用户从来源选择器发起（已知边界）。
- 修复录音临时目录创建失败被误报为"麦克风不可用/系统音频失败"的问题：现在正确提示"录音文件写入失败"，不再错误切换到麦克风或改写来源偏好。
- 修复旧预览线程迟到的"未检测到系统音频"警告可能永久滞留在新预览上的问题：过期线程的状态回调不再被接受。
- "系统"来源的说明补充了麦克风权限要求（系统播放捕获在 Android 上同样需要录音权限），四语言同步。
- 无障碍：错误与状态提示现在会被屏幕阅读器（TalkBack）主动播报，不再需要用户自行导航到提示文本。
- OPD2515（Android 16）真机验证：预览态关闭窗口后服务完整退出、日常前后台切换与录音-停止-丢弃回归全部正常；全量 Debug JVM 单测 911 例通过。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608170102`。远端 `latest.json` 已回读确认，APK SHA-256 为 `a93ed205ba5d18f35b188363edf400265ac9e1e4a3c9aaa17699de0d10a09e31`，大小 24,395,788 字节，与本地一致。
