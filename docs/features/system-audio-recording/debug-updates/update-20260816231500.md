# 2026-08-16 - 修复停止与麦克风故障同时发生时的漏判

外部审查第六轮 1 项并发竞态发现，核实属实并修复。

## 更新内容

- 补上一处并发窗口：如果麦克风硬件故障与手动停止（或达到容量上限的自动停止）几乎同时发生，此前故障信号可能被停止流程掩盖，重新录音仍会复用已失效的音频通道。现在故障在发生的瞬间就被直接记录（不依赖事件通知的送达时序），停止收尾时只要发现输入通道死过，重新录音一律走完整重建。
- 正常停止后的重新录音仍走快速路径，速度不受影响。
- 新增该竞态的专项单元测试；全量 Debug JVM 单测 911 例通过。
- OPD2515（Android 16）真机冒烟：录音、停止、重录、再录、保存全链正常。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608161507`。远端 `latest.json` 已回读确认，APK SHA-256 为 `99886c4943277877cc5503f7b7bb9adae64d4947b8674ac0d7ccd0244d98c762`，大小 24,395,728 字节，与本地一致。
