# 2026-08-15 - 录音停止态隐藏音频输入行并收回空白

用户指出录音结束后不应显示“音频输入”行，也不应为它保留空间；该行只有重新录音回到准备态后才应重新出现。

## 更新内容

- 诊断确认停止态的 `recordingToStopped()` 会主动调用 `showAudioInputForStopped()`，因此来源行必然重新显示。
- 同时确认上一版为容纳该行，把停止态 `TimelyClockView` 的目标顶部从原来的 `80dp` 下移到 `132dp`；仅隐藏 View 仍会留下约 `52dp` 的视觉空白。
- 新增 `AudioInputRowPresentationPolicy`，统一规定 `IDLE`、`PREPARED`、`ERROR` 显示可编辑来源行，`RECORDING`、`STOPPED` 完全隐藏。Fragment 每次渲染会话快照时统一应用该策略，避免不同状态转换路径再次遗漏。
- 删除停止态专用的来源行展示与定位路径，并把停止态时钟目标顶部恢复为 `80dp`，音频名称、计时与操作按钮重新使用加入来源行之前的紧凑布局。
- 新增 `AudioInputRowPresentationPolicyTest`，覆盖五种录音会话阶段；定向回归测试和全量 `:app:testDebugUnitTest` 均通过。
- 按仓库约束未使用 ADB，停止态最终视觉效果留给测试设备验收。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608151505`。远端 `latest.json` 已回读确认，APK 为 `http://120.25.194.207/everythingdone-updates/debug/apk/app-debug-202608151505.apk`，SHA-256 为 `03206641dd27d855ce1649f26ccd515f2f43e57fec3a55cb6c499e3dddc37524`，大小为 24,384,788 字节；远端说明包含停止态隐藏与 `80dp` 布局恢复，本地 APK 与远端元数据哈希一致。
