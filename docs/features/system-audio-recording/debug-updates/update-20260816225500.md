# 2026-08-16 - 修复麦克风硬故障后重新录音失败

外部审查第五轮 1 项发现，核实属实并修复。

## 更新内容

- 修复麦克风硬件级故障（系统回收音频通道等）导致录音自动停止后，点"重新录音"仍会失败的问题：此前会直接复用已失效的音频采集对象，再次报"无法启动麦克风"，需要手动重选一次来源才能恢复；现在这种停止后重录会自动走完整重建，一步恢复。正常停止与文件故障停止后的重录仍走原来的快速路径，速度不受影响。
- 新增该判定的专项单元测试；全量 Debug JVM 单测 910 例通过。
- OPD2515（Android 16）真机回归：正常停止后重录瞬时回到可录音状态、再次录音与保存正常。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608161449`。远端 `latest.json` 已回读确认，APK SHA-256 为 `e9be330e67cc265e5a62b06cacb128a1c958856e4f234664f82f68e6c84d37f6`，大小 24,395,728 字节，与本地一致。
