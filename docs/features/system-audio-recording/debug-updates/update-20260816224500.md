# 2026-08-16 - 录音收尾过程诚实化与错误分类重构

外部审查第四轮 4 项发现全部修复，并按同族问题自查扩大修复面。

## 更新内容

- 录音停止后、文件封装完成前，界面与通知不再提前宣称"已保留"：此阶段显示"正在保存录音…"，通知同步换为"正在保存录音"（不再继续走秒），保存与重录按钮淡化等待结果，"放弃"始终可用（大文件封装期间不必干等）；封装结果出来后再显示真实结论。
- 修复长时间封装期间切换前后台可能收错通知的问题：是否发"已停止"通知改在封装完成时按当下界面状态判定，不再固定于停止瞬间；自动停止（容量上限、授权被撤销、输入或写入中断）的原因现在会直接写进通知正文，不点开应用也能看到。
- 修复录音文件写入故障被误报为输入设备故障的问题：文件打不开/写不了现在明确提示"录音文件写入失败"，不再错误地切换到麦克风（也不再改写你的来源偏好）；混合模式下麦克风故障不再"回落"到刚刚失败的麦克风，而是明确报告并保留来源选择。录音中写入中断有专属提示"录音写入中断，已保留此前录音"。
- 无障碍改进：不可用的按钮现在对屏幕阅读器（TalkBack）正确宣告为"已停用"，封装失败后保存按钮的朗读描述改为"录音未能保留，无法保存"。
- 失败提示文本改为整块居中、多行左对齐（不再逐行居中）。
- 以上文案覆盖简体、繁體（台/港）、英文四语言。
- OPD2515（Android 16）真机注入验证：失败/成功停止态的按钮可用性与无障碍属性逐项核对通过，窗口宽度恒定，后台停止通知链完整；全量 Debug JVM 单测 907 例通过。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608161431`。远端 `latest.json` 已回读确认，APK SHA-256 为 `1ad2b42216210623455366ee9287d8b51b85e31b841655f415461e1234de4e8b`，大小 24,395,728 字节，与本地一致。
