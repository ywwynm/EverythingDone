# 2026-08-16 - 录音封装失败路径诚实化与失败态紧凑布局

外部审查第三轮 4 项发现全部修复，另按用户验收反馈修正失败停止态的布局留白。

## 更新内容

- 录音收尾封装失败时（例如磁盘故障、空间不足），后台通知不再错误宣称"录音已停止并保留"：标题与正文明确说明"未能保留"。
- 收尾失败后的录音窗口不再显示文件名与导出按钮，保存按钮淡化且不可点，仅保留"重新录音"与"放弃"：界面不再诱导保存一个并不存在的文件。
- 失败停止态布局收紧：计时器不再下移让位（原为文件名行预留 80dp 造成顶部大片空白），失败提示紧跟计时器下方（原固定 180dp 悬空），整体骨架与录音中一致；正常停止的布局不受影响。
- 任何类型的收尾失败（不限于常见 IO 错误）都会清理中间数据并正确结束流程：修复了此前个别异常会让录音停留在"处理中"、后台通知一直挂着的问题。
- 修复收尾失败时提示文案与事实相反的问题：此前非空间不足的失败会显示"已保留此前录音"（实际未保留），现区分为"录音收尾失败，未能保留"的专属提示。
- 简体、繁體（台/港）、英文四语言录音文案完整性审计通过。
- OPD2515（Android 16）真机注入验证：失败路径（通知、窗口状态与布局、丢弃清理）与正常停止回归双向通过；全量 Debug JVM 单测 906 例通过。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608161322`。远端 `latest.json` 已回读确认，APK SHA-256 为 `af117190432372fdb3be768d085d4ac7bf3e2410c2f48116dd8695877c1c8edb`，大小 24,391,644 字节，与本地一致。
