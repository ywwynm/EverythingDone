# 2026-08-16 - 录音会话归属加固：跨进程认领、新建记事归属与恢复细节

外部审查第二轮提出 6 项发现（逐条核实全部属实，其中一项为上一批修复引入的组合缺陷），全部修复并真机回归。

## 更新内容

- 修复进程被回收后待处理录音可能被其他记事认领的问题：录音会话的存在性判定（跨记事拦截、"前往"跳转、通知与详情页返回入口）统一为同时检查进程内状态与持久化记录；服务侧增加归属防御，已有归属的会话不再接受其他记事的改写。回归中一并修复了返回入口在进程死场景下落错页面的残余问题（现按持久化记事重启直达）。
- 新建记事的录音会话在记事首次自动保存入库后，归属立即升级为正式记事：此后跨记事拦截、桌面图标接力、停止态跨进程找回全部生效。真机验证：新建页录音后回桌面点图标，直达已保存的该条记事并继续录音，不再开出多余的新建页。
- 修复进程恢复后的停止态点"重新录音"报"麦克风不可用"的问题：恢复的会话按真实未配置状态走完整的输入配置，重录一步到位。
- 录音封装因存储空间不足失败时给出明确的"存储空间不足"提示（此前笼统显示录音失败）；按用户裁定不保留中间数据。
- 修复快速关闭录音窗口时服务绑定可能泄漏的竞态；迟到的服务回调不再影响已关闭的窗口。
- 因容量上限、系统撤销授权等原因自动停止的录音，跨进程恢复后仍会显示停止原因。
- OPD2515（Android 16）真机回归全链通过；全量 Debug JVM 单测 905 例通过。

发布状态：已通过 `:app:publishDebugUpdate` 发布 Debug 更新 `202608161217`。远端 `latest.json` 已回读确认，APK SHA-256 为 `4c0bf8ebe680fa1b6b270c538d21e0abd2796d546dc8fc8aeb1a34141f230d12`，大小 24,390,460 字节，与本地一致。
