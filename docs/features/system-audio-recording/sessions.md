# 会话记录

## 2026-08-17 - 外部审查第九轮修复批（偏好确认收口）

第九轮 1 P2 + 2 P3 全实全修：系统偏好确认条件收窄为"预览真验证过"（配置期转后台不再确认，`startPreview` 成功后补记，幂等）；回落分支补 `lastStartFailedOnOutput` 分流（带 `previewRequested` 前置防标志残值）；`startPreview` 入口即清 `systemSilent`（恢复预览启动窗口不再闪现假静音警告）。真机回归全过、偏好无误写，911 例单测过。

## 2026-08-17 - 外部审查第八轮修复批（收尾闸与持久化时序）

第八轮 2 P2 + 1 P3，按用户要求逐项评估实机可发生性（收尾抢占：窄但真实；偏好过早持久化：罕见但违反自家"全就绪才提交"原则；静音迟到：窄）后全修：Service 级 `sessionClosing` 闸挡收尾期间的输入故障/投影撤销/静音事件；raw 流同步进 `startListening`（返回 true 即全链就绪，线程收流不收文件）；新采集启动沿兜底清零 `systemSilent`。真机回归（预览、dismiss 收尾、录停丢弃）全过，911 例单测过。

## 2026-08-17 - 外部审查第七轮修复批（异步时序清扫）

第七轮 4 P2 + 3 P3 全部核实属实并修复：dismiss 抢占收尾（onStop 按 mSessionClosing 跳过同步 + stopPreview 判 busy，真机实证服务死透）、超时后迟到任务的麦克风占用（追加串行清理任务兜底）、预览停止消费 lastInputFaulted（纯麦克风回 Dialog 自动重建，系统类记录为已知边界）、raw 目录创建失败分流 FILE_OUTPUT_FAILED（新引擎标志 lastStartFailedOnOutput，三处消费点）、引擎实时回调加 captureThread 身份闸（静音/容量迟到回调不再污染新会话）、纯系统来源 summary 补麦克风权限（四语言）、notice 加 liveRegion=polite。真机三场景验证全过，911 例单测过。

## 2026-08-16 - 外部审查第六轮修复（停止与输入死亡并发竞态）

第六轮 1 项窄竞态核实属实并修复：手动/容量停止与 read 死亡并发时，故障回调被 `shouldRun` 抑制或被 STOPPED 态忽略，上一轮按停止原因的判定失效。改为真相记录在源头：引擎 `lastInputFaulted` 在 read 失败处即刻置位（configure 复位），`stoppedSessionRemainsConfigured` 两路信号任一命中即重建。补竞态单测（911 例全过），真机冒烟复用路径无损。

## 2026-08-16 - 外部审查第五轮修复（麦克风硬故障后重录）

第五轮 1 项 P2 核实属实并修复：输入流死亡（ERROR_DEAD_OBJECT）自动停止后，完成快照把纯麦克风误标为已配置，重录复用失效 AudioRecord 再次失败。判定抽为纯函数 `stoppedSessionRemainsConfigured`——`CAPTURE_FAILED` 停止的纯麦克风判未配置走完整重建，文件故障与正常停止保持快速复用。新增 3 例单测（共 910 例全过），真机回归复用路径无损。

## 2026-08-16 - 外部审查第四轮修复批（收尾期诚实化）

第四轮外部审查 4 项全部核实属实并修复：停止中间快照改持 `FINALIZING`（"正在保存录音…"），停止原因只在封装成功后发布，前台通知同步、保存/重录收尾期淡化而取消保持可用（policy 新增 `cancelEnabled`）；停止完成通知改在完成时刻按 `dialogVisible` 判定（删 `fromNotification`），自动停止的原因文案带上通知正文；`CaptureSource` 按 OUTPUT/SYSTEM/MICROPHONE 三分类（修正上一批把文件故障误并入回落麦克风的错上加错，新增 `FILE_OUTPUT_FAILED` 与 `FILE_WRITE_INTERRUPTED`）；三按钮 `isEnabled` 同步 + 失败态保存按钮无障碍描述。另按用户要求提示文本改块居中、行 start 对齐。真机 dump 实测 enabled/cd 属性、后台停止通知链、成功回归全过，907 例单测过。

## 2026-08-16 - 准备态失败文案错位与提示撑宽修复

用户追问 Dialog 全部失败信息清单时发现三处"已保留此前录音"出现在从未录音的场景（准备态投影撤销、准备态采集流死亡、起录失败），全部修复：新增 `SYSTEM_CAPTURE_ENDED` 与 `RECORDING_START_FAILED` 两枚举及四语言文案，准备态流死统一改回落麦克风。用户随后发现失败提示会把 Dialog 撑宽——根因是窗口 WRAP_CONTENT 下宽度由时钟测量宽决定、长文本参与测量即胜出；按用户裁定改为提示用定型后的宽度换行显示（maxWidth 钳制 + 首帧延迟）。真机注入验证宽度恒定 771px、成功路径照旧，906 例单测过。

## 2026-08-16 - 外部审查第三轮修复批

第三轮外部审查 4 项全部核实属实并修复，全部集中在封装失败路径的诚实性：失败停止的通知按 `savedFile` 分支不再宣称"已保留"、Dialog 失败停止态不再显示文件名与导出且主按钮禁用淡化（重构为水平触发 `applyStoppedFileUi`，修掉首轮验证暴露的中间快照时序缺陷）、`saveToWaveFile` 捕获范围扩到 `Exception` 保证任何失败都删半成品并回报完成、新增 `FINALIZE_FAILED` 文案区分封装失败与采集中断（不再误说"已保留此前录音"）。附带四语言 27 键完整性审计通过。用户验收失败态截图后指出留白过大，随即修复失败态紧凑布局：时钟收回原位、提示对齐录音态位置，成功态零变化。OPD2515 注入式真机验证（移走 raw 强制封装失败）失败路径与正常停止回归双向通过，906 例单测全过。

## 2026-08-16 - 外部审查第二轮修复批

第二轮外部审查 6 项全部核实属实并修复：会话存在性判定统一"活跃或持久化"双查询（修掉上一批引入的"进程死后停止态被其他记事认领"组合缺陷，含返回入口对复用实例的重启兜底）、新建记事归属在首次入库后升级为正式 id（真机验证接力直达已保存记事）、恢复快照按诚实未配置状态走完整重配、封装失败删数据并给 ENOSPC 专属提示（用户裁定）、绑定请求与连接状态分离、停止原因入持久化。OPD2515 全链真机回归，905 例单测通过。

## 2026-08-16 - 外部审查修复批

用户引入外部静态审查（10 项），逐条核实全部属实。grill-me 逐项裁定后一次实施：深浅色切换改原地换肤（录音不再被 dismiss 丢弃，FableSol 经查证零改动）、第二记事录音入口拦截（UNDO Snackbar+前往）、停止态持久化与跨进程恢复（含详情页兜底入口）、授权状态入 saved state、4GiB 上限自动停止保留（仅单测覆盖）、WAV 封装即删 raw、Popup 选中项无障碍语义（delegate 注入不碰 ripple）、服务 temp 目录隔离、文档修正。不修：引擎真卡死自恢复（已知边界）、Popup 首项可见性、48dp 触摸目标。OPD2515 统一回归全部通过（含 force-stop 最恶劣场景的停止态恢复），905 例单测全过。

## 2026-08-15 - 完成系统音频录制设计确认

通过 `grill-me` 完成录音 Dialog 新增“麦克风”“系统”“麦克风+系统”输入来源的设计确认。已明确跨 Dialog 记忆、MediaProjection 授权与撤销、系统版本兼容、立体声保存和混音、FableSol 数据来源、回声处理、静音判断、前台服务后台录音、通知操作，以及选择器在准备态、录音态和停止态的展示规则。本次只完成分析与功能文档，不修改产品代码。

## 2026-08-15 - 完成系统音频录制实现与自动验证

完成服务化录音会话、三种输入来源、MediaProjection、固定增益混音、可变声道 WAV、FableSol 最终总线分析、后台通知和 Dialog 三态 UI。选择器复用项目 `PopupPicker` 视觉体系，并补充后台行为、隐私范围、系统撤销和通知权限说明。全量 Debug JVM 单测与 `assembleDebug` 通过；完整 Lint 仍被代码库既有问题拦截，真实系统音频与厂商行为留待真机人工验收。

## 2026-08-15 - 完成来源胶囊与 Popup 交互修正

将来源选择收敛为右侧独立胶囊：底色按 FableSol 未录音态的 `0.16` 系数淡化，文字保持完整记事色，并使用项目已有的下三角图标和胶囊 ripple。Popup 收窄到 272dp 目标宽度并按可见屏幕边界裁剪；选项改用前景渐变 ripple，并保留 90ms 点击反馈后再提交选择。同步把混合来源展示名称调整为“麦克风+系统”。标签后固定 8dp、胶囊自身吸收剩余宽度和内部下三角靠右的结构已经完成；复核发现固定 24dp 只对齐了时钟 View 外框后，又沿用播放 Dialog 的全局布局监听和 `contentLeftPx()` / `contentRightPx()`，把整行两端改为当前字体的稳定实际着墨边界。来源文字和胶囊实体边缘没有播放进度条手柄或保存图标的内部留白，因而不套用其 2dp/1dp 光学校正。全量 Debug JVM 单测和 Debug APK 构建均再次通过；未使用 ADB。

## 2026-08-15 - 诊断系统授权后录音 Dialog 锁死

确认输入来源的空值和非法值都正确默认为“麦克风”，不存在枚举默认值误设；但系统类来源在授权完成前就被 Fragment 持久化，授权流程中断时可能遗留“系统”。同时确认当前控件策略会在 `IDLE`、`ERROR` 和未配置 `PREPARED` 下同时禁用来源胶囊与开始按钮，因此任何未成功进入“已配置准备态”的授权结果都会形成无恢复入口的锁死。测试设备进一步确认同意系统授权和取消授权都会锁死，表明同意分支也未完成前台服务、projection token、系统 `AudioRecord` 与预览采集的完整配置；具体失败点尚需界面状态或日志区分。本次按诊断请求未修改产品代码，修复方案已记录到 `followups.md`。

## 2026-08-15 - 修复系统授权返回后的录音 Dialog 锁死

系统类来源改为只在投影、音频配置和当前所需预览成功后提交跨 Dialog 偏好，并用确认标记把旧版遗留的歧义系统类值一次性迁移回麦克风。开始按钮与来源胶囊采用独立可用性策略，授权返回后的 `IDLE`、未配置 `PREPARED` 和 `ERROR` 均保留来源恢复入口。服务同时覆盖前台服务、投影创建、回调注册、音频配置的异常，并为准备操作增加 8 秒超时收尾。新增两组策略回归测试；全量 Debug JVM 单测和 Debug APK 构建通过，未使用 ADB。

最终 Debug 更新 `202608151432` 已发布到阿里云；远端完整说明、APK URL、大小与 SHA-256 均已回读，本地 APK 哈希一致。首次发布 `202608151431` 的说明因 Markdown 二级标题切分只保留首段，修正日志层级后立即由最终版本覆盖。

## 2026-08-16 - 真机定位并修复授权结果丢失的根因

用户授权后在 OPD2515（Android 16）复现"授权允许/拒绝后来源胶囊永久失效"，探针日志证明 `registerForActivityResult` 回调从未执行：根因是 `DetailActivity.onActivityResult` 覆写后未调用 `super`，androidx `ActivityResultRegistry` 分发链被掐断。用户报告的三个现象（无法再更改来源、授权后采不到系统声音、重开 Dialog 不记忆）全部是该单点的下游表现；前一日的"锁死修复"只调整了状态机下游，未触及根因。修复补上 `super` 调用并把旧式附件分支限定 requestCode（防止授权 `RESULT_OK` 误触发 `addAttachment`），`ThingsActivity`、`SettingsActivity` 同类覆写一并修正；顺带消除捕获线程退出与 `stopListening` 持锁 join 互斥造成的固定 600ms 白等。真机回归覆盖允许、拒绝、重开记忆、后台录音接续、真实系统声音采集（音乐播放录得 -17.8dB 均值，对照静默 -91dB）与运行中切回麦克风；全量 Debug JVM 单测 901 例通过。

## 2026-08-16 - 图标接力与通知返回录音 Dialog

按用户选定的 3a 方案实现录音后台化后的返回导航。通知侧"最小改动"（channel 升重要度换新 id + 显式分组）实测被 Android 16 强制聚合覆盖，随即按 SDK 36 `hasPromotableCharacteristics` 的条件改用 colorized 前台服务通知，实测独立成卡且点击正确触发 contentIntent。图标侧在 singleTask 的 `ThingsActivity` 收到 launcher intent 且会话活跃时用服务保存的返回入口直达录音 Dialog。配套加固：服务记录会话所属记事并在通知落点复用错误实例时重启、`onDismiss` 与宿主清栈解耦。OPD2515 真机两条路径均端到端验收（时长接续、FableSol 实时渲染，截图存证），全量单测 901 例通过。

## 2026-08-16 - 通知记事色、返回卡顿根修与倾斜空窗

用户验收图标接力与通知返回后报告两项问题并提出通知配色期望。卡顿定位为 `eglSwapBuffers` 单次瞬态失败（surface 销毁与 GL 在途帧的竞争，`EGL_BAD_SURFACE`）被当作致命错误永久降级 Canvas 软件渲染、主线程被水面绘制打满；修复为 swap 前二次检查加按 EGL 错误码分级，瞬态失败改为等待下一次 surface。通知底色改用记事代表色，渐变记事以 largeIcon 圆形渐变徽章呈现（colorized 单色限制与 promotable 条件决定了这是渐变的唯一安全载体）。倾斜空窗用会话内最后重力样本在 Dialog 绑定时先摆正水面。OPD2515 真机复现-修复-回归全链路（100% janky→0%），901 例单测通过。附带查明自动化 artifact：`cmd statusbar expand-notifications` 在该设备上 8 秒后会自动回桌面 3 秒，真实手势无此现象。

## 2026-08-16 - 停止并保留后的返回路径验收与时长修复

用户询问通知停止后的行为正确性。真机逐条验收三条返回路径（"已停止"通知点击、桌面图标接力、最近任务切回）均正确恢复停止态 Dialog；"已停止"通知虽无 colorized 保护但因显式分组未被 Android 16 聚合。发现并修复一个缺陷：通知路径停止后重建 Dialog 的时钟不显示实际录音时长——快照新增 `recordedDurationMillis` 由服务在停止时刻填充。901 例单测通过。

## 2026-08-15 - 停止态移除音频输入行

用户纠正此前“停止后只读显示来源”的决策：录音结束后应继续隐藏整行且不占空间，只在重新录音回到准备态后显示。实现删除停止态来源展示路径，并把为该行下移的停止态时钟位置从 `132dp` 恢复为原来的 `80dp`。新增纯状态展示策略，五种会话阶段由同一入口决定来源行可见性；定向及全量 Debug JVM 单测通过，未使用 ADB。

修正版已发布为阿里云 Debug 更新 `202608151505`；远端说明包含停止态隐藏与布局位置恢复，本地 APK 和远端元数据 SHA-256 一致。
