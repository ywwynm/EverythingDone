## 国际化显示细节修正

本次更新修正三处英文/多语言界面细节：

- 抽屉头图设置项英文改为 `Drawer header image`，遵循设置界面普通条目的句首大写规则。
- 抽屉头图操作弹窗使用统一的操作单元宽度，并允许操作文字最多显示两行，避免 `Choose image` 或其它较长语言在图标宽度内贴边。
- 记事外观 panel、调整颜色 panel 的标题，以及颜色编辑器的 `Pure` / `Gradient` tab 在英文下显示为大写。

已通过 `:app:assembleDebug --console=plain --no-configuration-cache` 编译验证，并发布 debug update `202606261612`。
