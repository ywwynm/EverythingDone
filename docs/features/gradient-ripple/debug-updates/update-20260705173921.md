# 修正渐变 ripple 触摸扩散

- 修复 `GradientRippleDrawable` 可能把初始化时的 `(0,0)` 旧 hotspot 当成本次触摸点的问题，避免波纹看起来固定从左上角向右下角扩散。
- 保留背景本身的线性渐变方向，继续复用 `ThingBackground.orientation`，不把 ripple 改成径向或其它方向。
- 颜色面板“纯色 / 渐变”和计时数字风格“实心 / 描边”tab 复用已有 foreground ripple，只更新颜色，减少按下切换时的动画卡顿。
