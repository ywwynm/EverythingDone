## 渐变 ripple 反馈微调（第三批续）

### 主要改动
- 顶栏图标 ripple 的 centered 语义修正：波纹仍**从触摸点开始扩散**，但圆心在扩散过程中向控件中心迁移，铺满时正好居中（仿系统 ripple 的 origin 收敛）；不再一按下就从中心冒出。
- 顶栏图标 ripple 固定半径 22dp → 21dp。

### 验证重点
1. 顶栏图标按下：波纹是否从手指按下处开始扩散，并随着铺满逐渐居中到图标中心。
2. 半径 21dp 的视觉大小是否合适。
