## 2026-07-05 计时数字风格 tab ripple 修正

- 用户反馈设置界面“计时数字风格”dialog 顶部“实心 / 描边”两个 tab 的触摸 ripple，与记事详情里调整记事颜色面板“纯色 / 渐变”tab 不一致：波纹没有从触摸位置散开。
- 本轮对照 `DoingDigitStyleDialogFragment` 与 `ThingBackgroundEditor`，确认两者都使用 `GradientRippleDrawable`，但计时数字风格 dialog 需要显式把 tab 的触摸坐标转发给 foreground drawable，确保自绘 ripple 以真实触点为圆心。
- 已为“实心 / 描边”两个 tab 安装触点转发：在 `ACTION_DOWN` / `ACTION_MOVE` 时调用当前 `GradientRippleDrawable.setHotspot(event.x, event.y)`，同时不消费触摸事件，原有 click 切换逻辑保持不变。
- 未改变 tab 的选中态文字渐变、未选中提示色或字体列表布局。
- 未使用 adb。
