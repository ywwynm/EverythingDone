## 设置字体选择器宽度与文字颜色微调

根据反馈继续微调设置界面的 timely 字体选择 dialog：

- dialog 宽度从屏幕 92% 改为 280dp，对齐设置应用语言时使用的 `ChooserDialogFragment` 宽度。
- dialog 标题改为 App 默认 accent + accent2 渐变文字。
- 字体名称文字统一使用 App Chrome 的提示性文本颜色，不再使用普通次级文本色或选中态白色。

验证：`:app:assembleDebug` 已编译通过。未使用 adb。
