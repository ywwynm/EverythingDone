# 撤回 Phase 2，回到 Phase 1 调参后状态

用户决定撤回倾斜物理 Phase 2（水体下坠/水团），回到 Phase 1 调完参数的状态（原 debug 202607060818）。

## 本次改动

- **完全删除 Phase 2 代码**（整摊水体重力下坠 `updateWaterDrop`/`mDropOffset`/`DROP_*` 及此前的弹道水团粒子残留），逐处反向撤销回 Phase 1。
- 保留 Phase 1 完整状态：自由液面速度场晃动（1 阶本征模 sin(π·u)、约 3~4 次干净回荡）、浪包平流、静止门控、6 层错落（`mLayerSloshAmp`/`mLayerSloshShift`）、D47 波峰净空/波谷限深复原、去均值等。

## 已验证

- 本地 `:app:assembleDebug` 通过；Grep 确认无 Phase 2 残留符号。
- 未使用 adb，未连接物理设备或模拟器。

## 说明

- 倾斜时的"晃动/回荡"仍在（Phase 1）；**倒置/快速倾斜时的"空中下坠"已移除**（那是 Phase 2）。
- 静止时音频波浪与之前一致。
