# 偏好 · audio-visualization-fable-sol

## Python 先暴露完整光学机制滑杆，再决定是否进入 Android（2026-07-24）

- 用户要求先只在 `audioVisualizerSimulatorFable` 增加一个独立参数组，实时调节折射强度、
  折射位移、水体清澈度/透射比例、Beer–Lambert 吸收系数、IOR、主体宏观直射光强、
  微法线强度和连续流纹。
- 这些参数需要允许拉到足够显著的范围，供用户自行做动态 A/B；当前生产观感应继续作为
  默认基线，尚未目测确认前不得同步到 Android。
- 新增滑杆必须继承修改前的有效默认状态：此前为 `0`、已关闭或因机制移除而等效为
  `0` 的项目，新增滑杆后默认仍为 `0`，不得借接入调参面板自动开启。
- 先前删除或否决的连续微表面、流纹类机制可以在本轮作为明确标注的 Python 试验项恢复，
  但不能借试验名义修改 Android 共享 shader 的生产默认值。

## 音乐测试集中的每首歌都应有可触发巨浪的高潮（2026-07-22）

- `E:\projects\audioVisualizerSimulator\assets` 中用于回归的音乐都存在足以触发巨浪的高潮；`Back To December` 为 0 次不是可接受的负对照，而是需要修复的漏检。
- 调整通用 gate 时必须同时保持《Lose My Mind》母带严格为 4 次，不能以全面放宽阈值的方式恢复其它歌曲，进而让它出现第 5 次或让录音版新增错误巨浪。
- 回归应检查音乐测试集中的所有 0 次结果，不再把“无巨浪”默认解释为稀疏性通过。

## 用修改前录制的两段全曲 WAV 复核母带一致性（2026-07-22）

- 使用 `20260722163541-银花-全.wav` 与 `20260722163150-Lose My Mind-全.wav`，在最新代码下
  分别走 Python realtime 产品路径和 Android 48kHz 单声道产品回放路径，与对应母带比较。
- 不能只看巨浪次数；同时检查七境、水位、流速、普通波幅及其它动画驱动是否仍有明显差异。
- 两条录音是在本轮修改前由 Android 录制的，必须按真实采集产物处理，不得转换成新的测试 WAV，
  也不得用歌曲名、绝对时间或单曲专用分支拟合。
- 若存在很大差异，应继续修正通用实时分析/动画链，并保持 Python 与 Android 同步。

## 中远层曲线平滑问题必须先用真实音乐 mask 对齐（2026-07-16）

- 上一轮仅加宽最远两层环境波并没有解决用户看到的问题；不得把该量化指标当作视觉问题已经修复。
- 使用 `HOYO-MiX - 银花玉鉴逐人来 Where Tender Night Embraceth Thee.flac` 走真实 Python 音频链复现，先在实际渲染帧上用 mask 分别标出连续小波峰、轻度小阶梯和局部冒尖，等用户确认双方看到的是同一批问题后再改实现。
- 检查范围不只限于最远两层或密集峰群，所有中远层水体边界的轻微折线、台阶和不自然尖点都属于本轮平滑度问题。
- 必须通过单变量消融区分高频音频响应、主浪/动态浪/环境波生成、显示重建与抗锯齿原因；若确认可见轮廓的高频响应导致这些瑕疵，则删除该轮廓设计，不以音乐响应为理由保留不平滑曲线。
- 两层水体彼此交叉形成的正常可见交界不是“冒尖”问题；mask 必须沿单条轮廓判断连续性，不得把层间相交点圈成缺陷。
- 红紫渐变不利于区分相邻层。本轮重新对齐时使用 Python 配色列表第二项蓝色“深海”（`#0E7490 → #67E8F9`），再让用户确认标注。
- 用户最终只确认两个画面区域：14.0 秒画面左上方的可见红线（实际依次由两层轮廓组成，且两层各自都存在问题），以及 112.0 秒画面上方中间的红线。新版 mask 中其余标注全部作废，不得作为修复目标或回归正样本。
- 几何反投影后，三个有效单层区段固定为 14.0 秒 `L6 x=-150..-35dp`、同帧 `L3 x=-25..+5dp`，以及 112.0 秒 `L7 x=+65..+150dp`。诊断与回归必须分别读取单层轮廓，不能把 14 秒的两条线合成一条边缘。

## 撤销 Python 表面反射软带，并清理锯齿状连续小波峰（2026-07-16）

- 用户最终判定表面反射软带会让水面显得杂乱；从 Python 模拟器中完整移除该试验效果及其全部控制项，不移植到 Android。下方此前关于恢复、调亮和动态门控该软带的偏好均由本条取代。
- Python 可选颜色需要覆盖：`#F02A4B → #6E62DF` 左右渐变、现有 10 个 Thing 内置纯色、`#00FFF6 → #4B7ADB` 左下→右上渐变、`#8A0751`、`#B6BF8F`、`#D72DB8 → #EB8B6D` 左右渐变、`#F9799A → #E7EB13` 左下→右上渐变、`#FE79B8 → #5F5080` 左上→右下渐变、`#2C6CDE → #0438B1` 上下渐变。
- 远层水面偶发聚集的连续小波峰会形成锯齿观感，属于不接受的波形；修复必须保留音乐驱动、宽缓主波和水面连续性，并同步 Python 与 Android。
- Python 参数面板只保留仍被当前渲染、动画或音频链路实际消费的参数；无效参数及其遗留控制项直接删除，并在交付时列明。

## 表面反射软带的 100% 白度与 1.2 增益只代表峰值，不得常驻（2026-07-16）

- 用户选择 `100% 中性白 / surface_strip_gain=1.2`，但该数值只用于最强的局部反射峰值，不能让每层沿整条水面持续出现白带。
- 2026-07-13 21:30 左右的目标原图来自 HDR 模式；表面反射软带的 HDR 核心亮度必须与该旧版一致。8-bit SDR 联系表只能判断形状、色相、覆盖与脏暗，不能再承担 HDR 亮度标定。
- 当前约 `3.0× reference white` 的 Python HDR 核心已被用户判定明显过亮；后续必须用历史版本与线性 FP16/scRGB 原值校准 HDR，不能把 SDR 下选定的白度/alpha 直接等同于 HDR excess。
- `surface_strip_gain` 是 2026-07-11 已有、直接控制同一条表面反射软带的参数；保留该强度滑块，不得再新增语义重复的第二个强度项。
- Python 面板需要为该软带提供独立启用开关，并把迎光出现范围与波峰/曲率增强作为可调整参数集中展示；这些控制不得连带关闭独立的 flow streak。
- 每个具备表面带权重的水层都可以出现该效果；具体位置和时刻必须随水面局部坡向、主光方向和波形变化而移动、缩短、消退及重新出现。
- 上凸曲率只用于柔和增强，不得重新引入 `crest edge` 式硬波峰门控；也不得用随机闪烁、独立呼吸或时间相位伪造流动。
- 动态表面带只能增加亮度，不得同时恢复原截图中的灰蓝、灰黑或混杂脏阴影。

## 使用指定青蓝渐变判断连续表面反射软带亮度（2026-07-15）
- 恢复软带的视觉对照使用 `#00FFF6 → #4B7ADB` 左下→右上渐变；不得用过于平静的水面或其它色板替代该次判断。
- 用户确认此前 46% 中性白候选仍明显暗于原截图，后续亮度探针必须覆盖更高白度及有效强度，而不是据此把较暗版本定稿。
- `crest edge` 是忘记撤回的独立实验，不属于目标软带；相关实现、参数与测试应全部撤掉，不得用它补目标区域亮度。

## 恢复最前层波浪内侧的乳白—浅青柔光带，不恢复脏阴影（2026-07-15）

- 用户确认目标是 2026-07-13 21:30 左右截图中，最前层波浪上边界下方、沿波峰方向单侧铺入
  水体的乳白—浅青色柔光窄带；它用于增强水体的圆润感、晶莹感。
- 不恢复截图中与其相邻的灰蓝、灰黑或混杂脏阴影，也不把深色边线、离散 glint、层内
  continuous sheen 或扩缩圆环当成同一效果。
- 后续若恢复该效果，Android 与 Python 必须保持相同视觉语义，并分别验证 SDR/HDR、边缘抗锯齿
  和动态帧率；实现前先向用户说明准确效果归属并等待确认。

## 必须区分离散亮银闪点与持续扩缩的油漆感光带（2026-07-15）

- 当前真机仍能看到不停收缩、扩散并逐渐形成圆环向外蔓延的光带；它让水体呈现油腻、漆面式
  高光，属于明确不可接受的动态形态。
- 用户确认该现象绝不是亮银斑/离散闪点：亮银斑本身不会自行扩散，也不会演变成向外传播的圆环。
  后续诊断不得再通过缩减或重做亮银闪点来代替定位这个独立效果。
- 必须在 Python 动态录制中分别隔离连续水体 pass、各类 optical mode、流光、体光、薄峰和界面
  结构，直接找出哪个实体的宽度、半径或剖面随生命周期变化；只修改真正的来源，并同步 Android。

## 动态光学诊断必须使用中高能且层级可见的音乐场景（2026-07-15）

- 风平浪静的静态截图无法复现持续收缩、扩散的油漆感光带，不得用这种画面判断问题是否存在或
  修复是否有效。
- Python 反馈环必须模拟有明显节拍和动态起伏的中高能音乐：适度持续响度、可辨低频、连续 onset、
  高频/闪烁驱动和中等浪包，并以多秒时间序列或视频检查动态形态。
- 所有单变量消融必须消费同一份确定性高能驱动和相同时间点，避免把不同波形误当成某项光学效果
  的差异；静态 18 色 PNG 仍只承担颜色与层界验收。
- 压力场景应采用中高能、明显起伏但不过载的音乐，不应把持续响度和涌浪积累推到第一层基础水位
  大幅升高、遮住后方层级的程度；诊断画面必须同时保留足够的九层可见面积。
- 用户已再次确认问题来源不是 glint 闪点。后续回归可保留“关闭 glint 后仍复现”作为排除证据，
  但不得修改 glint 数量、尺寸、峰值或形态来处理这条扩缩光带。

## HDR 离屏验收必须分离 SDR 图像判断与线性数值判断（2026-07-15）

- PNG 联系表只用于比较 Thing 颜色、层界、脏灰/乳白、光迹形状、连续性和空间分布；即使来源是
  FP16 HDR 场景，保存成普通 PNG 后也只是固定曝光的 SDR 观察图，不能据此判断超白亮度。
- HDR 亮度与层次必须直接读取线性 scRGB/FP16 原值，至少逐层统计峰值以及
  `>1.0/1.08/1.16/1.29/1.6/2.0× reference white` 的覆盖率，并结合连通区面积、非闪点
  excess 和分档伪彩图判断。不得把模型对 SDR PNG 的视觉感知当成 HDR 屏幕观感。
- 同一批 18 色仍需保留固定曝光 PNG，因为任意 Thing 颜色下的色相污染、饱和度、层界和形状
  问题不能只靠亮度统计发现；最终 Android tone mapping 与动态 HDR 观感继续由真机验收。

## 既定逐层特效与 HDR 等级不得被形态修复连带撤销（2026-07-15）

- 恢复 `lighten_far` 只裁决九层主体色板，不得同时推翻此前已经确定的逐层特效数量、强度、
  存在层级和 HDR 峰值。第 3～7 层仍应保留按景深稀疏衰减、在实机上可感知的闪点与连续受光，
  不能因为取消中远层圆环就把这些层的主要材质响应整体清零。
- HDR 亮度应回到上一提交已经确认的逐层等级。控制乳白感时优先缩小银白核心的面积、持续时间、
  同时出现数量和白色肩部覆盖率，不得再把“峰值亮度”和“覆盖率”绑定后一起降低。
- 不恢复大块亮银色白斑，也不接受规则圆环或周期扩张、收缩的光带。现有光带应继续优化为沿真实
  波峰方向分布、破碎、非对称、贴住水面的短光迹；生命周期以平移和自然显隐为主，尺寸不得跟随
  候选场持续呼吸。
- 水体需要与高光同源的暗面来建立立体感，但不得给亮带描黑边或配置独立的同心暗环。亮侧和暗侧
  应来自同一波面法线、太阳方向与遮蔽关系；暗部从当前层当前位置的水色保色变深，不混黑，也不
  乘暗环境反射、HDR 核心和最终像素。

## 恢复 lighten_far 混白并取消中远层圆环光斑（2026-07-15）

> 状态：静态 `lighten_far` 主体色板与零界面肩仍有效；“离散闪点仅保留第 0～2 层、关闭
> 第 3～8 层连续太阳反射”的范围已被最新真机反馈否决，不能再作为现行视觉合同。

- 九层主体颜色恢复旧 `lighten_far` 语义：第 0 层保持记事身份色，第 1～8 层按深度直接在
  OKLab 中混向白色，默认最远层混白量为 `86.4%`；不再使用固定 hue 色域贴边、相邻 ΔE
  重新分配、近三层保色或其它逐色特殊控制。
- 这里恢复的是静态 `depth × lighten_far` 混白阶梯；录音、HDR、mood 和 color breath 仍不得
  改写主体色，避免点击开始录音时第二、第三层再次整体跳亮。
- 不再保留会周期扩张、收缩的圆环式闪点光晕。第 3～8 层不生成离散光斑；中远层只保留连续、
  非圆环的微表面受光。最近三层也应减少离散闪点数量，并取消周期呼吸与解析外晕。
- 第二、第三层的折射/Beer 与连续太阳反射必须保持低占比，不能把恢复后的混白主体再次洗灰；
  允许牺牲一部分大块白斑强度来保持干净色彩，但不把折射和体积吸收完全关闭。
- `lighten_far` 阶梯本身承担层级区分，不再为其附加界面肩或记事色粗边。

## 晶莹水体第二轮真机反馈与推进顺序（2026-07-15）

> 状态：“第 3～8 层不能只剩主体色”的要求已由最新真机反馈再次确认；此前将其解释为取消
> 中远层全部离散光斑并关闭连续太阳反射，属于范围过大的修复，现以上方最新偏好为准。

- 当前层界不够分明，且界面肩偶尔形成明显、粗重的记事色边；后续必须同时提高相邻层可读性并消除粗描边感，不能只调大界面肩。
- 跨层移动的明暗结构可以保留，但边界必须连续、平滑，不能出现马赛克状折线或分段直线。
- 第 3～8 层不能只剩主体色；中远层也应有可辨但有层级衰减的闪点、光斑及其它水面材质响应，避免单调。
- 希望恢复现实海面式的局部高光光斑和整体波光粼粼感，让闪点之外的连续/成片结构也能产生局部 HDR 超白；不得恢复大面积乳白膜。
- 可以安排真实折射和 Beer–Lambert 体积吸收，但必须基于明确的背景采样、光程和能量合同，不能重新用主体乘暗或黑色覆盖伪造。
- 实施顺序固定为：先解决 Python 渲染卡顿，再利用离屏 HDR 图像与数值进行 Python 视觉调试，取得稳定效果后同步 Android；Android 实机视觉验收由用户完成，不使用 ADB。

## 第二轮真实 Thing 颜色测试集（2026-07-15）

- 测试必须覆盖现有 10 个内置纯色，以及以下真实输入：
  - `#F02A4B → #6E62DF` 左右渐变；
  - `#00FFF6 → #4B7ADB` 左下到右上渐变；
  - `#8A0751`、`#B6BF8F` 纯色；
  - `#D72DB8 → #EB8B6D` 左右渐变；
  - `#F9799A → #E7EB13` 左下到右上渐变；
  - `#FE79B8 → #5F5080` 左上到右下渐变；
  - `#2C6CDE → #0438B1` 上下渐变。
- 不要求每个颜色都完美；优先让九层在动态画面中分明、材质丰富且不脏。远层偏白可以接受，最终混白程度不得超过旧基线 `86.4%`。

## 九层颜色统一提亮，不再按个别颜色讨论方向（2026-07-14）

> 状态：其中“禁止向白点直线混合”的限制已由 2026-07-15 的 `lighten_far` 复测裁决取代。

- 第 0 层保持记事身份色，第 1～8 层统一逐层提亮；不再自动向暗，也不为不同 hue 建立方向分支。
- 提亮应尽量固定 hue、保持原始色度，只有目标色域迫使时才压缩；禁止恢复向白点直线混合。
- 具体 `ΔEOK` 和色度保留档位作为 Android/Python 同构调参，不再继续逐色裁决。

## 色域耗尽后用宽软保色界面肩补层界（2026-07-14）

> 状态：已由 2026-07-15 的静态混白阶梯与零界面肩裁决取代。

- 主体向亮路径通常在保留 `70%` 原始色度时停止；仅最近两条边界可以借用到 `60%` 硬底线，不能
  为中远层继续消耗色度并重新产生乳白主体。
- 剩余层界使用宽软、低幅、从当前层当前颜色派生的亮侧/深侧界面肩；不得混白黑、成为细描边、
  九条等强横线或改变主体腹部的逐层向亮关系。
- 界面肩在未录音 SDR 和录音 HDR 中保持相同；真实 Fresnel、折射、透射与 HDR 高光仍是独立材质
  分瓣，不能把界面肩包装成九层真实 Fresnel。

## 大多数主体层应越远越亮，禁止按色度评分自动改暗（2026-07-14）

- 九层主体的默认景深语义仍是越远越亮；默认粉、黄色、青色、绿色等常用颜色不能因为向暗更容易
  保留 `C8/C0` 或完成较大的 `ΔEOK`，就被自动生成为越远越暗。
- 向亮达到色域边界时，应先停止会造成乳白或明显消色的颜色推进，再用稳定的低频材质/界面线索
  补足相邻层界；不得把自动反向当作通用后备方案。
- 近白等完全没有上行余量的极端颜色继续单独处理，不能据此改变大多数颜色的向亮方向。

## 体积吸收独立于阴影，只影响透射光路（2026-07-14）

- 体积吸收不得用于压暗整层主体，也不得影响环境反射、镜面高光、闪点或 HDR；它只处理穿过
  水体介质的透射、折射或背景采样分量。
- 光程增加时允许透射色自然变深、适度提高饱和度并产生极小色相漂移，不要求保持主体阶梯的
  绝对 chroma；但必须限制色相漂移、最大光学深度和最低能量，避免浅色发灰和深色沉黑。
- 禁止以黑色 overlay、编码 sRGB 整体乘法或最终像素乘暗冒充体积吸收。

## 所有阴影必须从当前位置的水层颜色变深，禁止混黑（2026-07-14）

- 波背、宏观坡面、微表面和其它效果产生的阴影都不能直接混入黑色、中性灰或统一暗色
  overlay；暗部必须继续保持当前水层在该位置的颜色身份。
- “当前颜色”是已经应用该层景深颜色与横向渐变、但尚未加入环境反射、透射、次表面散射、
  镜面高光、闪点和 HDR 增益的主体色，而不是最终像素，也不是统一使用原始 Thing 色、第一层色
  或一套跨九层复用的固定深色板；远层较亮时，其阴影也应从该亮色有界变深。
- 物理遮挡可以削弱对应的直射光分量，但不能乘暗最终主体、环境反射和透射总和；最终暗部仍应
  读作更深的当前色，而不是覆盖在水面上的黑影。

## 主体阶梯默认保留原始绝对色度，不自动增艳（2026-07-14）

> 状态：主体保色路径已由 2026-07-15 的静态 `lighten_far` 混白裁决取代。

- 用户同意主体景深颜色默认以 `Ctarget=C0` 保持原始 OKLCH 绝对色度，不采用会随 `Cmax` 形状
  自动增艳或褪色的相对色域饱和度路径。
- 第 1～8 层主体不主动变得比记事原色更饱和；只有目标色域容不下时才允许受控降低色度，并在
  跌破保留底线前停止、换向或使用其它低频层序量。
- 局部反射、透射和其它材质响应可以形成更鲜亮的局部像素，但不反向修改低频主体色板的色度合同。

## 远层可以明亮，但不能靠消色获得亮度（2026-07-14）

> 状态：已由用户在 2026-07-15 明确接受的 `lighten_far=0.864` 混白方案取代。

- 远处水层明显变亮本身没有问题，不需要为了保留深色身份而刻意压低第 8 层明度或限制九层总
  明度跨度。
- 真正需要限制的是远层发灰、乳白和低饱和：提高明度时仍应保留与 Thing 身份色相关的色相与
  足够色度，不能继续沿中性白方向把 `a/b` 或 OKLCH `C` 系统性压到接近零。
- 评价候选路径时，远层更暗不再作为优点；应分别比较八个相邻边界、逐层色度保持和最终动态
  画面的干净程度。

## 相邻层级区分优先，最近三层必须保持强边界（2026-07-14）

- 九层颜色路径的首要验收对象是八对相邻层，而不是第 0 层到第 8 层的端点总差；即使首尾差距很大，
  只要任一相邻间距不足，水体仍可能在该处混成一片。
- 最近三层承担最主要、最容易重叠的水体材质，`L0↔L1` 与 `L1↔L2` 必须获得最强的相邻层级
  区分；不能为了限制最远层的总提亮而优先压缩这两段。
- `ΔEOK` 目标可以按相邻边界分别调整，不要求八段等距；验收采用色域映射和最终量化后的实际
  相邻距离，并且必须在录音 HDR 开启、局部光影叠加后继续检查近三层是否混合。

## 颜色阶梯必须逐层展示，不能只凭第 8 层端点判断（2026-07-14）

- 评估候选景深颜色路径时，至少要画出同一输入色的完整九层阶梯，不能只比较第 0 层与第 8 层；
  端点看似不同，并不证明中间八段都足够分明。
- 对照必须同时给出每层最终量化色值和每对相邻层的 `ΔEOK`，并把当前混白算法与候选算法放在同一
  视觉尺度下；深蓝等容易保持较深外观的颜色必须作为固定样本。

## 颜色身份不能以牺牲九层区分度为代价（2026-07-14）

- “第 1～8 层仍属于记事色系”不等于把九层都收敛成与记事原色几乎相同的外观；九层仍需具有
  明确、连续且稳定的主体差异。
- 当前远层问题可能不是“使用 OKLab”本身，而是简单把采样方向固定为中性白；应调研在感知颜色
  空间中沿更合适的明度—色度方向采样，使各层既关联记事颜色，又具有足够区分度。
- 新颜色路径必须同时验证浅色、近黑、高饱和色、低色度色和跨明暗渐变，不能只通过提高色度下限
  把所有层变成近似同色。

## 第 0 层低频主体基线继续作为记事身份色锚点（2026-07-14）

- 第 0 层进入局部材质计算前的主体基线继续等于记事原始纯色，或保留原始横向渐变关系；第 1～8
  层从该基准派生自适应景深阶梯。
- 锚点不要求第 0 层全部最终像素保持平色。局部反射、透射和有界明暗仍可产生材质变化，但必须
  留下稳定可辨的原色参照，不能让白色高光或灰暗阴影主导首层颜色。
- 当前第一层观感相对较好，后续重构不应为了修复第二、第三层而无必要地改变其主体颜色关系。

## 多数颜色越远可明显变亮，浅色无上行余量时才反向（2026-07-14）

- 典型中间色和大多数深色继续使用“主体越远变亮”的默认方向，允许明显拉开明度；前提是保留
  记事色相、足够色度和八个相邻边界，不再逐层大幅混向中性白。
- 浅色、近白色或其它没有向亮余量的颜色才允许把主体阶梯连续反向为越远略暗，或用其它低频
  层序量维持边界；近黑色可有限向亮，但不能变成灰色。
- 跨层主体阶梯不限制层内压暗。深色仍可在波背、长光程和直射光较弱处形成有界暗部，以承担
  体积和受光结构，而不改变其默认跨层方向。
- 渐变记事中的方向变化必须平滑，切换位置不能出现横向接缝或相邻水层突然合并。

## 主体阶梯按颜色余量自适应，不是全局改成越远越暗（2026-07-14）

- 当前实现确实是越远越接近白色；新的自适应原则取消这项对所有 Thing 颜色统一生效的固定规则，
  但不把它反向替换成所有颜色都越远越暗。
- 浅色、近白色在向亮余量不足时可以压暗或使用其它层序量；中间色、深色和近黑色分别按实际
  明暗与色度余量选择方向，同时保留记事颜色身份和相邻层差。
- “深色允许压暗”同时适用于层内受光与体积结构，不等于深色的跨层主体阶梯必须固定向暗。
- 渐变记事的自适应必须沿横向连续，不能因局部颜色跨过阈值而出现接缝或层序突变。

## 九层主体独立保证层序，不再把混白或 alpha 当成定义（2026-07-14）

- 九层低频主体外观本身必须保持近到远有序、相邻边界可辨；即使暂时关闭局部 HDR、闪点、阴影
  和其它局部光影，层界也应继续存在。
- 局部光影和 HDR 用于丰富层内材质，不负责补救本来不可辨的主体层级，也不得持续抹平边界。
- **水层景深阶梯** 是结果约束，不再预设逐层混白、提高明度或降低整体 alpha；具体实现必须结合
  任意 Thing 颜色的明暗与色域余量另行确定。

## 录音开始可以启用多类局部 HDR 材质响应，但必须保留九层边界（2026-07-14）

- 准备态没有 HDR、点击开始录音后进入 HDR 是既定且希望保留的状态关系；不应为了修复第二、
  第三层乳白而让录音前后始终保持同一 HDR 状态。
- 录音 HDR 态不只允许增强局部镜面和闪点，也可以增强局部表面反射、薄峰透射、受光明暗及其它
  有助于晶莹感、立体感和水体质感的光学响应。
- 九层水体相对分明的界限必须继续存在；局部光影和 HDR 可以丰富层内材质，但不得用整层或大面积
  近中性白增益抹平相邻边界、吞掉记事颜色身份或让第二、第三层持续乳白。
- 当前第二、第三层需要按基色混白、continuous sheen、SSS、表面带、透射、闪点和 presentation
  等职责分别审计累积能量，不能把修复方案预先限定为“只保留镜面与闪点”。

## 材质必须适配任意 Thing 颜色，尤其是浅色与近白色（2026-07-14）

- Thing 颜色可以落在色域内的任意位置；FableSol 不得假设主体色始终具有足够的向白提亮余量，
  也不得只以中高饱和、中等明度样本调参。
- 浅色、近白色、浅灰和低色度颜色必须仍能获得清晰的水层、波形与受光结构；当提亮余量不足时，
  不得继续硬混白，也不得用灰黑宽域阴影补偿层次。
- 深色、近黑色同样允许在非身份锚点区域继续有界压暗；不得因为其向下余量较少就禁止一切暗部，
  也不得让压暗吞没原始颜色身份或把不同深色收敛成同一黑色。
- 阴影或背光结构在浅色上必须保持颜色身份和干净观感；不能因为统一 RGB/亮度乘法而出现灰脏、
  固定冷偏或明显色相漂移。
- 后续材质合同和验收矩阵必须同时覆盖浅色、近白、低色度、深色与高饱和 Thing 输入，而不是只
  验证当前默认粉色样本。

## 材质升级优先解决非首层灰浊，并保持 Android/Python 同步（2026-07-14）

- 当前首要视觉缺陷不是第 0 层，而是其余水层偏灰、乳白、浑浊、暗淡，饱和度与局部对比不足；
  后续优化必须覆盖近、中、远层的整体材质关系，不能只强化最前层或少数 HDR 闪点。
- 目标是在不恢复灰黑脏影和宽域暗带的前提下，提高水体、波浪与光线的晶莹感、体积感和物理
  可信度；阴影只有在保色、局部、受光几何可解释时才可采用。
- 每项正式改动必须同步到 EverythingDone 的 Android 实现与
  `E:\projects\audioVisualizerSimulatorFable` 的 Python 实现；两端可以保留平台输出能力差异，
  但材质语义、参数合同和可比较的 SDR 基线必须一致。

## 水体参数优先采用简洁、熟悉的数值刻度（2026-07-14）

- 用户希望分层参数尽量从 `0.96、0.84、0.75、0.72、0.64、0.60、0.56、0.49、0.42、0.36、
  0.32、0.27、0.24、0.16、0.12、0.06、1.29、1.60` 等熟悉且容易辨认的数值中选取，避免
  使用 `0.948、0.793` 一类没有直接调参意义的幂函数展开值。
- 简化数值时仍须保持既定的物理职责、逐层单调关系和近中远层级，不为了凑数值而制造平台、
  反向增强或新的可见断崖。

## 水体是录音 dialog 的主视觉，允许使用更高 HDR headroom（2026-07-14）

- 用户确认该 dialog 最主要的内容就是水体/波浪动画，因此愿意把 HDR 期望 headroom 从当前
  `2.0×` 提高到 `3.6× reference white`，让真正受光的局部区域明显更亮。
- 镜面闪点峰值需要随新的 `3.6×` 上限重新分层；连续 HDR 银泽也允许适度提亮，但仍须低于
  最高闪点、限制覆盖和 APL，不能把整片水体变成大面积发光面。
- 实际输出仍应服从设备实时可用 headroom；不支持或余量不足的设备必须自然裁减，不得通过
  提高 SDR 基色补偿。

## 镜面闪点是水体流动感的主要载体之一，必须达到最高质量（2026-07-14）

- 用户确认当前镜面闪点是最可见的效果之一，并且能明显凸显水体流动感；后续不能把它当作普通
  装饰或只按数量参数处理，应作为最高优先级材质效果认真设计和验收。
- 质量评估必须同时覆盖：与真实坡面和太阳方向的对应、沿曲面的几何连续性、尺寸与亮度层次、
  HDR/SDR 质感、跨帧身份稳定、自然出生与消失、随水体运动的连续感以及远处抗锯齿。
- 扩展到第 3～6 层时只能逐层降低存在率和显著度，不能用矩形化简化、随机噪点、突然弹出/消失
  或其它会损害质感和流动连续性的办法换取覆盖。

## 水体材质效果必须跨九层连续衰减（2026-07-14）

- 第 0～2 层继续承担主要水体质感；第 3 层应比第 2 层稍弱，第 4 层再弱于第 3 层，之后随景深
  继续单调衰减，不能在“近三层”和其余层之间出现明显的效果断崖。
- 除最后两层外，其余层都应获得与其景深相符的弱化版水体效果；第 7～8 层可以接近不可见。
- 这一要求同时覆盖连续表面明暗、局部轮廓光学、离散闪点/光晕以及 HDR 超白亮度；不能只让
  参数表面上非零，却因候选门槛、全局竞争或其它门控而在实际画面中长期不可感知。
- 远层弱化不能反过来削弱第 0～2 层已经确认的主体表现，也不能重新引入宽域灰暗、脏纹或高频
  闪烁。

## 水体需要有限、保色、局部的阴影（2026-07-14）

- 用户在 D87 只提亮版本上确认水体仍偏平，允许重新加入阴影以恢复逐坡立体感。
- 新阴影必须保持水体干净：不能恢复 `blackMix`、灰黑色整层压暗、微法线脏纹或跨半个长波的宽域暗块；
  应保留环境/体积底色，只削弱局部背光坡的少量直射光，并朝 Thing 身份色派生深色变化。
- 该偏好取代下方 2026-07-14 记录中“只提亮不压暗”的临时限制；保色、封顶和远层不回脏的限制继续有效。

## 九层水体层间分明度采用 lighten_far 0.864（2026-07-14）

- 用户在 `0.96` 真机对照版后要求把默认 `lighten_far` 收到 `0.864`；远近水层继续通过比
  `0.60` 基线更大的混白阶梯拉开，但减少最远层过早接近纯白的风险。
- 本轮只改变该参数；坡面光、局部光学、水体 alpha 与 Python 的 320dp 宽度均保持不变。
- 这是旧材质的对照参数与历史验收记录；D119 已取消“景深必须逐层混白”的定义绑定，后续材质
  重构不再受 `0.864` 或混白方向约束。

## 水体需要干净，但不能退化为逐层平色（2026-07-14）

- 消除单层水体里的大范围灰暗块后，仍需保留轻微的“光贴波走”逐坡明暗，让水体基色具备连续
  立体感，不能只依赖几何轮廓、闪点、银泽和 SSS。
- 坡面体积优先使用保色、封顶、只提亮不压暗的方式；不得重新引入 `blackMix`、天空色混合或
  其它会让水体显脏、显灰的宽域压暗。

## Python 模拟器保持现有宽度（2026-07-13）

- `E:\projects\audioVisualizerSimulatorFable` 继续使用现有 `320dp×420dp` 容器，不改成 Android
  录音对话框的 `280dp×420dp`。
- 后续可以同步 Android 当前的映射、材质、颜色、效果裁决、ModernGL/共享 GLSL 与 HDR 数值
  语义；宽度差异作为模拟器的独立设计约束保留。

## 三项表层光学效果恢复到 Debug 202607130749（2026-07-13）

- 表面反射、薄峰透射和闪点外围解析光晕严格恢复 Debug `202607130749` 的几何、强度与
  着色器衰减；不恢复该版本中已关闭的 `body light`，继续保留当前
  `body_light_strength=0.36`。
- HDR 管线、分层峰值、颜色归一、更多窄闪点、远层景深阶梯和自动 SDR 回退保持不变，
  以便只比较三项表层光学效果。

## 薄峰透射在恢复基线上轻量收窄（2026-07-13）

- Debug `202607130840` 的其它宽材质恢复值保留，只把薄峰透射最大厚度由约
  `23dp` 收至约 `18dp`。`thin_glow_gain=0.55`、颜色、HDR eligibility 和分层峰值不变，
  用几何宽度单变量进行真机对照。

## HDR 已进入时恢复原有宽材质做对照（2026-07-13）

- 真机已确认 HDR 链路成功进入；下一版保留 HDR，把表面反射、薄峰透射和闪点外围
  解析光晕恢复到去雾收紧前的强度、几何与衰减，并恢复 `body_light_strength=0.36`
  进行真机对照。
- 本次不恢复已取消的固定偏冷色、周期色相旋转、远层 HDR 或顺流流光 HDR；
  水层景深阶梯、HDR 分层峰值和自动 SDR 回退保持不变。

## HDR 与水体通透感优化目标（2026-07-13）

- 希望 FableSol 支持真正的 HDR 显示能力，在兼容设备上提高可感知峰值亮度，而不只是把 SDR
  颜色整体提白。
- HDR 不能只出现在极少数闪点核心；正常观看时应有稍多一些、能够明确察觉的受光区域进入
  HDR，否则与当前 SDR 观感差异不足。增加覆盖时仍应保留层次，不能把整片水体一起提亮。
- 水体目标观感是更晶莹、通透、清澈；当前水体、波浪与光线显得偏冷灰、泛雾、对比不足，
  整体过于柔和。
- 目标不是完全去除雾感；需要保留适度的纵深空气感和九层相邻可辨的层序结果，不能因去雾而把
  各层主体收敛到一起。D119 已取代当时“越远越偏白、越透明”的固定手段约束。
- 同色提亮、压暗、色度控制、透明度及其它景深量都只是候选手段，必须按任意 Thing 颜色适配，
  不能预先指定其中一项全局取代现有阶梯。
- 后续优化应同时审视显示输出链路、色彩管理、亮暗结构、散射/空气透视、表面粗糙度与高光形态，
  不能把所有问题合并成单一的全局亮度增益。
- 已确认 HDR/SDR 共享几何、动画、身份色和 SDR 基线；具体 HDR 分层强度与覆盖对象继续在本轮
  问答中收敛。

## 解析光晕轻量强度为 0.21（2026-07-13）

- `analytic_halo_strength` 从 0.22 微调为 0.21；光晕范围、数学衰减、曲面内侧约束及其他三项
  质感参数不变。

## 本批四项质感优化一起实现（2026-07-13）

- 1/f 全局呼吸、微法线风梳纹理、朝阳 SSS 与解析光晕本批一起实现，不再逐项等待真机验收。
- 四项仍须保留独立强度参数，便于后续按观感单独调弱或整项删除。

## 双色深度散射轻量强度为 0.21（2026-07-13）

- Stage 2-2 的 `depth_scattering_strength` 采用 0.21；deep/subsurface 派生规则、浪峰收拢掩码、
  固定光向和其他水体参数保持上一版不变。

## GLES 表面边带不得比 Canvas 更厚或更白（2026-07-12）

- 表面软带、薄峰透光、波背阴影和远层羽化迁移到 GLES 后，感知厚度与亮度必须以 Canvas 定稿版
  为基准，不能因为 GPU 剖面不同而把每层轮廓都画成连续白边。
- Canvas 的 fade 带采用峰值 0.66 的半正弦透明度；GLES 必须保持相同峰值与积分光量。
- 在 GL 合成、颜色和光学语义尚未与 Canvas 对齐前，不得通过单独缩放表面软带或远层羽化的宽度、
  亮度、透明度来补偿整体观感；应先修复迁移差异，再基于相同参数比较。
- 2026-07-13 起，表面软带与远层羽化均恢复 Canvas 原始宽度和 alpha（100%）；此前 34%/62%
  与 15% 的 GLES 感知补偿全部撤销。

## 移除珍珠斑与猫爪暗纹（2026-07-12）

- 珍珠斑、猫爪暗纹不再属于 FableSol 的目标视觉，应从 GLES 主路径和红天空 Canvas 回退路径一起
  移除，不能只在当前后端隐藏。
- 对应的实体跟踪、阵风状态与事件映射也应删除，避免为不可见效果继续消耗 CPU。
- 镜面闪点与顺流流光保留；下一批继续表面软带、薄峰透光、波背阴影和远层羽化。

## 顺流流光不得越出水面轮廓（2026-07-12）

- 顺流流光是沿波面切线倾斜并随水流移动的高光，但几何必须只存在于水体内侧。
- 流光可以贴着波峰边缘滑动，不能因为完整椭圆跨越切线而有一部分露到空气侧；陡坡和宽流光
  同样必须遵守该边界。
- 长流光不能只沿中心点切线画成直线；必须随整段波峰曲率弯曲，避免两端从曲面穿出。
- 水面轮廓处应软入，不能用生硬裁切换来新的亮边或锯齿。

## 镜面闪点长高光同样不得越出水面轮廓（2026-07-12）

- 偏白、较亮、较长并随受光波峰移动的镜面闪点，与低透明度顺流流光是两个不同实体；诊断和
  回归必须分开，不能看到“斜高光”就默认属于流光。
- 镜面闪点也必须沿整段波峰曲率弯曲，并且只向水体内侧展开；不允许完整直椭圆的一半留在空气侧。
- 高光亮芯应柔和收拢，不能形成一条锐利、平直、像贴在曲面外侧的白线。

## OpenGL 失败回退必须显示红色天空（2026-07-12）

- OpenGL ES 未正常启用、EGL/GLSL/绘制失败或运行时切回 Canvas 时，Canvas 天空必须强制
  显示为纯红色，作为一眼可见的诊断标记。
- 正常 GL 路径不得出现该红色天空；不要只写隐藏日志或使用普通 Thing 色静态填充，让用户
  无法判断当前实际运行的渲染后端。

## 前后俯仰不得在中途硬截断（2026-07-12）

- 手机的完整 `−90°~90°` 前后俯仰都应有连续、单调的视觉响应。
- 可以为透视与水体惯性保留安全输出范围，但必须采用越接近极限越缓的软压缩；不得在
  `±55°` 或更早的观察角边界突然停止，也不得越过临界值后回退。
- 原始手机角度、用于水体惯性的角度和用于透视的观察角应分开表达，避免一个硬限幅同时
  截断多条视觉通道。

## 阴影必须保留记事颜色身份（2026-07-12；2026-07-14 修订）

- 浪面上的暗部不得出现脱离当前层当前色彩的灰黑或固定蓝黑色。
- 波背阴影、猫爪暗纹、宏观坡面与微表面负向响应，都应从当前位置已经应用景深阶梯和
  横向渐变后的主体色派生同色系深色；不得再用“混入一定比例黑色”表达变深。
- 变深优先降低感知明度并保持 hue 与原始绝对 chroma；仅在目标超出色域时受控压缩
  chroma，浅色和深色均不得因阴影自动变灰、变黑或换色相。
- 物理遮挡只削弱对应的直射光分量，不能把主体体色、环境反射、透射与 HDR 高光组成的
  最终像素整体乘暗；多条负向响应也不得重复叠加成脏斑。
- 阴影几何可以继续表达背光坡和局部暗纹，但颜色必须仍能辨认出当前层的记事色彩身份。
- 本节原有“当前层色混黑且混黑量随层深递减”的表述，已由 D126 的随层保色阴影合同取代。

## 录音完成后的文件名行与计时器等宽（2026-07-11）

- 录音结束后，上方重命名区域的 `EditText` 与 `.wav` `TextView` 组成的可见总长度，
  应与下方 `TimelyClockView` 一样长，不能更短。
- 两者共享同一组水平边距；文件名输入框弹性占用后缀之外的剩余宽度，后缀按实际内容宽度排列。

## 人声语义优先级（2026-07-11）

- 当前开始重点关注“人声是否存在”的准确检测，尤其是音乐混音中的唱歌、说唱、合唱与 spoken
  word 进出，而不是只区分 speech/music 或只看 top-1 音频类别。
- 目标平台以 Android 端侧实时分析为主；检测结果应能作为 FableSol 慢语义驱动，同时不得阻塞
  现有低延迟 DSP 与录音路径。

## 当前迭代优先级（2026-07-12 更新，取代 07-11 版）

- 当前主线：GLES 迁移三阶段（D41）——Stage 0 性能修复 → Stage 1 GLES 一比一
  复刻现有视觉 → Stage 2 逐像素视觉升级。
- HDR 成为明确目标后，用户偏好采用官方完整 HDR 支持的 SurfaceView，不继续承担
  TextureView 有限 HDR 的限制；API 26+ 应统一 SurfaceView，避免双容器分叉（D64）。
- HDR 应由运行时能力自动启用并自动回退 SDR，不增加正式用户设置项；诊断开关只保留在
  Debug 版（D65）。
- HDR 额外亮度只在正在录音时出现；准备态和停止态保持 SDR。状态切换应平滑改变内容增益，
  不得重建 Surface 或造成闪屏（D66）。
- 音量、onset 与节拍不直接乘到 HDR 增益；声音通过改变波形和光学条件间接改变高光分布，
  避免整片水面随响度过曝或闪烁（D67）。
- 不因尚未实测的 SDR 控件变暗风险预先限制 HDR 水面峰值或覆盖；先实现动画视觉上限，只有
  真机实际观感不好时再把控件问题独立处理（D68）。
- 最高 HDR 闪点核心可以趋近中性白但保留轻微 Thing 色，不能偏青白；中等亮度浪峰明显保留
  身份色，薄层透射保留最多身份色（D69）。
- HDR 不新增快速闪光或余辉包络，复用现有持久闪点的时间轨迹；若仍过柔，只独立调整统一包络，
  不维护 SDR/HDR 两套闪烁时序（D70）。
- SDR 不通过压暗中间调或高光肩部伪造动态范围；能力不可用时直接不生成超白增量，保持现有
  `<=1.0` 输出亮度（D71）。
- 去雾首步完全锁定环境和水层景深阶梯，只重组内部光学层；若仍显雾，再单独试验环境色，
  不把两类变化混在同一版中（D72）。
- “整体更亮”优先指局部高光峰值和主观晶亮感提升，不要求提高平均画面亮度；移除宽光后允许
  APL 小幅下降，且不得抬高环境或水体基色补偿，以免雾感回归（D73）。
- HDR 高光应有景深递减：近层使用完整峰值，中层保留较弱超白，远层基本保持 SDR；不得让九层
  相似的白色峰值削弱远近辨识（D74）。
- 需要更多高光时，增加近、中层的窄高光片段，而不是扩宽单个柔光；保留清晰核心和很弱的窄柔边，
  不得把整段浪峰画成连续白线（D75）。
- 顺流流光只承担运动与流向表达，保持 Thing 色系和 SDR 亮度；不得让长寿命移动亮带获得 HDR
  超白增量，HDR 预算只给表面反射、受光浪峰和镜面闪点（D76）。
- 高光、受光浪峰和顺流流光不再独立旋转色相，只沿当前位置的 Thing 色到中性白变化；保留亮度
  呼吸和出现频率变化，最深传播路径仅允许不超过约 `2°` 的轻微冷移（D77）。
- 首轮去雾只移除或收紧冗余宽光，不额外压暗波背、深水、Thing 基色或中间调；若仍显扁平，
  再把阴影加深作为独立 A/B，且不得变成脏灰或纯黑（D78）。
- 实施分两次 Debug 验收：先发布只含共享 SDR 材质去雾、色相归一和窄高光调整的版本；确认方向后
  再发布 FP16/scRGB HDR、分层超白峰值与自动回退，不能在首轮混合两类变量（D79）。
- **A6（声音驱动表达批）在 GL 计划整体完成后解冻**（D44，取代"视觉批目测通过
  后即解冻"的原约定）。A1~A4 的声音感知、映射和境状态系统仍保持现状。

## 性能与渲染架构（2026-07-12 grill 问答确认）

- 流畅验收：View 锁 60Hz，静置/说话/倾斜全程稳定 60fps（D38）；不追 120Hz。
- 新渲染路径必须 API 26+ 严格像素级一致（D40）——排除 AGSL 新特性，这是
  选择 OpenGL ES 的直接原因之一。
- 旧 Canvas 水体渲染在 GLES 验收后彻底删除（D42），不做长期双路径。

## 已否决的视觉方向（2026-07-11）

- A5.5 的“三段光学合成 + 组内层降权 + 向上展开的大面积浪顶平面 + 深度吸收 + 折射视差”
  整体观感不如此前 A5，用户要求完整回退。后续不得把这一整套方案重新应用；若借用其中单一手法，
  必须独立、小幅试做并与 A5 基线直接 A/B，不能再次同时改变层次、颜色和表面结构。

## 视觉质量红线（2026-07-11，升级调研问答中用户明确）

- **不要尖而窄的浪**：注入波包设最小宽度下限与峰形锐度上限，波高与宽度联动
  （高浪必须宽），任何"冲击性→峰形"映射（前端清单第 11 项）都在此约束内工作。
- **不要敏感的水位颤动**：水位通道只允许潮位式慢变（低通 + 死区 + 斜率限制）；
  快速能量一律进波场，不进水位。
- **宁少勿烂**：此前的水面粒子类效果实现质量不佳（用户 2026-07-11 反馈）。每个
  视觉手法必须在 Python 模拟器里先做到"单独看足够好"并经用户目测认可，才允许
  移植 Android；做不好的手法宁可整个砍掉，不上线降级版。
- **层内连续镜面不得显示为图案**（2026-07-15）：用户先后否决会扩缩的弯月光带及其固定
  微面片替代。各层内部不得出现规则或随机的斜短光点、短划、圆环、月牙或 patch；这些形态不是
  边缘 glint。无法获得自然连续观感时，continuous sheen 应整项关闭，保留独立 glint、折射、
  Beer、SSS、surface/streak 与 transmission。

## 发布

本功能的迭代改动：改完代码、`:app:assembleDebug` 编译通过后**直接发布阿里云 debug**
（建 `debug-updates/update-*.md` 日志 + `:app:publishDebugUpdate` 传入），不再单独询问是否发布。
与全局 preferences 的发布规则一致（2026-07-10 用户就本功能再次确认）。

## 重力与屏幕方向

FableSol 的手机重力行为应与迁移前的 Opus 保持一致：完全倒置时水体来到录音 Dialog 顶部；
录音 Dialog 存续期间禁止宿主 Activity 自动旋转，关闭后恢复打开前的方向策略（2026-07-10）。

## Python 运行环境

分析、测试和运行 `audioVisualizerSimulatorFable` 时使用 Conda 环境 `everythingdone`（2026-07-10）。

## 放大精细度与帧率

- 放大观察时，九层波浪轮廓不得暴露明显折线、像素阶梯或颗粒感；Python 与 Android 应保持同一套连续曲面重建语义。
- 精细度优化不得通过盲目增加物理网格、改变波动方程或降低既有光效来换取；应优先修正显示分辨率、渲染侧连续重建和边缘覆盖。
- 在效果一致或更好的前提下继续降低逐帧 CPU 分配、重复材质计算与无效 GL 工作，目标仍是稳定 60fps。（2026-07-15）

## 浪形连续性

已经塑形完成的浪不得因音头或快速音频参数在短时间内生硬改变自身形状；只能依据物理传播、重力、
阻尼、边界，或显式进入的新能量/浪涌自然变化。该要求同时适用于 Python 蓝本和 Android 移植
（2026-07-10）。

## 环境波与二维方向谱应减形变但保留丰富度（2026-07-23）

- 用户通过 Python 临时诊断开关确认，环境波与二维方向谱是已成形轮廓持续重组的两个主要来源。
- 两者完全关闭后波浪过于单调，因此目标不是删除，而是降低其对宏观轮廓和时间改形的影响，同时
  保留细小、不规则但连续的水面丰富度。
- 优先在 Python 端提供含义清晰、可实时调节的参数项，用于独立控制两者的几何强度与时间演化；
  默认值须通过同场景 A/B 后再裁决，不能直接把临时诊断布尔值移植为 Android 正式设置。
- 目测选值阶段先把环境波与二维方向谱的六个核心连续参数集中到单独实验面板，避免在原有
  “连续水面”和“环境与流动”分组之间来回查找；面板应提供推荐起点与原效果恢复按钮。
- Android 调参界面新增“浪形”组：上述六项以及现有所有直接改变水面几何轮廓的设置统一迁入；
  设置名称应直接说明可见影响、保持简短，不向用户暴露难懂的算法术语（2026-07-23）。
- Android 的“浪形”必须紧跟“连续水面”；Python 同步使用正式“浪形”组与同一套名称。当前六项
  初始值为 `0.42 / 0.27 / 0 / 0.96 / 0.25 / 0`，两端默认保持一致；背景小浪强度在短暂试用
  `0.54` 后恢复为 `0.42`。

## 默认参数选择（2026-07-23）

- Android 设置中“相机仰角 / 波背自阴影 / 主浪增强时间 / 背景小浪强度 / 水位上升预平滑 /
  涨落攻击 / 响度瞬态加成”的当前默认值分别采用 `36° / 0.84 / 0.84s / 0.42 / 0.54s /
  0.36s / 0.21`。
- Python 蓝本必须同步使用完全相同的默认值；只调整默认值，不改变参数范围、步长与运行公式。

## 调参栏目命名

- 调参 UI 避免使用用户难以判断含义的实现术语；原“感知前端”统一改名为“声音分析与灵敏度”，
  Android、Python 及多语言文案保持同一语义（2026-07-18）。
- Python 参数控制面板的每个一级组别都必须支持独立折叠与展开；初始保持展开，折叠只隐藏
  控件，不改变参数值、模拟状态或 JSON 预设内容（2026-07-23）。

## 实时音画耦合与巨浪（2026-07-21）

- 音画相关性以实时、因果分析为主，不以离线前瞻或针对单曲时间戳的硬编码换取命中。
- `LIFT` 与 `CLIMAX` 都是证据可持续状态；只要当前音乐证据仍成立，就不能因固定最长时长
  自动退出，尤其不能把 `CLIMAX` 截断为 4.5 秒。
- 巨浪应作为 `PEAK` / `CLIMAX` 中可重复出现的乐句重音，而不是一个高潮段最多固定出现几次；
  同时必须保留因果冷却和突发显著性，避免退化为周期性机械触发。
- 《Lose My Mind》约 0:52～0:53、1:09～1:10、2:19、2:38，以及《银花玉鉴逐人来》
  约 2:23 的强高潮应能触发巨浪；录音版本在自身覆盖到的对应段落也应有相近反应。
- 《Lose My Mind》母带的验收是**恰好四次**：上述四处必须全部命中，23.68 秒及其它
  任意额外触发均为误触发；录音版在其覆盖范围内也只保留与母带对齐的对应触发，不以
  19.4 秒、58.9 秒等错位命中抵充目标。
- Android 移植须保留现有 HDR、视觉质量和性能路径；本轮不移植可选的人声神经网络，
  只迁移实时 DSP 感知标定、七境、流速/水位耦合、巨浪与相关动画结合。
- 2026-07-22 的 48kHz 录音 `20260722145549-lose-my-mind.wav` 开头约有 2 秒安静，
  因而与母带目标时刻按约 +2 秒对齐；录音内容只覆盖歌曲前约 1 分 40 秒，验收只检查其
  实际覆盖范围，不要求后两个母带目标出现在文件之外。
- 调整顺序固定为先在 Python 的实时分析回放路径定稿，再同构同步 Android；不得绕到旧离线
  导演、按歌曲名或绝对时间戳触发。泛化检查优先使用 `audioVisualizerSimulator/assets`
  中其它原始音乐文件；其中既有旧 WAV 的采样率与当前采集链不同，不再用作本轮泛化样本。

## 说话响度分档与巨浪回归约束（2026-07-23，用户口述）

- 说话不设"类别上限"：小声/偏小声/正常/偏大声/大声要有明显可见的区别，且大声
  （喊）可以达到音乐场景级的动画效果；否决"说话整体压在温和区间"的类别封顶方案。
- 专用测试素材 `E:\projects\audioVisualizerSimulator\assets\20260723140911-不同的说话声音.wav`
  （120.7s，48kHz，capture 档），分段时间表：5s 小声、15s 偏小声、30s 正常、42s 偏大声、
  54s 大声、64s（1:04）快速说话正常声、80s（1:20）快速喊单字正常声、92s（1:32）正常速
  正常声、107s（1:47）正常速偏大声；段间有安静间隙。用户在设备上回放确认各段听感差异
  明显，要求新算法把这些差异反映到动画上。
- 巨浪严格回归用例（任何映射改动后都必须保持）：
  - Lose My Mind 原曲（assets 下 ncm）：约 0:52-53、1:09-10、2:19、2:38 触发巨浪；
  - `20260722145549-lose-my-mind.wav`：开头约 2s 安静、整体比原曲晚约 2s，且录到约
    1:40 即停——对应时间点（前两处）要有巨浪；
  - `20260722163150-Lose My Mind-全.wav`：时间轴与原曲一致，巨浪时间点也应一致；
  - 银花玉鉴逐人来 flac：约 2:23 高潮巨浪；`20260722163541-银花-全.wav` 时间轴与
    原曲一致，巨浪时间点应一致。

- 用户主要感知的三个维度是**水位、流速、波浪的尖锐度**（2026-07-23）；音画映射
  设计与验收要围绕这三项组织。巨浪不是固定高度的装饰事件：水位高低不同的触发
  语境应产生可分辨的巨浪级别。

- 波形（浪的形状/尖锐度）本轮不改（2026-07-23）：用户偏好圆润、宽平的波形，
  明确"不能尖锐"。新的音画映射不得新增任何"强度→更尖"的映射；浪形组参数
  （D186~D190）、注入峰形公式与"不尖窄"红线全部保持现状，强度表达只走
  "更高且更宽"（既有陡峭度红线内）。混合路由（人声主导度连续混合两套锚定）
  已确认采用。

- 测试纪律（2026-07-23，用户强调）：验证音画映射时必须**离线渲染图像/视频并
  主观查验动画效果**（水位/流速/波形/巨浪的实际观感），不能只看数字指标；
  `E:\projects\audioVisualizerSimulator\assets` 下全部音频都要过测。用户目标：
  本轮把音画匹配彻底搞定。
