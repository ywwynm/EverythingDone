# FableSol Debug 更新

## 2026-07-11 - C3 定稿优雅档 + 移除诊断 + 修复应用内更新日志缺失

### 变更

- 真机确认纹理采样返工后三个 AGSL shader 全部存活（天空恢复正常、夸张档
  效果可见）。C3 回优雅档定稿：`absorption_gain` 0.80→0.35、`caustic_gain`
  1.0→0.5；移除天空诊断色代码。
- **修复应用内更新日志缺失**：`:app:publishDebugUpdate` 的 releaseNotes 依赖
  gradle 属性 `-PdebugUpdateNotesFile=<日志路径>`，此前几次发布未传该属性，
  latest.json 无 releaseNotes 字段，应用内因此看不到日志。本次起发布命令
  恒传该属性（调用规范已写入 `.claude/rules/gradle.md`）。

### 阶段 C 最终状态

- C1 渐变抖动（OLED banding 根治）+ C2 软带逐像素（连续钟形剖面、GPU 光栅）
  + C3 深度吸收（0.35）与焦散（0.5）全部存活并定稿；折射视差暂缓待用户裁决。
- 轮廓数据统一经 RGBA_F16 216×1 位图纹理采样（AGSL 禁 uniform 数组动态索引
  的合法等价物），位图帧内池化，稳态零分配。

### 验证

`:app:testDebugUnitTest`、`:app:assembleDebug` BUILD SUCCESSFUL；未使用 adb。
真机验收：光丝与深度感回到克制优雅、应用内能看到本条更新日志。
