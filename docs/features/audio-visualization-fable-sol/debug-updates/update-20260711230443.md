# 录音完成后的文件名行与计时器等宽

## 本次更新

- 用户反馈：录音结束后，上方重命名区域的 `EditText` 与 `.wav` `TextView` 组成的总长度短于下方 `TimelyClockView`，要求两者一样长。
- 诊断确认停止录音动画只改变纵向位置，不改变横向尺寸；差异来自文件名行内部把输入框和后缀分别固定为 `192dp`、`40dp`，而 `.wav` 文本不会填满固定后缀宽度。
- 修改 `app/src/main/res/layout/fragment_record_audio.xml`：文件名行改为与 `TimelyClockView` 相同的 `match_parent + 24dp` 两侧 margin；输入框改为弹性占据剩余空间；`.wav` 后缀按实际内容宽度排列。

## 验证

- XML 结构检查确认文件名行与 `TimelyClockView` 的宽度、起始 margin、结束 margin 完全一致。
- 已执行 `:app:assembleDebug --console=plain --no-configuration-cache`，构建通过。
- 已执行 `:app:publishDebugUpdate`，发布阿里云 debug 版本 code `202607111505`；远端 `latest.json` 已确认 APK 地址与 SHA-256。
- 本轮未使用 adb，未安装到物理设备或模拟器。
