# FableSol 三项表层光学效果恢复到 Debug 202607130749

## 用户要求与本轮修正

- 用户在 HDR 已确认进入、并先后试过宽材质恢复与薄峰收窄后，要求把表面反射、
  薄峰透射和闪点外围光晕重新恢复到 Debug 更新码 `202607130749` 对应的观感。
- 本轮只恢复这三项，后来单独加回的 `body light` 继续保留；HDR 管线、分层峰值、
  颜色归一、更多窄闪点、远层景深阶梯与自动 SDR 回退均不改变。

## 实现内容

- `FableSolMaterialPolicy`：表面反射带恢复为近层最大约 `9.4dp`；薄峰透射恢复为
  满强度最大约 `11dp`；解析光晕几何恢复为核心长度 `1.18×`、厚度 `2.25×`、
  alpha 系数 `0.18`。
- `FableSolParams`：`thin_glow_gain=0.38`、`analytic_halo_strength=0.10`；
  `body_light_strength=0.36` 保持启用。
- `optical.frag`：解析光晕恢复为 `exp2(-6.5*r²)` 衰减和 `0.72` 边界软出。
- Canvas 诊断回退与 GLES 主路径继续共享同一材质策略，避免两条路径出现参数差异。

## 验证

- 定向材质、光晕、光学路径与 shader 契约测试通过。
- 完整 `:app:testDebugUnitTest` 共 106 项测试，0 失败、0 错误。
- environment、water、optical、present 四组 GLES 程序均通过 `glslangValidator` 链接。
- `:app:assembleDebug` 成功，APK 内的 `optical.frag` 已核对为本轮恢复值；
  `git diff --check` 无空白错误。

## 发布

- 已通过 `:app:publishDebugUpdate` 发布阿里云 Debug `202607130907`。
- APK：`http://120.25.194.207/everythingdone-updates/debug/apk/app-debug-202607130907.apk`
- SHA-256：`151f6f70cff9a9c1e96495274db379c1d85b1456173999de3a245f703f06a50c`
- 文件大小：`21802689` 字节。远端 `latest.json`、APK HTTP 状态与 Content-Length
  已回读核对一致。
