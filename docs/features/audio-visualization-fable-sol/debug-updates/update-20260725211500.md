# FableSol 调参更新 2026-07-25（二）

## 性能面板改为设置开关，默认关闭

- 设置里"音频海浪动画参数调节"Dialog 的末尾新增"调试"组，内含"屏上性能面板"开关：控制录音界面顶部的帧率与分段耗时面板，默认关闭，改动即时保存，下一次打开录音界面生效。
- 上一版为定位掉帧而临时常开的性能面板从本版起不再默认显示；需要读数时到设置里打开即可。面板关闭时连读数的统计格式化都不会执行，不留任何开销。
- 该开关只在 debug 版存在；正式版没有这一组，面板代码也会被编译期整段移除。本版没有任何视觉与水体行为改动。
