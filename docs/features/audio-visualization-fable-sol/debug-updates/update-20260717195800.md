## 海浪动画性能优化合集：最高 120fps + 渲染调度修复

- 录音界面与调参界面的海浪动画跟随屏幕刷新率渲染（上限 120fps）：
  高刷设备上系统会切入相应的高刷模式，60Hz 设备保持既有节奏；水体
  速度与画面内容完全不变；
- 海浪 GL 渲染线程提升为显示级优先级，多线程水面采样改为小块工作
  窃取分配，避免多核调度把逐帧关键路径放到慢核；
- 水面采样与网格构建并行化、渲染状态去重，逐帧 CPU 成本下降；
- 全部优化不改变任何视觉效果：确定性对照下优化前后输出画面逐像素
  一致；系统省电或发热降频时动画自动回落到相应刷新率。
