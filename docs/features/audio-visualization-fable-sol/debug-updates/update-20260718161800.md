## 海浪动画：调参项继续精简（"注入"组 12 项固化进实现）

- "注入"组 12 项调参（注入增益/单浪高度/浪宽上下限/渐入时长/节奏波包
  强度与门槛/顺流偏置/级联延迟/远浪触发三项）按既有默认值固化进实现：
  连续水面主路径只消费九层高度的逐层均值，音头行波的空间形态本就被
  均值化，这些滑杆调节无可感变化；
- 注入机制本身完整保留（音头点击/波群/重音浪/稀有远浪照常工作），
  行为与画面零变化；调参 Dialog 的"注入"组整组消失，界面更精简。
