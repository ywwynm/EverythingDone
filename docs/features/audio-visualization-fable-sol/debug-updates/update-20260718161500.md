## 修复：上一包海浪动画卡顿且效果异常（GL 渲染崩溃回退）

- 修复上一包（202607180728）海浪动画明显卡顿、观感异常的问题：删除
  表面光学参数时，水面片元着色器里 uTimeSeconds / uSurfaceHeadingRad
  两个 uniform 失去了唯一使用者，被 GLES 链接器裁剪；渲染器仍按名查询
  导致 GL 路径每帧崩溃，静默回退到 Canvas 软件绘制（画面特性不同且
  性能差）。现已删除死 uniform 的声明与上传，GL 硬件渲染路径恢复；
- 新增静态守护测试：渲染器查询的每个 uniform 必须在对应 shader 正文中
  被实际使用，防止同类回退再次发生；
- 上一包的参数精简内容不变：17 项归零无感的表面光学参数与对应功能
  已移除，厚度透光、波峰银边、天空反射、HDR 等保留项观感不变。
