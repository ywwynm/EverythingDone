# FableSol 调参更新 2026-07-25（三）

## 性能面板改为设置开关，默认关闭

- 设置里"音频海浪动画参数调节"Dialog 的末尾新增"调试"组，内含"屏上性能面板"开关：控制录音界面顶部的帧率与分段耗时面板，默认关闭，改动即时保存，下一次打开录音界面生效。
- 修正该开关行右侧勾选框的按压水波纹：现在跟随当前记事配色的渐变，不再是系统默认的半透明黑色。
- 该开关只在 debug 版存在；正式版没有这一组，面板代码也会被编译期整段移除。面板关闭时连读数的统计格式化都不会执行，不留任何开销。本版没有任何视觉与水体行为改动。
