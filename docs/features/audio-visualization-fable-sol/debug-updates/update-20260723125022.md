## 背景小浪强度默认值恢复为 0.42

### 用户要求

- 将 Android 设置中的“背景小浪强度”默认值从 `0.54` 恢复为 `0.42`。
- Python 蓝本同步使用 `0.42`。
- 修改完成后发布阿里云 Debug 更新。

### 实现

- Android `FableSolParams.kt` 的 `ambient_gain` 默认值已改为 `0.42`。
- Python `src/wavesim/params.py` 的同名参数默认值已同步为 `0.42`。
- 两端设置目录与面板回归已更新，锁定 `ambient_gain=0.42`；其余环境波、斜向浪和 D188
  中另外六项默认值保持不变。
- 本轮只修改新建参数容器使用的默认值，不改变取值范围、步长、浪形分组或环境波计算公式。
  已有 Android SharedPreferences 和 Python JSON 预设仍优先于默认值；需要查看新默认效果时，
  可使用恢复默认或新建配置。

### 验证与修正

- Python 参数目录与设置面板专项 22 项通过；Android 设置目录专项通过。
- 首轮 Python 全量回归发现一项宏观阴影着色器隔离测试使用了产品默认环境波强度来建立像素
  覆盖门限。默认值降低后，受影响像素从要求的 `>200` 变为 `191`，但着色器公式没有变化。
- 该测试已改为显式固定 `ambient_gain=0.54`，使它继续只验证阴影着色器语义；原像素门限没有
  放宽。修正后单项与完整回归均通过。
- 最终 Python 全量 341 项及 26 个 subtests 全部通过；Android 完整 JVM 单测通过。
- 两个仓库的 `git diff --check` 均无错误。
- 未使用 ADB，也未连接物理设备或模拟器。

