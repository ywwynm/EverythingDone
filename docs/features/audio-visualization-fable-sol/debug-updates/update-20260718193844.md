## 海浪动画：恢复波背自阴影（默认开启）

- 早前参数精简时删除的"波背自阴影"应要求恢复：背光坡面沿波脊出现的
  随层保色暗带（本层色降明度派生，不发灰、不混黑），让波浪重新读作
  "亮脊紧贴暗窝"的滚动体积；
- 默认强度恢复为 0.80，即装即生效；可在调参 Dialog 的"外观与光学"组
  0~1.2 连续调节，归零即完全关闭；
- 与删除前的唯一差异：已删的"空气透视压缩"按其归零值固化，暗带在远层
  不再额外变淡一档，其余逐位一致；
- 底层为共享 shader 的既有 mode 9 剖面，GL 主路径与 Canvas 回退同步
  恢复，无性能变化。
