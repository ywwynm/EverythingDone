## 海浪动画调参界面：暂停下实时调参 + 按钮行间距修正

- 暂停现在只冻结波浪运动与声音驱动，画面仍持续渲染——冻结画面上
  拖动质感、外观与光学等渲染类参数、切换 HDR、切换颜色都能实时看到
  效果（与桌面模拟器的暂停语义一致）；影响波浪运动方式的参数（环境
  与流动、主浪、涨落、注入、段落）仍需继续播放后才有表现；
- 暂停中切换颜色不再自动恢复播放，静止波形上同样能看到新颜色从
  右侧涌入；
- "恢复默认/确定"按钮行与上方分隔线的间距改用带分隔线场合的标准
  间距（与选择应用语言弹窗一致），上下不再失衡。
