## FableSol：新增“声音分析与灵敏度”调节

- Android 新增“声音分析与灵敏度”栏目，可调自校准窗口、静音门和动态扩展；调节会在当前录音会话的下一批音频开始生效，并保留到下次打开；
- Python 同一栏目改用更清楚的名称，离线分析和 `--sim-audio` 现在也会读取这三项参数，不再只对实时分析生效；
- Python 离线分析按参数区分缓存，滑杆停止 350ms 后再触发重分析，避免拖动时反复计算；
- 默认参数保持原有声音分析结果；Android 全量 JVM 测试和 Python 164 项测试均已通过。
