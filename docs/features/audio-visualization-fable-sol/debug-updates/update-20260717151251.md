## 海浪动画调参界面三轮反馈修复

- 预览右上角两个按钮去掉圆形衬底，图标带一点透明度，亮色模式下
  不再是实黑；
- "表面亮带"默认归零（与模拟器视觉对齐，仍可在列表中拉回）；
- 去掉最下方"系统"分组；
- 修复切换颜色时的动画卡顿：换色过程中只实时更新可见的滑杆颜色，
  动画结束后一次性补齐其余；
- 修复参数列表在顶部/底部触发拉伸回弹时预览画面掉帧的问题（改用
  上下分隔线提示到边，不再使用系统拉伸特效）。
