## 海浪动画：恢复闪点（"闪点数量"调参重新生效，默认仍关闭）

- 早前删除表面光学参数时，闪点的出生场随镜面反射项一并失效，"闪点
  数量"成了无效调参。应用户要求恢复：镜面反射项以既有强度写回出生场
  （坡度对准光源的镜面瓣 × 毛细碎裂），毛细曲率通道同步恢复；
- "闪点数量"默认仍为 0（画面与上一包完全一致）；在调参 Dialog 的
  "质感提升"组把它拉起即可看到阳光碎闪沿太阳路径在浪面上生灭，
  支持 0~1 连续调节；
- 新增出生守护测试：默认 0 必须无闪点、拉到 1 必须真实出生，防止
  该链路将来再次静默失效。
