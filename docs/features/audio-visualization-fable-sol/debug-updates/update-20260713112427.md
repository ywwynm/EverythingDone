# FableSol 整项回退 Stage 2-1 高光压缩

## 本次更新

- 删除 PBR Neutral 高光压缩及其最终 fullscreen 合成 pass。
- 删除对应的 RGBA8 离屏 framebuffer、纹理、参数和专项测试。
- 恢复 Stage 1 已验收的默认 framebuffer 直接输出，高光不再被额外压暗。
- 保留 Stage 1 的 GLES/Canvas 观感对齐、光学效果和 EGL 生命周期修复。

## 验证

- 73 项 Debug 单元测试全部通过，无失败或错误。
- Debug APK 构建通过，并确认不再包含 `tone_map.frag`。
- 未使用 adb。

已发布阿里云 Debug `202607130324`。
