# 决策记录 · audio-visualization-fable-sol

## D150 用保持形状的 C1 深度插值消除纵向轨道造成的小尖峰（2026-07-16）

指定音乐的 14.0 秒 `L6/L3` 与 112.0 秒 `L7` 三个用户确认区段证明，真实缺陷不是音频高频、
`AmbientSet`、`HeroWave`、`DynamicWave`、196 列显示重建或抗锯齿。根因是连续水面把
`z + orbit_z` 映射到九层均值时使用分段线性插值：每条锚行恰好位于整数层节点，平滑的纵向轨道反复跨越节点时，
节点两侧不同的一阶斜率会被投影成真实的小冒尖和轻台阶。

Python 与 Android 的 GL/GLES 和 QPainter/Canvas fallback 四条路径统一改用等距 Fritsch–Carlson/PCHIP 三次
Hermite 深度剖面。它严格命中九个层锚点、在节点处保持 C1、对单调输入不超调；只替换层均值随深度的基线插值，完整保留
packet 的高度、`orbit_x/orbit_z`、音乐状态、光学高频、顶点数、4x MSAA、shader 与 HDR/SDR 亮度策略。

真实音乐修复后，三个确认区段的 95% 截尾空间 jerk 为 `0.000797 / 0.001322 / 0.000936dp⁻²`，均低于
`0.00145dp⁻²`；三个用户否定区段也保持通过。上一轮 D148 的 L7/L8 环境波加宽由错误的静默全层指标得出，既未解决
真实样本又改变宽浪构图，因此撤回并恢复 `84/72dp`；D150 取代 D148 作为本问题的定因与修复决策。

## D149 Python 参数面板只保留当前有效参数（2026-07-16）

逐项追踪 `GLOBAL_SPECS` 的生产消费者并做运行时消融后，删除七个对当前输出完全无效的参数：
`pearl_shift_deg`、`crest_width_dp`、`hue_temp_deg`、`analytic_halo_strength`、`color_breath`、
`hero_punch`、`hero_punch_decay_s`。其中两个 punch 参数对应的状态也一并删除；快速事件继续只走
`DynamicWave` 物理注入。`lighten_far` 在 `0.864` 已达到既定远层白混合上限，因此滑块最大值收窄到
`0.864`，不再暴露无效果区间。

`crest_on` 虽不影响默认 ModernGL 连续水面，但仍控制 QPainter fallback，故保留。感知前端与段落链已经接通，删除
“M3/M4 接入后生效”的过时提示；“连续水面原型”改名为“连续水面”。旧 JSON 预设中的已删除键由现有
`Params.from_dict()` 忽略，不影响加载。

## D148 远两层环境波改为宽缓波长，消除连续小波峰聚集（2026-07-16）

固定静默输入即可复现图中最远两层的连续小波峰，因此它不是音乐频率或节奏设计。单变量消融确认根因是 L7/L8 的
`ambient_len_dp=84/72dp` 过短：随机谱还会生成约 `42.77～80.93dp` 的短环境波，远景透视压缩后形成锯齿状峰群；
Hermite 重建只放大约 1.7%，不是根因。

Python 与 Android 同步把 L7/L8 基准波长改为 `120/132dp`，保留相同环境波数量、振幅、流速、音乐驱动和抗锯齿路径。
20 秒静默扫描中，L7 的局部高频 RMS/峰谷差由 `0.134/0.504dp` 降至 `0.059/0.305dp`，L8 由
`0.311/1.375dp` 降至 `0.059/0.213dp`。该修改不增加逐帧计算量。

## D147 撤销 Python 表面反射软带试验，并把新增配色与方向绑定（2026-07-16）

用户最终判定 `surface_strip` 表面反射软带会让水面显得杂乱。Python 因此完整删除 QPainter/ModernGL 的软带发射链、
HDR excess 策略和 `surface_strip_on`、`surface_strip_gain`、`surface_strip_facing_threshold`、
`surface_strip_curvature_boost` 四个控制项；独立 `flow_streak` 流光保留。Android 从未同步本轮软带试验，故不改其既有
共享 optical mode 4。本决策取代 D142、D144、D145、D146 的恢复与待同步方案，也不恢复任何灰黑脏阴影。

Python 配色注册表改为“名称、颜色、默认方向”三元组；在保留原有 12 项的基础上加入 10 个 Thing 纯色与用户指定的
7 个其余配色，共 29 项。点击配色会同时应用其声明方向；方向 3 在 QPainter 与 ModernGL 统一纠正为
“左上→右下”。默认绯紫继续作为既有项，方向纠正为“左→右”，不重复登记。

## D146 Python 软带 HDR 峰值恢复 7 月 13 日标尺，既有强度同时控制 HDR excess（2026-07-16）

用户确认最初参考图来自 HDR 模式，并判定当前约 `3.0× reference white` 的软带核心明显过亮。原 PNG 只是无 HDR
色彩块/配置文件的 8-bit RGB 截屏，只能判断 tone mapping 后的形状与观感；绝对标尺改以 2026-07-13 21:26
构建的历史策略和 raw FP16/scRGB 输出为准。该构建的 mode 4 软带逐层峰值是
`1.40/1.36/1.30/1.22/1.14/1.08/1/1/1`；当前
`3.20/2.70/2.24/1.96/1.60/1.29/1.18/1.08/1` 是 7 月 14 日才引入的后续增亮，不属于参考图。

Python 因此只把 `SURFACE_REFLECTION_PEAKS` 恢复到 7 月 13 日曲线；全局 `3.6×` headroom、glint 与 transmission
预算均不改。为保持 D145“不增加重复强度参数”的决定，既有 `surface_strip_gain` 现在同时控制 SDR alpha 与 HDR
excess：有效峰值为 `1 + (historicalPeak - 1) * clamp(gain / 1.2, 0, 1)`。增益只缩放能量，不进入
eligibility/smoothstep，故不会改变软带的空间支持、动态出现位置或抗锯齿形状。

固定 `#00FFF6 → #4B7ADB` 场景的 L0 隔离 raw FP16 峰值由 `3.010×` 降为 `1.214×`；历史 shader 重建约
`1.23～1.25×`。默认/半强度的 HDR 增量比例约 `1:0.5`，SDR 对不同 HDR 峰值逐字节一致，on-off 增量没有负亮度
脏影。当前仍只修改 Python，等待真 HDR 目测确认后再同步 Android。

## D145 Python 软带使用独立控制组，QPainter 流光与软带解耦（2026-07-16）

`surface_strip_gain` 并非本轮新增：它由 2026-07-11 的 `be72b42` 引入，提交态默认值为 1.0；D144 只按用户选择把默认值改为
1.2。它控制的就是同一条 `surface_strip` 表面反射软带，因此保留并重命名为“峰值强度”，不再增加重复的第二个强度参数。

Python 参数面板新增独立“表面反射软带”组：`surface_strip_on` 明确开关、既有 `surface_strip_gain`、
`surface_strip_facing_threshold`（迎光出现门槛，默认 0.04）和 `surface_strip_curvature_boost`（波峰曲率增强，默认 0.55）。
门槛沿固定 0.54 facing 过渡宽度平移；曲率增强继续使用永不成为硬门的
`1 - boost + boost * sqrt(smoothed_crest)`。四项由 ParamSpec 自动接入控件、重置与 JSON 预设。

QPainter 原先把独立 flow streak 嵌套在 `_draw_surface_strip()` 内，造成白带开关/强度错误控制流光。本轮拆成独立路由：
白带开关只影响 mode 4 表面反射，`flow_streak_gain` 继续单独控制流光；ModernGL 原本已经分离，现用测试锁定两端语义。

关闭连续水面后的卡死与材质公式无关。`dd92c90` 删除了 `_draw_flow_streaks()` 内的局部 `QRadialGradient` import，却保留
实例化；第一个进入可见寿命的流光因此抛出 NameError。HDR legacy 路径又只在正常返回时 `painter.end()`，随后级联为
`QPaintDevice: Cannot destroy paint device that is being painted`。修复恢复顶层 import，并让 VisCanvas、GlVisCanvas 与 HDR
离屏 QPainter 均用 `finally` 释放绘制设备，清理后仍继续抛出原始异常而不吞错。

## D144 Python 表面反射软带采用方向性法线响应，100% 白度与 1.2 仅作为局部峰值（2026-07-16）

用户选择 `100% 中性白 / surface_strip_gain=1.2`，同时否决各层整条常亮。Python 的 `surface_strip` 因此不再把
D83 的基础宽度直接画满整条轮廓，而是由同一局部门控同时调制宽度和 alpha：先按主光所在侧翻转波面坡度，再用连续
smoothstep 得到方向性 facing；facing 低于阈值时严格归零。平滑后的上凸曲率只通过
`0.45 + 0.55 * sqrt(crest)` 柔和增强，不承担硬开关，避免恢复 D86/crest-edge 式只认波峰的窄线。

该响应完全来自逐帧水面几何与光源方向，不再叠加 `pink_mod` 独立呼吸，也不使用随机门控；因此符合条件的 L0～L7
都可以随波浪移动、缩短、消退和重新出现，L8 继续保持既有零权重远景哨兵。颜色峰值固定为纯白，gain 默认改为 1.2；
HDR eligibility 继续使用更严格的 facing、crest 与动态衰减交集，SDR/HDR 都只增加能量，不生成负亮度或脏阴影。

ModernGL 与 QPainter 使用同一方向性公式；共享 optical shader、4× RGSS 和场景 4× MSAA 均未修改。当前仅完成 Python
验证，Android 暂不改动，待用户确认动态观感后再做同构迁移。

## D143 完整撤回 Python 的 crest-edge 遗留实验，并恢复 thin-glow 基线（2026-07-15）

用户确认 `crest edge` 是忘记撤回的独立实验，要求全部移除。全仓审计确认 Android 与共享 GLSL 本来就没有
`crest_edge`、`uCrestEdge*` 或 `bodyEdgeW` 实现；遗留仅存在于 Python 未提交工作区，且链路不完整：五个参数和五个 uniform
上传没有 shader 消费者，front-fill 顶边坡度/曲率载荷在当前 shader 早退分支中也不参与像素输出，测试却错误断言不存在的
`bodyEdgeW`。

Python 因此删除五个 `crest_edge_*` 参数、五个 uniform 上传、front-fill 的实验性坡度/sheens/曲率载荷及对应测试改写；同批被抬高的
`thin_glow_gain` 从 `0.55` 恢复为 Android 与已提交 Python 基线的 `0.38`。保留正式 `crest_glint`、`crest_glow`、
`crest_veil`、`crestPinch`、`thin_glow` 功能本身，以及本轮独立的 `surface_strip` 连续软带恢复。文档保留历史并明确标记撤回，
不再把 crest edge 当成可用候选。

## D142 Python 首轮只恢复连续表面带几何，保留当前干净着色与局部 HDR 资格（2026-07-15）

用户确认目标为 `surface_strip` / optical mode 4 后，先只在 Python 做单变量试验，不同步 Android。Python 专属
`clean_surface_band_width_dp(s)` 恢复 D83 的连续内展公式：
`(1.2 + (5.8 + 2.4 * crest) * facing) * (1 - 0.45 * depth)`，近层因此重新具有 `1.2dp` 连续基础宽度，
迎光波峰最大约 `9.4dp`。Android 共享 `surface_band_width_dp()` 仍保持当前约 `3dp` 的局部门控，避免在用户目测确认前扩大改动范围。

本轮不整体回滚 D83：继续保留当前位置本层水色的身份色着色、D111 逐层 alpha 衰减、轮廓下方 `0.2dp` 单侧半正弦剖面，
以及 ModernGL 的 `facing * crest_gate` HDR eligibility。尤其不恢复 `environmentHorizon` 混色、旧宽 HDR 资格、`blackMix`、
纵向明暗宽域或任何灰黑阴影。这样 SDR 可以得到连续软带，而 HDR excess 仍只出现在迎光波峰。

固定蓝青中高能场景证明几何恢复后形状存在，但当前仅 `+0.045 OKLab L` 的颜色肉眼仍明显弱于历史截图。诊断脚本另外生成
`0% / 18% / 32% / 46%` 向中性白推进的非生产候选；该轮随后被用户判定整体仍暗于原截图，未写入正式渲染路径。

## D141 光学 pass 逐像素程序化形状用 RGSS 超采样；亚 glint 核心保持锐利不为 SDR clip 让步（2026-07-15）

## D141 光学 pass 逐像素程序化形状用 RGSS 超采样；亮 glint 核心保持锐利不为 SDR clip 让步（2026-07-15）

D140 的 MSAA 只多采样**几何覆盖**，不重算片元着色。glint、streak、surface reflection、halo、
transmission 等光学实体的**形状是在 `optical.frag` 里用 `smoothstep`/`sin` 逐像素程序化算出来的**，
MSAA 完全不抗这类形状边缘的锯齿。放大真机截图确认 glint 附近仍有残余锯齿。

共享 `optical.frag` 因此对光学 pass 单独做 4x 旋转网格超采样（RGSS）：覆盖计算抽成
`opticalCoverage(vec2 uv, out ...)`，单样本最终预乘输出抽成 `shadeOpticalSample(vec2 uv)`，
`main()` 用 `dFdx/dFdy(vLocalUv)` 把四个子样本放在四个不同的 x 与 y 子位置再平均。glint 多为近
水平光带，RGSS 的纵向分辨率明显优于轴对齐 2x2。实体较大时导数极小、四点几乎重合，输出与逐像素
一次着色一致，不改变既定 glint 尺寸、剖面、峰值与观感；D129 界面肩剖面、mode 判定和 HDR excess
逐层峰值合同均不变。只用 GLES 3.0 核心的片元导数，无需 sample shading 或任何扩展；光学实体只占
少量像素，4x 片元开销可忽略。两端共享同一 shader，一次修复覆盖 Android 与 Python。

诊断同时确认另一类残余**不是缺少抗锯齿**，而是 HDR 超白 clip 到 SDR 的固有现象：准备态（SDR）的
反射很淡、平滑无锯齿，只有录音态（HDR）核心变亮（峰值约 `2.3× reference white`）才出现台阶。薄而
亮的 glint 线性强度梯度陡，SDR 可见边（线性越过约 `1.0` 处）本质是亚像素宽，`avg(线性)` 后再 clip
无论多少采样都无法抗掉——clip 发生在着色下游的显示/截图 SDR 映射。这在真机 HDR 屏上更柔和，只在
SDR 截图里明显。用户裁决：**保持 glint 锐利/亮度，接受该固有残留，不为 SDR clip 展宽、柔化或压暗
最亮核心**，以维持 D103～D118 的最高质量 glint 合同。若将来要进一步减轻，只能作为独立的 glint 观感
改动单独裁决，不属于抗锯齿修复。

## D140 场景离屏改用 4x MSAA 消除放大后的波浪边缘锯齿（2026-07-15）

放大真机截图后，九层弯曲界线、水天轮廓和光学几何仍呈明显阶梯锯齿与颗粒。根因是整条离屏链
（Python 与 Android）都渲染到**单采样** FBO，没有任何多重采样；D139 的 `waterEdgeCoverage`
只是一条 `±0.5px` 的 depth01 平滑，方向盲、宽度不足，无法解决近水平弯曲界线上的横向台阶。
D139 的原生 DPI 修复实际只作用于 Python：Android 的 `uRasterScale` 恒为 1，早已按 surface
实体分辨率渲染，因此那轮改动对 Android 锯齿基本无效。Python 离屏对照证明该锯齿是纯采样不足：
2x2 超采样与 4x MSAA 都能直接消除台阶，而单采样加 coverage 不能。

两端场景离屏改为 4x MSAA：几何画进多重采样 renderbuffer，再 resolve 进单采样 scene_texture，
折射与 present 继续采样这张已 resolve 的纹理。`pre-water` 折射背景保持单采样（只是平滑环境，
且是折射采样源，不需要也不能接 MSAA）。九层主体、界面、光学、HDR 的材质与颜色仍逐像素**只计算
一次**：MSAA 只对几何覆盖做多采样。九层界线是几何边（每组三角带的远边压在其后一组之上），因此
逐像素一次着色的 MSAA 足以修复，无需 per-sample 或超采样着色。`waterEdgeCoverage` 保留，MSAA
之上只再叠加约 1px 的轻微软化，不改变既定视觉合同。

采样数按内部格式查询后取 `min(4, 支持值)`，保持与 sceneTexture 同格式（SDR RGBA8 / HDR
RGBA16F），维持 D134 的 FP16 精度语义。不支持该格式多采样或建立失败时原子回退单采样，仍保留
D134 的 FP16→RGBA8 目标回退与不改写输出颜色空间的契约；即回退顺序为
`请求格式 MSAA → 请求格式单采样 → RGBA8 MSAA → RGBA8 单采样`。Android 走可移植的
`glRenderbufferStorageMultisample` + `glBlitFramebuffer` resolve（GLES3.0 保证），未依赖
`GL_EXT_multisampled_render_to_texture`（Java 无绑定）；对话框 surface 很小，4x resolve 带宽
可接受，多采样数据位于 tile 内存。这与 D139 反对的 pre-water FP16 Blit 不是同一路径：pre-water
仍是两目标各画一次环境，不做 Blit。效果指标经 18 色 FP16 回归确认未变（远/近响应比、相邻主体
色差、超白覆盖不动），Python `build_gl_frame` 与桌面 GPU 完成时间几乎无回退（约 +1.4%）。

## D139 物理网格保持 216 点，显示侧使用 196 列 C1 重建与实体像素边界覆盖（2026-07-15）

放大观察暴露的颗粒和折线不能通过盲目增加物理点数解决。`N_POINTS=216` 覆盖包含画外余量、
海绵区和注入区的完整物理域，屏内只会看到其中约 96～126 点；提高它会改变 `DX_DP`、CFL 上限、
传播速度和既有波形。两端因此继续固定 216 点物理模拟与 97 行纵深网格，只把显示侧横向网格从
最多 120 列改成固定 196 列。高度和横向坡度使用物理节点值/导数做 Hermite C1 重建，水平轨道与
纵深坡度使用 Catmull–Rom；轨道越过 ±10dp 边界时同步把解析导数归零，禁止已夹平的几何继续
产生虚假波峰光学。正常场景在 1.5 DPR 下相对 640 列参考的轮廓误差 `p95≤0.035px`、
`max≤0.117px`；强注入压力帧仍保持 `p95≤0.186px`、`p99≤0.350px`、`max≤0.521px`，继续升到
216 显示列的边际收益不足以抵消顶点和光学构建成本。

Python 的 640×840 是逻辑尺寸，不再同时充当底层 Render Target 尺寸。普通 GL 与 D3D11/scRGB
HDR 窗口都按 `logical size × devicePixelRatio` 建立 FBO、Composer 和 swapchain；共享 vertex
shader 的 `uRasterScale` 只缩放栅格位置，程序化微法线、渐变和其它材质计算仍使用设计空间，
折射偏移单独换算为实体像素。录制输出继续固定 640×840：HDR 由 GPU 在线性域缩小、撤销 scRGB
scale 并编码为 RGBA8；SDR 窗口复用同一张原生 DPI 水体图生成视频副本，开始录制不得把窗口退回
低分辨率拉伸。

九条可见界线使用严格局限于一个实体像素以内的亚像素解析 coverage，但不再输出半透明水体。
完整单侧 1px 坡道会侵占薄的中远层主体并让 18 色相邻层色差中位数近乎减半，因此 coverage 只
处理轮廓命中的残余台阶，主要精细度由原生 DPR 与 C1 重建承担。片元在
线性空间内直接混合 `vBehindColor` 与当前层结果并保持 alpha=1，front fill 的 behind 明确合成
第 1～8 层；这样既不会露出浅色环境形成白色点线，也不会在 SDR 的 sRGB 固定功能混合中产生
暗脏边。它只平滑栅格覆盖，不添加身份色描边、界面肩、额外亮度或跨层光学实体；独立 glint 的
峰值、数量、生命周期和 D136 的逐层合同保持不变。

新增显示精度必须由等价或更大的无损性能收益支付：Python 删除无人消费的 `micro_x/micro_z`，
把二维相位三角函数拆成可分离的一维计算，缓存三次重建权重并使用无复制 VBO 上传；两端把坡度
预滤波改成双缓冲交替，只在最终复制一次。Android 还缓存静态 Thing 材质及其 uniform，GL 错误
稳定后每 129 帧抽检。Python 的 immediate-mode 桌面 GPU 实测一次环境绘制后 copy 更快；Android
常见 tile GPU 则保留两个目标各画一次极简环境，避免 FP16 Blit 强制 attachment resolve 和外存
往返。两端输出合同一致，但复制策略按平台带宽模型分开，不能把桌面结论直接外推到手机。

## D138 层内 continuous sheen 整体退出，不再以点、短划或光带替代（2026-07-15）

用户复核 D137 的动态截图后确认：原扩缩光带虽然消失，但稳定微面片把同一效果缩成了分布在每层
内部的斜短光点，仍然不好看；这些点再次明确不是位于水体边缘的离散 `glint`。因此 D137 的
固定微面片方案被否决，层内 continuous sheen 的可见 SDR 反射、HDR excess 及其配套暗斑整体
退出，不再换成另一种点、线、环或 patch 图案。

共享 shader 删除固定微面片、GGX/Smith 连续太阳反射、bounded SDR reflection、同源 facet
暗面与 HDR sheen excess，同时删除 `uSunReflectionCoverageWeights`、`uSheenCoverageScale`、
`uHdrSheenPeaks` 及对应 varying。Android/Python 的连续 sheen 峰值表、覆盖率表、渲染上传和
`sheen_patch_scale`/`sheen_intensity` 参数一并删除，避免 Python 面板继续暴露无效旋钮。旧 preset
中的多余字段由 Python 参数加载器忽略。

这项删除不影响独立的边缘 `glint`、surface reflection、streak、thin transmission、折射、
Beer–Lambert、SSS 或各自已经确认的逐层数量与 HDR 峰值。`aSheenSlope/vSheenSlope` 名称暂时保留，
但其低通坡度现在只服务折射、Beer、SSS 与 HDR transmission，不得重新用于可见连续反射。HDR
transmission 保留既有不随微法线分块的 `V·H` Fresnel 标量，避免以透射名义重新产生层内点状
纹理。D138 取代 D137 的生产方案；D137 的诊断数据只作为失败候选记录，不得据此恢复微面片。

## D137 连续镜面覆盖使用稳定有界微面片，不再显示动态响应等值线（2026-07-15）

**状态：已由 D138 否决。固定微面片虽消除了扩缩等值线，但形成用户不接受的层内斜短光点。**

用户确认当前油漆感光带不是离散 `glint`，并要求动态诊断既能显示明显音乐响应，又不能因第 0 层
基础水位过高遮住中远层。单变量消融已证明：`full`、`no_glint` 与 `water_only` 都保留同一组
扩张、收缩的弯月光带，只有关闭连续 sheen 才消失。根因因此固定为连续 GGX 路径把动态
`sunResponse` 的窄阈值等值线直接当作空间覆盖，而不是闪点、表面带或波包半径变化。

连续镜面从此把“微面片在哪里”与“该处 GGX 能量多高”分开。空间覆盖使用屏幕空间
`42×16px`、按层加盐、稳定播种的短各向异性微面片；时间只平移连续坐标，不改变面片尺寸、半径
或命中阈值。GGX/Smith/Fresnel 只调制已经存在的面片能量，不得反过来决定几何边界。抗锯齿
footprint 必须从 `floor`/hash 前的连续坐标导数解析计算，不能对不连续的椭圆距离调用 `fwidth`。
逐层存在度从 uniform 的 `uSunReflectionCoverageWeights[uStartLayer]` 读取，禁止用跨层插值 varying
在单个面片内部切出未经抗锯齿的直边。D136 已恢复的闪点数量、HDR 峰值表和 `lighten_far` 主体
颜色合同均保持不变。

同源暗面只允许使用同一微面片 `spatialGate`，在当前线性水色上做保色曝光降低；不得用较宽的
活动覆盖重新生成跨层暗带，也不得混入黑、灰或压暗最终 HDR 高光。第 0 层前景填充不需要微法线、
hash、GGX、折射或 Beer–Lambert，shader 以 uniform `uFrontFill` 提前返回，保持既有颜色和 alpha
合同并降低片元成本。

回归场景固定为 129 BPM、中高能量、适中主浪，前景水位必须让九层仍可观察；`EmptyOptics`
明确排除 glint。四个主时相同时要求连续高光正残差峰值大于 `0.12`、支持面积大于 `256px`、
最大 8 连通域不超过 `512px` 且不超过总支持面积的 `24%`，禁止用整体关效通过测试。完整 16
时相实测最大连通域中位 `93px`、最大 `105px`，最大占比 `9.29%`；60Hz 相邻帧 mask IoU 中位
`0.8716`，证明形状已打散且保持时间稳定。

## D136 高光恢复既定逐层合同，峰值、覆盖率与同源暗面分开建模（2026-07-15）

保留 D135 的静态 `lighten_far` 主体色板、录音/HDR 不改写主体色以及零界面肩；撤销 D135 对
第 3～7 层特效数量和 HDR 峰值的过度收缩。镜面闪点恢复容量
`4/4/3/3/2/2/1/1/0`、出生权重、长度、核心 alpha 和 D103～D118 已确认的五组 HDR 峰值；
第 8 层仍不生成离散闪点。解析 halo 继续为零，不能用恢复数量之名重新引入扩张、收缩的圆环。

离散闪点使用出生时固定尺寸、沿真实水面向水内单调衰减的短实心曲面带；运行中只跟随位置和
强度，不再用锚点尺寸或周期函数改变半径。连续太阳响应删除 `216×129` 低频椭圆 patch，直接由
同一组宏观坡度、带限微法线、GGX/Smith/Fresnel 和光向生成破碎亮区。逐层权重只收窄命中面积，
不得线性降低命中像素的 HDR 峰值；由此把“存在数量、空间覆盖率、SDR 反射预算、HDR 峰值、
中性白比例”拆成独立职责。

同一片元的 `normal` 与 `lightDir` 同时决定受光高光和背光坡面。暗部只在当前线性色上小幅降低
曝光，深色允许压暗，浅色自动减弱，禁止混黑、混灰或在最终 HDR 合成结果上整体乘暗。SDR 高光
肩部主要保留 Thing 身份色，只有更小的 HDR 核心趋向银白。光学实体改用预乘 SDR 颜色加不受
alpha 二次衰减的有界 HDR excess，最终 presentation 统一按设备可用 headroom 截断；这项合成
规则修订 D80、D131 中仍让 alpha 同时控制峰值和覆盖率的旧描述。

折射与 Beer–Lambert 合成只允许当前层透明度参与一次。顶点阶段额外输出尚未混入当前层的
`behindColor`，片元阶段先对不可变背景执行折射和体积吸收，再以当前层透明度和主体色合成；
不得把已经包含当前层的颜色再次与同一层混合。体光 `mode 8` 必须把实时 `volume` 作为 HDR
资格传入，避免逐层峰值表成为不可达路径。第 8 层既没有正向连续 HDR 覆盖，也不得单独保留同源
负向暗面。

离屏验收固定分为两条证据链：8-bit RGB PNG 只判断颜色、层界、浑浊感、形状和空间分布；HDR
亮度只读取 FP16 线性 scRGB 原值，并按 `1.0/1.08/1.16/1.29/1.6/2.0× reference white`
输出逐层覆盖率和伪彩图。普通 PNG 即使来自 FP16 场景，也不得被描述为 HDR 图片或用于判断最终
超白亮度；Android 系统 tone mapping 与运动观感仍以真机为准。

## D135 主体恢复静态 lighten_far 混白，离散闪点收回最近三层（2026-07-15）

**状态：主体色板部分仍有效；特效与 HDR 收缩部分范围过大，已被 2026-07-15 最新真机反馈否决。**

后续只能保留本项的静态 `lighten_far`、录音/HDR 不改写主体色以及零界面肩裁决。离散闪点容量
`3/2/1/0…`、第 3～8 层连续太阳反射归零、近层 HDR 降峰和由单一存在权重同时缩减 SDR/HDR
响应，均不得继续覆盖 D103～D118 已确定的逐层数量、强度与峰值合同。新的空间形态、覆盖率和
亮暗耦合方案尚待基于 Python 消融结果单独裁决，不能再次通过整层清零来修复圆环或乳白问题。

真机复测否决 D132 的固定 hue 亮向色域阶梯：虽然第二、第三层不再乳白，但主体变得灰暗、发脏，
九层分界也不如旧方案清楚。主体颜色恢复为
`mixOklab(identityColor, white, depth01 × clamp(lighten_far, 0, 0.864))`；默认
`lighten_far=0.864`，第 0 层精确保留记事色，第 8 层不超过 `86.4%` 混白。横向渐变的四个身份
停靠点分别执行同一公式。mood、color breath、录音与 HDR 状态不进入主体公式，防止开始录音时
近层整体跳亮。所有界面肩权重归零，由混白阶梯直接承担分界。此项取代 D132 的主体色路径、相邻
ΔE 分配和量化残差界面肩，也取代 D130 中“禁止向白点直线混合”的限制。

上一轮把离散闪点和解析 halo 扩展到九层后，真机上形成过多周期扩张、收缩的圆环光斑，尤其中
远层明显。离散闪点从此只保留在第 0～2 层，容量收敛为 `3/2/1/0/0/0/0/0/0`；第 3～8 层继续
保留连续 GGX 微表面、折射和受限透射，但不再生成离散光斑。闪点删除正弦呼吸尺寸调制和解析
外晕，只让核心随真实受光锚点平滑移动。此项修订 D134 的“九层均保留非零闪点”和合并光晕
合同，不撤销 D134 的连续微表面、不可变背景折射与 Beer–Lambert 分瓣。

18 色离屏消融进一步确认，恢复主体色板后第二、第三层仍可能被背景透射和连续白色反射轻微
去饱和。Beer/折射的背景混合上限因此由 `0.049` 收到 `0.016`，连续太阳反射逐层权重最终取
`.72/.49/.21/0/0/0/0/0/0`。这组参数使第二、第三层跨颜色中位色度保留率约为 `98.2%/96.0%`，
同时保持非零折射与 Beer 响应；不得为少数深色继续添加逐色特殊分支。

## D132 九层主体采用亮向色域贴边弦长阶梯，界面肩仅补量化误差（2026-07-15）

**状态：主体色阶与界面肩已由 D135 取代。**

第 0 层继续精确等于记事身份色，第 1～8 层统一向亮。路径固定 OKLCH hue，在每个明度上取
sRGB 色域可容纳且不高于原色的最大 chroma，并在色域迫使下降后禁止远端回弹；最远明度不得
超过旧 `86.4%` 混白所对应的 `L0 + 0.864 × (1-L0)`。这保留“远处偏亮”的景深方向，同时
避免沿中性白直线主动降低饱和度。

八条相邻边界的目标 ΔE 为 `0.045/0.045/0.042/0.036/0.032/0.027/0.024/0.021`。
颜色余量不足时，先给八条边各保留 `0.014`；若仍不可行，则求八条边的最大共同等距；可行时
先同步满足最近两条边，再按 `1/.78/.62/.50/.42/.36` 给其余六条边分配余量。每一步必须按
相邻 OKLab 端点弦长在色域路径上求位置，不能用累计曲线弧长代替。录音、mood 与 color breath
不得再改变主体色阶；这些动态能量只进入局部光学和 HDR。

界面肩不再弥补“目标 ΔE 与色域可用量”的差额，只允许补偿 8-bit 量化造成的微小损失；当主体
相邻 ΔE 已达到 `0.012` 时权重必须为零，剩余情况也不得超过 `0.12`。深度方向渐变不附加界面
肩。由此以主体色差承担九层分界，避免宽粗身份色描边。D132 取代 D129 的主体缺口补偿职责和
D130 的 `.70/.60 chroma floor` 截止，但保留 D126 的随层保色阴影原则。

## D134 晶莹水体采用连续微表面、不可变背景折射与受限体积吸收（2026-07-15）

水体连续受光统一采用 GGX/Trowbridge–Reitz 法线分布、height-correlated Smith 可见性与
Schlick Fresnel；水的折射率固定为 `1.333`，法向入射反射率固定为 `0.020373`。低频坡面密度
采用 Cox–Munk 海面统计的设计方向，逐片元导数在任何非一致材质分支前求值，用 footprint
扩大有效粗糙度并带限微法线。SDR 反射预算封顶 `0.160`，太阳响应系数为 `72`；HDR 只开放
局部超白余量，局部门控使用 `response/(response+0.129)^2`，不得把整层或主体身份色抬到白点。

屏幕空间折射必须读取一张已经完成、之后不再写入的背景纹理，禁止同时采样当前 scene 写附件。
Python 与 Android 都维护 `pre-water`、`scene` 两张同尺寸离屏目标：先把相同环境写入两者，再在
scene 上绘制水体；HDR 离屏失败时两张目标必须原子回退到同一 SDR 格式。折射按 Snell 定律计算，
位移受视口和层深限制；Beer–Lambert 只衰减折射/透射分量，以 `exp(-sigma × opticalPath)` 在线性
光空间执行，再与当前主体身份色做有界凸组合。浅色使用感知明度保护，避免吸收把浅灰绿、浅粉、
浅黄压脏；主体、反射、闪点和 HDR excess 不参与体积吸收。

离散闪点核心与解析光晕共用一条扩展曲面带和一次 draw，核心/光晕比例打包进 mode 小数部分，
shader 内分别求解析覆盖；曲面带按 `≤3.2dp` 目标段长直接取 `6～32` 段，不再逐轮插点。九层均
保留非零闪点容量，连续微法线、SSS、银泽和透射按景深连续衰减，不在最近三层之后硬截止。
颜色变换性能优化只能缓存“精确 8-bit RGB → 固定 OKLab 变换”的纯函数结果，不得缓存、降采样
或近似水面几何与动态光照。

## D133 Python 与 Android 连续水面采用每层十二个纵深子区间（2026-07-15）

Python 视觉基准把九条层级锚线之间的纵深采样从每层 3 个子区间提高到 12 个，共 97 行。目的
是降低逐顶点坡度和受光量在三角形之间形成的直线/马赛克边界，并为逐片元 specular AA 提供更
平滑的宏观输入。索引缓冲容量必须从 `Z_ROWS` 推导，不能保留 `8×3×119` 的硬编码。

这项加密不得以牺牲交互性能为代价：640×840、FP16、动态模拟、GPU finish 的 Python 离屏基准
应维持 60fps 量级。颜色纯函数稀疏缓存和闪点核心/光晕合并后，高活动稳态保留 23 个闪点与
6 条流光，中位帧耗时为 `15.52ms`（约 `64.4fps`），P95 为 `18.86ms`。Android 同步为相同
`ROWS_PER_LAYER=12`、`Z_ROWS=97`；闪点纵深轴使用 `ROWS_PER_LAYER/3=4` 行，避免加密后
光学实体厚度缩为原来的四分之一。实机视觉验收仍由用户完成。

## D131 晶莹材质链以局部光学、屏幕空间界面肩和当前位置颜色统一收口（2026-07-14）

本轮实施不只调整九层颜色。Android GL/Canvas 与 Python ModernGL/QPainter 同步采用以下最终合同：

- D129 的界面肩从水体 ribbon 内的比例着色迁到真实轮廓几何，宽度按相邻分离缺口连续取
  `7～14dp`；每条内部轮廓先画较弱深肩、再画亮肩，两侧均使用峰值 `0.66` 的半正弦剖面，
  在轮廓本身和带外覆盖率归零，不形成细描边。第 8 层与环境之间不画内部界面肩。
- 纯黑端的亮肩额外获得向上明度余量，近白端的亮肩连续衰减并由同色深肩承担分离；浅粉、浅黄、
  浅青不得因 gamut mapping 退成中性白。两侧目标色都从当前层四停靠点及其 residual weight 派生，
  不混入白、黑、灰或环境色。
- 表面反射带先按当前位置和实际 Thing 渐变几何采样主体色，再沿该样本的固定 hue 提亮
  `ΔL=.045`；禁止以 `mix(start,end,.3)` 的单一中间色覆盖整条双色或互补渐变。
- 局部 SSS 从每一层最终四停靠点分别派生，不再回跳原始 Thing 色，也不主动执行 `C×.85`。
  明度增量为
  `(.045 + .025×(1-smoothstep(.35,.75,L))) × (1-smoothstep(.90,.985,L))`，目标上限
  为 `.965` 且不得反向压暗原色；纯白保持纯白。
- 界面肩、随层保色阴影、体光、轻纱、表面反射、流光和闪点按
  `interface → backShade → body/thin → veil → surface/streak → glint` 合成。界面肩与阴影均先于
  反射/透射，闪点最后绘制，后续半透明介质不得衰减已经存在的表面响应。
- 连续 HDR 银泽仍只由方向、Fresnel、波峰/坡度和低频 patch 共同门控，不恢复全域白色基线；
  局部响应系数设为 `3.0`，使默认强度在 FP16 中跨越足够量化级。录音增益只开放局部 excess，
  不改变九层主体、界面肩或 SDR 基线。

光学 HDR 不再执行 `excess/alpha` 反向预补偿。闪点 HDR 资格使用共同出生门后独立归一化的
`birthGate × min(intensity/.30,1)^.8`；源 alpha 与空间 coverage 自然控制最终合成能量，避免低
alpha 实体生成巨大白色源。该项修订 D80、D116 中关于 inverse-alpha 预补偿的旧描述。

## D130 九层主体颜色统一逐层提亮，不再按颜色分支（2026-07-14）

用户要求结束对个别颜色方向和色域预算的继续裁决：第 1～8 层统一相对第 0 层逐层提亮，不再根据
输入色自动选择向暗、不再为粉/黄/青/绿等颜色建立不同方向，也不把 Display-P3 或 HDR headroom
纳入主体阶梯分支。第 0 层继续保留 Thing 身份色锚点。

实现使用固定色相的感知明度上移，尽量保持原始绝对 chroma；目标色域无法容纳时才做必要的保色
映射，禁止重新沿白点直线混合。具体相邻步长、色度软目标与硬底线降级为 Android/Python 同构
调参项和回归观测量，不再作为需要逐色裁决的产品分支。D129 的随层保色界面肩继续承担颜色提亮
不足时的层界辅助，但不改变“所有主体逐层提亮”的单一方向。

本决策收口并取代 D120～D129 中仍可能被解释为按颜色选择方向的部分；其余关于相邻边界、近三层
优先、主体/HDR 分离、保色阴影与界面肩的约束继续有效。

## D129 向亮色域耗尽后的层界缺口由随层保色界面肩接管（2026-07-14）

**状态：界面肩职责继续有效；具体 `.70/.60` 色度预算已由 D130 降级为内部调参参考。**

九层主体按 D128 保持越远越亮。向亮路径默认在 `C/C0=.70` 的软目标处停止；只有为了保护最近的
`0↔1`、`1↔2` 两条边界，才允许继续消耗到 `.60` 硬底线，不得为拉开中远层而继续降低色度。
具体相邻 `ΔEOK` 目标仍可调，`.70/.60` 也必须通过最终动态画面复核，不解释为文献阈值。

颜色路径停止后，每个相邻边界尚未满足的分离缺口，由常驻 SDR/HDR 的 **随层保色界面肩** 接管：
沿低频水层轮廓形成宽软、低幅、从该层当前位置主体色派生的亮侧与更弱深侧响应。它不得混入白、
黑、中性灰或环境固定色，不得收缩成 1px 描边、九条等强横线或逐层交替明暗；局部响应不能改变
主体腹部，也不能让九层平均亮度停止递增。

界面肩是一项受限的感知层序后备，不宣称九层分别具有真实 Fresnel 界面。真实 Fresnel、环境反射、
折射、透射和 HDR specular 继续按连续水面的视角、法线与光路独立计算。未录音与录音 HDR 使用
相同主体阶梯和界面肩；录音状态只允许额外局部光学分瓣进入 HDR，不得再次整体抬亮第二、第三层。

## D128 主体景深阶梯以越远越亮为全局方向先验（2026-07-14）

**状态：已由 D130 收口为所有主体统一逐层提亮。**

用户否决按可用色域和远层色度保留自动在“向亮/向暗”之间择优的候选算法：该评分会让默认粉、
黄色、青色和绿色等大多数样本越远越暗，违背 FableSol 的景深外观。第 1～8 层主体仍以越远越亮
作为全局方向先验；不能只因为向暗更容易取得较高 `C8/C0` 或更大的八段 `ΔEOK`，就自动翻转整条
Thing 或渐变的深度方向。

当 sRGB 向亮色域不足时，算法必须先在不乳白、不显著消色的边界停止继续提亮，再由稳定、低频、
非黑阴影且不依赖录音 HDR 才可见的其它层序量补足相邻界限；不得静默切成大面积越远越暗。
近白等完全没有上行余量的极端输入如何分配剩余层差继续单独裁决。本决策取代 D120/D121 中“上行
余量不足即可自动反向”的部分，但保留二者关于渐变连续、不得趋白消色和层内光照独立的约束。

## D127 体积吸收与阴影分离并只作用于介质透射光路（2026-07-14）

FableSol 的物理体积吸收不纳入 D126 的随层保色阴影，也不能作为压暗主体的替代手段。它只作用于
穿过介质的透射、折射或背景采样分瓣，并在线性光空间随光程增加执行有界的逐通道衰减；主体身份
色、环境反射、镜面高光、闪点与 HDR excess 不参与该衰减。

吸收谱由当前位置的身份色关系派生。光程变长时，透射结果可以自然变深并适度提高饱和度，也允许
物理吸收造成的极小色相漂移，不强制沿 D125 的恒绝对 chroma 主体轨迹；但必须限制色相偏移、最大
光学深度和最低剩余能量，防止浅色退成灰雾或深色沉入黑泥。不得用黑色 overlay、编码 sRGB 标量
乘法或最终材质总和乘暗冒充体积吸收。

在当前尚未采样真实背景、没有完整介质厚度的 2.5D 水体中，可以先使用受控的近似光程，但必须
明确其分瓣职责，不能把 additive 薄峰光或整层体色称为物理透射。具体吸收强度、色相偏移和能量
下限继续通过 Android/Python 同构原型调节。

## D126 所有负向明暗响应采用随层保色阴影（2026-07-14）

FableSol 中由波背、宏观坡面、微表面或其它材质效果产生的阴影，必须以当前位置已经应用
景深阶梯和横向渐变后的水层主体色为颜色来源，再形成同色系的更深颜色；禁止向黑色、中性灰或
跨层复用的固定暗色混合。远层即使明显更亮，其阴影也必须从该远层当前色出发，而不是突然回到
第一层或原始 Thing 的深色。

这里的“当前位置主体色”明确处于材质合成顺序中的低频锚点：已经完成九层景深配色和 Thing
横向渐变采样，但尚未加入环境反射、透射、次表面散射、镜面高光、闪点与 HDR 增益。阴影不得从
已经包含这些光学分瓣的最终像素反向派生，否则会把本应保留的晶莹响应一并压暗。

显式暗色带或体色变化应在感知空间中主要降低明度，保持当前 hue 与绝对 chroma；目标色域无法
容纳时才受控压缩 chroma。物理遮挡则不生成黑色“阴影色”，而是只削弱对应的直射光分量，保留
主体体色、环境反射和其它未被遮挡的材质分瓣。禁止用最终 RGB 总和乘暗来模拟遮挡。

本决策把这类结果统一命名为 **随层保色阴影**。它适用于 Android HDR/SDR、Canvas 回退、共享
GLSL 与 Python 两条渲染路径；不能只修复命名为 `shadow` 的单一函数，也必须审计实际承担负向
明暗的微法线与合成步骤。物理体积吸收按 D127 使用独立的介质透射合同。

## D125 主体景深色板默认保持原始绝对色度且不自动增艳（2026-07-14）

第 1～8 层低频主体色板默认采用 `Cdesired=C0`：沿自适应明度方向移动时尽量保持 Thing 输入色
的绝对 OKLCH chroma，不使用 `C/Cmax(L,h)` 作为恒定饱和度合同，也不主动把主体提高到超过
`C0`。这样既避免向白路径主动消色，也避免色域切片形状导致某些颜色先过度增艳、随后快速褪色。

当固定 hue 下的目标色域无法容纳 `C0` 时，可以降低 chroma，但必须由色度保留下限约束可用明度
路径；在跌破下限前停止、换向或引入其它低频层序量。具体保留率继续以 `60%/70%/80%` 多档视觉
对照收敛，不在本决策中写死。

该合同只约束九层低频主体色板。局部反射、薄峰透射、镜面闪点和其它受光材质响应仍可产生更鲜亮
或更接近中性高光的局部像素，但不得反向把大面积主体自动增艳或消色。

## D124 远层明亮与远层消色分开裁决（2026-07-14）

FableSol 不把第 8 层或其它远层“较亮”本身视为缺陷，也不为保持深色身份而单独限制九层总明度
跨度。远层只要继续属于 Thing 身份色系、保持足够饱和与干净观感，就可以获得明显高于近层的
感知明度。

需要受限的是随深度趋向中性轴的消色：景深颜色路径不得为了提亮而系统性把 OKLab `a/b` 或
OKLCH `C` 压到接近零，也不得把高明度与乳白、发灰绑定。明度跨度、相邻层级距离和色度保持从此
作为三项独立约束验收；“最远层更暗”不再是候选方案的优势。

本决策修正 D121 中“只能小幅提高感知明度”的限制：典型中间色和深色仍默认越远越亮，但可在
色相、色度和相邻边界均合格时扩大亮度跨度。浅色或色域边界颜色若无法继续提亮而不显著失色，
仍按 D120 自适应反向或使用其它低频层序量。

## D123 景深阶梯按八个相邻边界验收，近三层优先（2026-07-14）

九层水体颜色不能再以第 0 层与第 8 层的端点总差作为主要分明度依据；正式验收对象是八对相邻
层级。任一相邻边界不足都可能让两片水体在运动和光影覆盖下混合，即使首尾颜色相差很大也不能
判定景深阶梯合格。

最靠近观察者的三层具有最高辨识优先级。`L0↔L1` 与 `L1↔L2` 必须得到最高等级的受保护间距；
后续边界可以采用不同目标，但不能低于动态画面仍可辨识的底线。八段目标允许不等距，不再由
单一端点提亮量间接决定。

相邻距离以完成色域映射和最终输出量化后的实际 `ΔEOK` 为数值检查，并同时在准备态 SDR 与录音
HDR 动态画面中验收。`ΔEOK` 只是一项可调的生成与回归参数，不代替边界视觉判断。前两段是否
等值、`L2↔L3` 如何过渡以及其余五段怎样收敛，仍留给下一轮阶梯对照确定。

## D122 第 0 层低频主体基线承载记事身份色锚点（2026-07-14）

第 0 层继续作为 FableSol 的稳定颜色参照：其进入局部材质计算前的低频主体基线必须等于 Thing
原始纯色，或完整保留 Thing Background 的横向渐变关系，不参与第 1～8 层使用的景深颜色派生。

身份锚点约束的是基线，不是要求第 0 层所有最终像素保持平色。局部表面反射、薄峰透射、受光
明暗、体积光程和其它有界材质响应仍可改变第 0 层局部输出，但必须留下稳定、可识别的原色参照，
不得让中性白高光或灰暗阴影成为该层的主要颜色身份。

第 1～8 层以第 0 层锚点为输入，按照 D119～D121 形成自适应 **水层景深阶梯**。本决策收敛 D117
尚未确定的身份锚点落点，并保留当前用户认为观感较好的首层基线，不要求重做首层颜色关系。

## D121 多数颜色保留越远略亮，只有上行余量不足时反向（2026-07-14）

**状态：方向选择部分已由 D128 修订。** 大多数颜色越远越亮已提升为全局方向先验；上行余量
不足时不再由色度评分自动改为向暗，而是在保色边界停止并使用其它低频层序量。以下保留原裁决
形成过程。

典型中间色和大多数深色继续沿用“主体越远略亮”的默认深度方向，但只能沿记事颜色关系小幅提高
感知明度并保持色度下限，不再通过大幅插值到中性白制造层差。近黑色同样可以有限向亮，但不得
因此变成灰色或与其它深色收敛。

当浅色、近白色或色域边界颜色没有足够的向亮余量时，主体阶梯可以连续转为越远略暗，或由其它
低频层序量补足差异。渐变中发生方向切换时必须平滑，并避免在切换位置让相邻层差归零。

跨层主体阶梯与层内光照是两项独立职责：深色水体仍可在波背、长光程或直射可见度较低的局部
区域有界压暗；这不表示其九层主体基线必须越远越暗。D121 收敛了 D120 中尚未决定的典型颜色
默认方向，具体切换条件和次要层序量继续后续裁决。

## D120 主体阶梯明暗方向按颜色余量自适应（2026-07-14）

**状态：自动反向部分已由 D128 取代。** 色域余量仍用于限制可走的向亮路径，但不再自行选择
整条主体越远越暗；渐变连续与无接缝约束继续有效。

FableSol 不再要求所有 Thing 颜色都采用同一个主体阶梯明暗方向。各输入颜色可以根据实际向亮、
向暗和色度余量选择能够保留颜色身份与相邻层差的方向；渐变输入的选择必须连续，不得形成横向
接缝或逐层方向跳变。

“自适应”不等于把当前“越远越接近白色”整体替换为“越远越暗”。当前实现确实通过
`mixOklab(identityColor, white, depth × lighten_far)` 固定让远层趋白；D120 取消的是这项全颜色
固定规则。深色仍允许在层内受光结构中压暗，近黑也可以在阶梯中有限向亮；浅色或近白色则可以
在向亮余量不足时改用有界压暗或其它层序量。

本决策只确定自适应原则；典型中间色随后由 D121 确认为继续默认“越远略亮”，何时切换方向以及
方向切换处由哪一项次要层序量维持相邻层差，继续由后续 grilling 裁决。

## D119 水层景深阶梯定义为独立于局部光影的层序结果（2026-07-14）

FableSol 的九层低频主体外观必须自身保持由近到远的有序关系，并使相邻水层在不依赖局部 HDR、
闪点、阴影或其它局部光影时仍然可辨。局部材质响应可以丰富各层内部，但不能承担层界存在与否，
也不得持续抹平相邻主体的边界。

**水层景深阶梯** 从此只规定上述结果，不再绑定“随距离逐层混白、提高明度并降低整体 alpha”这组
固定手段。混白、明暗、透明度、色度、反射、光程或粗糙度等候选量只有在适配任意 Thing 颜色、
保留 **记事身份色锚点** 且不制造乳白主体时才可参与实现，不能单独成为产品定义。

本决策保留 D59 的九层层序目标，但取代其“必须单调混白并降低 alpha”的实现约束；D60 锁定旧参数
的对照仍作为历史实验记录，不再限制后续材质重构。主体阶梯的明暗方向随后由 D120 确认为按颜色
余量自适应。

## D118 录音 HDR 态保留多类局部光学响应并服从九层边界（2026-07-14）

FableSol 继续只在开始录音后进入 HDR，准备态和停止态保持非 HDR；不得以保持主体基线为由取消
这项状态变化。HDR 也不限定为只增强镜面闪点：局部表面反射、薄峰透射、连续受光结构及其它能够
提高晶莹感、立体感和水体质感的材质响应均可使用实际可用 headroom。

所有 HDR 与 SDR 材质项仍须服从九层水体相对分明的边界和 **记事身份色锚点**。禁止的是整层或
大面积近中性白增益、多个独立效果无预算叠加后持续抬高主体，以及因此造成第二、第三层乳白、
低色度或与相邻层合并；并不禁止连续水面本身获得局部 HDR 响应。

本决策修正“录音 HDR 只能出现在局部镜面和闪点”的过窄建议；各层基线如何形成单调层序、局部
效果可以覆盖多大面积及怎样共享能量预算，继续由后续 grilling 分别裁决。

## D117 记事原色作为身份锚点而非全域固定填充色（2026-07-14）

FableSol 必须保留一处稳定、可识别的记事身份色锚点：纯色记事保留原始颜色，渐变记事保留原始
横向渐变关系。身份锚点不等于全部水体像素必须严格等于原色；其它区域可以因表面反射、光程、
受光与材质形体产生有界明暗和色度变化。

浅色、近白色允许在非锚点区域适量压暗，为 SDR 波面保留形体对比；深色、近黑色也允许继续压暗，
但不得让不同输入收敛成同一黑色。后续颜色派生必须按输入色的实际上下明度/色度余量连续适配，
保留色相关系并进行目标色域映射；固定混白、固定 `ΔL`、逐 RGB 通道裁切或灰黑覆盖不能再被视为
适用于任意 Thing 颜色的完整方案。

本决策只确定颜色身份与材质响应的关系，尚不裁决各水层的最终明度顺序、身份锚点具体落点或每种
材质分瓣的数值范围；这些项目继续通过本轮 grilling 逐项收敛。

## D116 闪点时间修正只补上 HDR 共用包络，不重做已验收生命周期（2026-07-14）

D70/D92 已经通过 `0.30s` attack、`0.80s` release、`0.018～0.060` 低强度软门、`0.015` 退休
阈值和 `1/15s` 跟踪步长上限解决 SDR/CPU 侧的主要突消问题，本轮不得重复重写这套生命周期。
现有出生初始强度 `candidate * 0.12`、`0.10s` 位置跟随和当前锚点匹配行为也先保持不变。

**状态：HDR 资格与 alpha 合成细节已由 D131 修订。** 闪点 mode 3 继续复用
`vHdrEligibility` 通道，但其值为
`birthGate × min(intensity/.30,1)^.8`：与 SDR 实体共享出生/退出门，同时在真实 track 强度
`.30` 达到满额。HDR mask 另乘空间 coverage；shader 不再执行 `excess/opticalAlpha`，实体 alpha
自然控制最终 SDR 与 HDR 合成能量，不能让低 alpha 闪点绕过淡入淡出。

实现阶段为 HDR 峰值退出、出生首帧和相邻锚点交叉补回归测试；只有测试、录像或真机观察确认仍有
出生跳变或身份交换时，才另行评估“初始强度归零”与“全局一对一匹配”，不能把未证实的理论改进
混入本次已收敛的生命周期修正。

## D115 解析光晕按层弱化并降低专用发射阈值（2026-07-14）

解析光晕保留当前近层材质基线，逐层 alpha 系数采用：

```text
[1.00, 0.96, 0.84, 0.75, 0.64, 0.49, 0.36, 0.24, 0.00]
```

`HALO_ALPHA_SCALE=0.18`、`analytic_halo_strength=0.10`、相对核心长度 `1.18×`、厚度 `2.25×`
和内部展开 `1.18×` 均保持不变。最终 alpha 按
`0.18 * 0.10 * layerWeight * layerAlpha * aerialContrast * visibility` 计算；在默认空气透视下，
第 6/7 层完全成熟时的最大值约为 `0.00405/0.00243`。光晕继续保持 SDR、保留身份色，只依附于
存活的核心 track，不建立独立光斑或单独申请 HDR 峰值。

光晕专用几何发射阈值由 `1/255` 降到 `1/1024`，避免第 6～7 层的非零权重被 CPU 硬阈值提前
裁掉；片元解析衰减和时间软门仍负责最终可见性。D113 的核心/光晕共用曲面采样边界继续有效，
几何比例以已按层收紧的核心尺寸为基础，不另建一套远层简化网格。

## D114 闪点核心最大 alpha 定为 0.9129 并按层缓慢衰减（2026-07-14）

镜面闪点核心共用的最大 alpha 由当前 `0.92` 修订为用户指定的 `0.9129`，第 0～8 层相对系数为：

```text
[1.00, 0.96, 0.84, 0.75, 0.64, 0.60, 0.56, 0.49, 0.00]
```

最终核心 alpha 按 `0.9129 * layerWeight * layerAlpha * aerialContrast * visibility` 计算；HDR 峰值
仍由 D107 独立控制，`0.9129` 不是亮度或 headroom 上限。以默认 `aerial_contrast=0.50` 计算，
第 7 层完全成熟时的最大实际核心 alpha 约为 `0.9129 * 0.49 * 0.5625 = 0.252`。远层主要通过
D103 的低容量和低出生权重保持安静，不能把真实但稀有的单个闪点压成灰弱噪点。

镜面方向、坡度、曲率、Fresnel 与 specular AA 必须先完成候选有效性判定；现有 `depthGain` 和
空气透视不得继续在 `GLINT_FIELD_FLOOR` 之前消灭候选。候选成立后才分别应用 D103 出生权重、
D113 几何尺寸、本决策 alpha、空气透视与 D107 HDR 峰值。低强度软门和时间常数继续单独裁决。

## D113 闪点按层收紧几何并使用自适应曲面细分（2026-07-14）

镜面闪点继续从真实镜面候选的空间宽度生成，不改成固定长度；第 0～8 层沿水面长轴系数与向水体
内部的最大展开分别为：

```text
长轴：[1.00, 0.96, 0.84, 0.75, 0.64, 0.56, 0.49, 0.42, 0.00]
内部最大展开：[2.56, 2.40, 2.24, 1.96, 1.60, 1.50, 1.36, 1.29, 0.00] dp
```

最终半长最低保留 `2.4dp`；D94 的太阳碎光路径仍把内部展开乘以 `0.72～1.00`，所以第 7 层
实际约为 `0.93～1.29dp`。本表把 D94 的近层 `2.6dp` 修订为用户指定的 `2.56dp`，并以明确
逐层值取代原有近远线性插值。

曲面网格不再固定使用 10 段：以不超过 `3.2dp` 的目标段长和局部轮廓弦误差共同决定细分，段数
限制为 `12～32`，高曲率区优先增加细分。闪点核心与解析光晕共用同一组曲面采样边界，避免两套
矩形段在波面上错位；实现时必须同步校核或扩展顶点容量，并通过测试禁止因容量不足而静默截掉
后绘制的闪点。本决策只确定几何，不改变 alpha、HDR 峰值、出生策略与时间包络。

## D112 波背局部阴影延伸到第 6 层并把景深衰减集中到 alpha（2026-07-14）

波背局部阴影只覆盖第 0～6 层，第 7～8 层不再生成独立暗带。逐层参数为：

```text
宽度：[1.00, 1.00, 1.00, 0.84, 0.72, 0.60, 0.42, 0.00, 0.00]
alpha：[1.00, 0.75, 0.56, 0.42, 0.24, 0.16, 0.12, 0.00, 0.00]
```

`BACK_DARKEN_L=0.10` 继续作为各层共同的保色明度差上限；移除阴影颜色当前的
`(1-depth)^2` 衰减，改由上述 alpha 表集中承担景深显著度，再保留 `aerial_contrast`。这样前六层
在空气透视后的最终明暗对比与当前实现基本相当，但第 3～5 层暗带逐渐变窄，第 6 层新增一个很弱
的局部版本。背光坡、波峰邻近、平水归零、局部阈值和 Oklab 身份色规则均保持不变；禁止重新混黑
或让阴影横跨大范围水面。第 7 层只使用 D110 的极弱连续宏观明暗。

## D111 局部表面反射带以慢缩宽度和快降 alpha 延伸到第 7 层（2026-07-14）

第 0～8 层局部表面反射带采用两组独立权重：

```text
宽度：[1.00, 0.96, 0.84, 0.75, 0.72, 0.64, 0.60, 0.56, 0.00]
alpha：[1.00, 0.96, 0.84, 0.64, 0.49, 0.32, 0.16, 0.06, 0.00]
```

宽度表取代 `surfaceBandWidthDp()` 中的 `(1 - 0.45 * depth)`，alpha 表取代当前
`(92 - 34 * depth)` 的线性部分；近层 `92/255` 基准、`surface_strip_gain`、录音呼吸、
`aerial_contrast`、迎光坡与波峰双门控以及轮廓平滑继续保留。几何从当前第 0～6 层弱化延伸到
第 7 层，第 8 层不生成；HDR 峰值继续使用 D107 的独立逐层表。

宽度故意比 alpha 衰减慢：第 7 层在满局部门控下的理论最大宽度约为 `3dp * 0.56 = 1.68dp`，
避免远层反射带被压成不稳定的亚像素线；其 alpha 只有近层的 `6%`，还会继续受到局部门控和空气
透视约束，因此只能形成极低覆盖痕迹，不能恢复成连续白边。

## D110 连续表面明暗、微法线与 SDR SSS 采用易调节的逐层衰减表（2026-07-14）

第 0～8 层连续表面效果不再保留各自散落的景深幂函数结果，而使用三组明确的层级权重：

```text
宏观坡面提亮：[1.00, 0.96, 0.84, 0.75, 0.64, 0.49, 0.32, 0.16, 0.00]
宏观坡面阴影：[1.00, 0.96, 0.84, 0.60, 0.42, 0.27, 0.12, 0.06, 0.00]
微法线与 SDR SSS：[1.00, 0.96, 0.84, 0.64, 0.49, 0.32, 0.16, 0.06, 0.00]
```

这些权重用于**取代**现有独立景深门控，而不是在旧门控之后再次叠乘，避免中远层被双重衰减。
宏观坡面提亮作为低频形体保留最远；保色坡面阴影更快退出；微法线与 SDR SSS 居中，其中微法线
仍须保留距离带限、导数抗锯齿和现有近层强度上限。所有通道均不改变水体基色与 alpha，第 7 层
只留下极弱痕迹，第 8 层完全退出。本决策取代采用 `p^0.7`、`p^1.1` 精确展开值的临时建议，便于
后续按用户熟悉的数值刻度直接调节。

## D109 mode 8 薄峰透射保持弱肩部并只覆盖第 0～4 层（2026-07-14）

独立 `mode 8` 薄峰透射带的 HDR 峰值保持：

```text
[1.08, 1.06, 1.04, 1.02, 1.00, 1.00, 1.00, 1.00, 1.00]
```

几何继续只生成第 0～4 层，不向第 5～6 层扩展。它只作为连续 SSS 之外很弱、很窄的几何肩部；
第 5～6 层已经由 D108 的连续 SSS 提供 `1.14×/1.08×` 透射，不再重复叠加独立亮边。这样避免
同一束透射能量重复记账，并防止中远层重新出现多条连续白边。

## D108 连续 HDR SSS 透射峰值定为 1.6/1.5/1.36/1.29/1.21/1.14/1.08/1/1（2026-07-14）

第 0～8 层连续 HDR SSS 透射目标峰值正式定为：

```text
[1.60, 1.50, 1.36, 1.29, 1.21, 1.14, 1.08, 1.00, 1.00]
```

透射继续复用 D93 的 `sunriseSubsurfaceMask()`，并乘以 `(1-Fresnel)` 与统一 HDR gain；目标峰值
不是整层亮度。第 0～2 层适度强化，第 3～6 层获得连续弱化版本，第 7～8 层回到 SDR。颜色继续
从本层 subsurface 身份色归一化派生，不混灰、不改色相，峰值始终低于同层的镜面闪点、局部表面
反射和连续银泽。

本决策修订 D93 的近层 `1.45×` 常量和到 `depth01=0.62` 归回 `1.0` 的单一曲线，后续改用上述
逐层目标并在层间连续插值。独立 mode 8 薄峰透射肩部是否调整仍单独裁决，不能与连续 SSS 重复
记账。

## D107 局部表面反射峰值高于银泽，并把极弱 HDR 尾部延伸到第 7 层（2026-07-14）

第 0～8 层局部表面反射带 HDR 目标峰值定为：

```text
[3.20, 2.70, 2.24, 1.96, 1.60, 1.29, 1.18, 1.08, 1.00]
```

这里的数组是 HDR 峰值，不是公共材质存在度或 alpha 权重。局部反射带比连续银泽更窄，并受
迎光坡与波峰双门控，因此单位面积峰值应不低于连续银泽；其总光量仍可因覆盖更小而更低。反射
带几何从当前第 0～6 层弱化延伸到第 7 层，第 7 层以 D101 的 `0.06` 存在度、原有局部门控和
独立 alpha 衰减限制覆盖，第 8 层不生成 HDR 反射带。

为了避免第 7 层出现“局部反射带比精确闪点更亮”的倒置，第 7 层闪点峰值由 D105 的 `1.00×`
修订为 `1.16×`，最终闪点峰值为：

```text
[3.60, 2.80, 2.40, 2.00, 1.60, 1.36, 1.29, 1.16, 1.00]
```

第 7 层仍受容量 1、出生权重 `0.16` 和真实候选门槛限制，只极少出现；连续银泽在该层仍为
`1.00×`。本决策修订 D96、D103、D105 中“第 7 层严格 SDR”的限制，第 8 层继续严格 SDR。

## D106 连续 HDR 银泽峰值定为 2.7/2.4/2.1/1.8/1.5/1.29/1.08/1/1（2026-07-14）

第 0～8 层连续 HDR 银泽目标峰值正式定为：

```text
[2.70, 2.40, 2.10, 1.80, 1.50, 1.29, 1.08, 1.00, 1.00]
```

银泽继续使用 D92 的低通宏观法线、Fresnel 与太阳方向响应，只提高有效受光区域的峰值，不扩大
掩码覆盖，不改变 SDR 基色。第 0 层银泽低于 `3.6×` 闪点最高峰；第 1～5 层保持连续下降；
第 6 层允许极弱的 `1.08×` 连续银泽尾部，第 7～8 层严格回到 SDR。

本决策修订 D96/D105 中“第 6 层宽域水体保持 SDR”的表述：第 6 层**基色和无受光区域**仍为
SDR，但经低通法线与 Fresnel 约束的连续银泽可达到 `1.08×`。SSS 透射、局部表面反射带和其它
HDR 通道不得随本数组自动提高，继续单独分配峰值。

## D105 3.6× headroom 下闪点峰值定为 3.6/2.8/2.4/2.0/1.6/1.36/1.29/1/1（2026-07-14；第 7 层由 D107 修订）

第 0～8 层镜面闪点 HDR 核心峰值正式定为：

```text
[3.60, 2.80, 2.40, 2.00, 1.60, 1.36, 1.29, 1.00, 1.00]
```

第 0 层使用 D104 新增的完整 `3.6×` 内容余量；第 1～3 层保持明显 HDR 层次；第 4～5 层继续
收敛。第 6 层虽最多同时 1 个且出生权重仅 `0.36`，但单个真实闪点允许达到 `1.29×`，通过
稀有程度而非把核心压到近乎不可见来保持远景克制。第 7 层仍可极少出现 SDR 闪点，第 8 层退出。

本决策只修订 D96 对第 6 层**闪点核心**的 `1.05～1.08×` 限制；第 6 层宽域水体继续保持 SDR，
局部表面反射和其它 HDR 通道仍须使用各自独立峰值，不能自动提升到 `1.29×`。所有峰值继续受
设备实时 headroom 封顶。

## D104 水体主视觉请求上限提高到 3.6× HDR headroom（2026-07-14）

录音 dialog 以水体/波浪动画为主要视觉内容，HDR 管线的内容上限与 SurfaceView 期望 headroom
由当前 `2.0×` 提高到 `3.6× reference white`。设备实际输出仍取当前 `Display.getHdrSdrRatio()`
与内容上限的较小值，并沿用向上平滑、向下立即服从设备上限的过渡；不支持 HDR 或实时余量不足
时自然回落，禁止抬高 SDR 基色补偿。

`3.6×` 是全管线可申请的最高范围，不代表所有 HDR 效果共同达到该峰值。最高额度优先分配给
第 0 层镜面闪点；连续银泽可以比当前更亮，但因覆盖显著大于闪点，必须使用独立、较低的峰值和
覆盖约束。SSS 透射、局部轮廓和其它通道不得自动按 `3.6/2.0` 等比放大。具体闪点与银泽峰值表
继续单独裁决。

## D103 闪点容量修订为 4/4/3/3/2/2/1/1/0，并采用独立出生权重（2026-07-14；第 7 层 HDR 由 D107 修订）

第 0～8 层镜面闪点同时存活容量上限修订为：

```text
[4, 4, 3, 3, 2, 2, 1, 1, 0]
```

软配额出生机会积累权重定为：

```text
[4.2, 3.6, 2.4, 1.92, 0.96, 0.6, 0.36, 0.16, 0.0]
```

权重是出生机会积累速度，不是数量或平均占用，所以第 0 层权重可以高于容量。第 7 层最多同时
1 个闪点，以 `0.16` 的低权重极少出现，并按 D96 严格保持 SDR；第 8 层容量和权重均为 0。
没有真实几何候选时仍不得补点，未消费机会按 D99 回流全局池。

本决策修订 D102 的第 7 层容量；D102 关于容量不是保底、单个闪点不得降低几何和时间质量的约束
继续有效。出生权重不再由“容量 × 公共锚点”自动计算，而以本决策数组为准。

## D102 九层闪点容量上限定为 4/4/3/3/2/2/1/0/0（2026-07-14）

第 0～8 层镜面闪点同时存活容量上限正式定为：

```text
[4, 4, 3, 3, 2, 2, 1, 0, 0]
```

第 3 层保留与第 2 层相同的瞬时容量、第 5 层保留与第 4 层相同的瞬时容量，用于在真实坡面候选
集中出现时维持连续的流动质感；层间主次由 D101 公共锚点、D99 软配额目标和实际候选质量继续
区分。容量只是硬上限，不是固定数量或平均占用保证，第 3～6 层不得因容量较高而长期顶满。

闪点随景深主要通过目标占用、出生频率和出现机会减少，并辅以适度的长度、alpha 与 HDR 峰值
衰减；单个存活闪点仍须满足 D100 的完整曲面几何、身份跟踪、自然包络和抗锯齿质量。该数组
取代分析稿建议的 `[4,4,3,2,2,1,1,0,0]`。

## D101 九层水体公共材质存在度锚点定为 1/0.927/0.81/0.64/0.48/0.30/0.16/0.06/0（2026-07-14）

第 0～8 层的公共材质存在度锚点正式定为：

```text
[1.0, 0.927, 0.81, 0.64, 0.48, 0.30, 0.16, 0.06, 0.0]
```

该数组只表达各层目标存在感和单调主次，是 D95 四个效果族的共同深度坐标，不得直接作为所有
效果的统一强度乘数。第 0～2 层维持主体，第 3 层相对第 2 层平缓下降，第 4～6 层继续有序退出，
第 7 层只剩极弱痕迹，第 8 层退出。各效果族可以按自身频率、几何和 HDR 职责重新映射，但映射
结果必须保持相同层序，不得反向抬高远层或形成新的硬截止断崖。

本决策取代分析稿中建议的 `[1.00, .93, .82, .65, .47, .30, .15, .04, 0]` 试算值；实现和测试
统一使用用户指定的精确锚点作为基准。

## D100 闪点先判真实几何再衰减，并作为最高优先级流动材质（2026-07-14）

闪点候选形成不再在几何门槛之前乘当前 `max(0, 1-depth01/0.42)`，因为该项会让第 4～5 层
主镜面候选严格归零。候选先根据镜面方向、局部坡度、曲率和 specular AA 判断真实反射几何；
通过有效门槛后，再按离散实体族的层级权重降低出生机会、尺寸、alpha 与 HDR 峰值。不得通过
全局降低几何门槛接纳噪声，也不得生成缺乏局部坡面依据的保底闪点。

用户同时确认镜面闪点是当前最可见、最能凸显水体流动感的效果之一。后续实现与验收将其列为
最高优先级材质效果：除分层存在率外，必须审视曲面内侧几何、太阳方向一致性、尺寸与亮度层次、
跨帧身份、自然出生/消失、随水体运动的连续性、specular AA 以及 HDR/SDR 质感。第 3～6 层可以
逐层降低显著度，但不能以矩形化、低质量几何、随机噪点或时间跳变换取覆盖。

## D99 闪点跨层分配采用软配额并回流未使用额度（2026-07-14）

D94 的跨连续面统一出生池继续保留，但不能再仅以全局最高分长期让第 0～2 层占尽出生机会。
每个具备闪点职责的水层按其目标占用率积累有限的出生机会；只有该层存在超过有效几何门槛的
真实坡面候选时，软配额才可被消费。候选层相对目标占用不足时获得有限优先权，第 0～2 层仍因
目标占用、容量和候选质量更高而保持主体地位。

某层没有合格候选或已经达到容量时，其未使用额度立即回流全局池，由当前质量最高的有效候选
使用。不得为满足配额生成与局部坡度无关的随机闪点，不采用逐层机械轮转，也不保证每层固定
保有数量。具体目标占用率、容量和候选门槛在后续层级锚点与候选形成裁决中确定。

## D98 顺流银线只延伸到第 4 层并继续保持 SDR（2026-07-14）

顺流银线从当前第 0～2 层延伸到第 4 层，但仍是稀疏的流动事件而非持续材质。第 0～2 层保持
现有数量、强度和生命周期；第 3 层最多同时 1 条，初始强度约为第 2 层的 `45%`，并降低出生
频率；第 4 层最多同时 1 条，初始强度约为第 2 层的 `20%`，出生更稀疏。第 5～8 层不生成
顺流银线。

D76 的亮度边界继续有效：所有顺流银线严格处于 SDR，不获得 HDR 超白峰值。第 3～4 层的数量
上限不能解释为固定保底，只有符合原有流动事件条件时才出生；实现阶段还应让强度和出生间隔随
公共层级锚点连续变化，避免在第 2→3、第 4→5 层出现新的突跳。

## D97 波峰薄雾只延伸到第 4 层，远处能量归入连续表面（2026-07-14）

波峰薄雾继续作为 D63 所定义的唯一独立柔雾通道，但覆盖从当前第 0～2 层平滑延伸到第 4 层。
第 0～2 层保持现有表现；第 3 层强度以第 2 层为参照约为 `45%`，第 4 层约为 `18%`。第 5～8 层
不再生成或绘制独立薄雾，其无法解析的微弱波峰能量由 SDR SSS 与连续银泽承接。

这一分界用于避免中远层重复出现多条柔白边，使水层重新泛雾或混在一起。后续实现必须以连续
深度权重进入和退出，不能把第 2→3、第 4→5 做成新的二值跳变；百分比作为首轮调试锚点，允许
在固定基准截图与真机对照后小幅收敛。

## D96 第 6 层只保留极弱局部 HDR，第 7～8 层严格回到 SDR（2026-07-14；由 D105～D107 修订）

第 6 层允许在确有局部反射几何或有效闪点候选时进入约 `1.05～1.08× reference white`，用于避免
第 5→6 层形成新的 HDR 截止断崖。该层的水体基色、宽域连续面和其它非高光区域仍保持 SDR；
不得通过整层增益、提高 APL 或放宽无几何依据的随机亮点实现。第 7～8 层不分配超白亮度，所有
HDR 峰值严格为 `1.0`。

这一边界修订 D74 中“第 6～8 层基本 SDR”的分组：第 0～2 层仍承担最高 HDR 峰值，第 3～5 层
逐层减弱，第 6 层只剩极弱局部尾部，第 7～8 层才是严格 SDR。具体第 6 层峰值在实现与真机
对照时从上述区间内收敛，不由本决策固定为单一值。

## D95 水体材质采用公共层级锚点与四个效果族衰减（2026-07-14）

九层水体不再由各效果各自散落的 `layer <= N` 形成近三层与其它层的材质断崖。后续实现先定义
一组只表达第 0～8 层主次关系的公共层级锚点，再由四个效果族分别解释：连续表面族负责宏观
坡面光/阴影、微法线、SSS 与连续 HDR；局部轮廓族负责表面反射、薄峰透射、背坡窄阴影与
波峰薄雾；离散实体族负责闪点、解析光晕与顺流银线；远景景深族负责第 7～8 层羽化。

公共锚点只保证从近到远单调衰减，不作为所有效果的统一乘数。低频形体应比高频细节保留得更远，
无法解析的离散镜面能量应逐步转入稳定的统计银泽；远景羽化继续保持远层专属。第 0～2 层仍是
主体，第 3～6 层依次获得弱化版本，第 7～8 层可以接近不可见。具体锚点、HDR 截止、顺流银线与
波峰薄雾的覆盖以及闪点分配策略继续逐项裁决，不能由本决策提前推定。

## D94 Sun-glitter 使用跨连续面的统一出生池（2026-07-14）

镜面闪点继续复用 D70 的锚点跟踪、`0.30s` attack、`0.80s` release、低强度软门和单点亮度，且不新增音频或节拍乘数。变化只发生在出生选择：所有可生成闪点的水层先把未匹配锚点放入同一个候选池，再由一个总出生额度跨层选择；候选分数乘以固定太阳方位导出的连续路径权重，路径外仍保留 `0.12` 的概率底，避免形成常亮光柱。路径近处较宽、远处较窄，并随深度连续偏移，不允许每层各自生成互不相关的亮点系统。

闪点几何沿真实相邻深度行方向轻微展开，近层最大 `2.6dp`、远层最大 `1.3dp`，只改变短轴的水面内展开，不增加数量、不改变 HDR 峰值。Android GLES 与 Python ModernGL/QPainter 使用同构路径、统一出生预算和几何规则；Python 宽度继续固定为 `320dp/640px`。

## D93 水面 SSS 以 Fresnel 互补预算进入 HDR（2026-07-14；逐层峰值由 D108 修订）

Step D 复用现有 `sunriseSubsurfaceMask()`，只在 scene-linear HDR 分支给真实水面增加超白透射差量；SDR 的 `addSunriseSubsurface()` 公式与输出保持不变。近层目标峰值为 `1.45× reference white`，到 `depth01=0.62` 平滑归回 `1.0`，并始终受设备实时 `uHdrHeadroom` 和统一 `uHdrGain` 封顶。透射乘以 `(1-R)`，与 Step C 银泽的 Fresnel 反射预算互补：掠射角让位给反射，较正视角保留身份色透射；颜色从本层 `subsurfaceColor` 归一化派生，不混灰、不改色相。

独立 optical mode 8 仅保留很弱的肩部，逐层峰值收为 `1.08/1.06/1.04/1.02/1.0…`，避免与真实水面上的 Step D 重复记账。该变化不改变几何、闪点数量、音频映射或 SDR 基色。

## D92 HDR 银泽使用独立低通法线，闪点只在不可感知后退出（2026-07-14）

HDR 水体银泽不得继续直接放大 25×最多 120 顶点网格上的原始坡度。原始坡度仍供几何、微法线、
SSS 和 D91 背坡阴影使用；银泽新增一组专用坡度属性，在实际渲染网格上做横向 3 次、纵深 4 次
`[1,2,1]/4` 预滤波，再进入 Fresnel。近层目标 headroom 从 `1.6` 提到 `2.0`，Fresnel×太阳
响应以 `0.70` 次幂拓宽；只在 scene-linear HDR 分支生效，SDR 基色不变，最终仍受设备
headroom、深度退出和统一 HDR 增益约束。Python 保持 320dp/640px 宽度，只同步新增顶点属性。

镜面闪点取消 `intensity < 0.04` 的硬裁剪。强度 `0.018～0.060` 使用 smoothstep 软门与原
幂次包络相乘，track 在 `0.015` 退休前已经不可感知；两显示帧间隔用于闪点身份跟踪时封顶为
`1/15s`，暂停、卡顿或恢复后的长时间差不得在单帧内吃完整个 `0.80s` release。Android Canvas
回退和 Python QPainter/ModernGL 使用同一包络；顺流流光仍按真实时间推进，不受此封顶影响。

`depthScattering` 函数、`uDepthScatteringStrength`、固定为 0 的参数和两处调用全部删除。
`deep` uniform 仍是 D91 背坡阴影的身份色目标，`subsurface` uniform 仍由日出 SSS 消费，二者
不是死代码，继续计算和上传。本决策在 Android 定稿后一次性同步 Python，避免两端反复改动。

## D91 Android 同步局部保色阴影并以共享 shader 为唯一实现（2026-07-14）

用户确认 Python 的受限坡面阴影方向可用，要求同步 Android 并发布真机对照。Android 新增默认参数
`macro_shadow_luma_cap=0.018`，保留 D87 正向同色提亮；背坡只使用宏观法线相对参考法线的负向
`N·L` 差，沿用 D90 的 `0.08～0.18` 坡度门、`0.35～0.70` 深度退出、crest 局部性和
`0.30` 宽域底权重。阴影朝未混白 Thing 身份色派生的 `deepColor` 移动，混色量按最终 linear RGB
亮度损失反解；不混黑、不读取微法线、不改变整层曝光，参数归零精确恢复 D87。

GLES 在共享 `water.vert` 中执行该规则，Canvas 回退计算同一 deep 目标、crest 收敛度和亮度封顶。
Python ModernGL 改为直接复用共享阴影函数，只保留自身对 `depthScattering` 的恒等覆盖，避免双重
注入和后续公式分叉；QPainter 公式保持同构。`lighten_far=0.864`、Python 320dp 宽度、深度散射
默认关闭、微法线、HDR、SSS 与其它局部光学均不变。

本决策取代 D90 中“Android 保持 D87、共享 shader 不修改”的临时范围限制；D90 的门控、亮度上限
和禁止宽域脏化约束继续有效。

## D90 Python 先试验受限直射光亏损，Android 保持 D87（2026-07-14）

用户确认 D87 只有同色正向坡面光仍偏平，要求先在 Python 实现调研方案。Python 的 ModernGL 与
QPainter 新增可归零参数 `macro_shadow_luma_cap=0.018`：保留 D87 正向增益，只对宏观法线相对
参考法线的强负向差生成局部背坡阴影；`N·L` 使用 `0.08～0.18` 门，深度在 `0.35～0.70`
淡出，`crestPinch` 把宽域底权重限制为 `0.30`。阴影朝未混白 Thing 身份色派生的 `deepColor`
移动，并按最终线性亮度损失反解混色量，不使用 `blackMix`、微法线暗纹或整层曝光压低。

初始调研值 `0.07～0.17` / 局部底权重 `0.45` 在真正三角形插值后仍可形成约 `133px` 的
可感知连续暗段，实施时据最终像素收紧为上述值；固定 `t=4s` 的 ModernGL 最长暗段约 `73px`、
QPainter 约 `49px`，最大 8-bit 实测亮度损失约 `1.42%`。十个运动时间点中最坏连续段
`135px`，但该帧最大损失仅约 `0.92%`，宽域部分保持低于强阴影量级。

本决策仅改变 `E:\projects\audioVisualizerSimulatorFable`，Python 继续保持 320dp；Android
共享 shader、参数和发布版本不修改，继续遵守 D87，待用户先目测 Python 动态效果后再决定是否同步。

## D89 将 lighten_far 从 0.96 收到 0.864（2026-07-14）

用户在 D88 的 `0.96` 真机对照版后要求改为 `0.864`。Android 与 Python 默认值同步调整；静态
九层的基础混白步进约为每层 `10.8%`，介于原 `0.60` 基线的 `7.5%` 与 D88 的 `12%` 之间。
本决策取代 D88 的正式默认值，其它颜色公式、D87 坡面光、alpha、环境与局部光学均不变，
Python 继续保持 320dp 宽度。

## D88 将 lighten_far 提到 0.96 进行层间分明度对照（2026-07-14）

用户希望九层水体更加分明，要求把 `lighten_far` 从 `0.60` 调到参数上限 `0.96`。本轮仅改变
Android 与 Python 的默认值，不调整九层 alpha、环境色、D87 正向坡面光、表面反射、闪点、
SSS、薄峰透射或水面几何；Python 继续保持 320dp 宽度。

`FableSolLayerColorPolicy.lightenAmount()` 仍按
`depth01 * (lighten_far + moodBright * 0.08 + breath)` 计算并钳到 1，因此最远层在动态提亮时
可能更早到达纯白。本版作为真机视觉对照，重点检查层界是否更清晰以及远层是否因过白而损失颜色。

## D87 以封顶的同色正向坡面光恢复水体立体感（2026-07-14）

用户在 D86 干净填充色版本上确认大范围脏阴影得到控制，但水体基色因逐层平色而显得过平。
固定帧对 1%、1.5%、2% 三档上限及多档响应进行了横向取样：`1.5%` 上限配合 `0.12`
响应时，第 1/2/4/5 层的 61px 宽域亮度变化约为 `1.5/1.5/1.54/1.76` 个单位；低于
旧纵向受光近层约 4 个单位的宽域变化，同时已能跨过 8-bit 下约 1 个单位的平色量化区间。

因此恢复纵向长波坡面对整层填充色的轻量调制，但只允许 `max(fullNdl - referenceNdl, 0)`
产生正向增益；增益严格按基色 RGB 同比例相乘，不混入天空色，不计算 Fresnel 候选色，不使用
`blackMix`，任何背光坡面都逐通道返回原基色。相对增益响应为 `0.12`，近层封顶 `1.5%`；
深度权重从近层 `1.0` 平滑衰减至远层 `0.45`。Android GL、Canvas 回退以及 Python 的
ModernGL/QPainter 路径使用同一数学规则，Python 继续保持 320dp 宽度。

本决策仅取代 D86 中“纵向长波受光对填充色恒等”的部分；D86 对深度散射默认关闭、表面反射带
局部门控以及其它窄域光学的约束继续有效。

## D86 大范围填充色明暗退出，水体体积只由局部光学表达（2026-07-14）

用户确认 Python 第一轮只限制纵向受光后，近中层仍存在可感知的大范围阴影。固定恒色帧逐项消融
确认有三个宽域来源：`depth_scattering` 对第 1/2/4/5 层产生约 12/15/11/7 个亮度单位的整体
压暗；纵向长波受光产生最高约 4 个单位的宽域抬亮；旧 `surface_strip` 在非波峰位置也保留
`1.2dp` 基础宽度，并可在中层形成约 11 个单位的宽域抬亮，使相邻基色被感知为阴影。

Android 与已经验证的 Python 干净水体策略统一：`depth_scattering_strength` 默认归零；GL 与
Canvas 回退的纵向长波受光对整层填充色恒等；表面反射带改为迎光与波峰双门控，无波峰或非迎光
位置宽度为 0，近层最大约 `3dp`。波背阴影、薄峰透射、波冠轻纱、镜面闪点和流光继续作为局部
光学保留。`water.frag` 的 HDR 掠射银泽不属于本次大范围脏阴影，保持现状。

本决策取代 D36 的纵向正负受光输出规则以及 D83 的 `1.2～9.4dp` 连续表面反射宽度；D83 的
薄峰透射、解析光晕和其它 HDR/材质约束继续有效。深度散射派生色与 shader 路径暂保留，默认
强度为 0，便于保留材质结构和独立诊断能力，但不再进入正式默认画面。

## D85 Python 默认复用 Android 共享 GLSL，HDR 桌面链先定位为数值诊断（2026-07-13）

Python 当前连续水面默认使用 ModernGL 独立 OpenGL 3.3 上下文，直接读取
`shared/fablesol/glsl/` 的 7 个 Android 共享 shader；桌面适配只替换 GLSL 版本声明并移除
ES precision，不复制第二份 shader。绘制顺序保持 environment → 逐层 water/optical 交错 →
front fill/第 0 层 optical → present；present 同步 16dp 圆角和准备态 0.16/活动态 1.0 的
360ms 过渡。`auto` 建立 GL 失败或运行时失败时回退 QPainter，
`gl` 可用于要求显式失败，旧九层 A 基线仍走 QPainter。

桌面 HDR 先同步数值语义：线性 `RGBA16F` 场景、显示余量上限、逐层核心/受光浪峰/透射峰值和
360ms 录音态增益过渡均与 Android 同构；线性场景峰值可单独读取验证。Qt/QImage 最终输出经过
sRGB 编码并仅作为 SDR 预览，不把它表述为系统级 HDR 窗口输出。若以后需要桌面真 HDR，另行验证
操作系统、窗口 surface、色彩空间和显示器能力，不改变当前共享几何与 SDR 基线。

## D84 Python 保持 320dp 宽度，其余效果继续同步 Android（2026-07-13）

用户明确要求 Python 模拟器宽度不变，其余差异均可调整。因此 Python 继续以
`320dp×420dp` 作为自洽的物理与渲染容器，不改成 Android 的 `280dp×420dp`，也不通过内部
280dp 物理加外部缩放伪造一致。双端对照需要按归一化横向坐标、相同特征帧和相同时间点验收，
不把可见波长数量或墙面位置的完全重合列为目标。

除宽度外，允许同步当前 Android 的映射与效果裁决、表层光学参数、Thing 身份色轴、全局 1/f、
镜面抗锯齿、双深度散射、微法线、朝阳 SSS、解析光晕、ModernGL/共享 GLSL 和 HDR 数值语义。
Python 的文件输入、离线导出、参数面板及 QPainter legacy 后端继续保留。

## D83 三项表层光学效果精确恢复 Debug 202607130749（2026-07-13）

用户要求把表面反射、薄峰透射和闪点外围解析光晕恢复到 Debug
`202607130749`，同时保留后来单独加回的 `body light`。本次采用该 Debug 对应的
第一阶段 SDR 材质值，而不是继续沿用 D81～D82 的宽材质对照值：

- 表面反射带恢复
  `(1.2 + (5.8 + 2.4 * crest) * facing) * (1 - 0.45 * depth)`，近层最大约 `9.4dp`；
- 薄峰透射恢复 `(1.6 + 9.4 * signal) * sqrt(signal)`，最大约 `11dp`，
  `thin_glow_gain=0.38`；
- `analytic_halo_strength=0.10`，解析光晕几何恢复为核心长度 `1.18×`、厚度 `2.25×`、
  alpha 系数 `0.18`，着色器衰减恢复 `exp2(-6.5*r²)` 和 `0.72` 边界软出；
- `body_light_strength=0.36` 保持启用，不随三项效果回退到 `202607130749` 的关闭状态。

D81～D82 对上述三项表层光学效果的恢复与收窄结论由本决策取代；其它决定继续有效。
FP16/linear scRGB HDR 管线、HDR eligibility 与分层峰值、颜色归一、更多窄闪点、
远层景深阶梯、波形、顺流流光和自动 SDR 回退均不改变。

## D82 薄峰透射从 23dp 轻量收至 18dp（2026-07-13）

用户在四项宽材质恢复版之后，单独要求薄峰透射再收窄一些。为保持可归因性，
只将 `FableSolMaterialPolicy.thinGlowThicknessDp()` 从
`(3.0 + 20.0 * signal) * sqrt(signal)` 调整为
`(3.0 + 15.0 * signal) * sqrt(signal)`，使满强度最大厚度从约 `23dp` 降为 `18dp`。

`thin_glow_gain=0.55`、薄峰透射颜色、绝对海拔/曲率门、HDR eligibility、分层峰值和
只覆盖第 0～4 层的范围均不变。D81 恢复的表面反射、外围光晕与 `body light`
继续保持；HDR 管线、颜色归一、远层景深与自动 SDR 回退不变。

## D81 HDR 保留，四项宽材质恢复去雾前基线（2026-07-13）

用户真机确认 Debug `202607130828` 已成功进入 HDR，但要求下一版把表面反射、
薄峰透射、闪点外围解析光晕恢复到第一阶段去雾之前，并重新启用 `body light`
进行真机对照。恢复值以收紧前代码为唯一基线：

- `body_light_strength` 由 `0.0` 恢复 `0.36`；
- 表面反射带宽度恢复 `(2.2 + (10.5 + 4.0 * crest) * facing) * (1 - 0.45 * depth)`，
  近层最大约 `16.7dp`；
- 薄峰透射恢复 `(3.0 + 20.0 * signal) * sqrt(signal)` 的最大约 `23dp`，
  `thin_glow_gain` 由 `0.38` 恢复 `0.55`；
- `analytic_halo_strength` 由 `0.10` 恢复 `0.21`，几何恢复为核心长度 `1.38×`、
  厚度 `4.2×`、alpha 系数 `0.24`，着色器衰减恢复 `exp2(-4.6*r²)` 和 `0.82`
  边界软出。

D80 的 FP16/linear scRGB 管线、实时 headroom、录音态启停、分层 HDR 峰值与自动
SDR 回退全部保留。`body light` 与 analytic halo 仍不分配 HDR excess；表面反射和薄峰
透射保留现有 HDR eligibility 与峰值，只恢复其共享材质宽度和 SDR 能量。不恢复
`pearl_shift_deg`、`hue_temp_deg` 或固定青/蓝派生色，也不改 environment、阴影、远层羽化、
水层 alpha、波形、闪点数量和顺流流光。D72、D75 中关于这四项必须收紧的实施结论
由本次真机对照决策修订，其它景深与 HDR 边界继续有效。

## D80 HDR 使用 SurfaceView 独立的 FP16 线性 scRGB 管线（2026-07-13）

代码与 Android 官方行为核对确认：`Window.setColorMode()` 不会改变 `SurfaceView` 的
surface 颜色管理，因此 FableSol 不把录音 Dialog 整个 Window 切换为 HDR。API 34+
只在当前显示器报告 HDR 且实时 `hdrSdrRatio` 可用时尝试 HDR；EGL 必须同时
支持 `EGL_EXT_pixel_format_float`、`EGL_KHR_gl_colorspace` 与
`EGL_EXT_gl_colorspace_scrgb_linear`，并成功建立 float component config 及 linear scRGB
window surface。任一条件失败都在同一 `SurfaceView` 上自动回退 RGBA8 SDR，不创建
第二套水体渲染器。

HDR 场景先在 `GL_RGBA16F` framebuffer 中以线性值合成，再输出 linear scRGB；
`GL_RGBA16F` 不可渲染时取消 HDR excess 并回退 SDR scene target。API 35+ 仅在
RECORDING 且 HDR 链路已就绪时，为 `SurfaceView` 请求 `2.0×` desired headroom；
PREPARED/STOPPED 以 `0.36s` 统一增益平滑回到 SDR，状态变化不重建 surface。显示器
headroom 下降时立即服从实际上限；上升时才平滑展开。

只有镜面闪点核心、符合朝向的窄表面反射和少量薄峰透射可生成
`>1.0` 线性值；其它光学实体的 HDR eligibility 固定为零。光学 pass 仍使用 `SRC_ALPHA` 保持
SDR 外观，但 D131 已取消 reference white 之上 excess 的 inverse-alpha 预补偿；源颜色只申请
当前局部峰值，实体 alpha 与 coverage 自然决定写入场景的实际能量。
SDR 路径的 excess 严格为零，不加入 tone mapping、shoulder 或全局曝光补偿。

## D79 材质去雾与 HDR 分成两个 Debug 版本验收（2026-07-13）

用户同意按可归因顺序实施并发布：第一阶段只建立共享 SDR 材质基线，完成内部宽光去重、色相归一
和窄高光片段调整；环境、景深阶梯、阴影与 HDR excess 保持不变。该版本先通过真机判断水体是否
更通透、颜色是否仍保留 Thing 身份以及高光覆盖是否合适。

只有第一阶段方向确认后，第二阶段才启用 FP16/scRGB HDR 输出、D57/D74 的分层超白峰值、运行时
能力检测和自动 SDR 回退。两个阶段不得合并到同一首轮 Debug 中，以免无法区分材质清晰度与显示
峰值各自造成的变化。

## D78 首轮去雾不额外压暗阴影，只移除冗余宽光（2026-07-13）

用户同意首轮内部光学重组不主动压暗波背、深水区、ThingBackground 基色或现有中间调，也不提高
`back_shade_gain`。取消九层 `body light`、收紧重复柔光后，先观察由宽光减少自然产生的局部
对比提升。

该约束避免在同一轮同时“去亮”和“加暗”，使 APL 变化与材质清晰度的归因保持明确，也防止
FableSol 从柔雾直接摆向沉闷、厚重的水体。若首轮真机仍显得扁平，才允许把波背/深水阴影作为
第二轮独立、可归零的 A/B 项；阴影即使调整也必须继续由 Thing 身份色派生，不得变成脏灰或纯黑。

## D77 光学高光不再独立旋转色相，只沿 Thing 色到中性白变化（2026-07-13）

代码核对确认，当前高光颜色除固定冷色目标外，还通过 `pearl_shift_deg=6` 以约 `12s` 周期进行
`±6°` 色相摆动，并叠加 `hue_temp_deg×0.6`、即约 `+3°` 的固定偏移。用户同意取消高光、
受光浪峰和顺流流光的这些独立色相旋转。

重组后，各位置的光学颜色只沿“该位置的 ThingBackground 本地颜色 → 中性白”轴改变亮度与彩度；
不得通过周期或固定 hue rotation 产生额外冷暖漂移。现有强度呼吸、高光出现频率和实体生命周期
继续保留，不把色相静止误解为动画静止。仅最深传播路径继续允许 D58 约定的相对身份色不超过
约 `2°` 轻微冷移。

## D76 顺流流光保持 SDR，不参与 HDR 超白分配（2026-07-13）

现有 `flow streak` 是低 alpha、沿背景流平移的长寿命光学实体：单条约持续 `5～9s`，每层最多
约 `2～3` 条。用户同意继续把它定义为运动与流向线索，而不是满足镜面朝向条件的瞬时强反射。

因此 `flow streak` 保持 Thing 色系和 `<=1.0` 的 SDR 亮度，不生成 HDR excess。D57、D74～D75
的超白预算只用于表面反射、受光浪峰和镜面 `glint`；不得因流速、寿命包络或音频驱动让长亮带
进入 HDR，以免形成发光拖尾并混淆 D17 已区分的流光与镜面闪点语义。若流光本身需要调整，只能
作为共享 SDR 材质对比的独立 A/B 项处理。

## D75 HDR 覆盖通过增加窄高光片段实现，不扩宽柔光（2026-07-13）

用户同意把“高光稍微多一些”落实为近、中层上更多离散且较窄的合格高光片段，而不是加宽单个
闪点、`halo`、`thin glow` 或 `crest veil`。高光采用“清晰核心 + 很弱的窄柔边”双尺度结构；
允许适度提高窄片段数量、连续性和浪峰占用率，但不得把整段浪峰变成连续白线。

当前镜面闪点核心本身较窄，但 `analytic halo` 以核心厚度约 `4.2×` 展开，并与多类波峰柔光
叠加。实现时应先收紧、降能或合并这些外围通道，再调整合格片段的空间密度。不得用更宽的低频
亮区完成 D57 的中等 HDR 覆盖，否则会抵消 D72～D73 的去雾和局部对比目标。

## D74 HDR 高光随水层深度递减，远层不争夺超白峰值（2026-07-13）

用户同意在 D57 的亮度档位之上增加明确的景深分配，避免九层水体都以相似超白峰值破坏 D59 的
层级辨识：近层 `0～2` 可完整使用约 `1.6～2.0×` 的镜面核心；中层 `3～5` 仍保留较多高光，
但超白峰值约收在 `1.2～1.5×`；远层 `6～8` 基本保持 SDR，不产生明显 `>1.0` 的高光。

远层继续通过既有的偏白、透明、柔化和羽化表达距离，而不是依靠 HDR 峰值。深度衰减只约束额外
高光能量，不改变几何、九层 alpha、`lighten_far` 或既有景深阶梯。这样超白覆盖主要分布于近中
六层，满足“比极少数亮点稍多”的目标，同时让近层最晶亮、远层保留空气感。

## D73 “整体更亮”指局部峰值与主观晶亮感，不要求提高 APL（2026-07-13）

用户同意把最初的“整体亮度更高”界定为：高光峰值更明确、局部明暗对比更强，最终使水体在主观上
更明亮、更晶莹；不要求平均画面亮度（APL）或整片水体填充亮度同步上升。D72 取消九层独立
`body light` 后，如果 APL 自然小幅下降，可以接受。

首轮不得通过抬高 environment、ThingBackground 水体基色、九层 alpha 或宽范围补光，补偿被移除的
宽光能量；否则会重新降低局部对比并带回雾感。亮度提升集中由更窄的表面反射、受光浪峰与镜面
闪点承担，其中 HDR 设备按 D57 获得受控超白峰值，SDR 设备则依靠共享材质重组获得更清晰的局部
对比。APL 只作为防止意外整体变暗或过亮的诊断指标，不作为必须上升的优化目标。

## D72 去雾首步锁定环境，只重组内部光学层（2026-07-13）

代码核对显示当前 `environment_tint=0.16`，而 `body light` 覆盖九层、`thin glow` 覆盖前五层、
`crest veil` 覆盖前三层，另有 surface band、analytic halo 与远层 edge feather 叠加。用户同意把
D60 的首轮隔离进一步拆分：第一步连环境 top/horizon/bottom 的颜色、亮度和插值都完全锁定，只
处理内部光学层累计。

该步骤按 D63 取消独立九层 body light 并并入统一材质/透射预算，收窄 thin glow，保留低能量
crest veil；同时继续锁定 `lighten_far`、九层 alpha、远层 edge feather、水面几何和景深阶梯。
只有内部重组经真机 A/B 后仍显得过雾，才允许开启第二个独立环境色 A/B，不得在第一步中用环境
变化掩盖内部光学层的归因。

## D71 SDR 从源头不生成超白增量，不加入高光肩部压缩（2026-07-13）

用户同意：当设备、显示器、surface、实时 ratio 或录音状态不允许 HDR 时，将内容可用 headroom
直接约束为 `1.0`，使 HDR excess 从源头为零；不先生成 `>1.0` 场景值再由应用 tone-map 回 SDR。
所有既有 `<=1.0` 的 SDR 输出保持原亮度，不加入从中间调或高光以下开始的 soft knee、自动曝光、
全局压暗或峰值重分配。

该规则延续 D52 对高光压缩的真机否决：不能为了在 SDR 中模拟 HDR 层次，再次让现有高光变暗。
D58～D63 的去雾、配色和材质能量重组仍同时改善 SDR/HDR；只有 D57 定义的额外亮度维度在 SDR
能力下消失。SDR 与 HDR 继续共用同一材质计算和参数，不维护独立调色版本。

## D70 HDR 复用现有闪点生命周期，不增加专属闪光包络（2026-07-13）

代码核对确认：当前 GLES 镜面闪点是由 `FableSolGlOptics` 跟踪的持久实体，锚点强度约以
`0.30s` attack、`0.80s` release 平滑，并叠加既有慢呼吸；它不是逐帧随机出现的一帧亮点。
用户同意 HDR 直接复用该实体、位置、强度轨迹和生命周期，只扩展符合 D57/D69 条件的空间核心
亮度，不新增 HDR 专属的快速 attack、短脉冲、余辉或节拍闪光。

若真机上 HDR 闪点仍显得过于柔和，只允许把现有统一闪点包络作为单一、可回退的 A/B 项调整，
不得在 SDR/HDR 之间维护两套时间语义。受光浪峰和薄层透射继续随自身连续光学条件变化，也不套用
离散闪点包络。

## D69 最高 HDR 镜面核心趋近中性白，中间层保留身份色（2026-07-13）

用户同意细化 D58 的“同色系”边界：`1.6～2.0×` 的最高镜面闪点核心可以随亮度降低彩度、趋近
中性白，但仍保留轻微 ThingBackground 色倾向，不成为完全无身份的纯白斑点，也不得统一偏成
青白或蓝白。该低彩度只表达强反射光，不重新引入固定绝对色相。

`1.15～1.4×` 的受光浪峰和窄表面反射带应明显保留 ThingBackground 色相；`1.05～1.2×` 的
薄层透射保留最多身份色。由此形成“峰值核心最接近中性、浪峰仍有身份、透射身份最强”的彩度
阶梯。该规则修订 D58 对表面反射的绝对同色解释，但不改变只有最深传播路径允许约 2°轻微冷移
的边界。

## D68 不因假设的控件变暗预先限制 HDR 视觉上限（2026-07-13）

用户明确否决“为了保护同一 Dialog 的 SDR 文字和按钮，预先压缩 HDR 亮芯、缩小超白覆盖或自动
回退 SDR”的优先级。本轮应先完整实现 D57 的三级亮度与中等覆盖，按动画本身的视觉效果上限
验收；不得仅依据部分 OEM 可能压低混合界面 SDR layer 的理论风险，提前降低水面峰值或覆盖。

文字与控件的感知亮度只列为真机观察项，不作为当前 HDR 内容预算、能力启用或发布的先验门槛，
也不预先提亮控件补偿。只有用户在实际设备上明确认为控件表现不好时，才把该现象作为独立问题
复现、量化和裁决；届时不得静默改写已验收的 HDR 水面基线。

## D67 HDR 峰值不直接乘以音量、onset 或节拍（2026-07-13）

用户同意：HDR 额外亮度继续服从表面朝向、坡度、曲率、光向、Fresnel、薄层透射与材质能量
预算，不新增由音量、onset、节拍、prominence 或其他音频特征直接控制的 HDR gain。声音仍按
现有模型驱动波形、微表面状态与光学实体的出生条件，因此可以间接改变 HDR 高光的数量、位置、
持续时间和可见面积，但不能把同一个光学条件下的单个高光直接推得更亮。

该边界用于避免大音量时宽范围过曝、节拍闪烁和“亮度计”式表达，同时保留 Voice Waveform 对
声音的自然响应。HDR 与 SDR 仍共享同一套声音到几何/材质的映射；差异只发生在 D57 已定义的
局部受光结果是否可以超过 SDR reference white。

## D66 只有正在录音状态产生 HDR 额外亮度（2026-07-13）

用户同意：录音 Dialog 整个可见生命周期保持同一个 HDR-capable SurfaceView/EGL surface，状态
切换不得通过销毁和重建 surface 改变输出能力；但只有 `RECORDING` 状态允许场景线性结果中出现
高于 SDR reference white 的亮度。`PREPARED` 与 `STOPPED` 状态的全部内容保持 `<=1.0×`，现有
水面模拟、层级和柔和展示仍可继续运行。

进入 `RECORDING` 时，HDR 内容增益随现有水面 `0.16 → 1.0` 过渡逐步开启；停止录音时同步平滑
收回到 SDR，再保持 `0.16` 的展示状态。能力探测与 surface 配置不因状态切换反复执行；API 35+
的 desired headroom 可以随状态调整，但不得引发 surface 重建。该状态门控服从 D65 的自动能力
判定，并且只改变额外亮度，不改变几何、动画、身份色、材质语义或 SDR 基线。

## D65 HDR 自动启用，不增加正式用户设置项（2026-07-13）

用户同意：FableSol 的 HDR UI 渲染由运行时自动探测，不增加正式版手动开关。只有 API 34+、
当前显示器支持 HDR、`Display` 的实时 HDR/SDR ratio 可用且大于 1、FP16/scRGB EGL config 与
SurfaceView window surface 均创建成功时，才启用 HDR 输出；任一条件不满足或运行中 headroom
降至不可用时，自动回退同一 SurfaceView、同一场景结果的 SDR 输出。

API 35+ 可对 SurfaceView 请求 D57 所需的约 `1.5～2.0×` desired HDR headroom，并继续服从
系统返回的实时 ratio；API 34 不假定固定 headroom。Debug 版可以提供能力状态、false-color、
强制 SDR 和受保护的 HDR 探针，用于诊断与 A/B，但这些不构成面向用户的外观设置，也不得绕过
实际 surface/display 能力强制提交无效 HDR。正式版不因 HDR 自动切换而改变几何、动画、身份色、
材质语义或 SDR 基线。

## D64 渲染容器由 TextureView 改为统一 SurfaceView（2026-07-13）

用户确认：Android 官方能力表将 `SurfaceView` 标为完整 HDR 支持，而 `TextureView` 只在
Android T+ 提供有限 HDR 支持，因此 FableSol 的 GLES 容器由 `TextureView` 改为
`SurfaceView + 自管 EGL`。API 26+ 统一使用同一个 `SurfaceView`，不维护 HDR 设备使用
SurfaceView、旧设备继续 TextureView 的双容器分支；API 26～33 输出同源 SDR，API 34+ 仅在
显示器、实时 headroom、FP16/scRGB EGL 链路均满足时启用 HDR。

SurfaceView 保持在宿主窗口下方，不调用 `setZOrderOnTop(true)`，录音文字与控件仍由普通 View
叠在其上。由于 API 34 以前 SurfaceView 忽略 `0 < View alpha < 1` 的中间透明度，准备态与停止态
的 `0.16 ↔ 1.0` 过渡改为渲染器内的 presentation alpha，不再依赖 Host/View alpha。圆角也不得
假定普通 View outline 能裁切独立 surface；渲染器必须通过带 alpha 的输出明确形成圆角，并把
API 26、API 34 与 API 35+ 的无矩形漏边、控件遮挡、弹窗动画和 surface 重建列为发布门槛。

Android N 起 SurfaceView 的平移与缩放会和 View 渲染同步，项目 minSdk 26 已覆盖该边界；因此
现有弹窗约束不足以继续承担 TextureView 有限 HDR 的代价。本决策修订 D41、D56 与 ADR-0016，
但不改变 D40 的同源材质约束，也不授权新增第二套水体实现。

## D63 只保留 crest veil 作为独立柔雾通道（2026-07-13）

用户确认：为保留适度雾感，`crest veil` 继续作为唯一独立的柔雾通道，但必须保持低能量、克制覆盖，
并严格留在 SDR reference white 以内。它表达波冠附近的风格化柔化，不承担主要亮度，也不得通过
扩大范围补偿表面反射或透射。

现有九层 `body light` 不再作为独立宽光带叠加，而应并入统一的透射/体色计算，与 Fresnel 反射
共享能量预算。`thin glow` 继续表达较窄的浪峰透射；三者不得再重复表达同一片内部亮度。具体
`crest veil` 强度和宽度先以独立 A/B 校准，不修改 D59 的景深阶梯。

## D62 晶亮感以表面反射为主、内部透射为辅（2026-07-13）

用户确认：本轮亮度与晶亮感主要由表面反射、受光浪峰和镜面闪点承担；浪峰内部透射只作为较窄、
较弱的辅助，不再用大面积 body light 或 crest veil 塑造整体内发光。高光允许在材质上更集中，
但不得改变现有柔和浪形或把宏观波峰变尖变窄。

后续光学能量预算必须先满足反射主、透射辅的层级，再应用 D57 的 HDR 三级亮度。宽范围内部光
只能保留在 SDR 基线内，且不得通过扩大覆盖来补偿被削弱的峰值。

## D61 首轮不加入折射或焦散（2026-07-13）

用户确认：本轮先用现有反射、浪峰透射、光学层能量重组与 HDR 完成清澈度提升，不新增背景折射、
填充视差或焦散。此前 A5.5 的折射视差与多项结构变化组合已被整体否决；当前第 0 层不透明，也
缺少可被真实看穿的背景语义，不能为了“剔透”直接恢复假折射扰动。

只有上述主路径完成并经真机确认仍缺少透明介质感时，才允许把轻微折射作为单一、小幅、可完整
关闭的 A/B 项独立试验，不得与层级、配色、焦散或波形变化同批。焦散仍维持既有整体删除结论，
本决策不提供恢复条件。

## D60 首轮去雾对照锁定水层景深阶梯与远层羽化（2026-07-13；执行顺序由 D72 修订）

用户确认首轮隔离范围：`lighten_far`、九层 `alpha` 以及远层 `edge feather` 的强度和宽度保持
现状，不在同批调整。首轮只允许修改环境底色，以及 body light、crest veil、thin glow、halo
等内部宽柔光的颜色与能量关系，用于验证过量雾感是否主要来自环境和光学层累计。

该对照不得改变水面几何、波形、层级顺序、ThingBackground 身份色或音频映射。环境变化可能
改变 edge feather 采样到的颜色，但其几何、alpha 与出现条件仍须保持不变。首轮结果若已经清澈且
层级稳定，就不再调整景深阶梯；若仍需修改，返回 D59 的相邻层可辨识约束另行裁决。

2026-07-13 修订：D72 将本范围拆成两个独立步骤。第一步连环境也锁定，只重组内部光学层；仅当
结果仍过雾时，第二步才允许单独调整环境。

## D59 保留水层景深阶梯，不以完全去雾为目标（2026-07-13）

用户明确：不同水层越远越偏白、越透明，是区分层级的既有设计职责；优化不能把各层颜色拉近到
互相混合。目标是减少过量的材质泛白和柔化，而不是完全取消雾感或空气透视。

后续必须把两类现象分开处理：

- 水层景深阶梯：按深度单调提高明度/混白并降低 alpha，用于九层水体的前后分离，必须保留；
- 材质雾感：近白环境色与 surface band、body light、crest veil、thin glow、edge feather、halo
  等宽柔光累计形成的低对比泛白，可以重新分配和收敛。

“同色提亮取代混白提亮”只作为待验证候选，不得直接全局替换景深阶梯。首轮对照应锁定既有水层
颜色/透明度阶梯，只调整环境与内部光学层，以隔离雾感来源；若仍需修改景深阶梯，必须保持相邻层
可辨识度和远近单调性，并再次经用户验收。

## D58 表面中性同色，仅最深传播路径允许极轻微冷移（2026-07-13）

用户确认取消所有固定绝对色相目标：`thin glow` 不再向 165°偏移，deep 不再向 220°偏移，
subsurface 不再向 150°偏移。表面反射、受光浪峰与薄层透射均保持 ThingBackground 的同色系，
不得跨记事颜色持续注入统一的青蓝或蓝绿色气质。

只有最深水层中具有足够传播路径的体色可以保留相对当前身份色不超过约 2°的轻微冷移，用于维持
纵深，而不是作为全局水体色温。次表面透射默认保持同色或随光源产生克制的中性提亮，不使用固定
绿色目标。本决策修订 D53 的 deep/subsurface 色相部分；D53 的轻量双色结构、明度/彩度派生与
独立强度开关不因本次裁决自动删除。

2026-07-13 修订：最高 HDR 镜面核心允许按 D69 趋近中性白；中等亮度浪峰与薄层透射继续保持
身份色。该镜面去彩度不视为固定色相偏移。

## D57 HDR 覆盖不得仅限极稀疏闪点核心（2026-07-13）

用户否决“只有极少数镜面闪点核心进入 HDR”的过度保守分配：若正常观看时与现有 SDR 的差异
不明显，就没有达到加入 HDR 的目的。兼容设备上除少量最高亮度核心外，还应让更多实际受光的
浪峰段和薄层透射以较低增益进入 HDR，形成可明确察觉但仍有层次的中等覆盖。

用户确认采用三级内容亮度分配，数值均表示相对 SDR reference white 的场景线性倍数，并在运行时
服从设备实际 headroom：

- 闪点核心与最强浪峰约 `1.6～2.0×`；
- 较多受光浪峰与窄表面反射带约 `1.15～1.4×`，作为正常观看时主要可见的 HDR 差异；
- 少量浪峰薄层透射约 `1.05～1.2×`；
- 环境、水体基色和宽光晕不得超过 `1.0×`。

本决策不授权抬升环境、整片水体基色或宽范围均匀光幕；额外亮度仍须由表面朝向、曲率、光向与
透射条件约束。设备可用 headroom 低于目标时，各级亮度保持顺序并连续压缩，不得通过提高 SDR
基线补偿。

## D56 HDR 只放宽兼容设备上的受控局部高光亮度（2026-07-13；覆盖由 D57 修订）

用户确认：D40 的 API 26+ 一致性继续严格约束水面几何、动画时序、材质语义、ThingBackground
身份色与 SDR 基线；但在 API 34+ 且显示器、当前 headroom、EGL 扩展和 SurfaceView 实测均满足
条件时，允许受控高光区域使用 SDR reference white 以上的额外亮度。API 26～33、非 HDR 显示器或
能力探测失败时必须回退到同一材质的 SDR 输出，不得改变波形、配色身份或动效。

这项放宽只用于真正的 HDR 亮度维度，不重新授权 AGSL、设备专属几何、额外材质效果或其他
按 API 分级的视觉差异。Display P3 等广色域能力与 HDR 分开命名、分开探测，不能用扩大色域
冒充提高动态范围。HDR 覆盖范围由 D57 继续修订。

## D55 四项持续质感作为可独立关闭的一批接入（2026-07-13）

应用户明确要求，本批同时接入 1/f 全局呼吸、风梳微法线、朝阳 SSS 与闪点解析光晕，作为 D50
“逐项验收”的一次显式例外。四项分别由 `global_pink_breath_strength`、`micro_normal_strength`、
`sun_sss_strength` 与 `analytic_halo_strength` 控制，任一参数归零时不得影响其余三项。

1/f 呼吸只调制持续环境波幅、稀有二维波包的出生间隔和镜面闪点的新生速率，不改写已经形成的
音频主浪，也不让已有闪点随呼吸消失。风梳微法线由水体片元 shader 的三倍频解析导数值噪声生成，
沿 `surface_heading_deg` 拉伸和推进；每个倍频按远近行采样足迹带限，并复用
`specular_aa_strength` 控制过滤过渡。朝阳 SSS 只读取固定日照方向、视角、深度、同色派生的
subsurface 色与 `crestPinch`，方向瓣指数限制在 4～10，禁止接瞬态音频。解析光晕作为镜面闪点
亮芯之前的曲面内侧实体，使用数学指数衰减，不使用模糊纹理或越过水面轮廓。

## D54 解析镜面抗锯齿接入真实闪点坡度高斯路径（2026-07-13）

当前 GLES 镜面闪点不是 fragment shader 的传统 `pow(N·H,p)`，而是
`FableSolGlOptics.buildGlints()` 用 `exp(-(Δs/σ)²)` 对宏观坡度与十项毛细坡度谱选峰，再生成
持久曲面内侧光带。因此 Stage 2-3 不新增无消费者的 shader 指数，而在
`FableSolOpticalWaveSet` 按当前列采样足迹解析带限：每个波长在每波 2 个样本以下完全转为方差，
4 个样本以上完整保留，中间用 smoothstep 过渡。

被带限的坡度方差按高斯卷积关系进入 `σ_eff²=σ_base²+2·variance`，被带限的曲率方差以统计
RMS 补回稳定的微表面活性，并以 `σ_base/σ_eff` 保持一维积分能量。只有闪点选取使用带限后的
slope/curvature；体积光等既有光学
继续使用未过滤毛细坡度。`specular_aa_strength` 独立默认 1，设为 0 时过滤量和方差均为 0，逐值
恢复原闪点路径。此项不改变闪点实体数量上限、跟踪、颜色、曲面内侧几何、双色散射或水面运动。

## D53 Stage 2-2 采用轻量双色深度散射，不改变现有高光（2026-07-13；色相由 D58 修订）

高光压缩回退版已获用户确认，继续执行 D45 的下一项“双色深度散射”。每个水体渐变色在 CPU
的 OKLab/OKLCH 语义下派生 deep 与 subsurface：deep 使用 `L×0.70、C×1.15`，向蓝绿色最多
偏 8°；subsurface 使用 `L×1.15、C×0.85`，向绿色最多偏 4°。超出 sRGB 时沿同一 OKLab
色相收缩彩度，不用逐通道硬裁切改变色相。

GLES 顶点新增 Gerstner 横向轨道位移导数形成的浪峰收拢量；shader 只按远近视角、收拢量和
既有固定光向混合两个派生色，不接音频瞬态。`depth_scattering_strength` 独立默认 0.21，避免
大面积压暗；现有相对纵向受光、高光、光学实体、波形和运动均不修改。本项单独真机验收，
不满意则连同色板、顶点属性、shader 分支、参数和测试整项删除。

2026-07-13 修订：deep 向 220°最多偏 8°、subsurface 向 150°最多偏 4°的绝对色相目标退出
目标视觉，替换规则见 D58。

## D52 整项撤销 PBR Neutral 高光压缩，恢复 Stage 1 直接输出（2026-07-13）

真机反馈 Stage 2-1 使高光不够亮，整体观感反而变差。按“一项不好即整项删除”的迭代规则，
删除 PBR Neutral shader、RGBA8 离屏目标、最终 fullscreen pass、参数和专项测试，不保留关闭的
死代码；GLES 恢复到 Stage 1 已验收的默认 framebuffer 直接输出路径。D51 仅保留为被否决方案的
历史记录，不再代表当前实现。待本次回退版确认高光亮度恢复后，才考虑下一个独立质感项。

## D51 PBR Neutral 只作为线性高光 shoulder，不改 Stage 1 中间色（2026-07-13）

Stage 2-1 采用 Khronos PBR Neutral 的官方 `F90=0.04`、`Ks=0.8−F90`、`Kd=0.15` 曲线，
但当前输入已经是 Stage 1 最终介质颜色而非原始 PBR radiance，因此增加保护：在线性 Rec.709 中，
最大通道不超过 `Ks=0.76` 时严格原样返回；更亮时补回 F90 再运行官方曲线。整个场景先渲染到
RGBA8 离屏目标，再经单个 fullscreen pass 做 sRGB→linear→高光压缩→sRGB；独立参数
`pbr_neutral_strength` 默认 1，可整项关闭。这样保持普通记事色和低亮饱和通道逐通道恒等，只让
接近白色的高光平滑收束且不改变通道差方向。

## D50 Stage 1 观感一致通过，进入 Stage 2 单项质感升级（2026-07-13）

用户确认 `202607130124` 的 GLES 与 Canvas 观感一致，Stage 1 视觉复刻通过。后续进入 D45 的
Stage 2，继续坚持一次只加入一个独立质感项、真机确认后再推进下一项。首项为 Khronos PBR Neutral
启发的色相保持高光压缩；不得同批加入双色散射、微法线或其他表面结构变化。

## D49 撤销光带感知补偿，先修复 GLES 迁移差异（2026-07-13）

用户重新判断白带的参数可能没有问题，异常更可能来自 GL 迁移。撤销 D47/D48 的 GLES 专属缩放：
表面软带宽度、峰值透明度与远层羽化 alpha 全部恢复 Canvas 原值。代码审计确认 GL 存在纵向受光、
层内渐变、抖动覆盖、光学 alpha 混合、体积光带、波冠轻纱与闪点细节等实质差异；必须先修复这些
迁移问题，再判断是否需要视觉调参。A1 的 EBO 生命周期故障也必须在同批修复。D47/D48 仅保留为
诊断过程记录，不再代表当前参数决策。

## D48 最远两层羽化与表面软带分别校准（2026-07-13）

第 7、8 层上方的连续白带来自 `edge feather`，而表面软带只作用于第 0～6 层；两者必须作为
独立视觉通道校准。按真机反馈，表面软带宽度由 Canvas 的 52% 回调到 62%，峰值透明度由 48%
继续降到 34%；远层羽化保留原柔化宽度，alpha 最初缩放到原参数的 30%，后续真机反馈继续降为
15%。后续反馈须先按层号
和网格模式识别实体，不能用表面软带参数处理最远两层羽化。

## D47 GLES 表面软带采用独立感知补偿（2026-07-13）

真机复测确认：即使 GLES 与 Canvas 已逐项对齐几何宽度、颜色、基础透明度和
`0.66×sin(πr)` 剖面，表面软带仍显得更厚、更白。因此以真机感知一致覆盖纯数学一致：仅对
GLES 表面软带最初把峰值透明度缩放为 Canvas 的 48%，几何宽度缩放为 52%；薄峰透光、波背阴影、
远层羽化继续保留 Canvas 参数。两项缩放分别锁定亮度和厚度，避免再次用一个参数掩盖两个问题。
后续真机校准由 D48 接续。

## D46 OpenGL 失败统一回退 Canvas，并以纯红天空示警（2026-07-12）

Stage 1 期间由 `WaveVisualizerFableSolHost` 同时持有 GL 与旧 Canvas 实现；正常状态只运行
GL，避免双份 Simulation/CPU 消耗。任何 EGL 初始化、GLSL 编译/链接、绘制或 swap 失败时，
宿主隐藏 GL、启动 Canvas，Canvas 天空强制为纯红色，同时保留 `[DEBUG-FABLESOL-GL]`
错误日志。红色只表示后端失败，不参与正常视觉设计。GL 验收并按 D42 删除旧 Canvas 后，
失败策略再收敛为静态红色诊断面或明确错误 UI。

## D38 流畅验收标准：View 锁 60Hz、含倾斜全程稳定 60fps（2026-07-12）

用户确认真机症状为"全程不够顺滑 + 倾斜手机时卡"（全程性掉帧指向每帧固定
CPU 开销，非离散事件）。本轮性能工作的验收标准：可视化 View 锁定 60Hz
（120Hz 面板上不跟随满刷新率），静置、说话、倾斜晃动全程稳定 60fps 不掉帧。
水面运动柔和，60Hz 视觉无损，每帧预算 16.6ms，发热更低；不追 120Hz。

## D39 允许临时帧耗时仪表（D22 的边界澄清，2026-07-12）

架构选型前，允许在 debug 版临时加入应用内帧耗时仪表（FrameMetrics 百分位 +
onDraw 分段计时，日志文件或小字浮层），用于区分 CPU 逻辑耗时 / GPU 渲染耗时 /
调度降频三类归因。这不是 D22 禁止的"正式测试装置"，而是一次性诊断探针，
性能验收结束后整体移除。项目禁用 adb 的约束不变，数据由用户真机回传。

## D40 新渲染路径要求 API 26+ 严格像素级一致（2026-07-12；由 D56 局部修订）

用户裁决：本轮渲染升级产生的新路径必须在 minSdk 26 起的所有设备上严格像素级
一致，不接受"核心一致 + 逐像素增强分级"。该约束排除一切 AGSL 新特性
（API 33+ 专属），可选工具只剩 OpenGL ES（全 API 覆盖）与纯 Canvas。现存的
抖动/软带/深度吸收三项 AGSL 增强本身构成 33+ 分叉，属历史遗留；若新路径落地
OpenGL ES，应将其吸收统一，消除现有分级。

2026-07-13 修订：上述“严格像素级一致”不再包含 HDR 兼容设备上由 D56 定义的受控局部高光额外
亮度；除此之外的几何、动画、材质、身份色与 SDR 基线约束保持不变。

## D41 渲染架构迁移 OpenGL ES 统一渲染器（2026-07-12；由 D64 修订容器）

用户裁决：为同时满足 D40 严格像素级一致（API 26+）、D38 稳定 60fps 与逐像素
质感上限，可视化渲染迁移 OpenGL ES——最初选择 TextureView + 自管 EGL 独立渲染线程；
2026-07-13 经 HDR 能力调研与用户裁决，容器改为统一 SurfaceView + 自管 EGL，旧设备不保留
TextureView 分支，具体合成约束见 D64。本决策推翻 plan-2026-07-11"明确不做 SurfaceView/GL 迁移"与 D20 的
AGSL 阶段 C 立项路线；详细论证见 ADR-0016 与
research-2026-07-12-render-architecture.md。

分阶段：Stage 0 通用性能修复（锁 60Hz、稳态零分配、传感器后台合并、位图上传
治理、临时帧仪表 D39）——任何架构下都需要且全部可保留；Stage 1 GLES 渲染器
一比一复刻现有连续水面视觉并经真机验收（顺带消除 API 26-28 上 drawVertices
无硬件加速的既有覆盖洞与 AGSL 三项 33+ 分叉）；Stage 2 逐像素视觉升级（候选
见 research-2026-07-12-water-visual-quality.md TOP-10）。物理/分析/映射层
不受影响，仍为 Kotlin CPU + Python 蓝本同构。

## D42 GLES 验收后删除全部 Canvas 水体渲染（2026-07-12）

GLES 渲染器真机验收并经过一个发布周期观察后，删除全部 Canvas 水体渲染代码：
连续水面 ribbon、九层完整填充回退（D30 的 surface2d_on=0）与 AGSL 三件套
（抖动/软带/深度吸收）。此后视觉只写一遍，单一事实源。EGL 初始化失败的
极端场景降级为静态记事色填充，不保留第二套水体实现。D30 的内部回退条款
随本决策废止。

## D43 模拟器新增 moderngl 渲染后端，与 Android 共享同一份 GLSL（2026-07-12）

Python 先行纪律（D16/D22）在 GLES 时代的延续方式：audioVisualizerSimulatorFable
新增 moderngl GL 渲染后端，与 Android 直接共享同一份 GLSL 着色器文件；物理、
分析、映射层继续保持 numpy/Kotlin 双端同构 + 差分测试。视觉手法仍先在模拟器
目测认可再上真机，但渲染层从"双端各写一遍 + 目测对齐"变为"同一份文件共享"。
旧 QPainter 渲染路径保留作历史对照，不再承载新视觉。

## D44 GL 计划整体完成后再解冻 A6（2026-07-12）

排期：Stage 0 性能修复 → Stage 1 GLES 一比一复刻 → Stage 2 逐像素视觉升级
全部完成后，再解冻并推进 A6（声音驱动表达批）。取代 2026-07-11"视觉批目测
通过后即解冻 A6"的原约定；A1~A4 的声音感知、映射与境状态系统继续保持现状。

## D45 Stage 2 视觉升级纳入全部十一项候选（2026-07-12）

用户圈选"全部都要"：首波五项按序推进——①色相保持高光压缩曲线（Khronos
PBR Neutral）②双色深度散射（记事色 OKLab 派生 deep/subsurface + 浪峰收拢掩码）
③解析镜面抗锯齿（波分量坡度方差→高光指数）④风耦合（Cox-Munk 闪点统计与
粗糙度-天空带共享同一粗糙度场，闪点纵向拉长 ≤2:1）⑤1/f 呼吸调制；随后二波
六项——微法线风梳纹理、朝阳 SSS、解析光晕、线条笔意（原型标签，最后做）、
Display P3 广色域、近岸弯月面细线。

执行纪律不变：逐项独立开关、一次只改一项、模拟器 moderngl 目测认可后才上
真机、做不好整项砍（宁少勿烂）；A5.5 教训继续有效——不得同批同时改变层次、
颜色和表面结构。技法出处与参数细节见
research-2026-07-12-water-visual-quality.md。

## D37 前后俯仰采用完整输入与分通道软压缩（2026-07-12）

用户确认 `±55°` 并非必须，要求优化前后俯仰的临界停止。旧链路先在传感器入口和
`FableSolSimulation.setPitch()` 将角度硬截为 `±55°`，连续曲面内部再次截断；同时渲染观察角
以 `38°` 为基准硬限制在 `14°~68°`，使静态透视实际在前俯 `+30°`、后仰 `−24°` 就进入平台。

新增 `FableSolPitchPolicy`，把三种语义拆开：`pitchDeg` 保存手机完整 `−90°~90°` 俯仰；
`motionPitchDeg` 用归一化 `atan` 曲线将完整输入单调软压缩到 `±55°`，继续保护水体惯性幅度；
观察角用响应更快的归一化 `atan` 曲线，把完整输入按基准两侧分别软压缩到 `14°~68°`。
两条输出在整个物理范围内严格单调，只有手机到达 `±90°` 才到安全端点，不再在中途形成平台。

连续曲面的欠阻尼弹簧改用 `motionPitchDeg`，纵深透视改用原始 `pitchDeg` 经策略映射；内部
`±55°` 仅作为不接受越界目标的防御性安全阀，不再直接作用于传感器输入。该设计保留原最大
惯性幅度和观察角范围，同时让普通角度附近保持较快响应、越接近极限变化越缓。本决策取代
D30 中“手机法向分量转换为 `±55°` 前后俯仰”的输入限幅约定。

## D33 显式阴影统一由当前层颜色混黑生成（2026-07-12）

**状态：已由 D126 取代。** 本节保留当时从固定蓝黑阴影迁移到当前层色的历史背景；其中
“混黑”及固定百分比不是现行合同，禁止据此恢复实现。

用户真机发现浪面经常出现难看的灰黑区域，要求阴影改为更深的记事颜色。诊断确认两条
显式路径违反该语义：猫爪暗纹把所有层色混入固定蓝黑 `#080C16`；波背自阴影采用
OKLab 固定降明度后再冷偏 `−hue_temp_deg`，部分颜色会丢失原记事色身份。

新增 `FableSolShadowColorPolicy` 作为唯一显式阴影颜色入口：波背阴影使用当前层色在
OKLab 中混黑 18%，猫爪暗纹混黑 24%。两者不再接受固定灰黑/蓝黑色，也不再做额外冷暖
色相偏移；几何强度、透明度、出现条件、空气透视和连续水面物理不变。深度吸收的标量
暗化本身等价于基色混黑，不属于本次固定偏色问题。

## D34 阴影混黑量随层深递减（2026-07-12）

**状态：已由 D126 取代。** 现行实现应从当前层当前横向位置的主体色派生保色深色，不再
计算或衰减“混黑比例”；阴影强度仍可按层深与光照关系独立变化。

用户指出 D33 的固定混黑比例仍不正确：远层水体本身经过空气透视已经更淡，同样比例的
黑色会在远层形成更显眼的暗斑。波背和猫爪的有效混黑量统一乘
`max(0.35, 1 − 0.85×depth01)`；第 0 层保持 D33 的 18%/24%，越远逐层降低，最远层
最多保留基础黑度的 35%。

当前波背只绘制到第 5 层，其混黑量约由 18% 降至 8.4%；猫爪只绘制到第 2 层，约由
24% 降至 18.9%。该衰减只改变阴影色，不重复修改已有的层 alpha、空气透视或阴影几何，
避免远层被多套规则直接裁掉。

## D35 远层阴影黑度采用平方衰减（2026-07-12）

用户进一步明确“远方的黑色要很淡很淡”。D34 的线性衰减与 35% 下限仍过重，改为
`max(0.05, (1−depth01)²)`。波背基础 18% 因而在第 2 层约为 10.1%、第 5 层约为
2.5%、最远端约为 0.9%；猫爪基础 24% 在其最远参与的第 2 层约为 13.5%。

平方曲线让近层保持可读阴影，中远层迅速淡出；5% 是对基础黑度的比例下限，不是直接
混黑 5%，因此最远波背实际只混黑约 0.9%。

## D36 纵向法线负向受光收敛到基色混黑轴（2026-07-12）

用户复测指出主要灰黑区域并非波背或猫爪，而是与光线相关。定位到连续水面新增的
`applyLongitudinalLight()`：完整三维法线与旧二维法线分别计算 `N·L`、透射和 Schlick
Fresnel 后，旧实现用 `base + full − reference` 逐 RGB 通道相减。背光坡的负差值中包含
天空反射色差，会让主题色同时丢失明度与色彩身份，形成大面积灰黑。

保留完整物理候选色的明暗判断：候选亮度不低于基色时原样保留正向受光与天空偏色；候选
更暗时，只提取相对亮度下降量作为阴影强度，并重新生成基色混黑结果。近层最大混黑 14%，
实际量乘 `sqrt(darkness)`；层深继续乘 D35 的 `max(0.05,(1−depth01)²)`，远层负向受光
接近不可见。实现使用 packed ARGB 与通道乘法，避免在逐顶点路径增加对象和 OKLab 开销。

## D30 Android 默认采用连续 2.5D 水面，旧九层渲染仅作内部回退（2026-07-12）

用户确认将 `audioVisualizerSimulatorFable` 当前连续水面更新迁移到 Android。Android 以
`FableSolContinuousSurface` 承载 25 行二维方向波场：九层既有轮廓继续作为 Z 向几何、
颜色和声音角色锚点，二维场只叠加去除深度均值后的相对翻滚；第一层是唯一前景剪影。
渲染默认走连续 ribbon，旧九层完整填充路径保留为 `surface2d_on=0` 的内部回退，不暴露
用户调参入口。

连续路径同时迁移主波向/方向扩散、有限相干波包、深水色散、Gerstner X/Z 轨道、随波数
增强的耗散、前后俯仰欠阻尼惯性和纵向法线相对受光。手机三维重力的屏幕内分量继续控制
左右滚转，法向分量转换为 ±55° 前后俯仰；普通实时 onset 保留原前景即时响应，只有稀有
远浪与段落事件注入纵向波包。现有 ThingBackground 配色、第 0 层身份色、九层固定 alpha
累积和完整光学族继续使用原 FableSol 语义。

本决策取代 D24 中“视觉实现尚未获准移植 Android”的阶段性限制；声音驱动扩展冻结部分
仍然有效。本轮 Android 迁移不把 Python 的调参面板、命令行 A/B 或 `prototype_*` 命名带入
产品代码。

## D31 连续水面使用投影安全采样窗与分层批量网格（2026-07-12）

真机首轮发现 Z 轴翻滚时远处水体两侧偶发空白，且整体明显卡顿。连续路径不得再复用
旧九层按未投影湿润跨度计算的采样窗：窗口必须同时覆盖最远行最多 16% 弱透视收缩、
`z01≤1.1` 的画外余量、±10dp Gerstner 轨道位移和至少 2 个网格列的边缘余量；离散
索引必须向外包含边界外第一列，不能向内取整。

渲染保持 25 行和九层锚点不变，但 24 条 ribbon 不再各自构建 cubic Path 与
LinearGradient。每个相邻九层锚点之间的 3 条 ribbon 合并为一次带逐顶点颜色的三角网格，
全水面固定为 8 次网格提交；共享边使用同一顶点坐标，颜色边界通过逐 ribbon 重复顶点
保持清晰。九层渐变几何每帧只扫描一次，颜色停靠点、顶点色和网格缓冲复用。

二维方向波采样使用等距 X 网格相位递推，有限相干波包的二维高斯拆为 X/Z 可分离包络，
替代每个 Z×X 采样点重复调用 `sin/cos/exp`。该变换只减少重复计算，不改变波数、相位、
包络、九层角色或 25 行纵深分辨率。

## D32 倾斜时固定连续渲染列预算，边界剖面只以 30Hz 更新（2026-07-12）

用户复测确认常态播放不卡，主要卡顿集中在倾斜手机期间。诊断发现宽 276dp 时，投影安全
采样窗的原始列数会从水平 `110` 增至 `30°=172`、`45°=186`、`60°=190`、`90°=160`；
若全部直接送入 25 行网格、顶点着色和九层光学，倾斜期间渲染工作量最高增加约 73%。

连续路径实际渲染列数因此固定为最多 120 列：水平原始列数不足 120 时全部保留，超过时在
完整安全窗口两端之间等距插值；首尾必须精确对应原始窗口首尾，不能以丢弃画外列换性能，
故 D31 的两侧覆盖保证不变。物理模拟仍使用完整 216 点网格，只有渲染投影降采样。

同一诊断还发现 `rebuildBc()` 在持续倾斜 2 秒内重建 227 次，几乎跟随 120Hz 物理子步；
原因是 `span`、`wallBlend` 和 `tiltAgit` 每个子步都改变缓存键。边界剖面是空间阻尼系数，
统一限为 30Hz 更新，最大滞后 33ms；波方程仍以 120Hz 求解，倾斜浪形激励和传感器输入
不降频、不额外低通，以免改变姿态手感。

## D29 文件名行与计时器共享水平约束（2026-07-11）

录音结束后，文件名输入框与 `.wav` 后缀的可见总长度必须与 `TimelyClockView` 一致。此前文件名行
外层写死为 `232dp`，内部又将 `EditText` 和后缀分别写死为 `192dp`、`40dp`；虽然外层数值碰巧
等于对话框宽度减去计时器两侧边距，但 `.wav` 文本不会填满固定的 `40dp`，可见右边缘会提前结束，
而且两处约束容易独立漂移。

文件名行与计时器统一使用 `match_parent` 加两侧 `24dp` margin；`EditText` 使用
`0dp + layout_weight="1"` 占据后缀之外的剩余空间，`.wav` 后缀使用 `wrap_content`。今后调整
计时器的水平边距时，应同步调整文件名行，保持两者等宽。

## D25 Hero 声音能量改为上游出生、空间传播（2026-07-11）

为根治连续“你好”时既有浪偶发上抬，D17 的“包络只作用于出生处”优先于较早 D12/ADR-0006
对全局慢标量包络的许可。保留每层六模态 `HeroWave`、低/中/高频段身份、A3 音高映射、A4
境乘子和 `Prominence` 旋律层事件；但 `heroBandTargetDp` 不再直接缩放整段可见解析波。

每层新增三频段空间能量包络：目标先按原攻击/释放常数平滑，只写入上游画外出生区，再以
`|flow|×1.5 + 0.45×waveSpeed` 的群速度随流进入可见区。可见区内已经存在的 Hero 只传播，
不会因下一帧响度、频段或音高改变而整体重塑；均匀潮位仍归 swell/level 通道。Python 与
Android 必须保持同构，并以双向不变量验收：传播到达前可见轮廓不得分叉；到达后不同声音
必须产生可测差异，防止以关闭 Hero 响应伪修复。

## D24 当前先收敛视觉材质，冻结声音驱动扩展（2026-07-11）

当前阶段不继续扩展声音如何驱动水；A1~A4 的声音分析、映射与境状态保持现状，A6 的
HNR/arousal/looming/冲击性、“张力=相位”等声音驱动项目暂停。先在 Python 完成并目测收敛
统一光照、表面法线、厚度、遮挡、反射、透光、折射和纵深合成，再决定是否恢复声音驱动优化。

视觉实现仍遵守 Python 先行、用户动态目测认可后再移植 Android 的流程；本决策不授权提前移植。

## D14 美学章程：克制的抒情（2026-07-11）

升级方向定位采用"克制的抒情"：以平静陪伴为基底（录音对话框里用户的主任务是说话，
水在周边视觉工作），但保留完整表达阶梯——七境式的状态分辨率、只在真峰值出现的瞬态
高潮，以及"张力=相位"级别的作品手段。表达力由阶梯的分辨率承担，不由反应幅度承担。
一句话章程（暂定措辞）："你的声音是掠过一小片真实水面的风——它服从重力，短暂记住
你的能量，在你说完时归于平静。"运动总则："静中有动、动中有静"。

**范围事实**：当前主打麦克风录音可视化（录音内容可能包含音乐）；后续将加入离线音乐
文件的可视化。音乐相关投入（节拍、speech/music 门控、高能境）须按可复用设计，原版
被裁掉的离线路径（`offline.py`/`sections.py`）未来可能回归。

**为何**：设计奖调研显示评审奖励克制、同步感与概念纯度（Odio/Journey/Monument
Valley 先例）；"准确反映声音"与"获奖美感"在 B 定位下不冲突。

## D15 表达状态：锚点配方 + 连续插值，非离散状态机（2026-07-11）

mood 性格档的继任机制：七个外观锚点（idle / silence / quiet-speech / active-speech /
melodic / loud-music / climax（瞬态）+ settle（尾声））各自是一组完整参数预设（波长
分布、chop 量、闪带宽度、猫爪率、驻波增益等）+ 触发条件；运行时由感知输入（响度
包络、4Hz 波动强度、谐波性、粗糙度、speech/music 门控、arousal）在锚点间**连续插值**，
迁移带迟滞与不对称时间常数（升快降慢）。用户永远看不到状态切换。

**境不是旅程而是查询表**（蒲福风级语义）：平淡人声整段录音只驻留 quiet-speech ↔
active-speech，表达力来自境内微观动态（4Hz 呼吸、音节猫爪、闪带宽窄、句中悬停、
句末沉降）；melodic/loud-music/climax 仅在录到歌唱、音乐、喊叫时出现。这同时修复
现状 mood 无锚点漂移导致普通语音落在未调参组合的问题。

命名：代码用功能名；马远《水图》对应题名（洞庭风细、湖光潋滟、层波叠浪、云舒浪卷、
细浪漂漂等）保留在文档与注释中，作为灵感来源与设计词汇。调参主战场明确为
quiet-speech / active-speech 两境的境内动态。

## D16 乐队分层：九层职责重分配，深两层"无动于衷"（2026-07-11）

取代"每层听全频段、远景=高频几何"的 `LAYER_ROLE_WEIGHTS` 模型：

- 装饰层（0~1）：onset/爆破音的水花与毛细闪光——高频内容改由前层**光学**表达；
- 旋律层（约 2~3）：音高轮廓与重读（语音主唱）；
- 织体层（4~6）：音节密度纹理 + 慢频段包络；
- 深层（7~8）：**永不响应逐帧人声**，只承载永恒慢涌，并以数十秒时间常数跟随能量
  积分与段落（"长曝光"语义）。

**为何**：Chion anempathetic 原则——有"不在乎的世界"，上层响应才读作"倾听"而非
仪表；透视上远水本就动得更慢更少，"远层跟齿擦音抖"违背距离感；高频以近层闪烁
表达（Journey glitter 纪律）比远层几何波动更像空气感。

**实施流程**（适用于本轮全部表达/映射改动）：先在 `audioVisualizerSimulatorFable`
Python 版实施，用户目测认可后再同构移植 Android/Kotlin 并补差分测试。

**修订（2026-07-11 用户目测）**：深层长积分只在**有声期间**生效；静默/停止后深层
以 τ≈2s 与全场一同快速归静，不做数十秒余韵沉降（第 8/9 层拖几十秒不落被明确否决）。
涌浪沉降半衰期同日由 6s 调至 3s。"结束沉降"整体求快、不求余韵。

## D17 三层时间记忆 + 波群包络，且包络只作用于浪的出生处（2026-07-11）

时间结构分三层：风浪 chop 即时跟随音节（现有注入+毛细纹）；涌浪 swell 以半衰期
积累短语级能量，是"结束沉降"尾声的本体——**默认半衰期 3s**（用户 2026-07-11 目测
6s 沉降过慢后修订；参数范围 1.5~10s 可调）；深层
（7~8）以 20~40s 长积分跟随整段录音能量史。涌浪对水位的贡献设上限并计入体积守恒，
防止长录音水位持续偏高。

响度进入涌浪 register 采用**波群包络**：能量以组为单位（一组约 2~4 个浪）乘载，
浪在组内生于尾、亡于首；一句话成为一组浪，句间停顿是组间间歇。

**D12 延伸（用户 2026-07-11 强调）**：已塑形的浪不得被随意改形（不得在波峰附近
凸起小包、不得突然变尖或变平），除非改变是必要、柔和且符合物理的（如新浪从边界
涌入引起的自然相互作用）。因此波群包络与涌浪记忆均只在**浪的出生处**起作用——
新生浪的振幅由包络/能量库决定，随后按正常物理传播；绝不以乘性场调制已存在几何。
水位类变化仅允许潮位式的缓慢均匀偏移。

## D18 注入反克隆纪律；"张力=相位"按受控边界试做（2026-07-11）

用户报告现状缺陷：强事件下九层在右侧同一位置出现九个近似相同的波包并同步流动，
"像复制粘贴一样"。成因是 `inject()`/`injectRhythmWave()` 对全部九层循环注入，抖动
（σ=7~16dp）相对波包宽度（100~216dp）过小、逐层固定延迟、形状同源。纪律：

- 同一事件的注入目标只限其**角色层**（D16），任何单一事件不得进入超过约 3 层；
- 逐层时序来自波群包络的自然传播差，废除全层固定 cascade 延迟；
- 逐层宽度/峰形/travel 独立变化，空间散布量级须与波包宽度可比；
- rhythm wave 重设计为单层（或两层交替）的单一行波，不再是九层齐发。

"张力=相位"（Whitney）采纳为试验项：以各层有效推进速度的缓慢偏置实现（洋流式、
浪形连续），相干度上限约 0.6 永不完全对齐，触发需持续数秒渐强证据 × speech/music
门控加权（平淡语音不触发）；静息态回到失谐（不均齐为常态）。对齐只作用于 hero
长模态相位，绝不同步化注入事件，避免再造克隆感。优先级在双 register 与乐队分层
之后，Python 先行。

## D19 音频前端 11 项全采纳；YAMNet 暂缓、条件触发（2026-07-11）

前端清单三批全部采纳：P1（自适应白化+SuperFlux、K 计权 momentary/short-term 响度、
Scheirer–Slaney speech/music 门控）、P2 核心（YIN/MPM F0 + voicing、4Hz 波动强度→
呼吸容积、因果音节率、重音 prominence 事件）、P2 修饰（HNR→清澈度、arousal 复合→
境迁移、looming→波群生长率与新浪前倾、冲击性→注入峰形——受"不尖窄"红线约束）。
全部经典 DSP 自实现，无第三方库依赖；Python/Kotlin 同构 + 差分测试。

YAMNet（+5.7MB）暂缓：先以免费门控上线，真机验证出现可感知的 speech/music 误判
再引入；架构预留 1Hz 语义输入接口，届时纯增量接入。**为何**："宁少勿烂"红线下
优先减少可动部件；5.7MB 与 ML 运行时的长期维护承诺应由已证实的缺陷兑换。未来
离线音乐文件模式不受实时约束，届时另评估离线分析方案。

## D20 渲染首批六手法（Canvas/Python 可达），AGSL 推迟到表达层收敛后（2026-07-11）

首批：
1. **闪光带升级**——废除"恒宽带内物体平移"式实现（用户明确否决"轨道车厢"观感）。
   闪点是波面微元坡度对准光源的瞬时事件：**原地生灭**（寿命约 150~400ms、各自独立
   包络），"带"只是闪点出现概率的包络（高斯软边、宽度=活动度），闪点大小不一、
   沿面微拉长、主题色染近白。验收：定格见柔边光斑贴在坡度合适处；播放是明灭，
   不是行驶。
2. **猫爪暗纹**——音节触发的局部暗色毛细斑块顺流漂移消散（OpticalWaveSet 的
   局部化+暗化变体）。
3. **句末沉降尾声**——D17 记忆结构的编排呈现（chop 快死、涌浪 4~8s 沉降、闪带收窄）。
4. **驻波呼吸（试验标签）**——仅 melodic 境把墙侧吸收缓慢调低使反射显现，墙侧出现
   原地起伏的共鸣呼吸；语音境保持海绵吸收。Python 目测不喜欢即砍。
5. **顶边羽化**——低能量时最上层轮廓向背景溶解，能量升高变锐利。
6. **波峰珍珠斑**——现有 crest glow + pearl 偏色的合并升级（非新增手法）：以峰为
   单位的局部软斑，强度=f(峰高×圆润度×坡向对光源对准度)，边界被 OpticalWaveSet
   长波长分量咬出不规则；单侧（受光峰肩）、向下慢淡出；斑心染色近白、边缘环用
   ±6° pearl 色相偏移；只在近 0~2 层、圆润峰上，语音电平同屏 ≤3~4 个；尖峰无斑
   （与"不尖窄"红线自洽）。验收：读作"贴在峰肩的软鹅卵石光泽"，无盖章椭圆感。

全部 Python 先行、用户目测认可后移植。AGSL 单 pass、bloom/RenderNode 推迟到表达
层收敛后另行立项（避免外观未定型时同一套视觉写两遍）；OLED 渐变 banding 等只有
shader 能根治的问题接受暂时存在。

## D21 环境天空升级为"环境状态"子系统：明暗模式修复 + 时段色温（2026-07-11）

用户报告暗色模式 bug：录音前天空昏暗，点击开始录音后变为偏白背景。定位：
`AudioRecordDialogFragment` 在 PREPARED/STOPPED 把整个可视化 View 压到 alpha 0.16
（暗色对话框背景透出），录音开始拉回 1.0；而 `drawEnvironment()` 天空基色硬编码
WHITE、不感知 DayNight——alpha 拉满时白色天空全量显形造成翻转。

- **修复层（优先）**：天空基色跟随应用明暗模式，暗色模式改用记事色派生的深色基；
  录音前后过渡保持色彩连续（alpha 渐变不得引起天空色调翻转）。此项属 Android
  集成问题，不走 Python 先行（流程惯例的例外）。
- **装饰层**：修复后的天空叠加 ≤5% 本地时段冷暖偏移（夜冷/黄昏暖），只动天空，
  不动水体各层与第 0 层身份色（D1/D6 不变）。

## D23 计划收尾：暗色天空修复后置；reduced-motion 变体不做（2026-07-11）

D21 修复层不插队，后置处理（时机由用户再定）。系统"移除动画"下的 reduced-motion
退化变体经用户明确否决（"完全不要保留，没有任何意义"），不进任何阶段范围；调研中
的 WCAG 2.3.3 论据已向用户呈现，此为产品决策。执行计划见
`plan-2026-07-11-expression-upgrade.md`，重点先落在 Python 版实现。

## D22 验收装置只建 Python 侧；Android 验收 = 用户真机视觉反馈（2026-07-11）

延迟测量（onset→首个可见响应的端到端毫秒数，含"快触发 <50ms"回归）以 Python WAV
回放脚本实现，随表达层改动跑回归；渲染手法逐项在 Python 里做到"单独看足够好"并由
用户目测认可。Android 侧不建正式测试装置（无 debug 覆盖层、无自动截图墙），移植后
发布阿里云 debug，由用户实机测试并以视觉感知反馈驱动迭代。

## D1 配色接 ThingBackground（2026-07-10）

物理与高光一比一复刻原版；配色不用原版独立 palette，改接记事 `ThingBackground`，
支持纯色 + 渐变，渐变方向沿用记事 8 向 `Orientation`（复用 `BackgroundUtil.createLinearGradient`）。
原版"远层混白空气透视 / 色彩呼吸 / OKLab 混色 / 珍珠偏色 / 高光"逻辑全部保留，只把每层
两个基色 c1/c2 换成记事色：c1=bg.color；c2=渐变取 endColor，纯色取 `BackgroundUtil.lighter(color)`。
环境天空同样由记事色派生浅色。

**为何**：录音对话框需与当前记事主题一致，不能突兀地换成绯紫配色板。

## D2 坐标：dp 视觉标尺固定，对话框高度对齐 420dp（2026-07-10，宽度部分由 D8 修订）

Fable 按 dp 定义的波长、浪高、速度和网格采样间距继续一比一复用。`fragment_record_audio.xml`
把高度 360→420dp，对齐 `HEIGHT_DP`；渲染按 Android `displayMetrics.density` 把 dp 换算为 px。
本决策最初保留了 320dp 固定物理宽度，后由 D8 修订为运行时最终实测宽度。

## D3 采样率复用现有 44100Hz（2026-07-10）

原版 48000Hz；复用现有 `AudioRecorder` 的 44100Hz 采集。`FRAME_RATE=SR/HOP` 自适应
（512 hop → 86.13 帧/秒 vs 原版 93.75），所有基于 `FRAME_RATE` 的时间常量随之自适应，
算法结构不变；仅 N_FFT=2048 的频率分辨率有极小差异（21.5 vs 23.4 Hz/bin），视觉无感。

## D4 移植实时 Foote 新奇度段落检测（2026-07-10）

移植 `features.py` 的 `_NoveltyDetector`（倒谱 DCT + 棋盘核，实时因果路径），驱动性格档
（mood）切换。语义 MusiCNN（`semantic.py`）原版默认停用，不移植；离线段落导演
（`sections.py`）不移植。段涌巨浪 `surge_gain` 默认 0，段落主要表达为性格档差异。

## D5 线程模型：音频线程产帧，UI 线程消费（2026-07-10）

对照原版"音频进程 + UI 60fps 定时器"解耦。音频线程 `feed` PCM 得 frames/events 进并发
队列；UI 线程 vsync 每帧 drain 队列 → 只对最新 frame `applyFrame`、对每个 event
`applyOnset`/`applySection` → `Simulation.update(dt)` → 渲染。`Simulation` 只被 UI 线程
访问，无需锁。

## D6 第 0 层保持 Thing 原始颜色（2026-07-10）

距离屏幕最近的第 0 层代表当前 Thing 的主体身份色：纯色 Thing 的第 0 层必须完整使用
`background.color`；渐变 Thing 的第 0 层必须保留原始 `color`、`endColor` 与
`orientation`。原模拟器中“纯色 palette 自动生成向白混合 45% 的第二端色”不再用于第 0 层，
`color_breath`、`moodBright` 与 `lighten_far` 产生的混白也必须随 `depth01` 增长，因此第 0 层
混白量恒为 0。远层空气透视、声音明度变化、OKLab 层内混色、高光与物理逻辑继续保留。

**为何**：原模拟器 palette 是独立视觉配色，而 EverythingDone 的 `ThingBackground` 是记事身份色。
最近层若继续使用模拟器的自动提亮规则，会使纯色和渐变记事在正常录音态下都比记事本身浅灰，
也违背旧 `WaveVisualizerOpus`“主体/前景层使用记事本色”的既有语义。

## D7 PREPARED/STOPPED 持续监听并驱动水面（2026-07-10）

Android 录音对话框在 PREPARED 和 STOPPED 状态继续监听麦克风，以低透明度水面提供实时预览；
进入 RECORDING 时只开始写入录音文件，不重置 `FableSolRealtimeAnalyzer`、不重置其时间基准。
停止录音后立即重新开始监听也属于设计行为。保存 WAV 的起止仍严格由 `mIsRecording` 控制。

**为何**：这是 EverythingDone 的录音交互语义，不需要复刻 Python 桌面播放器只在
PLAYING/RECORDING 消费驱动的状态机。

## D8 物理容器宽度使用 View 最终实测宽度（2026-07-10）

运行时容器宽度必须来自 `WaveVisualizerFableSol.onSizeChanged(w, ...)` 的最终像素宽度除以 density。
不得直接使用 XML 的 `280dp`，也不得根据 TimelyClockView 的声明宽度自行推算；父布局、Dialog 测量和
屏幕约束共同决定出的最终 View 宽度才是唯一来源。

`REFERENCE_WIDTH_DP=320` 只保留为网格间距 `DX_DP` 的设计基准和 View 尚未测量时的回退值。
运行时的容器湿润跨度、沿重力方向尺寸、体积守恒倾斜水位、墙面边界、屏幕坐标注入中心以及
段落 surge 的 75% 宽度均使用实测容器宽度。波长、浪高、速度、固定 dp 注入宽度和
`DX_DP` 不随 View 缩放，以免改变原版的空间尺度、传播速度和数值稳定性。

## D9 自校准前增加频率边界与绝对可听度安全门（2026-07-10）

Android 近静音录音可能包含近 Nyquist 强电子干扰、低频嗡声和非平稳 AGC/噪声泵动。纯相对静音门与
flux 百分位会把这些不可闻或极低电平变化放大成约 0.5 响度和密集 onset，因此“自校准”不能等于
“完全没有绝对可信度边界”。

FableSol 前端统一采用以下规则，Python 蓝本与 Kotlin 实现保持一致：

- A 加权总能量、底噪、质心与平坦度只使用 16kHz 以下频谱；
- 32 个 flux 对数频带严格按相邻边界 `[edge[i], edge[i+1])` 求和并止于 12kHz，不再使用
  `reduceat` 最后一段延伸到 Nyquist 的语义；
- Analyzer 和时间基准重置后从静音状态启动；
- −66dBFS 为零可听度置信、−54dBFS 为满置信，中间采用 smoothstep；响度、低/中/高频动态与
  onset 包络/强度统一乘该置信度。绝对门仅拦截极低电平假活动，可信声音进入后仍使用原有底噪、
  慢中心和滚动百分位自校准；
- flux 环接收全部帧作为基线，保证稀疏真实脉冲无需先积满多个“有声帧”。

回归样本 `20260710215433.wav` 在 Python 48kHz 路径由 90 个假 onset 降至 3 个、平均响度由
0.272 降至约 0.004；Android/Kotlin 原生 44.1kHz 路径为 4 个 onset、平均响度约 0.008。

## D10 Android 重力倾角覆盖完整圆周并保持 Opus 方向锁定语义（2026-07-10）

录音 Dialog 的重力可视化必须接受完整 360° 屏幕平面方向。完全倒置手机时渲染角保持 ±180°，
水体填充方向转向 Dialog 顶部，不得沿用 Python 桌面调节滑块的 `[-90°, 90°]` 限制。
传感器角度跨越 179°↔−179° 时，Simulation 选择与当前角度最近的等价角，避免水体反向旋转 358°。
墙面过渡按水面偏离水平面的角度计算，因此 0° 和 180° 都视为水平水面。

`AudioRecordDialogFragment` 在 `onCreateView()` 锁定宿主 Activity 的当前方向，并在
`onDestroyView()`/`onDismiss()` 恢复原 `requestedOrientation`。该代码在 Opus→FableSol 迁移中没有
改动，继续作为录音 Dialog 的既有行为保留。

## D11 Android 采集会话启动阶段抑制低频暂态（2026-07-10）

部分手机的 AudioRecord/MIC 在采集启动后会输出持续数秒、随后衰减的强低频暂态。样本
`20260710231609.wav` 前 0~3 秒约为 −20~−23dBFS，A 加权能量约 70%~90% 位于 250Hz 以下，
约 4.5 秒后才降至 −55dBFS 以下。该信号真实存在于 PCM；应用当前显式关闭 AGC/NS/AEC，不能把它
简单归因于应用层“先录噪音、再开始降噪”。

Kotlin Analyzer 在每个采集会话开头增加自适应预热门：低频主导且尚未稳定的输入不进入底噪、中心、
flux 与视觉输出；连续稳定静音或可信非低频内容达到 0.3 秒后立即开放，最多等待 4.5 秒。若超时后仍是
低频背景，则把当前电平种为噪声底。该门只影响 FableSol 可视化，不裁剪、滤波或延迟保存的 WAV，
避免在用户未授权的情况下改变录音内容。

## D12 已形成的浪形不得被瞬态参数直接重塑（2026-07-10）

已经显示的浪只能因相位传播、波动方程、阻尼、边界、重力，或显式注入的新能量自然变化。
onset 不得直接改写整层 HeroWave 的振幅和频段模态；快速事件统一进入有穿屏限流的 DynamicWave
物理波包。HeroWave 只表达慢变化的声音背景：默认攻击由 0.16 秒改为 0.85 秒，各模态最短约 0.72 秒；
几何粗糙度另用 1.2 秒慢状态，快速粗糙度/毛细度仍可用于光学高光，但不能重塑宏观轮廓。

真实样本的拍击 onset 位于约 6.30、6.76、7.15、7.26 秒。禁用物理注入时，旧 onset 仍使既有轮廓在
6 帧内变化最高约 0.56dp RMS；极端频段目标单帧变化约 3.97dp RMS。修复后前者严格为 0，后者约
0.39dp RMS；真实拍击窗口不再出现远层同步突变，剩余变化来自连续传播和新注入的物理能量。

## D13 连续流速由表层事件率主导，节拍不得删除 subdivision（2026-07-11）

人的主观速度由表层事件密度、节拍层级和强弱等多种线索共同决定。FableSol 的连续流速采用 1 秒快速事件率与 3 秒保持事件率：快速通道以 72% 权重负责及时上升，慢速通道作为下限负责稳定与自然释放。已通过 D9 绝对可听度/SNR 边界的 onset，在显著度加权之外固定保留 75% 原始 subdivision；`beatConfidence` 不得再降低该保底。

tempo/phase 继续负责节拍脉冲和相位同步；tempo 对连续流速仅能在剩余行程内提供最高 12% 的正向佐证，不得以中等 BPM 向下覆盖已经较高的表层事件密度。onset strength 主要控制波高、注入能量、punch 与材质。分析侧速度攻击为 0.35 秒，Simulation 流速执行平滑为 0.48 秒；物理流速上限和全局 `flow_gain` 不因本决策提高。Python 与 Android/Kotlin 必须维持同构实现。

**为何**：固定 3 秒分母会把新出现的密集声音系统性低估约 3 秒；可靠节拍下删除弱 subdivision 会把快速元音或密集音符误判为中速；tempo 与 density 的凸组合还会让约 110 BPM 反向拉低每秒 6~8 个事件的表层速度。两段 Android 真机录音证明瓶颈位于感知映射，而不是 213.6dp/s 的第 0 层物理上限。

## D14 GLES 倾斜期间边界剖面按显示帧限额重建（2026-07-12）

连续倾斜会让 `span`、`wallBlend` 与 `tiltAgit` 持续变化。边界剖面仍以最高约 30Hz 接受新参数，
但九层不得再集中在一个显示帧内重建：除首次初始化外，每帧最多处理 5 层，下一帧完成其余层；
即使该帧因延迟补跑多个 120Hz 物理子步，也不得突破显示帧预算。

边界系数只依赖 `abs(u)`，左右点必须共用同一次计算并镜像写入，保证数值与逐点实现一致、把每层
指数运算减半。传感器采样、120Hz 波方程、倾斜几何和水体惯性不降频。Debug 性能日志继续保留
`physics` 总耗时，并增加 `steps`、`bcLayers`、`bc`、`waves`、`surface`、`compose`，用于真机判断
剩余卡顿是否仍来自边界重建。

## D15 GLES 光学实体保留 CPU 身份跟踪并按层穿插绘制（2026-07-12）

闪点、珍珠、流光和猫爪属于数量少、生命周期逻辑复杂的实体，不转成片元着色器内的逐帧随机噪声。
CPU 继续维护跨帧身份、锚点匹配、攻击/释放、顺流平移与寿命；每帧只生成少量带局部 UV 的椭圆
三角形，GLES 片元着色器负责径向软边。实体总量使用固定上限与预分配顶点缓冲，稳态不扩容。

绘制顺序必须复刻 Canvas 层遮挡：每个远侧水层区间画完后立刻画该层实体，再由后续近层覆盖；
最后画第 0 层深水填充和第 0 层实体。不得把所有实体统一放到水体之后，否则远层闪点会错误浮到
近层水面之上。光学实体使用标准 alpha 混合，水体主体仍保持不透明覆盖与原有逐层合成。

## D16 顺流流光采用水体内侧单边几何（2026-07-12）

流光不得使用“中心点切线上的直椭圆”。其长轴必须沿整段水面轮廓分段采样并随曲率弯曲；短轴从
每个采样点的轮廓开始，只向水体内部展开。片元透明度在轮廓后的短距离内从零软入，并在长度两端
与水内下缘软出。这样既不会有高光从长轴两端或短轴外侧露到空气中，也不会因硬裁切形成新亮边。

闪点和珍珠不受本决定影响：它们分别表达贴边镜面闪光与圆峰斑块，有各自的跨轮廓视觉语义。
回归必须同时检查实际流光网格的局部法向坐标全部非负，以及每个顶点均位于其横坐标对应的水面
轮廓以内；不能只检查中心切线、最终顶点数量或截图主观结果。

## D17 镜面闪点与顺流流光必须分通道约束（2026-07-12）

镜面闪点 `glint` 是高 alpha、偏白、随受光峰锚点呼吸和跟随的长高光；顺流流光 `streak` 是低
alpha、按寿命持续随背景流平移的高光。二者视觉上都可能呈斜带，但身份、运动和亮度不同，诊断、
顶点范围记录与回归必须分开。

镜面闪点不得继续使用跨轮廓的完整直椭圆。保留其峰值检测、持久身份、位置跟随和呼吸强度，几何
改用与流光相同的逐段曲面带：沿整段轮廓弯曲、只向水内展开；镜面闪点另用更窄的亮芯和更长的
软出区，避免形成锐利平直白线。固定光学顶点容量提升到 1536，只增加预分配上限，不允许稳态扩容。

## D18 珍珠斑与猫爪暗纹退出目标视觉（2026-07-12）

珍珠斑 `pearl` 与猫爪暗纹 `gust` 从 Android GLES、Canvas 回退、Simulation 状态和 Mapper 事件
入口整体删除。不得保留不可见跟踪器、阵风 FIFO 或逐帧推进；Python 同构清理由后续双端收敛时
处理，本轮 Android 主线不再生成或消费这两类效果。

保留镜面闪点 `glint` 与顺流流光 `streak`。Stage 1 下一批按现有 Canvas 语义迁移表面软带、薄峰
透光、波背阴影和最远两层羽化，不借删除两项效果之机改变其他光学参数。

## D19 四类表面效果统一使用分层曲面带（2026-07-12）

表面软带、薄峰透光、波背阴影和远层羽化进入 GLES 时统一由 `FableSolGlOptics` 生成沿轮廓的
可变厚度三角带，复用同一预分配顶点缓冲与分层穿插绘制，不恢复 Canvas Path/Gradient 或另建
每效果纹理上传。

- 表面软带仅 0～6 层，宽度由迎光坡、上凸曲率和深度共同控制；
- 薄峰透光仅 0～4 层，继续使用高出均线 4～14dp 的绝对海拔门与上凸薄度；
- 波背阴影仅 0～5 层，颜色继续走当前层记事色混黑、随深度快速减弱；
- 羽化仅 7～8 层，以 `calm01` 控制环境地平色跨轮廓溶解。

前三项只向水体内侧展开；羽化为溶解轮廓，可按既有语义跨越轮廓两侧。片元着色器只根据带内
相对深度软入软出。光学顶点固定容量提升为 20000，覆盖最坏层数与 120 列，不允许运行时扩容。

## D20 GLES 曲面带透明度严格复刻 Canvas 半正弦（2026-07-12）

Canvas/AGSL 定稿的 fade 带透明度为 `baseAlpha × 0.66 × sin(π·relativeDepth)`，峰值 0.66，
归一化积分光量为 `0.66×2/π≈0.420`。GLES 表面软带、薄峰透光、波背阴影和远层羽化必须使用
同一剖面；不得使用中央覆盖率 1.0 的宽平台。

几何厚度继续按 Canvas 的原公式，不因视觉白边问题先行缩窄。若峰值和积分偏高，同宽几何会同时
显得更白、更厚；应先恢复剖面一致，再由真机判断是否还存在颜色或几何偏差。峰值与数值积分必须
有自动回归，防止后续 shader 调整重新放大量层边缘。

## D151（2026-07-16）质感提升批·三改动以零默认 uniform 门控进入共享 shader

依据 2026-07-16 双份独立调研（本仓库 research-2026-07-16-water-look-uplift-latex 与
research-sol-ultra-0716）的交集，实施三项改进，全部先落 Python、经用户目测后再接 Android：

1. **统一光场**：`water.frag` 新增掠射差分光泽 `grazingSheen`（uniform
   `uGrazingSheenStrength`，逐行视角近 1.0×→远 0.37×仰角、只保留相对平坦水面的
   菲涅耳增量、光泽色 = vSubsurfaceColor 派生 + 太阳侧向白 ≤10%）；
   `environment.frag` 新增日出光锚（`uSunAnchorStrength`/`uSunAnchorQ`，
   地平线亮核 + 太阳侧梯度，锚点 x 与 glitter path 同源）。
2. **厚度透光**：`water.vert` 以 `uLayerMeanYPx[9]`/`uThicknessRangePx` 计算
   "高出层均值高度"厚度代理（varying `vThickness01`）；`water.frag` 新增
   `thicknessGlow`（`uThicknessGlowStrength`，目标色 = subsurface 线性提亮
   `uGlowDerivedBoost`≈1.35，保色相饱和比、不向白），并把同一厚度掩码并入
   HDR 透射 excess（`backlitTransmissionExcess` 增加 thicknessMask 参数）——
   SDR 透光与 HDR excess 同源。定位为"波峰透光/薄峰透光"实体带的材质版替代，
   目测认可后可考虑降档或退役实体带。
3. **闪点统计化**：模拟器 `gl_optics._glints` 以 Cox-Munk 均方坡
   （mss=0.003+0.00512U，U=5·wind·sparkle01，阻尼 0.5）展宽 σ；
   守恒分两半——锚点查找用未调暗的展宽场（数量↑），单体以
   conserve=widen^-0.6 收敛 alpha 与 HDR 资格（容量表 D136 在响亮段封顶数量，
   完全守恒会把单体压过头）。

**关键约束**：所有新 uniform 零默认 = 关闭；已用 4 配色 × 2 时刻验证
strength=0 与改动前逐位一致（maxdiff 0）——Android 在 Kotlin 未接线前行为不变。
模拟器面板新增"质感提升（试验）"组（uplift_sheen / uplift_anchor /
uplift_thick_glow / uplift_glow_boost / uplift_glint_wind，默认 0，1.0=建议档）。
桌面 GL 基准：全开 15.33ms vs 基线 15.28ms（增量≈0）。评审图与差异面板见
模拟器 `tmp/uplift-review/compare/`。QPainter 后端未实现（归零即回退），
Android 移植清单见 sessions.md 本日条目。

## D152（2026-07-16）质感提升批·首轮目测裁决与修复

用户 GUI 目测后对 D151 三改动的裁决与随之落地的修复：

1. **厚度透光定档**：`uplift_thick_glow` 默认 1.29、`uplift_glow_boost` 默认 1.6；
   旧"薄峰透光"实体带（`thin_glow_gain`）默认 0.38 → 0 并标记 deprecated
   （被材质版厚度透光取代，参数仅留回退）。
2. **掠射光泽整项砍除**（宁少勿烂）：shader 函数、uniform 与 Python 上传全部移除；
   D151 中该项作废。
3. **日出光锚可见性修复**：根因是提亮目标取"地平线色向白 25%"，而地平线环境色
   本身接近白（environment_tint 仅 0.16）——向白提亮不可见。改为 CPU 派生的
   晨曦染色 `uSunAnchorColor = mix_oklab(地平线色, 身份层0起点色, 0.42)`，
   shader 向该色染色。修复后 t45 差异 mean 6.95/P99 48（此前 0.5/5.6）。
4. **闪点风力修复与定性**：出生率随 widen 加速（生灭节奏可感）、守恒指数
   0.6 → 0.5；归一化锚定 base×widen（只补偿 specular AA，风力不压暗整场）。
   定性结论：容量表（D136）封顶数量，风力通道的静帧收益天生有限
   （t70 超白 758→489，单体守恒收敛），可感价值在动态端
   （生灭节奏、带宽随能量变化、"安静=少而亮"对比）；去留待用户在
   动态 GUI 中裁决。
5. **暂停冻结**：暂停音乐时画面完全静止（`VisCanvas.freeze_probe`，
   ST_PAUSED 时不推进模拟与音频泵、仍重绘同帧——参数滑杆改动即时可见），
   继续播放无时间跳变。三种画布（QPainter/GL/HDR）共用同一实现。

## D153（2026-07-16）质感提升批·终裁：仅厚度透光存活并上线 Android

用户动态目测终裁：**日出光锚**（效果不好）与**闪点风力 Cox-Munk**（无感）
均整项移除（Python 参数/上传/shader/优化器全链路删除，environment.frag 恢复原状，
gl_optics 恢复原 σ/归一化路径）；**厚度透光**为质感提升批唯一存活项，
定档 uplift_thick_glow=1.29、uplift_glow_boost=1.6，薄峰透光实体带
（thin_glow_gain）默认归零 deprecated。

**暂停冻结修复**：暂停后播放按钮失效的根因是按钮使能刷新位于 _pump 末尾而
冻结分支跳过了整个 _pump。新增 freeze_tick 轻量回调（仅刷新按钮/时钟/
离线分析轮询，不消费特征、不驱动 mapper——暂停期间 apply_silence 不再
污染能量状态），冻结语义保持"画面完全静止、参数滑杆即时可见"。

**Android 移植与发布**：FableSolParams 增加两参数并把 thin_glow_gain 置 0；
FableSolGlRenderer 网格构建后计算 9 锚行均值 y（与 Python layer_mean_y_px
一比一）并上传 uThicknessGlowStrength / uGlowDerivedBoost / uLayerMeanYPx[9] /
uThicknessRangePx(22dp×density)；共享 shader 已含 vThickness01 与
thicknessGlow/thicknessExcessMask。契约测试按新设计更新
（backlitTransmissionExcess 双参签名、透射掩码同源合并、thin_glow 全层归零、
params 新默认），FableSol 146 项单测全绿。阿里云 debug 发布
**202607161231**（APK SHA-256 ec156ea3…48c2，含中文更新日志）。
真机验收要点：厚度透光在 18 色上的观感、录音态逆光波冠的 HDR 透射、
60fps 帧率（新增成本：每帧 9×196 次均值求和 + 每像素约 10 ALU）。

## D154（2026-07-16）厚度透光独立权重表与近层覆盖偏置

用户复测 202607161231 后指出：厚度透光实际只在约 1~4 层可感，第 0 层与中远层
（4~8）几乎没有。裁决与落地：

1. **独立权重表**：新增 `THICKNESS_GLOW_WEIGHTS = (1.0, 0.96, 0.84, 0.64,
   0.56, 0.49, 0.42, 0.36, 0.27)`（4~8 层按用户指定整体上提一档），经新 uniform
   `uThicknessGlowWeights[9]` 上传；不再借用 `SDR_SSS_WEIGHTS`（避免连带改旧
   SSS 通道）。shader 侧带回退：未上传该表（旧接线）时自动回退 SDR_SSS 权重，
   已发布 APK 行为不变。
2. **近层覆盖偏置**：`water.vert` 厚度代理增加
   `nearBias = 0.45 × clamp(1 − depth01×4, 0, 1)`（层 0=+0.45 → 层 2≈0），
   让最近条带整体进入透光渐变、波峰仍最亮。
3. **验证方法论修正**（用户指出两处错误）：灰度差异面板的放大倍数由 P99.9
   离群值主导改为 P95 定标（`amp = min(160/max(P95,2), 60)`，闪点级离群值允许
   饱和）；前后对比必须同管线——两侧都走 `--hdr-debug` 线性 FP16 场景，
   SDR 视图与 HDR −1EV 视图各比各，杜绝普通 SDR 链路与线性链路混比。

## D155（2026-07-16）适当放宽 D6：front fill 水线带参与厚度透光

D154 落地后第 0 层透光仍完全不可见。根因：第 0 层在画面中的可见主体是
front fill（第 0 层轮廓线以下的整块前景水体），而 `water.frag` 对 front fill
在所有光学之前早退（D6 身份色锚点使然），厚度透光从未在其上运行；波浪网格
在锚线 0 即结束，第 0 层"条带"实际不存在。

用户裁决：**D6 身份色锚点适当推翻**——front fill 参与同一 `thicknessGlow()`。
首版实现把透光 clamp 在"层均值线 + 偏置"的水平线处，用户复看后追加裁决：
第 0 层透光面积不得小于第 1 层，**必须突破这条固定水位线，面积与颜色淡出
都遵循自然物理规律**。最终模型：

- **入射量**（波峰门）：水面处的厚度代理 `vThicknessSurface`（波峰越高出
  层均值，进入水体的光越多；波谷为 0）——与波浪侧同源，水线接缝连续；
- **衰减**（近表亮环 + Beer–Lambert）：水面下前 0.35×范围（≈8dp）保持全
  入射（近表层散射亮环），随后乘 `exp(−(深度−亮环)/λ)`，λ =
  2.5×`uThicknessRangePx`（55dp）；下边界不再是直线，逐列跟随其上方的
  实际水面轮廓指数淡出。亮环与放缓的 λ 来自用户追加裁决"第 0 层透光
  亮度不得输给第 1 层"（首版 λ=2×范围、无亮环，整团平均亮度偏低）；
- **第 0 层提亮补偿**：fill 的透光目标 = `min(subsurface × uGlowDerivedBoost
  × 1.35, 1)`（等效 2.16）——第 0 层身份色最纯最深（无 D135 混白），同等
  提亮倍数下绝对亮度天然低于近层条带；×1.35 将其补偿到感知同亮，浅色身份
  被 1.0 钳制自然封顶，波浪侧 boostScale 恒 1 不变。实测（默认色 t70 水线
  下 0–40px 盒）：提亮 8.45→14.81，绝对亮度 115.7→122.0（条带参照约 136）；
- `water.vert` 输出两个 varying：`vThickness01`（像素处原始代理，顶点端
  不 clamp——fill 只有上下两排顶点，先 clamp 再插值会把衰减线性拉伸到
  整块填充）与 `vThicknessSurface`（本列水面处代理，逐列常量）；片元
  `thicknessThin()` 以两者之差还原水面下深度。波浪网格顶点即在水面上，
  两值恒等 → 衰减为 0，波浪侧行为不变；
- fill 的本列水面 y 经闲置的 `aSlope.y` 传入（fill 宏观坡度恒 0：Python
  `gl_scene.front[0::2/1::2, 3]`、Kotlin front 顶点 slopeZ 分量）；顶边
  另继承 row 0 的 sheen slope 供迎光门连续（底边 0 → 中性 0.5）；
- `water.frag` front-fill 早退分支内、参考白钳制前合成 thicknessGlow，
  由 `uThicknessGlowStrength > 0` 门控（未接线平台零默认关闭不变）；
- 不改变 HDR 路径：front fill 仍无 HDR excess，透光目标色
  `min(subsurface×1.6, 1)` 被参考白钳制约束，SDR 安全（验证：超白像素
  与峰值 Y 逐位不变）。

D6 其余合同不变：第 0 层混白量恒为 0、静态色板、渐变身份保留；
深处大面积主体随指数衰减回归身份纯色。

## D156（2026-07-16 夜）波峰银边：剪影掠射镜面线（试验，待目测定档）

用户圈选 research-sol-ultra-0716/compare 中 GPT 效果图（奇数编号）各层波浪
上边缘的细长发光白边，期望动画中呈"银丝游动"。视觉解剖：贴上轮廓的
1~2px 亮芯 + 向水体内侧 2~4 倍宽柔晕，沿边亮度不均（受光凸肩最亮、长段
缺席、两端收尖），近白略带层色，近层强远层近无。

**物理定性**（决定实现路线）：这是波唇的剪影掠射镜面线，而非拉长的
glint 实体——①剪影处 N·V→0、菲涅耳→1，任何水色的波唇都近乎全反射
天空/太阳（近白）；②镜面高光沿最小曲率方向（沿脊）拉长成线，横向宽度
∝1/横向曲率（峰越尖线越细亮）；③半角对准门 + Cox–Munk 微坡涨落带来
沿边间歇与游动。故为**确定性 shader 效果**（几何门驱动），间歇性来自
几何而非随机生灭。

**实现**（共享 water.vert/frag，零默认门控）：
- `crestRimProfile()`：到自身剪影的屏幕像素距离（与 waterEdgeCoverage
  同源），高斯亮芯（σ=uCrestRimWidthPx≈1.2dp）+ 0.30×指数内晕；
- `crestRimEnergy()`：profile × 半角对准门（facing²，normal.x 已含风梳
  微法线 → 沿边涨落）× 峰锐度门（vCrestPinch^0.75，波谷/平段无线）×
  逐层存在度 `uCrestRimWeights[9]`（CREST_RIM_WEIGHTS：1/0.92/0.78/0.60/
  0.45/0.30/0.18/0.10/0.05）；
- `crestRimColor()`：mix(subsurface 身份色比, 白, 0.72)——反射天空的银
  不吃水体吸收；
- 波浪带与 front fill 各对自身剪影生效（水线即第 0 层剪影，fill 迎光
  信号用 D155 顶边 sheen slope）；SDR 部分入参考白钳制；HDR excess
  峰值 = 1 + 1.4×weight（第 0 层 2.4、最远 ≈1，守近层 HDR 充足约定），
  录音增益门控，波浪带与 fill 双路都有（fill 首次获得 HDR 通道，仅限
  银边）。

**闪点试验期归零**：新参数 `glint_capacity_gain` 默认 0（银边与 glint
语义相近，先隔离评审；容量表不动，恢复拉回 1 即可），glint 相关测试
显式开 1 保留合同覆盖。基线渲染（闪点 0、银边 0）超白像素归零，证实
此前静帧超白几乎全部由闪点核心承担。

**首轮数据**（rimbase→rimv1，t70）：SDR 差异 mean 0.30 / P99 6.9（线状
效果面积小，数值天然低）；银边贡献超白 117px、峰值 1.091；−1EV 视图下
银丝仍亮（真超白）。评审图 tmp/uplift-review/compare/rim1-*。
参数 `uplift_crest_rim` 默认 1.0（0~1.6），"银丝游动"需在 GUI 动态确认；
用户定档后移植 Android。

**2026-07-17 v2 修订（用户裁决"山舞银蛇"：不够亮/太短/频率低/内部有
截止边界）**：
1. 内部边界根因 = 光晕 6×宽度处硬截止残留 4.6% 台阶 → 改平滑窗
   （3.5w~7w smoothstep 连续归零），实测尾部台阶 8.86→1.93/255（不可见）；
2. 线被切碎根因 = 迎光门用了含风梳微法线的 normal.x（高频抖动）→
   波浪带与 fill 统一改用平滑 sheen 坡度派生的 normal.x，线长由宏观
   几何决定；
3. 加长/提频：半角门放宽为 clamp(0.55+0.9·s·n) 不再乘方；峰锐度门
   提前饱和 pow(clamp(pinch×1.6),0.5)；权重表中层上提
   (1/0.94/0.84/0.70/0.56/0.42/0.28/0.16/0.08)；
4. 提亮：SDR 系数 0.85→1.0；HDR 峰值系数 1.4→2.0（第 0 层 3.0，
   仍低于闪点核心历史 3.6）。
v2 数据（t70）：SDR P99 6.9→22.4，超白 117→1415px，峰值 1.091→1.658，
峰值增量 +36%；t45 超白 766px。测试补两处隔离：直射分瓣单调测试×2
关断银边（bounded 峰值归一会破坏隔离前提）。评审图 compare/rim2-*。

**2026-07-17 v3~v6 修订（用户裁决："亮度对齐闪点核心、长度再×几倍"）**：

- **亮度**：HDR 峰值系数 2.0→2.6——第 0 层峰值 1+2.6=3.6，与闪点核心
  历史峰值同档（用户明示），逐层随权重衰减；SDR/HDR 拆分：SDR 银线与
  HDR 超白共用形状项，方向门各自组合（单权重，修正 v1/v2 的 w² 隐误）。
- **长度关键发现（v3 实测）**：pinch 沿轮廓 P50=0、P99≈0.002~0.07、
  层 0 近乎全零——**任何以 vCrestPinch 为乘法门的方案都会把银蛇饿死在
  10%~20% 的脊段**（v1~v3 短的真根源）。峰锐度门整项删除；物理依据：
  剪影掠射反射本就沿整条轮廓存在。
- **"不常出现"改由音频活跃度承担**：CPU 侧把 `0.30+0.70×sparkle01`
  乘进 uCrestRimStrength（`GlFrameData.crest_rim_activity`）——场强随声
  而动、HDR 峰值预算不变，沿用闪点 sparkle 前例（D67 合规）。
- **v4 教训（全轮廓描边被否决）**：方向门加 0.35 底后无熄灭态，
  整条轮廓常亮成"描边"，违反最初"不常出现"约束；v5 实测纯线性方向门
  也不熄灭（典型 sheen 坡度平缓，raw 常年 0.3~0.7）。
- **v6 终形**：方向门 `smoothstep(0.40, 0.82, clamp(0.5+1.1·s·n))`
  制造真实熄灭区——银蛇 = 轮廓的受光坡段，段长约半波长（数百 px、
  较 v2 长数倍），两端平滑收尖，背坡与长平段无线。
v6 数据（t70）：SDR 差异 mean 3.04/P99 94.9，超白 12410px、诊断峰值
顶到天花板 2.0；同屏约 10 条独立银蛇，近粗远细。测试补一处隔离：
MSAA 几何 AA 回归关断银边（逐像素着色细线不在 MSAA 覆盖语义内，
剖面为 ≥2.4px 解析高斯无锯齿风险）。177 项全绿。
评审图 compare/rim6-*、rim6-vs-v2-t70.png。

**2026-07-17 v7 修订（用户裁决：粗了一点、中远层太明显）**：
亮芯 σ 1.2dp→1.0dp、光晕幅度 0.35→0.30；权重表中远层下调一档
(1/0.92/0.78/0.52/0.38/0.26/0.16/0.09/0.04)；新增**空气透视变细**——
片元线宽乘 `0.55+0.45×weight`（第 0 层全宽、最远层 ≈0.59 倍），
"粗"与"远层显眼"由同一机制一起收敛。v7 数据（t70）：SDR mean
2.19/P99 79.6，超白 7682px（中远层 excess 随权重回落、近层不变）。
177 项全绿。评审图 compare/rim7-*、rim7-vs-v6-t70.png。

**2026-07-17 v8**：晕幅 0.30→0.16（用户直接定档）——光晕向水体的
渗出收敛，银丝本体更干净。评审图 compare/rim8-*、rim8-vs-v7-t70.png。

**2026-07-17 v9/v10 再修**（发布 202607170010 后用户续调）：
v9 亮芯 1.0→0.8dp、权重与空气透视加陡（0.45+0.55×w）；v10 亮芯
0.8→**0.6dp**、中远层权重按用户指定 **0.42/0.27/0.16/0.10/0.05/0.0129**
（近三层 1.0/0.90/0.72 不变）。亮度对比 glint 的定量结论：SDR 双方核心
均达纯白 (255,255,255)，HDR 同一诊断天花板下银丝峰值（顶死 2.0）已高于
glint（1.869）——"感觉没 glint 亮"是点 vs 线的能量集中度与贴亮边低
对比的感知差。银丝三控制项参数化并入 GUI（质感提升（试验）组）：
`uplift_rim_width`（银丝粗细 0.3~2.0dp，默认 0.6）、`uplift_rim_halo`
（光晕幅度 0~0.5，默认 0.16）、`uplift_rim_peak`（HDR 峰值 1~3.6，
默认 3.6）——新增 uniform uCrestRimHaloAmp / uCrestRimPeakBoost
（峰值−1，零默认关闭），uCrestRimWidthPx 改每帧参数驱动。

**2026-07-17 v11 银丝滑动（视差行为）**：用户要求复刻旧 glint"逆流跑"
的感知。物理依据：深水**群速 = 相速/2**——波峰以相速前行，镜面事件
包络随波群走、相对波峰恒向后滑移（glint 逆流跑的同源现象）。实现：
低频值噪声调制场（λ≈160dp，各层 seed 不同）沿 +x 以**半流速**滑动
（相位 = ∫0.5×flow01×213.6dp/s，沿 sim 时间积分——暂停冻结时静止），
波浪左滚、银丝亮结右滑。调制深度 = 0.65×新参数 `uplift_rim_slide`
（银丝滑动 0~1，默认 1，0 = 关闭恢复均匀银丝）。新 uniform：
uCrestRimSlidePhase / uCrestRimSlideScale / uCrestRimSlideDepth
（零默认关闭）。相位探针验证单调正向；静态呈亮结沿线分布
（compare/rim11-vs-v10-t70.png），滑动感需 GUI 动态确认。

**2026-07-17 v12 滑动修复（用户"完全没看到滑动"，双根因）**：
1. **GPU sin 大参数精度**：调制噪声 seed 行 y≈41 时 hash21 内
   `sin(~1.3e4 rad)` 在 GPU 上精度崩坏、整行输出偏平——on/off 比值
   实测恒 [0.98,1.00]（调制完全失效）。修复：亮结改由**正弦承载**
   （保证全动态范围），小输入值噪声只做相位抖动、各层相位偏移去同步；
   修复后调制幅度实测 [0.26,0.52]（深度 0.75 理论下限 0.25）。
   教训：GLSL 噪声输入必须保持小数值（代码库既有噪声全部 /42 缩放
   正是此因）。
2. **相位跨帧积分在单帧渲染路径恒 0**：渲染器状态在"离屏只渲最后
   一帧"时 dt=0。修复：相位改为 **sim.t 纯函数**——恒速 55dp/s、
   对 λ=240dp 取模（防大参数 sin），无跨帧状态、GUI/截图/Android
   行为一致、冻结静止、Kotlin 零负担；流速耦合放弃（低流速时慢到
   不可感，探针实测 flow01 常 <0.5）。λ 160→240dp、深度 0.65→0.75、
   亮结经 smoothstep(0.30,0.72) 锐化成明确亮段。
   验证：同 sim 状态强制两相位 → 712px 差异（shader 消费相位确认），
   55dp/s 横穿 320dp 容器约 5.8s（明确可感）。177 项全绿。
   2026-07-17 用户定档：滑速 55→**64dp/s**（两端同改，横穿约 5s）。

**2026-07-17 v13/v14 波峰全覆盖与顶点辉光（用户两连裁决）**：
1. **v13 波峰包络**（"银丝只挂波峰右侧、错过顶点"）：方向门降级为
   ±28% 倾斜（做主门会在顶点半开区断线）；主门改**局部凸性**
   `dFdx(sheen坡度)>0 × 宽坡度窗`——"高出层均值"判据对宽缓涌包失效
   （均值被涌包抬走，t45 实测整场消失），凸性判据物理上也更对（唇线
   高光聚于剪影凸段）。底 0.55（首渲 0.30 把长翼跑者压暗、t45 超白
   归零，二渲回调）。HDR 超白仍锐方向门控。
2. **v14 顶点辉光**（"顶点应显著最亮"）：SDR 丝体已顶参考白，"更亮"
   由**发光面积**承担——顶点（凸性强 × 坡度近零）叠加 3.2 倍宽软光球
   （bloom），闪光驱动 = 0.45+0.55×亮结（银丝在场即常亮、亮结滑过冲
   顶）×方向倾斜；HDR 顶点段超驱（内部 ×2.6 再乘 PeakBoost 2.6）——
   理论 6~7 被 headroom 钳死在 3.6 = 全场绝对最亮点（真机 3.6 屏顶满；
   离屏诊断链天花板 2.0 只能显示"顶到 2.0"）。
   数据（vs v10，t70）：SDR P99 36.2→46.5、超白 3623→3651 且集中于
   顶点；t45 超白 674→1794。插曲两则：GLSL 保留字 `flat` 不能作变量名
   （编译错误一轮）；**凸性符号错误**——vSheenSlope 是"高度向上为正"
   的物理坡度，波峰 = 高度极大 → 坡度导数为负，首渲取了 +dFdx 把辉光
   整场点在波谷（用户目测抓出，v14b 取 −dFdx 修正）。177 项全绿。
   评审图 compare/rim14-*、rim14-vs-v10-t70.png。
3. **v15 连续塑形**（用户裁决：v14 独立亮球与银丝"明显是两个东西"）：
   删除叠加式 bloom/flash，改为**同一剖面随顶点度连续变化**——
   crestRimProfile(apex01)：线身宽 ×(1+0.45·apex)、晕幅 ×(1+2.2·apex)、
   晕铺展 3.2→4.6 倍宽；能量 SDR ×(1+1.1·apex)、HDR 太阳项
   +2.2·apex（超驱后仍被 headroom 钳 3.6）。亮结滑到顶点的冲顶由
   body×apex 乘积自然涌现。数据（t70）P99 47.8、超白 3428；t45 超白
   1184。177 项全绿。评审图 compare/rim15-*、rim15-apex-zoom.png。
4. **v16 粗细恒定、纯亮度渐变**：用户裁决 v15 顶点线宽膨大 +
   晕铺展在细线上像"打了个结"的光斑。剖面形状回到全程恒定
   （crestRimProfile 无参），顶点强调只保留能量项：SDR ×(1+1.1·apex01)、
   HDR 太阳项 +2.2·apex01——顶端最亮、随 apex01 场向两翼平滑衰减，
   银丝滑动时亮结经过顶点自然到达最亮。数据（t70）P99 29.7、超白
   2122；t45 超白 598（较 v15 回落 = 光斑面积移除）。177 项全绿。
   评审图 compare/rim16-*、rim16-vs-v15-apex.png。
5. **v17 太阳柱限定顶点高亮（每层 1~2 处）**：用户裁决"任何相对高的
   波峰都有高亮区 → 断断续续，最好每层只有一两处"。物理依据：顶点
   高亮是**光滑波唇上的宏观镜面点**——光滑凸面每凸段至多一个镜面点，
   且只有波峰位于太阳柱内所需坡度才可达；无微面片（Cox–Munk）兜底，
   故比闪点（容量表允许多处散布）更严格集中于柱内。实现：apex01 ×
   太阳柱包络 `crestRimSunColumn()`（柱心与 sun_glitter_policy.
   path_center01 同构：0.5+tan(方位角)×0.28×(depth−0.5) 钳 0.18~0.82；
   柱半宽取更窄的 0.15→0.07），新 uniform uCrestRimSpanX0Px/
   uCrestRimSpanPx（row 0 可见跨度）供换算 x01。银丝本体（天空掠射
   反射，宽光源）保持沿峰连续不受柱限制。数据（t70）超白 2122→1675、
   t45 598→342（高亮凝聚入柱）。177 项全绿。评审图 compare/rim17-*、
   rim17-vs-v16-t45/t70.png。
6. **v18 全平滑过渡（真机截图裁决"过渡突兀、第 1 层 4 处高亮"）**：
   四项根因逐一移除——①凸性判据的 dFdx 是插值 varying 的屏幕导数，
   **逐三角形恒定、跨列（3~6px）跳变**且会把一个波峰劈成多段：顶点/
   覆盖判据全面弃用 dFdx，改"坡度近零（宽过渡带 0.03~0.30）× 波峰
   显著度 crestRimProminence（vThicknessSurface − nearBias 平滑场，
   波谷负值天然排除）"；②太阳柱半宽收窄 0.15/0.07→0.11/0.055；
   ③亮结 λ 240→360dp（同层亮体 1~2 个；**相位取模必须同步等于波长**
   否则回绕跳变）、过渡带加宽 0.24~0.78、调制深度 0.75→0.60（段间
   谷底 0.40）；④覆盖门的坡度窗（wings）整项删除——陡坡把其过渡带
   空间压缩成硬边、且与显著度职责重复；方向倾斜减为 0.80+0.20、顶点
   SDR 增益 1.1→0.9（缩小饱和白平台）。沿丝追踪实测：典型过渡落差
   （P90/±20 列）30→22/255。177 项全绿。评审图 compare/rim18-*、
   rim18c-layer1-zoom.png。
   教训：插值 varying 的 dFdx 逐三角形常量，不可用于需要空间平滑的
   亮度场；坡度门在陡坡处必然空间压缩成硬边——平滑场优先用高度/
   显著度这类本身随几何缓变的量。

**2026-07-17 v8 定稿并移植 Android + 发布**：FableSolParams 增
uplift_crest_rim=1.0 / glint_capacity_gain=0.0（并新增 setForTest 测试
覆盖入口）；FableSolMaterialPolicy 增 CREST_RIM_WEIGHTS；
FableSolGlRenderer 静态上传权重表、每帧上传 uCrestRimStrength
（× 0.30+0.70×sparkle01 活跃度）与 uCrestRimWidthPx(1dp×density)；
FableSolGlOptics 闪点容量门与 Python 同咽喉点（findAnchors/轨迹更新
共用 gated cap，Canvas 回退路径不动）。契约测试三处更新：材质曲线表
加银边、闪点身份/几何测试×2 显式开数量门、parity 的 bodyBlock 断言
起点移到 fill 分支之后（fill 银边 HDR excess 是唯一许可例外）。
Android 149 项全绿、assembleDebug 通过、APK 内已核对 crestRimShape 与
0.16 晕幅。阿里云 debug 发布 **202607170010**（SHA-256 3bd6c606…c8f4，
含中文日志）。真机验收要点：银蛇长度/亮度/游动感、闪点缺席观感、
HDR 太阳段超白、60fps。


## D157（2026-07-17）设置内调参 Dialog：全量生效参数 + HDR 开关 + 实时预览

设置界面（用户界面卡片）新增"音频海浪动画参数调节"入口，点击先申请
RECORD_AUDIO，通过后打开 FableSolTuningDialogFragment：

1. **实时预览与录音界面同源**：顶部固定 240dp 高的
   WaveVisualizerFableSolHost（同一 GLES 宿主：九层水体、HDR、重力
   倾斜、AudioRecorder 实时驱动），背景取 App.defaultAccentBackground、
   呈现 alpha 恒 1，无录音按钮/时钟。逻辑空间 420dp 高、water.vert 以
   视口中心对齐——240dp 恰好容下九条水线（最近层水线位于中心下方
   114dp）。窗口高度收敛 min(648dp, 94% 屏高)，参数区 ScrollView 用
   weight 吸收差额，预览与按钮行永不被顶出。
2. **参数目录 = 实际消费 ∩ 已注册**：FableSolTuning.GROUPS 收录 82 个
   标量参数、9 组（质感提升/外观与光学/连续水面/环境与流动/主浪/涨落/
   注入/段落/系统），标签、范围、步长与 Python params.py 的 GUI 规格
   同源；排除 canvas 回退独占键（crest_on、surface2d_on）、逐层数组
   （lget 系）与前端未消费键（agc_window_s 等）。滑杆用均匀插值映射
   （粒度≈step、两端点精确可达——lighten_far 上限 0.864 这类 step 不
   整除的范围，若按步长量化会永远取不到上限/默认值）。
3. **热更新通道**：调节即时经 WaveVisualizerFableSolHost.setTuningValue
   → 渲染线程 pendingTuning（inputLock 门）→ drainAndApply 在 GL 线程
   写入共享 params，并把 materialColorKey 置空强制重建静态材质色
   （lighten_far/color_breath/environment_tint 平时只在背景变化时重算）。
   所有效果 uniform 均逐帧读 params；静态权重表全部来自策略常量，不受
   调参影响。Canvas 回退路径直写（物理/渲染同在 UI 线程）。
4. **持久化**：独立 SharedPreferences 文件 fablesol_tuning 只存偏离
   默认值的键（滑杆松手时写入、等于默认即删除存储项）；FableSolParams
   新增运行时 set()（setForTest 变为别名），各渲染器构造 params 后立即
   FableSolTuning.applyStored（先于 sim/mapper 构造，二者共享同一
   实例）——录音界面下次打开自动生效。恢复默认 = 清空存储 + 全量推
   默认值 + 重建参数行。
5. **HDR 开关**：布尔项存同一文件（默认开）；预览端激活条件 = 设备
   支持（SDK≥34 且 display.isHdr 且 isHdrSdrRatioAvailable）∧ 开关，
   设备不支持时整行置灰并标注"（设备不支持）"；录音 Dialog 的
   setRecordingHdrActive(true) 改为读该开关。
6. **补注册两个漏移植参数**：FeatureMapper 一直在读 swell_halflife_s /
   deep_integral_s，但 FableSolParams 从未注册——get 走 0.0 兜底再被
   max(…,0.5)/max(…,1.0) 钳制。按既有实效值 0.5 / 1.0 注册（观感逐位
   不变），与 Python 原版默认 3.0 / 30.0 的差异记入 followups 待裁决。

新文件：FableSolTuning.kt、FableSolTuningDialogFragment.kt、
dialog_fablesol_tuning.xml；字符串 13 个语言全部补齐。Android 161 项
单元测试全绿、assembleDebug 通过（按规则未做 adb 实机操作）。

## D158（2026-07-17）调参 Dialog 二轮打磨：沉浸预览、换色涌入、暂停冻结与样式统一

用户对 D157 首版的九项裁决落地（第九项滑动帧率暂缓，见文末）：

1. **预览沉浸化**：去掉标题，预览贴顶满宽；渲染端 present.frag 新增
   uCornerRadiusBottomDeltaPx（下两角半径 = max(上角+增量, 0)，按
   gl_FragCoord.y 半分上下），Dialog 预览把底两角切成直角与参数区无缝
   衔接，顶两角保持 app_chrome_dialog_popup_corner_radius（与 Dialog
   轮廓同一 dimen，精确吻合）。默认增量 0 = 四角同半径，录音界面与
   Python（不上传该 uniform）逐位不变。
2. **取景上移**：新 API setContentVerticalOffsetDp（Host→…→Renderer），
   buildFrame 在旋转前顶点空间加 R^{-1}·(0, dy) 补偿向量（屏幕上是纯
   垂直平移，倾斜不横漂），fill 底同步平移。Dialog 用 -36dp：第 0 层
   水线距下边缘从 6dp 提到 42dp，波谷不再被裁。物理、optics、银丝
   跨度等全部随 vertexData 继承，无一处特判。
3. **换色（新颜色的波浪从右侧涌入）**：water.frag 新增揭示门
   uColorRevealEdgePx/uColorRevealSoftPx（gl_FragCoord.x 的 smoothstep，
   软带 72dp；SoftPx<=0 默认关闭恒 1）；两个输出点 alpha 由常量 1 改为
   colorRevealAlpha()（两端合同测试同步更新）。渲染端过渡（1600ms
   smoothstep）：主遍每帧以 OKLab 插值配色重建材质（环境天空与 optics
   同色渐变），再以目标配色第二遍重绘水体+front fill（SRC_ALPHA 混合
   + 揭示门），边界从右缘推进到左缘后落定目标色；进行中再次换色以
   当前插值色接力。optics 不重画（主遍已是插值色，被新色浪头盖过即
   "涌入"的一部分；当前闪点容量默认 0）。UI 侧同步 1600ms 渐变
   （ArgbEvaluator、12 档节流——SeekBar tint 每次重建 drawable，82 条
   全量重设需节流），组头渐变字/滑杆/复选框/行涟漪/确定按钮全部跟随。
   颜色池 = accent 渐变 + 内置 10 色（R.array.thing）+ 用户记事背景
   （ThingDAO 后台去重），随机不重复当前色。
4. **暂停/继续**：预览右上角按钮（换色按钮左侧），View 层停帧循环即
   冻结最后一帧（GL surface 保留内容；音频帧照常入队、恢复只取最新，
   dt 有 MAX_DT 钳制无跳变）。暂停中点换色自动恢复播放。按钮用录音
   Dialog 侧键同款半透明圆底 + 圆形涟漪。
5. **样式统一**：HDR 行复选框改 BackgroundUtil.applyCheckboxAccent
   （渐变勾选），行涟漪改 GradientRippleDrawable（设置界面同款）；
   确定按钮改 applyTextBackground + applyAccentRipple（AlertDialog
   确认钮同款）；按钮行上方新增分隔线，与顶部线一起指示滚动状态
   （canScrollVertically ±1）。
6. **文案与密度**："质感提升（试验）"组更名"质感"；滑杆行上下内边距
   4/0→9/5、组头上边距 14→22，行间不再拥挤。

滑动卡顿（第九项，本轮未动）：根因即 Dialog 窗口 preferredRefreshRate
= 60（整窗锁 60Hz，用户设备高刷屏上滚动明显钝），叠加预览持续渲染。
按用户指示留待下轮处理（方案备选：仅预览 SurfaceView 请求 60Hz 帧率
而窗口不锁、或调参 Dialog 不锁刷新率）。

合同更新：Android FableSolGlShaderParityTest 与 Python test_gl_backend
的输出行断言改为 colorRevealAlpha() 版本。Android 149 项、Python 177
项全绿；assembleDebug 通过。

## D159（2026-07-17）调参 Dialog 三轮反馈：亮带归零对齐 Python、换色卡顿与 overscroll 掉帧修复

1. **surface_strip_gain 默认 1.0 → 0.0**：Python 端"表面亮带"特效已整项
   移除（test_surface_strip_removed），Android 一直默认开着——归零即与
   模拟器视觉对齐；调参 Dialog 仍可拉回。四个亮带合同测试按 D156 闪点
   先例显式 setForTest 开启被测特效。
2. **"系统"组删除**：demo_mode 不再暴露在调参 Dialog（目录仅移除，
   参数本身仍注册）。
3. **角标按钮去衬底**：透明背景 + 圆形涟漪；图标 imageAlpha=176，
   亮色模式下不再是实黑（tint 仍 on_surface_secondary，54%×69%≈37% 黑）。
4. **换色动画卡顿修复**：根因是每档全量重设 82 条滑杆——setSeekBar-
   Background 重建 thumb/track drawable 并各自 requestLayout，12 档 ×82
   次 dirty measure 把主线程一帧打爆，Choreographer 断流连带预览掉帧。
   改为档中只更新 ScrollView 视口内可见的滑杆（offsetDescendantRect
   判定，每档 ~6 条），动画结束一次性全量补齐；组头/复选框/涟漪/按钮
   仍每档更新（invalidate 级，便宜）。
5. **overscroll 拉伸掉帧**：Android 12+ 的 stretch 是对整块列表内容每帧
   变化的 RenderEffect，GPU 开销大到拖垮上方 GL 预览；该 ScrollView
   改 overScrollMode="never"，到边提示由上下两条滚动指示分隔线承担。

Android 149 项全绿、assembleDebug 通过；shader 未动，Python 端无涉。

## D160（2026-07-17）表面亮带整项移除 + 调参 Dialog 四轮细节

1. **表面亮带（surface_strip_gain）整项移除**：Python 端 D147 撤销软带
   试验时已删除 GlOpticsBuilder._surface_band 与全部控制项（有
   test_surface_strip_removed 守卫），Android 一直保留完整实现。按用户
   裁决对齐移除：FableSolGlOptics.buildSurfaceBand 与调用块、mode 4
   （OPTICAL_MODE_SURFACE_REFLECTION）发射、canvas drawSurfaceStrip /
   colorRampShader / drawGradientRampOneSidedBand、MaterialPolicy 的
   SURFACE_BAND_* 权重表与三个函数、FableSolSurfaceColorPolicy 类、
   params 注册、调参目录条目全部删除。共享 optical.frag 的 mode 分支
   与 Python 端同样保留为无发射死分支（两端对称，不动 shader）。
   canvas 端流光与亮带解耦保留：原来流光只在 strip 内触发（strip 归零
   即消失），现改为独立入口（自算迎光/摆幅、底色取层色中间调 stop1/
   stop2 中点，行为与 GL buildStreaks 的独立性对齐）。测试 149→143：
   band 专测 ×2、SurfaceColorPolicyTest ×2、MaterialPolicy band ×1
   删除；网格合同的 mode 4 断言反转为"不复存在"；builder 排序合同去
   surface 项。
2. **HDR 勾选框涟漪跟色**：复选框自身的圆形按压涟漪此前是系统默认色
   （行按压经 dispatchSetPressed 传导显现），补装设置界面同款
   GradientRippleDrawable.applyCheckboxRipple 并纳入换色 update。
3. **按钮行间距**：marginTop 从手写 4dp 恢复标准
   app_chrome_dialog_action_row_margin_top，与其它 Dialog 一致。

Android 143 项全绿、assembleDebug 通过；阿里云 Debug 202607170734。

## D161（2026-07-17）暂停语义重做：冻结模拟而非停渲染 + 按钮行 divided 间距

1. **暂停 = 冻结模拟与音频泵，渲染循环照跑**（对齐 Python canvas.py 的
   freeze_probe 语义："不推进模拟与音频泵，mapper 的静默衰减也被冻结"）。
   此前 Android 的暂停停掉了帧循环，冻结画面上调参毫无反馈。现改为
   渲染端 simulationPaused 门：跳过 sim.update 与重力应用；drainAndApply
   丢弃冻结期音频特征/事件（恢复后从最新实时输入继续），静默衰减计时
   锚随帧推移一并冻结；调参热更、HDR 增益过渡、换色揭示遍照常逐帧
   执行。GlView/canvas 的帧循环暂停逻辑撤销，API 更名
   setSimulationPaused（Host→GlView/Canvas→RenderThread→Renderer）。
   冻结下实时可见的参数 = 渲染每帧读取的全部光学/材质项（质感组、
   外观与光学组、相机仰角等）；作用于模拟推进的组（环境与流动/主浪/
   涨落/注入/段落）冻结下自然无表现，与 Python 行为一致。换色不再
   强制恢复播放（静止波形上也能看到新色涌入）。
2. **按钮行间距**：分隔线下方的动作行应使用
   app_chrome_dialog_divided_action_row_margin_top（参照选择应用语言
   Dialog fragment_chooser 的 separator_2 → 动作行结构），普通
   action_row_margin_top 是给无分隔线场合的，导致上距大于下距。

Android 143 项全绿、assembleDebug 通过。
## D162（2026-07-17）零视觉损失性能优化：Android 解锁 120fps 上限，两端逐帧成本下调

用户要求在**不产生任何人眼可感知视觉变化**的前提下提升渲染帧率（目标 Android 120fps）。
本轮修改分四类，全部通过"数学同式重排/纯状态去重"实现，未改动任何视觉合同：

**Android 帧率解锁**：`FableSolFramePacer` 支持动态目标频率；`WaveVisualizerFableSolGl`
每 250ms 跟随 `display.refreshRate` 把 pacer 目标设为 `min(实际刷新率, 120)`（显示模式
未知时保底 60）；录音与调参两个 Dialog 的窗口 `preferredRefreshRate`/`setRequestedFrameRate`
从 60 改为 120，请求系统切换高刷模式。物理仍是 120Hz 固定子步（120fps 渲染时恰好每帧
1 子步）、模拟由真实 vsync 时间戳驱动，水体速度与 60Hz 面板行为完全不变；系统省电/
热降频把显示压回 60/90Hz 时 pacer 自动跟随。D38 的"锁 60fps"验收由本决策取代。
这同时兑现 D158 留待的"调参 Dialog 滑动卡顿（窗口 60Hz 锁）"事项。

**Android CPU（渲染线程内确定性按行并行 + 预备量折叠）**：新增 `FableSolRowParallel`
（固定 `min(核数-2, 3)` 工作线程 + 调用线程，DISPLAY 优先级，行块连续切分）。
`FableSolContinuousSurface.sample()` 重构为"串行预备逐模态/逐波包标量（轨道系数按
Python 的 `q·a·(kx/k)` 折叠形式预乘，两端一致）→ 方向场累加/worldEta 合成/坡度三段
按行并行"；`buildFrame` 顶点填充同样按行并行（游标改行基址）。行间无共享写、每行
数学与串行逐条一致。`updateWaveVectors` 按 heading/spread 参数值缓存（纯函数）。

**Android GL 状态去重**：水体/前景/光学三种顶点布局各建专属 VAO 一次捕获，绘制循环
从逐组重设 5~6 个 attrib pointer 变为单次 `glBindVertexArray`；光学 pass 帧内全层共享
的 uniform（viewport/rotation/rasterScale/sceneLinear/hdrGain/hdrHeadroom）只在首个
光学层上传。绘制次序、混合状态与所有 draw call 内容不变。

**Python（消除解释器细碎调用）**：`prototype_surface.sample` 把谱模态+波包统一为
X/Z 可分离行，六次 `(97,J)@(J,216)` 矩阵乘一次合成 eta/orbit_x/orbit_z（替代逐帧约
130 次全场数组操作）；`simulation` 的九层环境波单次批量采样、波冠轻纱跨层批量化；
`sample_specular` 三角函数求值去重 + `resolved_amplitude` 向量化展开；`gl_renderer`
uniform 等值跳过上传 + 背景色解析缓存；`_find_anchors` 一次性筛局部峰替代逐轮全列
重扫（判据与选取顺序严格同语义）；`thin_glow` 厚度公式向量化；画布 QTimer 跟随屏幕
刷新率（上限 120fps，60Hz 屏保持既有 16ms）。

**实测（视觉一致性证据 + 性能）**：
- Python 确定性 129 BPM 高活动场（640×840 FP16 + 4x MSAA，RTX 5090 离屏）：帧 p50
  `10.23ms → 8.63ms`（97.7 → 115.9 fps，**+18.6%**）；10 个对照帧线性 scRGB **逐位一致
  （max|Δ|=0）**；177 项测试全绿（1 项既有失败与本轮无关，另行登记）。
- Android 实机（OPD2515 平板，锁屏 background cpuset 受限、demo 确定性驱动、EGL
  pbuffer 离屏 735×1102 FP16）：build p50 `37.2 → 31.5ms`、物理 `6.96 → 4.95ms`、帧
  `57.0 → 50.6ms`（**-11%**；受限小核环境压缩了并行收益，前台大核收益更大待复测）；
  GPU finish 不变（未改 GPU 工作量）；**两构建 5 个对照帧 RGBA 输出逐字节一致
  （max|Δ|=0/255）**；152 项 JVM 测试全绿。
- 新增 debug 专用离屏基准：`FableSolBenchmarkReceiver`（`am broadcast` 触发、锁屏可用、
  可落盘对照帧），Python 侧 `tools/perf_bench.py`（分阶段计时 + `--dump/--compare`
  逐像素对照）。

已知余量（不阻塞本决策）：前台 120Hz 端到端 FrameMetrics 验证与真机目测因设备锁屏
未完成，待解锁后复测；光学实体构建仍为串行（追加并行需按层缓冲重排，另行裁决）。

## D163（2026-07-17）平板实机定因：debuggable ART 运行时税是 30fps 主因；release 实测满帧 120fps

前台端到端复测（OPD2515 平板解锁后，录音态 + 真实音乐驱动）推翻了"CPU 算量不足"的
直觉：debug 构建的水面实际约 30fps（glFrame 间隔换算），且**优化前（当日上午历史日志）
同为 build 18~19ms / 约 30fps**——平板上的慢并非本轮引入。simpleperf 定因：CPU 周期约半数
落在 ART 运行时（`art::Mutex` 19%、CAS 原子 19%、通用 JNI 蹦床 9%、JIT 反复编译 4%+），
真正的水面数学合计不足 8%。`pm compile -m speed -f` 无效——debuggable 应用的 ART 忽略
AOT 代码、强制走可反优化的解释/JIT 路径，属预期行为。

两项修复随本决策进入主干：GL 渲染线程从默认优先级提为 `THREAD_PRIORITY_DISPLAY`
（默认优先级下 compose 实测 7.8ms，提档后首窗 2.75ms）；`FableSolRowParallel` 从固定
均分改为原子计数器发放 8 行小块的工作窃取（异构调度下固定均分的 latch 尾延迟 =
最慢核整块耗时；行结果与执行者无关，输出不变）。

决定性对照：release（minify，同 debug 密钥临时签名覆盖安装）在同一录音场景 atrace 统计
6 秒 722 帧 —— **实测 120.4fps 满帧**，证明 120fps 解锁 + 优化在非 debuggable 运行时下
完整兑现。结论与建议：手机日常 60fps 验收不受影响；平板/追求 120fps 的体验需要
非 debuggable 构建（后续可为阿里云通道增加 release 型 perf 通道或转正式 release 发布，
另行裁决）。lintVitalRelease 当前被 `restore_thing_folder` 多余翻译卡住（zh-rHK/rTW），
是独立既有问题。

## D164（2026-07-18）表面光学 17 参数整体移除：归零 A/B 无感即删（宁少勿烂收束）

用户在 Python 模拟器把"外观"组自"镜面高光强度"以下的 17 项调参全部设为 0 或
最小值，目测画面无可感变化，裁决：这些参数与其功能代码、渲染代码整体移除，
Android 同步。移除清单：crest_glint_strength、crest_lighten、crest_glow_strength、
crest_glow_depth_dp、crest_veil_strength、capillary_glint_gain、thin_glow_gain
（D152 已 deprecated）、flow_streak_gain、orbital_sway_dp、back_shade_gain、
aerial_contrast、pink_mod、specular_aa_strength、global_pink_breath_strength、
micro_normal_strength、sun_sss_strength、sun_sss_falloff。

随动删除（两侧一比一）：
- 整模块：Python pink_breath.py、specular_policy.py；Android FableSolPinkBreathPolicy、
  FableSolSpecularAaPolicy、FableSolOpticalColorPolicy、FableSolShadowColorPolicy。
- 状态机：crest_veil 场（Simulation）、capillary01 驱动链（mapper→simulation→
  OpticalWaveSet，毛细振幅只剩 0.012 固定基线）、流光 Streak 跟踪、闪点出生
  credit 预算（固化 strength=0 的现状行为：候选按分数贪心兑现、只受层容量约束）。
- 共享 GLSL：uMicroNormalStrength/uSpecularAaStrength/uSunSssStrength/uSunSssFalloff、
  uMicroNormalWeights/uSdrSssWeights、vMicroNormalWeight/vSdrSssWeight/vCrestPinch、
  windCombedMicroDerivative/octaveBandLimit/sunriseSubsurfaceMask/addSunriseSubsurface；
  optical.frag 删 mode 2（流光）/6（轻纱）/9（波背自阴影）分支；
  thicknessGlow 的 SDR_SSS 回退改为直接用 vThicknessGlowWeight；
  backlitTransmissionExcess 掩码只剩厚度透光项；continuousSlope = vSheenSlope。
- 权重表：MICRO_NORMAL/SDR_SSS/BACK_SHADE_*/FLOW_STREAK_*/CREST_VEIL_SOURCE/
  GLINT_BIRTH_WEIGHTS（credit 专用）。

已知连带（记录在案）：镜面高光项移除后，闪点出生场只剩"菲涅尔细节×天空反射"
（正常浪面远低于 0.08 门槛），即使把试验期 glint_capacity_gain 拉回 1 闪点也几乎
不再出生；闪点骨架（容量表、跟踪、HDR 峰值、太阳路径）按 D156 保留不动。
body_light_strength（默认 0）保留，其波峰增强项随 crest_glow_* 删除固化为
0.16 基线 × 2dp 纵深。验证：Python 157 测试全绿；Android JVM 测试全绿；
发布阿里云 Debug 202607180728。

### D164 补充（2026-07-18 当日回归与修复）：死 uniform 令 GL 整路回退 Canvas

首包 202607180728 真机表现为"动画效果大变 + 非常卡顿"。根因不是删除本身，
而是删除风梳微法线后 water.frag 的 uTimeSeconds/uSurfaceHeadingRad 失去唯一
使用者：GLES 链接器把仅声明未使用的 uniform 裁出 active 列表，
FableSolGlProgram.uniform() 的 check(location>=0) 首帧即抛，
WaveVisualizerFableSolHost.onGlFailure 静默切 Canvas 软件绘制。Python 桌面端
因 _uniform 的"缺失即跳过"防护而无恙——这正是两侧防护策略不对称的盲区。
修复（202607180743）：删除两个死 uniform 的声明与两侧上传；新增 JVM 静态
守护测试 everyQueriedUniformIsActuallyUsedInItsShaderProgram（GlRenderer 按名
查询的每个 uniform 必须出现在对应 program 的 shader 正文中，声明行不算）。
教训：改共享 GLSL 删功能时，必须同步审计"声明仍在但使用者清零"的 uniform，
Android 的 fail-fast 查询策略会把这种残留放大成整路回退。

## D165（2026-07-18）"段落"组 8 参数整组移除：段涌连根删、离线消费固化、mood 细节固化

用户查询该组来历（全部来自 git 化之前的 2026-07-10 基线快照，属最早一批原始
设计）并目测调整无感后裁决整组移除，按风险三档执行：
- 段涌连根删：surge_gain 在基线快照即默认 0、从未启用。删除
  apply_section/applySection 的巨浪注入分支（inject_depth_packet 的
  z_dominant 段涌调用、逐层回声注入）、LayerSim 的 surge_lift_dp/
  surge_lift_target_dp 状态机与水位合成项。apply_section 收敛为只
  set_mood（性格档切换保留，是段落事件在实时链路的全部效果）。
- 离线专用参数处理：section_delay_s 固化 1.0s 进 app.py 与
  main_window.py 的离线播放对时（补偿播放时钟领先扬声器）；
  lookahead_s 的"前瞻蓄势"整段删除（sections.py——蓄势为段涌让对比度，
  段涌删则蓄势无意义）。两者 Android 端本就未消费（Tuning 当初未收录）。
- 活参数固化：mood_transition_s → 1.5s、mood_spread_dp → 12dp 写死进
  Simulation.perFrame（两侧），性格档系统本身保留、行为不变。
调参 Dialog "段落"组（fablesol_group_sections）整组删除。验证：Python 157
测试全绿、Android JVM 全绿；发布 202607180757。

## D166（2026-07-18）"注入"组 12 参数整组固化：主路径均值化使行波形态不可感，机制保留

来历：10 项来自 git 化之前的基线快照，rhythm_wave_gain/min_strength 来自同日
3b43d3f（feat: enhance perceptual audio-reactive water）。用户目测调整无感。
定性分析：连续水面主路径 build_gl_frame 只取 sim.heights 的逐层均值（means）
作九条锚线水位，DynamicWave 行波（inject_layer 全部产物：装饰点击/波群/重音
浪/远浪三层）的空间形态被均值化，只剩几 dp 的瞬态均值扰动——注入参数因此在
B 主路径下全部不可感；incoming 三项虽控制可见的 2.5D 远浪波包（inject_depth_
packet），但触发稀有（强 onset+概率+冷却）且与自主出生波包难以目测区分。
不能连根删的原因：demo_mode（保留参数）、面板注入测试按钮、多份测试
（wave_shape_continuity/offscreen_birth/orchestra_dual_register）都依赖
inject_layer 机制。裁决：12 参数按既有默认值固化进实现（增益 1.0、幅 36dp、
宽 96~216dp、渐入 120ms、节奏 0.85/门槛 0.25、偏置 0.75、级联 0.054s、远浪
0.75/0.50/3.2s），注入机制与远浪 2.5D 完整保留，行为零变化。调参 Dialog
"注入"组整组退出；顺带删除 Python 面板上一轮遗留的空白"段落"组标题。
已知余量（不阻塞）：若将来连续水面改为逐列消费九层高度（非均值），行波
形态将重新可见，届时可考虑恢复调参项。

## D167（2026-07-18）波浪曲线单纯化：谱高频减半、Gerstner 峰聚拢减半、短波扩散收窄

用户真机目测（D164~D166 精简后）："本来连续的完整 sin 曲线中间叠加另一个/
几个曲线、出现生硬转折"。排查：与银丝无关（用户确认）、静止轮廓即有；三轮
精简对波形几何数学零改动（逐项核对），是表面光学（背阴影体积塑形、流光/
轻纱/微法线纹理）移除后的视觉暴露。沿 X 轮廓的复杂度全部来自
ContinuousSurface：方向谱 9 模态（最短 58dp 的高频叠加）+ 波包 + Gerstner
峰聚拢（q=min(0.58, 0.46/(k·amp·9)) 实算各模态全部顶满 0.58——峰锐化即
"生硬转折"主源）。compose_layer_field（九层残差进二维场）确认无调用者，
B 路径沿 X 形态与九层行波无关。
收敛定档（模拟器 --screenshot --seconds 6 前后对照）：
- base_amplitude 高频三模态（102/78/58dp）0.92/0.66/0.42 → 0.46/0.33/0.21；
- Gerstner q 上限/系数 0.58/0.46 → 0.32/0.26，波包 0.62/0.48 → 0.34/0.27；
- spread_scale 尾三项 0.94/1.06/1.18 → 0.82/0.88/0.94（交叉干涉减少）。
第一层大浪活力与远浪叙事不变。两侧一比一同步（FableSolContinuousSurface）。
后续余量：若仍嫌复杂可继续压高频或砍最短模态；若嫌过平可回调 q 至 0.40 档。

### D167 撤销（2026-07-18 当日）：叠加/转折确认为既有观感，单纯化不做

发布 202607180912 后补做了正确的验证（真实录音 rec_20260718_094835 驱动、
近层裁剪 y300-560、t=4/7/10/13s 前后 8 图网格——首轮静息全景图无效，用户
指正）。用户对照后裁定：近层轮廓的叠加小波与转折在参数精简之前即已存在，
属波形谱的既定观感而非回归，单纯化没有必要。D167 三处数值全部还原、注释
清除、两侧同步，波形几何回到 202607180817 状态；发布 202607180942。
留档结论（免将来重查）：沿 X 轮廓复杂度 = 方向谱 9 模态 + 波包 + Gerstner
峰聚拢（q 实算全模态顶满 0.58）；compose_layer_field 为无调用死代码；
若将来真要单纯化，第一档参考值与网格对照存于本条与 update-20260718164500.md。

## D168（2026-07-18）恢复闪点出生场：glint_capacity_gain 转正式可调项（默认 0）

背景：D164 记录过已知连带——镜面高光项删除后闪点出生场只剩"菲涅尔细节×
天空反射"（正常浪面 raw≈0.001~0.02，恒低于 0.08 出生门槛），capacity_gain
拉 1 也不出生。用户喜欢闪点，裁决恢复出生场、参数转正式：
- 出生场恢复：raw = reflection·facet·0.90 + fresnel_detail·sky·0.24。
  镜面强度 0.90 为原 crest_glint_strength 默认值**固化**（参数不恢复）；
  specular AA 的 normalization/sigma 展宽维持删除态（aa=0 等价）。
- 毛细曲率通道恢复：OpticalWaveSet.sample（Python）/sampleInto（Android）
  重新输出 curvature，供 facet=clip(|curv|/(0.004+0.006·rough))^0.58。
  0.012 固定基线的毛细谱 curvature RMS≈0.005，facet 峰值可达 1——无需
  恢复 capillary 音频驱动链。
- glint_capacity_gain：默认 0（关闭，画面与 202607180942 逐位一致），
  label"闪点数量（试验期归零）"→"闪点数量"，调参 Dialog 拉起即出。
- 守护测试（两侧）：默认 0 必须无出生；capacity_gain=1 + sparkle=1 +
  roughness 0.35 的真实动态浪面 120 帧内必须出生并产生 mode 3 几何——
  防止出生场再次被静默改死。
Canvas 回退路径同构恢复。发布 202607181001。

## D169（2026-07-18）恢复波背自阴影：back_shade_gain 回归"外观"（默认 0.80）

背景：D164 按"归零无可感变化"删除了 back_shade_gain 及整条渲染链。用户
真机复看后裁定波背自阴影需要保留（"亮脊紧贴暗窝"的体积明暗相接），要求
按原默认值恢复，Android 放"外观与光学"组、Python 放"外观"组。
- 恢复面（两侧一比一，代码取自删除提交的父版本）：参数注册（0~1.2，默认
  0.80，步长 0.02）；material_policy 宽度/alpha 逐层权重表（近三层全宽、
  第 7/8 层归零）；GL 端 _back_shade/buildBackShade（背光坡 smoothstep ×
  脊线曲率门 → Hann 平滑 → 保色暗带，mode 9）；宏观曲率随 _prepare_contour/
  prepareContour 一并恢复（D164 时与消费者同删）；Canvas 回退
  _draw_back_shade/drawBackShade + OKLab 降明度派生色链（Python 复用存活的
  color_policy.derive_shadow_color，Android 恢复 FableSolShadowColorPolicy，
  仅 backShade 所需部分——macroShade 已由 shader 权重表接管，不恢复）。
- 兑现的既有固化：aerial_contrast 已按 0 删除，原式空气透视因子固化为 1，
  暗带 alpha 少乘一项，其余逐位同删除前。
- 共享 optical.frag 零改动：mode 9 落入现行 >7.5 半正弦分支（与旧 >8.5 分支
  剖面逐字相同），HDR 透射提升带 <8.5 上界天然排除 mode 9；仅补注释。
- 验证：Python 158 测试全绿；run.py 截图 A/B（rec_20260709_184459 驱动 6s）
  默认 0.80 对归零最大差 18/255、1162 像素，GL 直连与 QPainter 回退双端确认。
  Android JVM 测试全绿（默认几何守护改为"唯一默认几何=mode 9 暗带、0..6 层
  有、7..8 层无、gain=0 时归零"；HDR 源码序列断言恢复 shoulder→backShade→
  body→glint；backShade 四停靠渐变逐顶点测试原样恢复）。
- 排查记录（免将来重查）：首轮截图 A/B "零差异"是对照组 params JSON 格式
  错误——params.load/from_dict 只读顶层 "values" 子字典，裸键被静默忽略，
  两轮实际同为默认值；恢复代码本身自始正确。--params 文件须写成
  {"values": {...}}。

## D170（2026-07-18）“主浪”调参目录收敛：删除旧 Punch、踩拍归流动、Python 补显式分组

用户确认 2026-07-18 双端一致性诊断后的收敛方向：

1. Android 删除 `hero_punch` / `hero_punch_decay_s` 的参数注册、调参条目、多语言文案、
   `heroPunch01` / `heroPunchBand01` 零状态及每帧零状态消费。两项在生产链路没有任何非零
   写入，删除不改变默认或可达画面。
2. `beat_gain` 两端统一归入“环境与流动”。它只在可信拍点短暂加速近中层环境波相位，明确
   不改变 `HeroWave` 相位或振幅；继续放在“主浪”会误报职责。
3. Python `ControlPanel` 显式构建“主浪”组，展示仍实际生效的 5 个全局参数：
   `hero_gain`、`hero_len_dp`、`hero_attack_s`、`hero_release_s`、`hero_breath`。
   `hero_max_dp` 继续保留在“水体（逐层）”；Android 本轮不扩展逐层数组调参 UI。
4. 参数目录测试必须锁定以上分组与死键缺席，避免以后再次以“已注册且有读取点”代替完整的
   生产者—消费者活性审计。

## D171（2026-07-18）“声音分析与灵敏度”双端接通：Android Analyzer 热更新 + Python 离线传参

用户确认保留并接通 `agc_window_s`、`silence_gate_db`、`expander_amount`，同时将难以理解的
“感知前端”统一改名为“声音分析与灵敏度”。

1. Android 在 `FableSolTuning` 中引入渲染器/音频分析两类目标，并新增包含三项参数的
   “声音分析与灵敏度”组；13 个语言资源同步补齐。参数仍使用原默认值与范围，继续沿用现有
   SharedPreferences 持久化，并由 Dialog 将当前预览值送到 `AudioRecorder`。
2. Android 新增线程安全的 `FableSolFrontEndTuning` 快照。`AudioRecorder` 每批音频送入
   `FableSolRealtimeAnalyzer` 前应用最新快照，因此当前录音会话内调节即可生效，且不让音频线程
   读取 UI 或渲染器状态。
3. Python 统一栏目名称；离线 worker、缓存键和因果分析链接收同一组三项参数。非默认值以
   `RealtimeAnalyzer` 的调节前后差分修正离线响度、频段、静音与 onset 驱动；默认值保持旧离线
   输出不变。GUI 对离线文件调节采用 350ms 防抖后重分析，`--sim-audio` 也读取同一参数快照。
4. 目录合同、范围钳制、Android Analyzer 动态效果、Python 缓存隔离/worker 转发/插值均由测试
   锁定。20 秒确定性音频的离线端到端对照确认非默认值可改变响度、三频段、flow 与静音帧，
   并生成独立缓存。

## D172（2026-07-21）因果七境与可重复巨浪：证据持续、乐句显著性、双端实时一致

1. 采用零神经网络的因果七境解码器：固定声学标定输出响度 `W`、运动 `K`、冲击 `A`、
   活跃度 `E` 及其慢证据；状态通过迟滞、最短停留和证据释放转换。`LIFT`、`CLIMAX`
   不设固定最长时长，证据持续就持续，证据释放后才退出。
2. 巨浪门控只在 `PEAK` / `CLIMAX` 的音乐运动成立时工作。新高潮可由结构上升触发；同一长高潮
   内后续巨浪必须有新的急剧到达证据，并受全局因果冷却约束。取消“每个高潮最多两道”的配额，
   但不把冷却结束本身当作触发条件。
3. 巨浪继续使用 840dp 的完整宽峰，按实际水流速度从画外传播；不设置特殊演出时钟，不冻结、
   拉长或反复改写已形成的浪形。
4. Android 以 Python 实时链为蓝本迁移 Analyzer、状态机、流速/水位通道、门控和物理巨浪；
   复用现有 GLES/FP16 scRGB HDR、120Hz 固定物理与逐像素材质。可选的人声神经网络不在本轮范围，
   避免增加端侧模型、延迟和功耗。
5. 验证先以三段真实音频做因果回放与 HDR 离屏截图，再执行 Android JVM 测试、assemble 和
   阿里云 Debug 发布；不得用歌曲名或绝对时间戳写入生产算法。

## D173（2026-07-21）安静更有流、高潮更克制、已显示的浪更少改形

用户观感反馈五条，逐条以真实音频离屏回放定量确认后修改；Python 定稿、Android 同步。
所有改动都是阈值/时间常数/输运速度一级，热路径没有新增算法（实测 `sim.update`
11.1 → 12.0µs，桌面 JVM 相对口径）。

1. **可闻即有流**。旧执行器的 10dp/s 听感微调在 K=0..0.25 上渐入，实测安静段
   K01 中位 0.17 时 L0 只有 1.8dp/s，穿屏需 176 秒，画面读作完全静止。改为
   20dp/s 且在 K=0..0.10 内渐入，低端锚点 24 → 42dp/s（K=0.25），高端几乎不动
   （K=1.0 190 → 200dp/s）。数字静音仍严格为 0。
2. **巨浪回到"每个高潮 episode 最多两道"**。取消配额后同一段副歌平台上的第三、
   第四记重击也会成浪（Lose My Mind 母带 98.5s / 174.8s）。段落通道保持不限次
   （段落边界本身就是稀有的结构证据），只把无结构支撑的 repeat 通道压回
   `REPEAT_PER_EPISODE = 1`；本地到达的攻击门 0.28 → 0.40（录音版多出来的 39.1s
   attack 只有 0.34，想要的 54.1s 有 0.45）。母带回到 4 次、录音版回到 1 次，且
   `grand_wave_audio_validation` 新增"评审过的曲目不得有额外巨浪"两项检查。
3. **静音是证据，不是"没有证据"**。旧实现在确认静默的 2 秒里直接 return，于是
   一次咳嗽推上 CLIMAX 后画面挂满 2 秒才塌到 SILENCE（实测 PEAK 段驻留 2.7s、
   期间平均 water 仅 0.17）。现在照常解码、只喂零证据；同时 LIFT/CLIMAX 只有在
   "正在充能"时才允许点亮，正在释放的残余电量不得在跌过阈值途中重新触发。
   深层长积分 30s → 14s（30s 让第 8 层流速比第 0 层整整慢 20.4 秒）。
4. **主浪包络必须与自己的载波同速**。三个尺度组过去共用一个"流速×1.5 +
   0.45×波速"的输运速度，而每组载波各按 `c=sqrt(g/k)` 行进：L0 安静时包络
   142dp/s、载波仅 93dp/s，包络从自己的波峰上滑过去，屏幕里的浪原地长高变矮。
   改为逐组按自己的相速度输运。同时把模态谱宽从 2.20~0.50（4.4 倍）收到
   1.84~0.63（2.9 倍）——冻结音频时的改形残差从 10% 降到 8%，可见起伏 RMS 反而
   从 8.2 涨到 9.6dp；声像几何位移单独限速 9dp/s（`pan01` 的 0.22s 时间常数会让
   整片主浪瞬移 ±24dp，光学不受影响）。
5. **要高就必须也宽**。给每个尺度组按自己最短模态的波长设振幅上限
   （`MAX_HEIGHT_OVER_LENGTH = 0.085`，约 Stokes 破碎极限 0.142 的六成），被削掉
   的能量转给还有余量的更长模态。实测 CLIMAX 段波峰 height/width 的 p90 从 0.134
   （贴着破碎线）降到 0.071，中位数约 0.05——宽度稳定在高度的 14 倍以上，而可见
   起伏 RMS 只掉 2~5%。

**未改**：`water_drive01` 仍以 3 秒 short-term 响度为绝对主轴，因此一次 0.3 秒的
咳嗽仍会把水位撑住约 3 秒。这是 ADR-0015 的既定刻度，不在本轮调整；"退潮慢"的
其余部分（档位冻结、深层记忆）已由第 3 条解决。

## D174（2026-07-22）水位的贴地过渡与限速软饱和；真机实时与录制 WAV 不可逐帧对比

`20260722001826.wav` 复现"Android 一上来就巨浪、水位几乎一直满、涨到一半卡一下"，
而 Python 打开同一文件几乎没反应。详见
`analysis-2026-07-22-live-vs-recorded-wav.md`。

1. **WAV 缺前导，两者本就不可比**。`RecordingThread` 从 `startListening()`（界面
   打开）起就喂 FableSol 分析器，却只在 `mIsRecording`（按下录音键）之后写文件。
   底噪、归一化中心、七境与巨浪 episode 都在那段没被录进去的前导里建立。对照实验：
   同一段音频，只把前导从"无"换成"12 秒安静"，就从"最多 CLIMAX 0.9 秒"变成
   "几乎全程满水位、CLIMAX 3.3 秒"。**保持分析器先于录音运行（界面一打开就该有
   反应）**；如需严格可比只有"把前导也录进文件"或"按录音键时重置分析器"两条路，
   都属产品决策，本轮不做。
2. **水位加连续的贴地衰减**。水位走固定绝对刻度，而采集档给低频 +18dB 搁架、整体
   +10.5dB，房间轰鸣的 K 计权短时响度足以落进满刻度区；此前唯一拦截是 A 计权静音门，
   门一开水位就直接冲到 ~1.0。现在按"高出 A 计权底噪多少 dB"做 smoothstep 折减
   （`NEAR_FLOOR_SPAN_DB = 14`）。实测两条时间线同一时刻 `loud_db` 只差 0.3dB、
   `floor_db` 差 2.3dB，却一边静音一边 water 0.983 + 水位 156.9dp——就是缺这条过渡。
3. **限速改软饱和**。`CriticalSpring` 的 33.6dp/s 限速是对输出硬钳位并改写 velocity，
   下一帧弹簧又算出更大位移，于是逐帧在"贴限速"与"弹簧自由"之间跳：实测 33.2% 的帧
   贴着限速、|加速度| 反复 ±350dp/s²（峰值 434），正是"卡一下、抽搐一下"。改用项目
   已有的 `soft_limit`（C∞）限制每步位移，速度与位移一致。修复后贴限速的帧 33.2% → 0%。

**回退记录**（避免重走）：试过把启动窗口的底噪播种改成"取窗口最小值"——最小值太乐观、
门更松；也试过给底噪加"学习期快时间常数"——底噪更快贴住房间声、门抬高，把本该有的
反应也门掉了。真正缺的是水位的连续过渡，不是底噪的收敛速度。

修复后双端同一 WAV：PEAK/CLIMAX 档一致 **100%**，water/kinetic/flow 相关
0.999/1.000/1.000，`level` mean|Δ| **0.48dp**。

## D175（2026-07-22）采集采样率改为运行期协商、优先 48kHz；离线路径按实时 hop 重放

用户第三段录音（`20260722091010.wav`，开 dialog 后立刻录，前导极短）仍然两端不一致：
Android 一上来就有一道巨浪、Python 打开文件没有，第二道两边都有。逐条查清后是
**两个独立问题叠加**，与"Android vs Python 的算法差异"无关。

### 一、离线路径把因果特征降采样了一半（已修）

`analyze()` 把实时链的因果特征 previous-sample hold 到 librosa 的
22050/512 = 43.07Hz 网格上，而实时 hop 是 48000/512 = 93.75Hz——**hold 到更粗的
网格等于丢掉一半 hop**，punch / energy_rising 这类瞬态证据的峰值直接被采样漏掉。
那段代码的注释还写着 "a causal previous-sample hold of the exact realtime hop
stream"，实际并非如此。现在整条时间线加密到实时 hop 网格
（`_densify_to_realtime_hops`），librosa 派生的慢变诊断量按 hold 上采样。

同时 `OfflineDirector.drive` 每个渲染帧只把**最后一个** hop 喂给 mapper，60fps
渲染帧之间通常跨过 1~2 个 hop，于是巨浪门的 0.2~0.3s 确认窗少看约 36% 的样本。
Android 的 `GlRenderer` 一直是"渲染帧可能一次收到多个 hop，必须逐个消费"，现已对齐。
（`tools/av_probe.py` 也修了：它绕过 `drive()` 直接调 `frame_at`，**完全没有回放
onset**，此前几轮的离线测量都偏低。）

修完后离线路径与实时链**在同一采样率下等价**：状态一致 99.5%、各通道相关 1.000、
巨浪 1 vs 1。缓存版本 44 → 45。

### 二、采集采样率：改 Android，不改 Python

剩下的差异全部来自采样率——同一段音频 44.1kHz 分析出 3 道巨浪、48kHz 只有 1 道。
用户提出"为什么不是让 Android 改成 48kHz"，采纳，理由有两条：

1. **手机很可能已经在偷偷重采样**。现代 Android 音频 HAL 原生跑 48kHz，`AudioRecord`
   请求 44100 会让框架插一层重采样器；而重采样恰好抹平瞬态，偏偏巨浪门就靠
   punch / energy_rising 判定。改成 48000 等于把这层去掉，拿到硬件原始流。
2. **全部标定都在 48kHz 上做**。D1~D174 的每个阈值、母带四个已验证的巨浪窗口，都是
   在 Python 模拟器的 48kHz 下定的。把 Python 改成 44.1kHz 是让工具偏离自己标定出的
   参数；把 Android 改成 48kHz 才是让设备对上工具。

**不能直接把常量改成 48000**：万一某台设备不支持，`getMinBufferSize` 返回
ERROR_BAD_VALUE，旧代码 `if (bufSize <= 0) return` 就是静默录不到音。因此改为
运行期协商 `RECORDING_SAMPLE_RATES = [48000, 44100]`，取第一个真正 INITIALIZED 的，
把实际值写进 `mSampleRate`，贯穿三个分析器与 WAV 头（三者本来就都以 sr 为构造参数）。

顺带修 `WaveAudioAnalyzerOpus` 的 `YIN_MIN_TAU=73 // 44100/600`、
`YIN_MAX_TAU=588 // 44100/75`——按 44100 写死，48kHz 下音高范围会从 75~600Hz 偏成
82~657Hz；改为由 `sampleRate` 与 `YIN_MIN_HZ/YIN_MAX_HZ` 推导。

**注意**：录音文件格式随之变成 48kHz（约大 9%），老录音不受影响。

### 验证

48kHz 输入下双端：water / flow / level 相关 **1.000**，`level` mean|Δ| **0.021dp**，
巨浪 1 vs 1，PEAK/CLIMAX 档一致 97.7%。Python 287 tests、巨浪验证 8/8 全绿；
Android assembleDebug + 全量单测通过。

**仍存的残差**：`grade_drive01` 相关 0.971、均值差 0.05（water/flow/level 已完全一致）。
怀疑来自 Python 侧可选的人声模型路径（Android 明确不移植），尚未定位，记在 followups。

## D176（2026-07-22）分析链 parity 是可执行契约：解码 −3dB、离线降采样、块相关性三修

D175 之后残留的 `grade_drive01` 0.05 均值差被定位并根除，同时挖出另外两个更严重的
缺陷。**本轮 Android 侧零改动**，全部修复在 Python 模拟器；改的不是 App 的行为，
而是我们衡量它的工具终于准了。Python 侧新增 ADR-0019 记录该契约。

### 三个真 bug（都不是调参问题）

1. **单声道解码丢了 3.01dB**（影响最大）。`decode_audio` 用 `ffmpeg -ac 2` 把单声道
   升成双声道，而 ffmpeg 走**等功率声像律**，每个输出声道乘 1/√2；`analysis_view`
   取回第 0 列时拿到的就是衰减过的信号。逐样本比值中位恰为 `0.70710677`。
   讽刺的是代码里已经有一段注释在防**反方向**的 +3.01dB（单声道被当两路重复计入，
   见 `source_channel_count` 的 docstring），却没注意同一条路径上还有个 −3dB。
   后果：模拟器打开手机录音时 `water_drive01` 均值 0.204 vs 真机 0.225、水位
   24.4 vs 26.5dp、少触发一道巨浪。**这就是用户反复说的"android 水位比 python 高
   不少"的全部来源。** 修法：单声道源按 `-ac 1` 解码后等幅复制——等功率律是给声像用的，
   单声道转双声道在播放语义上就是原样复制。

2. **离线时间线被降采样到 librosa 的 43.07Hz**，实时 hop 是 93.75Hz。D175 已修了主
   驱动字段，本轮补齐**所有**进入 mapper 的逐帧量（频段/音色/空间/韵律共 20 项），
   librosa 只保留它独有的结构产物（节拍、段落、tempo）。同时 onset 改为直接取实时链
   自己发出的事件——旧实现是用 librosa **重新检测**一遍、强度还按整段 p50~p98 归一，
   既换了检测器又是非因果统计，等于两条路径注入的根本不是同一批浪；`prominence`
   离线更是完全不产出，现已补上。

3. **前端不是"喂入块无关"的**。Android 每次 feed 512 帧、Python 离线前端 8192。
   可选歌声模型 `VocalStream` 的推理排程按 `now = time_base + samples_seen/sr` 算，
   取的是**块尾**时刻，推理窗口又取"当前缓冲的最后 3 秒"，两者都跟着块边界跑。
   实测 512/1024/4096 相对 8192，`grade_drive01` 最大差 **0.16**——而 PEAK 阈值才 0.54。
   更根本的是 **Android 侧根本没有这个模型**（`FableSolCausalStateEvidence` 只用 DSP
   `vocalSolo`，assets 里没有任何 onnx），模拟器却在自动加载它。CONTEXT 的措辞本就是
   "不要求可选歌声模型"——可选即默认不启用，现改为 `AUTOLOAD_MODEL = False`。

### 验证

48kHz 手机录音（50.6s，用户当天新录）：

| 对比 | 巨浪 | 状态一致 | 全通道相关 |
|---|---|---|---|
| Android vs Python 实时链 | 3 vs 3 | **100.0%** | 1.000 |
| Android vs Python 打开文件 | 3 vs 3 | **99.9%** | 1.000 |

巨浪时刻 Android `3.83 / 19.45 / 43.70` vs Python 打开文件 `3.83 / 19.43 / 43.69`
（最大差 0.02s）；水位平均差 **0.079dp**；解码逐样本 **0 差异**。
母带那条线不受影响，仍精确保留四处已验证重音（53.1 / 69.6 / 139.3 / 159.1s）、无多余。
Python 293 tests 全绿（含新增块无关性 6 项）。

### 门禁

Python `tests/test_feed_chunk_invariance.py`：512/1024/4096 与 8192 四种块长下，13 项
因果字段与事件表逐位相同；并断言歌声模型默认不自动加载。这条门禁是本轮最有价值的产物
——它把"两端可比"从愿望变成会变红的东西。

### 遗留（见 followups）

`CaptureInputProfile.loudness_trim_db = 10.5` 是在偏低 3dB 的离线路径上拟合的，
真机一直比该标定假设的热 3dB，是否需要重新拟合尚未评估；旧的 44.1kHz 录音 fixture
`20260707165742.wav` 已不是有效 parity 参照，待重录 48kHz 版本替换。
