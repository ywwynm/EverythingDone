# 从世界取色图标相机顶部细节调整

## 用户反馈

- 相机顶部凸起区域宽度需要小一点。
- 快门横线和机身顶边之间需要有一点 y 方向距离。

## 本次修改

- 调整 `app/src/main/res/drawable/ic_pick_from_world.xml`。
- 收窄相机顶部凸起区域。
- 将快门短横线略微上移，使其与机身顶边之间保留垂直间距。
- 保持相机外框、内部 Material `colorize` 滴管和当前颜色 tint 逻辑不变。

## 验证

- 已运行 `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug --console=plain`，构建通过。
- 已运行 `:app:publishDebugUpdate -PdebugUpdateNotesFile=docs/features/detail-color-sampling/debug-updates/update-20260626235410.md`，发布 debug 更新 `202606261554` 成功。
