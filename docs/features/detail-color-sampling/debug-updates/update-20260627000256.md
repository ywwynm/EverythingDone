# 从世界取色图标简化为滴管

## 用户反馈

- 前面的组合图标方案消耗了过多设计精力，最终决定直接使用滴管图标。

## 本次修改

- 重新绘制 `app/src/main/res/drawable/ic_pick_from_world.xml`。
- 删除眼睛、相机、地球等组合元素，改为单独的 Google Material Icons `colorize` 24dp 官方滴管路径。
- 保持 `ColorAreaView` 对图标的当前颜色 tint 逻辑不变。

## 验证

- 已运行 `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug --console=plain`，构建通过。
- 已运行 `:app:publishDebugUpdate -PdebugUpdateNotesFile=docs/features/detail-color-sampling/debug-updates/update-20260627000256.md`，发布 debug 更新 `202606261603` 成功。
