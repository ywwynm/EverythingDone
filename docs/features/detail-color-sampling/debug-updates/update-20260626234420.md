# 从世界取色图标改为相机外框与 Material 滴管

## 用户反馈

- 地球方案视觉仍不够理想，改为相机外框内放滴管。
- 相机外框可以接近添加附件弹窗“拍照”入口左侧图标的感觉，但需要高一点，方便容纳滴管。
- 滴管继续使用 Android 官方 Material 风格。

## 本次修改

- 重新绘制 `app/src/main/res/drawable/ic_pick_from_world.xml`。
- 图标改为偏高的相机外框，保留顶部取景器凸起和左上小短线，接近现有“拍照”入口的相机语义。
- 相机内部放入 Google Material Icons `colorize` 24dp 官方滴管路径的缩放版。
- 保持 `ColorAreaView` 对图标的当前颜色 tint 逻辑不变。

## 验证

- 已运行 `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug --console=plain`，构建通过。
- 已运行 `:app:publishDebugUpdate -PdebugUpdateNotesFile=docs/features/detail-color-sampling/debug-updates/update-20260626234420.md`，发布 debug 更新 `202606261544` 成功。
