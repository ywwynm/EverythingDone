# 从世界取色图标快门位置修正

## 用户反馈

- 相机外框内的短横线像是快门键，但它不应该放在机身内部。
- 快门键应位于相机外框的上边，也就是机身顶部。

## 本次修改

- 调整 `app/src/main/res/drawable/ic_pick_from_world.xml`。
- 将相机快门短横线从机身内部移动到左上方顶部机身位置。
- 保持相机外框、内部 Material `colorize` 滴管和当前颜色 tint 逻辑不变。

## 验证

- 已运行 `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug --console=plain`，构建通过。
- 已运行 `:app:publishDebugUpdate -PdebugUpdateNotesFile=docs/features/detail-color-sampling/debug-updates/update-20260626235131.md`，发布 debug 更新 `202606261551` 成功。
