# 从世界取色图标改为美人眼睛

## 用户反馈

- 相机外框与滴管方案仍不够理想，改为“美人的眼睛”方向。

## 本次修改

- 重新绘制 `app/src/main/res/drawable/ic_pick_from_world.xml`。
- 图标改为单色眼睛：杏眼外轮廓、虹膜、瞳孔，以及少量上睫毛。
- 保持 `ColorAreaView` 对图标的当前颜色 tint 逻辑不变。

## 验证

- 已运行 `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug --console=plain`，构建通过。
- 已运行 `:app:publishDebugUpdate -PdebugUpdateNotesFile=docs/features/detail-color-sampling/debug-updates/update-20260626235748.md`，发布 debug 更新 `202606261558` 成功。
