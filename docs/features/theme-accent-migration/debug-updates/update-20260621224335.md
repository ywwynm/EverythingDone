# 主题强调色渐变第六轮修复

这次继续修复上一版调试包中的布局和前景色细节：

- 各类 Chooser dialog 中的 radio item 左侧 padding 改为和 dialog 标题文本一致，radio 图标左缘重新对齐标题文本左缘。
- 统计页、关于页 FAB 的 icon 不再使用纯黑色；首页文件夹内 FAB 的 icon 也不再使用纯黑/纯白。统一规则为亮背景用 54% 黑，暗背景用 86% 白。
- 排查长按记事/文件夹进入选择模式后的 contextual actionbar：标题、关闭图标、菜单图标和最右侧 overflow 均走保留 alpha 的统一前景色路径，不使用纯黑/纯白。
- 记事列表 widget 中置顶文件夹的置顶标识从内容行移到 folder card 根布局的右上角 overlay，贴紧右上角且不再挤占正文布局。

验证：`./gradlew.bat :app:assembleDebug` 已通过。已通过 `:app:publishDebugUpdate` 发布到阿里云，debug update code 为 `202606211444`。
