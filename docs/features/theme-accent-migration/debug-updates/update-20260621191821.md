## Debug 更新 2026-06-21 (第二轮)

### 修复与改进

**CheckBox 选中态：** 修复了上一版 CheckBox 完全不显示的 bug——改用 `setButtonTintList` 保持 Material 原生勾选图标，tint 为代表色。

**Settings 蓝色迁移：** 所有 Settings 页面的 panel 标题图标和文字颜色、对话框强调色从 `blue_deep` 改为 accent 渐变代表色。

**Widget 置顶/进行中标记：** 改用 accent→accent2 渐变色 bitmap 替换原来的黑白自适应滤镜。

**开始做事 icon 尺寸：** 缩小至 20dp 避免上下裁切。
