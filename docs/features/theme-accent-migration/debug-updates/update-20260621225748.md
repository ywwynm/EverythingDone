# Radio 间距微调

这次修复上一版调试包中 Chooser dialog 的 radio item 间距问题：

- 保持 radio 图标左缘和 dialog 标题文本左缘对齐。
- 将 radio 图标与右侧文字之间的 `drawablePadding` 从 4dp 调整为 8dp，避免间距过紧。

验证：`./gradlew.bat :app:assembleDebug` 已通过。已通过 `:app:publishDebugUpdate` 发布到阿里云，debug update code 为 `202606211458`。
