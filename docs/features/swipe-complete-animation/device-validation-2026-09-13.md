# 2026-09-13 实机验证

- 用户明确授权在设备 `9018f404`（OPD2515，Android 16）调试；仅使用本轮新建的测试记事验证完成操作。
- 覆盖真实首页左滑的慢拖、暂停、回拖、取消、快速甩动完成，以及清理后卡片和触摸恢复情况。以录屏和实际卡片状态验证，不能仅以 JVM 测试通过代替。
- 当前状态：已连接设备，准备核对安装版本并建立操作与录屏回归流程。

## 首轮发现

- 已通过真实英文设置页选中粒子，但慢拖仍显示滑动。专用记事 `CodexAnimation0913` 的回拖取消未改变记事数量。
- 对比快照失败、几何变化、会话提前结束三条路径后，定向日志确认是坐标保护误触：创建会话时坐标 `[44,562]`，下一帧变成 `[43,561]`，快照完成时会话已经关闭。
- 这是卡片现有按压缩放反馈改变了 `getLocationOnScreen` 的原点，并非卡片真的换位。位置保护改为父容器屏幕坐标加卡片布局位置，同时忽略手势位移与按压缩放；真正滚动、重排和尺寸变化仍会清理。
- 后续继续以系统 MotionEvent 输入重复慢拖、暂停与回拖；原始时刻、截图和结果保存在忽略目录 `tmp/thing-animation-device/`。

## 修复后横屏实测

- 按卡片自身宽度映射进度，解决多列平板按整屏宽度计算导致拖过卡片六成、仅播放约一成的问题；ItemTouchHelper 的完成判定保持原有行为。
- 同一手势拖至约六成、停住、回至约两成、再归零松手。停住前后卡片区域像素最大差为 0；回拖后相对初始图的平均差从 114.47 降至 24.24，取消恢复后为 0.01/255，卡片仍在列表。
- 90 ms 快速左甩成功从首页移除测试卡片，准备早于完成回调的竞态未导致崩溃或卡片回闪。该卡片粒子首帧约 285 ms，仍有模型准备成本，不宣称零延迟。
- 用于定位的临时日志已移除。

## 竖屏实测

- 右侧列测试卡片拖动、停住、回拖归零：暂停两次截图最大像素差 0，取消后平均差 0.01/255，首页数量不变。
- 系统 `ACTION_CANCEL` 在中途终止手势：卡片恢复前后像素完全一致，数量不变。
- 1.4 秒慢速左滑超过原完成阈值：测试卡片移除，首页数量从 15 减到 14，原记事保持不变。

## 发布与清理

- 再连续完成四条专用测试卡片，首页数量逐条从 16 恢复到 12；每次都核对目标消失和数量减一。
- 调试更新 `202609131502` 已发布并安装到 9018f404，远端完整 APK、本地与已安装包哈希一致：`8f38efe7695f6d926c81623587008a8d70a178de5a11e3aa7abe8c38bcbb846c`。
- 六条测试记事已核对标题后移入可恢复的回收站，保留全部原记事。设备设置在验证后恢复为原来的涟漪／滑动和自动旋转。
