## 左滑回拖闪退修复、快速起滑优化与动画风格文案（20260914050401）

发布后核验：debug 更新 `202609132104`，26277014 字节，远端完整下载、本地及两台安装包 SHA-256 均为 `5cdfcb2b373439ebc8397ee96e2d341f25dd81ef280ad79ff04139706d1d0ad1`。实际发布包两台各 3 轮跨原点回拖通过，连续快速左滑首个非零画面分别约 132 ms、168 ms。本轮每台 3 条专用记事均经真实菜单移入可恢复的回收站；OPPO 恢复原 2 个文件夹、15 条记事和新建粒子／左滑渐隐设置，Samsung 恢复原 3 条记事和新建边框／左滑渐隐设置。原弹窗动画、自动旋转保留，观察标记及本轮外部图片源、触摸输入工具移除，采样关闭，最终无粒子渲染与观察器线程、无当前进程崩溃。未创建 Git 提交。

1. 修复左滑记事后不松手、向右回拖到原位及越过原点的闪退。两台设备都复现了 DecorView 绘制期间移除子 View 的异常；ParticleSwipeAnimator 现在立即关闭会话并隐藏旧画面，在当前绘制返回后释放叠加层及通知业务，兜底快照创建也避免在绘制中修改层级。
2. 继续优化按下后立即左滑的响应：首个粒子画面直接使用当前手指进度，在输入分发阶段更新距离，当前帧优先于尚未请求的轨迹预计算。保持原粒子数量、材质、随机规则、积分、压力迭代和回拖的连续插值。
3. 设置改名为“新建记事动画风格”“左滑完成记事动画风格”，后者“滑动”改为“滑动渐隐”，同步全部 13 套语言资源。
4. 在 9018f404（OPPO OPD2515）及新增 R5CW20BLNKL（Samsung SM-S9180）上，通过实际按钮、返回、文件选择及系统连续触摸测试。图片快速起滑旧／新样本范围：OPPO 178–196 / 132–164 ms，Samsung 201–242 / 152–193 ms。Samsung 慢拖 GL 帧准备 P95 从约 18–19 ms 降为 12–14 ms；尚不能保证稳定 60 fps，仍有准备等待和合成间隔波动。
5. 两台分别通过多轮跨原点、回拖同相位、图片圆角、自然恢复、新建保存三阶段顺序及入场返回中断回归。各六轮打开已有记事／返回／取消后无残留粒子渲染线程，30 项相关 JVM 测试通过。
6. GPU 验证保留边界：Samsung 61 帧对照全相等；OPPO 曾出现非稳定单帧差异，原路径自身重跑也不相等，相同种子第三轮跨路径 61 帧相等。本轮 20 个粒子资源及原生库与上一发布包字节一致，未将重复通过包装为跨运行逐像素恒等的保证。详细数据与失败结果见功能诊断记录。

发布后将核验远端、本地与两台安装包散列，使用实际发布包回归，并把本轮明确命名的测试记事移入可恢复的回收站、恢复原动画设置并关闭采样。不创建 Git 提交。
