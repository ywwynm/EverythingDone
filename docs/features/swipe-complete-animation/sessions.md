# 实现记录

## 2026-09-14 提交确认

- 用户确认提交当前实现。回拖崩溃、起滑响应、图片轮廓与国际化修复随功能归档，连续输入工具和双机验证记录单独成组；不将 GPU 跨运行差异或剩余帧率波动记为已解决。

## 2026-09-14 回拖崩溃与快速起滑

- 已发布 `202609132104` 并核验两台实际安装包，发布后各 3 轮跨原点及快速起滑通过。测试记事移入回收站，原数据、动画选项和旋转状态恢复，采样关闭；未创建 Git 提交。

- 双机复现 DecorView 在绘制中移除子节点的异常，改为立即隐藏／关闭会话、绘制返回后释放及通知业务。补齐跨原点、图片卡片、回拖同相位及取消回归。
- 首帧采用当前手指进度，位移在输入阶段更新，当前帧优先于额外历史预计算；图片快速起滑双机对照确认改善，Samsung 慢拖 GPU 帧准备耗时下降。数据边界及 GPU 非稳定差异保留在[诊断记录](reverse-and-latency-2026-09-14.md)。
- 三项设置文案同步 13 套语言资源，30 项相关 JVM 测试通过。两台各六轮已有记事打开／返回／放弃预备后无残留粒子渲染线程。

## 2026-09-14 起步响应与图片圆角

- 确认左滑完整轨迹预计算造成约 0.24 秒准备，改为按下预备零帧、识别后按需补齐。真实图片卡片在识别后约 43–52 ms 呈现第一帧非零进度，包含横竖屏样本。
- 根 OutlineProvider 的自定义轮廓进入离屏 RenderNode，首次占位也保留圆角。60% → 20% → 60% 的两个同相位画面逐像素相同；停住不推进，取消后原卡片恢复，快速甩动正确完成。
- 六轮已有记事打开／返回／取消后粒子线程均为 0，数量不变，PSS 没有持续上升。30 项相关 JVM 测试与 61 帧设备 GPU 对照通过。
- 版本 `202609131811` 发布并安装到 9018f404，实际发布包慢拖及图片圆角再次通过。尚有合成间隔波动，完整边界见 [专项记录](../thing-creation-animation/startup-and-outline-2026-09-14.md)。
- 本轮四条专用记事已移入回收站，原记事及设置恢复；最终粒子／观察器线程均为 0，无当前进程崩溃。

## 2026-09-14 真实手势性能补测

- 用户要求模拟真实按钮与返回。正式测试由系统输入驱动实际 UI，debug 观察器只读记录；最初内部驱动数据不计入验收。
- 慢拖实测每 16.7 ms 有输入，但画面约每 50 ms 才更新，定位到 60 档取整。先加可复现失败的回归测试，再以相邻缓存插值修复；缓存规模、共同模型及普通整数帧重放保留。
- 同一卡片同一手势前后各 4 轮，纹理间隔中位数从约 50 ms 降到 16.6 ms，P95 从 67–84 ms 降到 18–22 ms；停住、回拖归零、系统取消及快速甩动均通过。长卡片仍有 33–40 ms 的偶发间隔。
- 29 项相关 JVM 测试通过，无跳过；GLSL 实际编译通过。测量方法、CPU／GL、内存及未解决项见 [性能验证](../thing-creation-animation/performance-validation-2026-09-13.md)。
- 调试更新 `202609131636` 已发布并安装，远端完整下载、本地和设备安装包 SHA-256 一致。实际发布包再完成一轮新建保存、两轮慢拖取消，保存顺序通过，拖动中位数约 16.7 ms，P95 仍有 18.6–32.5 ms 波动。
- 13 条本轮测试记事移入可恢复的回收站，首页恢复为原有 13 条记事和 2 个文件夹。新建粒子／左滑滑动／弹窗档位 3 和自动旋转恢复；采样标记删除，粒子及观察器线程均为 0，最终进程 crash buffer 无异常。未创建 Git 提交。

## 2026-09-13 指定设备调试

- `9018f404` 上确认原按压缩放会误触粒子位置保护并退回滑动。改用父容器坐标和卡片布局位置；按卡片宽度计算消散进度，适应平板多列布局。
- 横竖屏真实 MotionEvent 的暂停、回拖归零、系统取消通过。停住两次截图卡片区域完全一致；系统取消后原图像素完全恢复。快速甩动及慢速超过完成阈值均正确减少首页数量，无进程崩溃。
- 28 项相关 JVM 测试通过，临时定位日志已清除。实际粒子首帧仍约需 0.3 秒准备；详细证据见 [实机验证](device-validation-2026-09-13.md)。
- 再连续完成四条测试卡片均正确减少数量。`202609131502` 发布并安装后，远端完整下载、本地与已安装 APK 哈希一致；测试记事移入可恢复的回收站，原记事与原有设置保留。未创建 Git 提交。

## 2026-09-13

- 设置新增“左滑完成记事动画”，可选滑动或粒子，默认保持滑动，全部 13 组语言同步。
- 共同模型完成正向解算后缓存 60 组呈现相位；拖动使用绝对距离直接索引，回拖不做反向积分，停止手指不会自动推进动画。粒子运动固定向左。
- 真实 View 保留 ItemTouchHelper 的逻辑位移；准备阶段在卡片原位绘制内容，粒子首帧提交后交接。取消、切换方向、卡片回收、尺寸位置变化及生命周期中断都恢复透明度并清理资源。
- 每张卡片独立持有手势，快速甩动可在准备完成后播放剩余过程，再进入原有完成和隐私认证路径。实际呈现相位按 Texture 时间戳对应，完成回调只执行一次，防止成功滑动后卡片重新出现或重复创建动画。
- 本地构建及 28 项相关 JVM 检查通过；手势测试覆盖正向、回退、恢复、停住、非法输入及提交／呈现进度差异。未使用 ADB，实际拖动手感与首次准备等待待用户试用。
- 已随新建记事动画发布阿里云调试更新 `202609131419`，远端元数据编号与本地 APK 的 SHA-256 已核对一致。没有创建 Git 提交。
