# 2026-09-13 实机验证

- 用户明确要求在 `9018f404` 上调试，授权仅针对该设备。设备为 OPD2515，Android 16。
- 上一轮的构建、JVM 测试与发布不等于视觉验证。本轮通过真实设置页、首页新建按钮、详情保存链路录屏检查三种动画及窗口衔接。
- 使用独立测试记事，保留用户原有记录。左滑跟手、回拖、取消、快速完成记录见 [左滑实机验证](../swipe-complete-animation/device-validation-2026-09-13.md)。
- 当前状态：已连接设备，准备核对安装版本并建立操作与录屏回归流程。

## 首轮发现

- 安装已发布包 `202609131419` 后，横屏英文设置的两项选择弹窗和保存正常。
- 首页点击新建、粒子完成后连续两次崩溃。堆栈为 `ViewGroup.removeFromArray` → `ParticleDismissOverlay.removeSelf`；出现层的 `onDetachedFromWindow` 同步调用 `afterRelease`，新建转场随即移除同一 DecorView 内更靠前的背景和触摸层。外层 `removeView` 持有的子节点下标已失效。
- 三项假设按顺序检查：同父容器移除重入、布局变化重复清理、Activity 生命周期重叠。普通静止新建也稳定复现，且调用链直接进入 `afterRelease`，确认第一项。
- 修复让主动释放在 `removeView` 返回后通知；外部 detach 则在主线程下一次消息通知。通知保持一次性。此故障依赖 Android ViewGroup 实际移除顺序，以相同设备真实新建入口作为回归，不用浅层 JVM mock 代替。
- 系统 screenrecord 写入文件被拒绝，截图与 UI 层级读取正常；继续建立可用的逐帧证据方式。

## 修复后补充

- 相同真实入口连续打开、返回和保存已不再触发 ViewGroup 崩溃。
- 通过连续系统截图发现键盘先于入场动画展开、覆盖尚在显示的首页快照。转场现在暂时保持输入法隐藏，内容入场完成后再恢复正文焦点并展开键盘；退出或配置变化不会补弹键盘。
- 系统录屏命令在此设备被拒绝／挂起，本轮改用 `screencap` 连续截图及粒子日志交叉检查。截图采样约 2–3 帧/秒，只用于验证视觉状态和衔接，不能据此声称动画达到特定帧率。
- 横屏英文界面三种设置分别选中、回显并成功打开和保存；新卡片数量逐次增加一条。边框光效全屏入口通过水平翻转原边框 View，将起点统一到右下，保留原路径实现和材质。
- 竖屏测试期间临时关闭自动旋转；原系统设置为 `accelerometer_rotation=1`、`user_rotation=0`，结束时恢复。

## 发布包与清理

- 已发布调试更新 `202609131502`，26,746,436 字节。远端完整下载、本地输出与 9018f404 实际安装包的 SHA-256 均为 `8f38efe7695f6d926c81623587008a8d70a178de5a11e3aa7abe8c38bcbb846c`。
- 六条专用测试记事均通过真实左滑完成。核对“已完成”中恰好为这六个完整测试标题及数量后，将它们移入可恢复的回收站；没有永久删除，也没有操作原记事。首页恢复为原来的 2 个文件夹、12 条记事。
- 发布包安装后，另外三轮在创建页恢复前台约 132–143 ms 时按返回键均正常回首页，同一进程持续存活，随后完整新建正常；恢复原动画选择和自动旋转设置。

## 设备录屏限制

- 本轮 `screenrecord --output-format=h264 --time-limit 1 -` 没有返回画面且未按时退出，产生设备进程 16597。对应本机 Python 与 ADB 进程已停止。
- 设备对该进程的正常终止和 SIGKILL 均返回 `Permission denied`，结束时它仍处于休眠／等待状态；没有扩大操作到设备重启或其它进程。开始前已有的录屏进程 4684、12709 未操作。
- 此限制不影响本轮已完成的 UI 输入、系统截图、构建、安装和功能回归，但不能宣称录屏进程已清理。应用本身的最终进程没有新的崩溃日志。
