## 新建记事与左滑完成动画（20260913221946）

设置中的原边框光效勾选项改为“新建记事动画”，可选择“涟漪”“边框光效”“粒子”。原先开启边框光效的用户继续使用该效果，其余保留涟漪。同一选项控制首页新建按钮与保存成功后新卡片的入场，粒子固定从右下方出现。所有 13 组语言资源同步更新。

保留现有 DetailActivity，不改成 View 或 Fragment。ThingCreationTransition 使用一次性首页快照承接本次零时长系统跳转，再在创建页完成布局后播放入场动画，避免动画全部结束后才开始初始化 Activity。启动成本仍存在，首次等待与窗口衔接需要设备试用确认。快照不会写入 Intent、文件或跨次缓存，动画结束和中断时释放。

新增“左滑完成记事动画”，可选择原来的“滑动”或“粒子”。粒子复用弹窗共同模型并固定向左，手指移动距离决定消散进度，停住和回拖均保持对应相位。松手后跟随原恢复／完成逻辑；甩动早于准备完成时，播放剩余画面再完成操作。保留原有隐私认证、记事状态处理及右滑开始做事逻辑。

ParticleSwipeAnimator 为每张卡片分别持有手势，保留 ItemTouchHelper 的逻辑位移，防止松手时进度归零。卡片回收、切换方向、尺寸变化、退出页面及 GL 失败均清理叠加层并恢复卡片；实际已显示进度按提交时间戳对应，完成回调只执行一次。新建粒子也纳入原有滚动门控和几何清理。

验证：本地 assembleDebug 成功；28 项相关 JVM 测试通过且无跳过，涵盖旧设置兼容、手势正反进度、非法输入、实际提交与呈现差异、共同材料、GLSL 编译和正逆时序；6 个相关文案在 13 组语言中完整且唯一。未使用 ADB，实际视觉、首次准备耗时和拖动手感待用户试用。

发布状态：已发布阿里云调试更新 `202609131419`；远端更新编号、SHA-256 与本地 APK 一致。实际视觉效果待用户试用，未使用 ADB、未创建 Git 提交。
