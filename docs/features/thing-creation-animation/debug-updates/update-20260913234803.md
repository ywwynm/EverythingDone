## 新建记事保存后粒子动画顺序修正（20260913234803）

用户反馈：保存完成后，粒子尚未播放完，完整记事就已显示；正确顺序应为先腾出位置、再播放粒子、最后显示完整记事。

在 9018f404（OPD2515 / Android 16）连续两次复现。逐帧观察真实 ThingsActivity、RecyclerView ItemAnimator 和粒子已提交进度，确认默认插入动画与粒子并发修改同一卡片的 alpha。粒子进度仅约 14%–22% 时，插入动画已把完整卡片显示出来。

ThingsActivity.playNewItemAnimation 的粒子分支现在让新卡片保持 INVISIBLE，保留布局空间并等待 ItemAnimator 实际完成，随后才捕获卡片并启动粒子。原有滚动门控、失效回调的代次检查与退出清理继续生效；完整真实卡片仍在粒子末帧已提交后恢复。不使用固定延时来估算腾位结束。

增加仅调试包可用的 NewItemAppearanceProbeActivity 和真实 UI 回归脚本。修复前两轮分别检出 36、39 帧提前可见；修复后横屏、竖屏及从滚动后的列表发起创建三轮均为 0 帧提前可见、0 帧列表动画与粒子重叠。28 项相关 JVM 测试通过，无失败或跳过。本次未改动涟漪、边框光效、左滑进度或弹窗共同材质。

发布后核验：调试更新 `202609131548` 已安装到 9018f404，远端完整下载、本地构建与设备安装包 SHA-256 均为 `5b0c301926b439f05d15cab1261e49b80c8fd70146b98bf45df9e3bcc21a21e3`。实际发布包再跑一次真实保存，提前可见与动画重叠均为 0 帧。本轮六条专用测试记事已移入可恢复的回收站，首页恢复为 2 个文件夹、13 条原记事；自动旋转已恢复，未创建 Git 提交。
