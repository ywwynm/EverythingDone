# 对话框从内部滑到外面松手会误关闭 2026-07-26

> 注意：发布任务只提取本文件的**第一个 `## ` 条目**作为应用内更新日志，因此全部内容必须写在同一节里，不能拆成多节。

## 对话框：从里面按下、滑到外面松手，不会再关掉

- 修复：在对话框内部按下手指（按在空白处、说明文字这类不响应点击的位置），滑动到对话框外面再松手，对话框会被当成"点了外部"而关闭。现在只要按下的位置在对话框内，无论手指最后滑到哪里松开，对话框都不会关闭。
- 全部对话框统一生效。点对话框外部关闭、返回键关闭、以及各对话框自身的按钮和滑动操作都和原来一样。
