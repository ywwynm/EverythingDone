# 附件重排 GIF 闪退修复

用户在移动包含逐一播放 GIF 的图片附件时遇到新闪退。诊断确认播放状态刷新复用了同一个已就绪的
`GifDrawable`，但在它仍在运行时再次调用 `startFromFirstFrame()`，触发 Glide 4.16.0 的非法状态
检查。

- 播放状态重复应用时仍更新循环次数和完成回调；
- 只有已停止的 GIF 才从首帧启动，运行中的 GIF 保留当前进度；
- 新增对应失败回归并完成全量单元测试。

本次不改变空间照片效果、模型、运行时组件或已有派生数据。
