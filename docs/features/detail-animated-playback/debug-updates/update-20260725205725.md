# 详情页动态内容播放 2026-07-25（二）

> 注意：发布任务只提取本文件的**第一个 `## ` 条目**作为应用内更新日志，因此全部内容必须写在同一节里，不能拆成多节。

## 逐一播放走得完了，切换不再闪，视频全屏可缩放

- **修复：逐一播放遇到静态图片就卡住。** 可播放的附件中间只要夹着一张普通图片，轮到它时队列就停在那儿，后面的动图再也轮不到。现在排队时会跳过当前播不了的项——普通图片直接跳过，视频与实况照片的动图预览还在生成的先跳过、生成好了再自动补上。
- **修复：逐一播放切换到下一个时画面闪一下。** 上一个刚停、下一个开播的瞬间会闪过一片空白，现在旧画面会一直留到新画面准备好才替换。
- **设置里的当前档位移到标题下一行。** 原先挤在标题右侧，中文下已经偏紧，其它语言的译文更长会挤成两段。
- **全屏预览里的视频现在可以缩放了。** 自动播放和长按播放期间都能双指放大、拖动查看，与实况照片一致；按住播放时用第二根手指缩放也不会打断播放。
