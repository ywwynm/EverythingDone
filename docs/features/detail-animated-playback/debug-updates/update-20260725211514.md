# 详情页动态内容播放 2026-07-25（三）

> 注意：发布任务只提取本文件的**第一个 `## ` 条目**作为应用内更新日志，因此全部内容必须写在同一节里，不能拆成多节。

## 两条自动播放设置的文案写清楚了

- 「在记事列表中自动播放作为封面的 GIF 与视频」→「**在记事列表中自动播放作为封面的动态内容（视频、动态照片、GIF）**」。原文案漏掉了动态照片，而它其实一直在这个开关的管辖范围内。
- 「在记事详情中自动播放动态内容」→「**在记事详情界面中自动播放动态内容（视频、动态照片、GIF）**」，把管辖的三类内容列清楚。
- 十三种语言的译文同步更新。
- 封面那一行的高度改为可撑开：文案变长后，德语、俄语、葡语在原先固定的行高里会被裁掉最后一行。
