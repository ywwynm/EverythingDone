# FableSol 视频导出：编码器说明写明落点与取舍；推导块版式调整

## 2026-07-28 编码器胶囊说明补齐三件事；结论行加粗并与推导过程分隔

**编码器胶囊下方的说明改为随选择变化。** 此前是一段固定文字，选「自动」时看不出这台机器上
究竟挑中了哪个编码器，也看不出是硬件编码还是软件编码。现在这段说明写三件事：

1. 该档位的特点。HEVC、AV1、H.264 各有独立描述，与 HDR 格式的说明形式一致：HEVC 同码率下
   画质明显优于 H.264 且承载全部 HDR 格式；AV1 压缩效率最高但硬件编码器普及度低、部分播放器
   与分享链路尚不支持；H.264 兼容性最好但画质较低且不承载 HDR。
2. 当前实际使用的编码器，并标明硬件编码或软件编码。该结论随输出格式与帧率变化，因此在三条轴
   任一变更后都会重新解析。
3. 灰色选项的含义。

**推导文字的版式调整。** 结论行（视频大小、帧率、输出格式、编码器等）改为加粗，并与其后的
推导过程之间空一行；推导过程与设备能力报告之间不再留额外间距，两者连成一段完整报告。
