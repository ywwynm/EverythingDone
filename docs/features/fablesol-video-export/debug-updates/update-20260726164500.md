# 音频附件对话框：进度条终于对齐了

## 进度条左缘对齐计时器

上一版里导出图标已经对齐，只有进度条纹丝不动——两者跑的是同一段对齐代码，差别在布局重力。

进度条原本是水平居中的，而这种情况下系统对左右外边距的处理是「先把控件居中，再把左右边距
之差当成一个偏移量」，所以设置左边距根本不等于「左缘落到这个位置」，实际效果被折算掉了一半
还带负号。导出图标是靠右对齐的，右边距被逐像素兑现，因此它一直是对的。

现在进度条改为靠左对齐，左缘按计时器数字的真实左缘落位。
