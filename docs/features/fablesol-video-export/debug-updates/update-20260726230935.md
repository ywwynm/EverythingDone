# 导出图标尺寸与编码设置文案

## 本次更新

- 录音 Dialog 的导出 FAB 保持 56dp touch ripple，padding 从 16dp 调为 17dp；
  音频附件 Dialog 的进度条右侧导出按钮保持 40dp touch ripple，padding 从 8dp 调为 9dp。
  两处 `video_frame_save` 可见尺寸统一由 24dp 缩小为 22dp，按钮位置、触摸范围和渐变着色
  均不变。
- FableSol 导出设置中的“码率模式”改为“编码模式”。
- 恒定质量模式的“体积随画面复杂度变化”改为“视频大小随画面复杂度变化”。
- 英语、简体中文、繁体中文（香港/台湾）、德语、西班牙语、法语、印地语、意大利语、
  日语、韩语、葡萄牙语和俄语共 13 套资源同步修改为相同语义。

## 验证

- 本轮未运行单元测试、截图测试或 adb。
- `:app:publishDebugUpdate` 构建并发布成功，阿里云 debug 更新码为 `202607261510`。
- 本地发布 APK、远端 `latest.json` 与重新下载的远端 APK SHA-256 一致：
  `499c59a302eba317885d288c68fa05b2d34462548fdeb59ffb0d0194bee5106e`。
