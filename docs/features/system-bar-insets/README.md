# System Bar Insets

Status: audit and planning for unifying runtime system-bar inset handling across
EverythingDone App Chrome surfaces.

## Documents

- `analysis.md` records the current code audit and candidate cleanup scope.
- `decisions.md` records durable scope and ownership decisions.
- `followups.md` records deferred inset cleanup candidates.
- `sessions.md` records implementation notes and verification status.

## Related Feature Records

- `docs/features/android-16-migration/` tracks the platform migration item that
  left bottom navigation-bar inset handling partially complete.
- `docs/features/home-contextual-toolbar/` records the recent home contextual
  toolbar status-bar overlay work.
- `docs/features/popup-picker-insets/` records popup and IME inset decisions.

## Related Global Records

- `CONTEXT.md` defines App Chrome as the surrounding interface outside Thing
  Backgrounds.
- `memory/*.md` remains the lightweight startup index.
