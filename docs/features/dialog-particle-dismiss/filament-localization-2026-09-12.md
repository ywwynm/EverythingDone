# 新录像中两条离群细缕的定位与诊断

更新 `202609120532` 后用户反馈细缕仍然存在，观感与修复前基本相同。本轮先确认新录像黄线圈出的两条窄曲线：面板上半部已粒子化、下半部仍完整时，左侧细缕向右下弯，右侧细缕向左下弯。它们位于粒子群内部，不能以主粒子群的正常外轮廓代替。

原始输入为 `b385642279edad91efde395d7e3a2ed7.mp4`（576×1280、30 fps、75 帧），标注截图为 `codex-clipboard-8e0dc383-1b52-4593-8009-04d366ef1cf2.png`。先配准截图与原始帧，记录两条细缕在连续帧的位置和变化，再以同一具体结构建立失败检查；完成根因定位前冻结正式模型，不沿用上一轮“已修复”的结论。

已定位到原录像第 37～38 帧（1.233～1.267 秒）。用户强调几乎所有关闭情况下都存在，本轮将其作为共同模型缺陷分析，不限定新录像种子；跨素材、方向与种子拆分静态表面、运动微片及颜色采样，确认窄曲线出自哪一层，再追踪材料来源。

12 组素材、方向和种子已分层复现。运动层仍存在窄曲线，排除原表面残留；七组独立消融中，关闭剥离力使目标细缕消失，而关闭引导场、局部时钟、速度下限或明暗变化不能消除。所有着色器替换均断言恰好命中一次，避免旧数值字符串失效造成假消融。

添加附件向下、种子 909602、进度 0.30 的可见细缕已追踪到 940 个材料样本：源位置是一条明显更宽的区域，来源法向的横向导数显示两侧朝同一曲线汇聚。该组剥离速度导数中位数约为 −0.0182/px；在当前短边和衰减时间下，足以在可见寿命内把源区域压成细线。上一轮随机剥离仍保留此压缩机制，固定窄框的对比度降低不足以证明修复。

局部压缩反馈候选根据剥离速度的对称导数计算压缩量，仅减轻会导致材料过度汇聚的剥离响应，原法向、表面交接、颜色、引导流场均保留。先在冻结基线旁进行独立消融，再进入共同模型。

已接入桌面与 Android 共同实现：建材时依据剥离场对称空间导数的最小特征值估计压缩，单独通过存储缓冲传入共同计算着色器，按衰减期内预计积累量柔和收减剥离。保留正常法向及主流场，没有删粒子、缩短寿命或降低透明度来隐藏细线。

回归固定发现问题时的 940 个材料 ID，并使用旧状态的主轴确定横向测量方向。该固定轴下源宽为 40.79 px，旧模型压至 9.56 px，最终模型为 26.32 px；原始材料数组逐字节一致。旧版明确失败、新版通过。先前候选表使用各候选自身主轴，数值不可与此固定轴回归混用。此检查只证明这个已定位结构不再过度压缩，不能替代多场景连续视觉检查。

整体保护检查发现初始反馈使钢铁侠完整动画区的低频结构误差增加 2.33%，超过本轮预先设置的 2% 保护范围。没有放宽断言：改为连续的二阶起效函数，让弱压缩区域尽量沿用原运动，强压缩才逐渐介入。最终候选固定来源宽度为 26.32 px，仍显著宽于旧版 9.56 px；结构误差变化收敛至 +1.53%。此项是为去除内部细缕作出的有限运动变化，不宣称华为全局相似度提升。

## 最终验证与发布

- 12 组素材、方向、种子完整画面前后对照已导出，包含添加附件向上／向下／左上／向右、调整颜色、语言、钢铁侠和灭霸，以及重复向下的不同种子。新增页面 `peel-compression.html` 提供原速、半速、逐帧和固定进度定位；原录像第 38 帧的准确定位图单列展示，不冒称复现未知的原录像种子。
- 14 项材料与 GLES 单测、18 个素材与 30 个方向、18 个屏幕内近中远输入通过。既有视频和新增对照共 194 个全部完成身份、编码和解码校验，没有重导历史 226 组标注。
- `9018f404` 和 `R5CW20BLNKL` 各完成 12 组独立建材及实际 GPU 对照。压缩输入数组一致；整体最大透明度 MAE 为 0.080568/255、合成 RGB MAE 为 0.057495/255，活跃粒子最大位置差约 0.008745 px。固定来源组在两台 GPU 上的宽度均为 26.32083 px，与桌面一致。
- 两台设备的真实近触点、远触点、返回关闭和动画层清理通过。三星保留三段真实关闭录屏并按原始时间戳逐帧核看；`9018f404` 的系统录屏写文件被拒绝，使用实际 GPU 输出和真实关闭日志验证，未改变设备权限。
- 阿里云更新 `202609120836` 已发布，发布包安装到两台设备后再次完成实际返回关闭，均回到主列表。远端、本地和设备安装包 SHA-256 均为 `15c1aa06dc8782f3d5cd73b32deff2ccf0179d4f891dc395f983b86cc9082c47`；模型身份为 `e7df515d097251d349c7838249b47783012847f58ed77ed0ffbf78083986f502`。

本轮根因证据、固定来源回归、跨端结果、真实关闭和发布身份汇总于 `tuning-0909-gpt6/analysis/recorded-filament-trace/final-acceptance.json`。这些证据确认所测输入中的过度压缩得到修正，不代表对所有未来随机输入的绝对保证，也不代表已经达到华为像素级一致。未创建 Git 提交，其他功能的修改保留。
