# 弹窗粒子消散统一规则调试版

## 本次调试更新

按用户要求，将已完成验证的统一规则版本重新发布到阿里云，供实际体验。本次发布未继续调整动画参数。

钢铁侠、灭霸、科比和应用内弹窗现共用粒子释放、运动与寿命规则，差异由尺寸、触点方向、源图材质和随机种子产生。桌面正式渲染已移除人物专用参数分支；Android 的 ParticleMicroflakeModel、ParticleMicroflakeRenderer 与预热路径读取同一份 rules.properties 和综合引导场。白底弹窗根据材质连续调节过渡，减轻粒子化边界过于清晰的问题。

此前已完成 10 项相关 JVM 测试、13 个场景和 130 组方向验证，并在 9018f404、R5CW20BLNKL 上检查语言、添加附件、渐变颜色弹窗及八方向触点、连续开关。全局规则冻结后通过五个全新素材和一个本轮未调参的真实长通知验证；所有素材使用同一份固定参数。综合引导场保留参考学习来源，局部侵蚀形态与华为仍有差异。

可在语言设置、添加附件或调整颜色弹窗中，通过取消、返回或点击弹窗外部体验消散效果。设备能力和系统动画设置仍沿用既有启用条件。本次将核对远端更新元数据、完整 APK 校验值及打包的共享模型资源；上述双设备验证为此前同一模型版本的结果。

## 发布验证回填

- 阿里云调试更新 **202609100451** 发布成功，Gradle 构建通过；远端元数据与本地一致，已嵌入本次发布说明。
- 完整远端 APK 下载校验通过，大小为 24,994,292 字节，SHA-256 为 `9535df7b0b899178afd96601392f52f64f76d904ba975b72069b8e96b84007a2`。
- 七份打包模型资源与共享源码清单一致，模型指纹仍为冻结验收版本。本次未重新操作设备；此前双设备证据单独保留。
- 发布日志与核对报告保存在本功能目录下 `tuning-0909-gpt6/analysis/publish-20260910125056.log` 和 `publish-20260910125056-checks.json`，不纳入 Git；本次未创建提交。
