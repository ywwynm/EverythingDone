# 对话框粒子动画：每次动画图案随机化

## 出现动画不再每次一样

此前粒子动画的全部"随机性"都来自确定性来源（逐粒子哈希与以像素位置为输入的噪声场），同一弹窗在相同位置、相同尺寸下，出现与消失动画的斑块图案、烟缕走向、凝实顺序每次完全相同。本次为每场动画引入随机种子：噪声域整体平移 + 逐粒子哈希扰动，使凝实顺序、斑块分布、烟缕形态每次全新，而静止层与粒子层共用同一种子，擦除衔接依旧严格对齐。出现与消失动画均已随机化，动画时长与整体观感不变。已在三星与 OPPO 真机上验证：同一弹窗连续两次动画图案明显不同，且无错位、无闪烁。
