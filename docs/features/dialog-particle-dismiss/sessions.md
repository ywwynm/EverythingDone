# dialog-particle-dismiss 会话记录

## 2026-09-12 按实际粒子分布修正内部窄流并发布

- 用户否定 `dispersion-0.7-edge`，要求孤立细缕不再出现，钢铁侠直接对比原始华为视频。冻结旧缺陷，匹配附件 270°／909602、90°／909602、0°／42 三个确切反例；单项消融排除显示层、只改引导场、惯性和局部时间等解释。
- 当前分布中的剥离汇聚是共同原因。保留每片原速度，添加单侧压力梯度修正，避免内部压成窄线，同时保留真实外边缘的自由展开。共同旋流低频微调直接对原片校准；留出帧结构误差由 3.634/255 降为 3.576/255，不据此宣称细缕消失或像素一致。
- 三个确定反例中的交叉细流、独立小弧、斜线与分叉不再形成原先的独立窄线；共 12 组完整过程和代表连续相位复核，保留钢铁侠横弧与下垂分支。完整原片对照仍有密度和释放时序差异；灭霸、科比及新增向上参考的既有差异也如实保留。
- 桌面和 Android 接入相同计算着色器。重播回归发现压力状态未清空，修复后 18 素材与 30 方向通过；重算 128 组源帧逐帧相同后复用编码，126 个标准视频加 24 个专项视频共 150 个完成当前身份及解码检查。14 项 JVM、9018f404 九组独立 GPU 和实际近远／返回关闭通过。
- 阿里云更新 `202609121309` 已安装 9018f404，远端、本地及已安装 APK 散列一致，12 项共享资源一致。最终包钢铁侠、附件两组及实际近远／返回关闭复测通过；一次图片拉取失败已恢复，再重新执行对照。R5CW20BLNKL 当前离线，待补测。未提交 Git，其他功能修改保留。详见[本轮诊断与证据](edge-flow-support-2026-09-12.md)。

## 2026-09-12 定位内部细缕的材料来源并修正过度汇聚

- 用户强调真机几乎每次关闭都有一条或几条突兀内部细缕。配准新录像第 37～38 帧，准确区分两条内部窄曲线与正常外轮廓。跨 12 组分层复现、七组独立消融，定位到共同剥离力造成过度汇聚；前次扩大随机强弱并未消除该机制。
- 固定追踪同一组 940 个材料 ID，建立旧版明确失败的回归。根据剥离场的对称空间导数估计局部压缩，在共同着色器中连续减轻强汇聚；源带横宽 40.79 px、旧状态 9.56 px、最终 26.32 px。材料身份、原法向、颜色和寿命不变，没有通过删除或隐藏粒子处理。
- 桌面与 Android 独立生成压缩输入，两台指定设备各 12 组实际 GPU 对照及真实近远／返回关闭通过；三星原生录屏逐帧复核，另一台系统录屏输出受限，使用 GPU 与实际关闭证据。14 项单测、18 素材与 30 方向、18 距离输入及 194 视频通过。
- 正常钢铁侠卷边作整体保护检查：拒绝结构误差增加 2.33% 的初始候选，采用弱压缩二阶起效后约增加 1.53%，原始 RGB 误差小幅下降；不宣称对华为整体相似度提升或达到像素级一致。新审阅页提供 12 组原速／半速完整对照及原录像准确定位图。
- 阿里云更新 `202609120836` 已发布并安装两台指定设备，发布包身份、实际返回关闭和动画层清理复测通过。未创建 Git 提交，其他功能修改保留。详见[定位、修复与最终证据](filament-localization-2026-09-12.md)。

## 2026-09-12 修复释放初段细缕及跨设备颗粒差异并发布

- 三段新真机录像证明此前的后段寿命修复不完整。提取真实面板和背景，以相近释放布局复现斜线、弯月细缕；原录像种子未知，不声称逐粒子重放。独立消融定位到同步剥离形成的窄密度峰，改为独立的 0.10～1.90 响应，平均力度不变，保留原卷曲和触点响应。
- 双设备局部光度检查发现基于 `sin` 的网格随机函数存在 GPU 差异，改用共同整数散列。没有放宽旧阈值，并补充网格、颜色和透明度检查；钢铁侠第 40 帧的跨端 RGB 误差从约 1.90/255 降到 0.009～0.012/255。
- 新旧细缕反例、18 场景与 30 方向、234 组既有状态、18 个屏幕内距离输入、14 项 JVM 测试通过。两台指定设备各九组独立材料和 GPU 对照，以及真实弹窗近远、返回关闭和层清理通过。170 视频已重新导出，包含三组真实弹窗素材的六个修复前后视频；完整原速画面和代表方向已复核。
- 继续细化共同释放与速度场。华为完整动画留出相位的结构误差下降约 1.91%，原始 RGB 误差上升约 0.25%；颗粒位置、局部覆盖和退出时序仍有差距，不能把跨端一致当成对华为近像素一致。
- 阿里云更新 `202609120532` 已发布并安装两台设备复测；远端、本地和设备安装包 SHA-256 一致，最终验证通过。未创建 Git 提交，其他功能的未提交修改保留。详见[诊断与最终证据](device-filament-origins-2026-09-12.md)。

## 2026-09-12 修复孤立尘缕并发布共同模型

- 固定用户截图的 0.667 相位复现，追踪 570 个旧材料样本并逐项消融，定位寿命延长及寿命／运动随机量耦合。先建立失败回归，再改为独立输运随机量和按出生时刻连续变化的寿命；未按截图区域删除材料。
- 小幅细化共同释放与速度场，粒径采用温和对数正态分布，保留卷边、原色和近远响应，弃用增强调亮的实验。标记区域亮度覆盖从 38811 降为 6734，参考 5957；完整留出帧 RGB 误差下降约 2.4%，低频结构误差上升约 1.4%，仍未达到近像素一致。
- 164 视频、18 场景与 30 方向、234 既有状态、18 真实距离输入及 14 项 JVM 测试通过。两台设备共 12 组独立材料与 GPU 对照，补充 0.667 相位回归，最大可见位置差约 0.00380 像素；实际弹窗近远、返回和层清理通过。
- 阿里云更新 `202609120157` 已安装两台设备并复测；三星曾遇电脑端 ADB 服务断连，恢复后重跑通过。远端、本地及安装包身份一致。浏览器复核完整原速／半速和颜色八方向，修正精确跳帧偶尔落到上一帧的问题。没有创建 Git 提交。详见[诊断与最终证据](filament-continuity-2026-09-12.md)。

## 2026-09-12 逐帧校准共同模型并发布

- 用户休息前要求按完整钢铁侠对照逐帧优化形状、位置与明暗，直接生成像素差，完成 Android 迁移和阿里云发布。冻结此前发布模型，用 61 相位、多个空间尺度及颗粒对比度定位误差，排除单纯增强反光、直接反写纹理退出代理和整体替换观测场的候选。
- 在共同归一化释放及输运场上增加有限宽尺度连续校准，联合调整剥离、寿命、细化和原色明暗；没有新增人物名称、内容区域或逐帧轮廓分支。同步桌面与 Android，修正量化前的非法负释放时刻，保留跨端验证门槛。
- 完整动画区留出相位的结构误差约下降 19.3%，原始 RGB MAE 从 10.436 降至 10.282；局部覆盖、时序与颗粒位置仍有差异，未达到近像素级一致。浏览器复核完整画面、差异视频及自有白底八方向；末次六组颗粒对比度消融没有整体收益，未纳入。
- 164 视频、18 素材与 30 方向、234 组既有轨迹、18 个近中远输入、14 项 JVM 测试通过；两台指定设备共 12 组独立建材与 GPU 对照，最大可见位置差约 0.003614 像素，真实近远／返回和层清理通过。
- 阿里云更新 `202609112015` 已发布并安装两台设备再次复测；远端、本地和安装包 SHA-256 一致。未创建 Git 提交，其他功能改动保留。详见[分析及最终证据](frame-difference-2026-09-12.md)。

## 2026-09-12 修正前沿衔接与角部收束并发布

- 根据钢铁侠三个整帧截图复现 0.404、0.562、0.574 附近：定位前沿接边偏迟、自由边界外推过大、中心过早破碎及拖尾过宽。否决过强时序候选，保留内部卷动，对角部入口、剥离力、原表面交接作有限调整；所有素材、方向和触点共用。
- 桌面与 Android 同步。修正 Android 插值后精度转换，避免新增梯度放大跨端误差；新增 108 字节的边界回归夹具，保持原检查阈值。
- 162 个视频、18 素材与 30 方向、234 组既有轨迹、18 个屏幕内近远输入、14 项 JVM 测试通过；两台设备 12 组独立建材与 GPU 对照最大可见位置差约 0.0029 像素。浏览器核看完整钢铁侠与关键相位，并补看灭霸、颜色八方向。
- 阿里云更新 `202609111846` 已发布；两台指定设备安装后各复核近远／返回关闭和层清理。设备日志滚动引起一次验证脚本误报，改为识别本次新增记录后通过复测。远端、本地、安装包身份一致。
- 本轮收束、纹理保留与拖尾宽度得到改善；数值不代表逐帧复刻。未创建 Git 提交，其他功能未提交修改保留。详见[分析及最终证据](front-coherence-2026-09-12.md)。

## 2026-09-12 撤回过强亮线，改善柔和交接并发布

- 用户继续指出六处钢铁侠差异，并否定中途候选的突兀白线，要求先看完整画面。取消窄窗统一加色，保留源色和已认可共同流动，细化材料采样与覆盖，减轻迎风角落过度展开，按释放梯度改善开放外缘的分散、颗粒份额和表面渐隐。
- 162 个原速、半速及组合视频已导出并核验，新增完整画面和左上角三栏对照。18 素材、30 方向、234 组既有状态、18 组近远输入及 13 项 JVM 测试通过；历史 226 组标注视频未重导。
- 三星恢复授权，两台指定设备共 12 组独立建材及 GPU 对照通过，并各完成真实近远点击、返回关闭和动画层清理。阿里云更新 `202609111620` 已发布并安装两台设备，发布后另复测返回关闭；远端、本地、安装包身份一致。
- 亮线候选仅作为被否定实验保留；当前仍有局部密度和参考时序差异，不宣称完整复刻。没有新建 Git 提交，其他功能的未提交修改保留。详见[分析、取舍和最终证据](surface-transfer-2026-09-11.md)。

## 2026-09-11 按用户认可提交当前粒子动画基线

- 用户反馈当前效果可以，明确要求提交。将上次提交以来的随机起始区域、停滞修复、长宽比例适配、多参考共同形态、弧边展开和实际触点远近调整合并为一次功能提交，包含桌面与 Android 实现、必要共享资源、回归脚本及功能文档。
- 本次只归档已验证的实现，不继续调整模型。提交前核对当前模型和视频身份与最新验收记录一致；沿用此前完成的 18 场景、234 组状态、13 项 JVM 测试及 9018f404 独立渲染和真实关闭结果，不重复导出或操作设备。
- 视频、截图、APK 和历史大产物保持本地忽略；必要模型数据及小型跨端测试夹具正常入库。其他功能与跨功能文件的未提交改动保留。提交标题和正文使用中英文，描述行为变化和验证，不使用内部迭代代号。

## 2026-09-11 加强弧边展开与可见的近远运动并发布

- 用户再次指出横向弧边曲率和流动不足、近中远仍难辨认。冻结调整前代码、资源与渲染帧，隔离速度响应、顺风下限和局部输运后，采用粒子解除后的短时法向展开；幅度含小幅随机变化，保留共同流场及下方拖尾。
- 距离改为同时换算各材料相对出生位置的实际位移和实际速度，幅度有界且默认值为 1；释放场、出生、寿命、随机布局与内容颜色不变，不向单个触点汇聚。桌面与 Android 共用计算着色器。
- 158 个原速／半速视频全部平铺导出并校验，新增弧边放大三栏和三素材双方向近中远单行对照。审阅页支持同进度、同位置切换近、中、远，实际检查弧边 0.55／0.63 及近远连续观感。
- 18 场景、30 方向、234 组状态及 13 项 JVM 测试通过；9018f404 的 13 组独立建材与 GPU 对照最大可见位置差约 0.159 像素。真实近远触摸、返回左上、连续三次随机关闭与层清理通过。三星 USB 调试仍未授权，不计作本轮已测。
- 阿里云更新 `202609110713` 已发布，远端、本地及设备安装包 SHA-256 均为 `fb36f0beabc352b39b54cdfa9f331ade9f56c12976a551b06febbff9f422addf`；发布包又完成实际返回关闭复测并回到主列表。模型、视频和安装包证据汇总于 `analysis/rim-flow/acceptance.json`。
- 向上参考早期释放、灭霸局部细缕等仍有差异；数值回归不替代审美验收。未创建 Git 提交，其他功能改动保留。详见[本轮诊断与验证](rim-flow-2026-09-11.md)。

## 2026-09-11 恢复弧边与拖尾分离，增强近远差异并发布

- 用户截图指向一条弧边随后呈现横向轮廓与下方拖尾，近中远也仍不明显。冻结本轮前的代码、资源和八素材帧；消融对照排除单纯风速／下限补偿，定位到随机坐标和释放时序变换错开进入卷边的源材料。
- 共同随机规则按局部起点独立程度调节变形幅度，保留镜像和大布局变化；共同速度表、释放基底和可信度不变。触点按该方向可用屏幕背景归一化，用有界增益改变速度与位移，不形成强点汇聚。Python、Android 同步。
- 新增钢铁侠整体／局部三栏对照，实际播放并定位分离过程；144 个双速度视频全部平铺并完成校验。234 组状态、18 场景、30 方向和 13 项 JVM 测试通过，没有重导历史 226 组标注视频。三素材六组实际近远输入的中位速度／位移比约 1.47～1.66。
- 9018f404 完成 13 组独立建材及 GPU 对照，最大中段位置差约 0.0813 像素；真实语言弹窗近远、返回左上和层清理通过。发布后又复测返回关闭与三次相同弹窗、相同方向的随机关闭，回到主列表。R5CW20BLNKL 仍未授权，不计作本轮已测。
- 阿里云更新 `202609110541` 已发布，远端 APK、本地发布包和安装包 SHA-256 均为 `0b742b3311ae4a1e2d538e70f798b9cb4f172101a250565e7d19c958578d5677`。当前资源、视频与设备证据统一于 `analysis/curve-split/acceptance.json`。
- 弧边亮度／时刻、向上参考早期释放、灭霸细缕仍有差异，未宣称逐帧一致。未创建 Git 提交，其他功能改动保留。详见[本轮诊断与验证](curve-split-2026-09-11.md)。

## 2026-09-11 保留原参考模型并扩展共同形态，本地同步 Android

- 用户纠正新增向上钢铁侠应当补充旧效果，近远差异须适度，演示触点必须可在手机屏幕内复现。过强目标汇聚候选完整归档且未发布；重新以加入新参考前的模型为基线。
- 联合分析初版钢铁侠、新增向上钢铁侠、灭霸、科比。保留原共同速度表，以方向和矩形几何、一次动画的随机输入组合原释放场与多个局部起点；起点数量、位置、尺度和延迟可变。去掉位置差除以剩余时间的强汇聚，改为有界速度增量及温和初始偏转。
- 18 个距离输入全部位于源素材实际手机屏幕内、控件外，近中远的速度和位移逐档递增，远近差约 7%～28%。三个旧参考输入的同身份粒子中位位置变化小于短边 1%。参考触点无法确认，明确区分观测、建模假设与筛选过的示例种子。
- 原有 128 视频之外新增 14 个共同形态示例，四组方向与种子原样用于钢铁侠、附件、颜色。入口 `videos/flow-family.html`，旧重点链接转向当前页面；全部视频仍平铺。参考释放时序和部分细缕密度仍有差异，不宣称精确复刻。
- 142 视频最终编码／解码及输入身份检查通过，画廊实际播放与逐帧定位通过；本地 APK 及 9018f404 安装包 SHA-256 一致。当前结果统一记录于 `analysis/flow-family/acceptance.json`。
- Android 随机输入对照发现硬件纹理过滤差异；固定建材排除 CPU 错配后改用共享的显式三线性插值，附件反例最大位置差从 1.015 降至 0.0204 像素，未放宽门槛。
- 234 组状态、18 场景渲染性质、30 组方向、12 个跨语言单测及 9018f404 的 13 组独立建材／GPU 对照通过。该设备实际语言弹窗近／远背景关闭、返回左上、层清理通过并回到主列表；三星仍未授权，保留待补项。
- 本轮保留为本地候选，未发布、未提交，未处理其他功能工作区改动。详见[联合分析与验证](flow-family-2026-09-11.md)。

## 2026-09-11 补充卷动形变、长宽尺度和触点距离并发布

- 用户指出补充运动后的边界仍机械平移、行程保守、长弹窗斜向前沿出现尖峰，并要求颜色八方向及三素材双方向三距离。补充调研 13 个论文／官方文档来源，记录物理启发与交互设计映射的区别。
- 保留共同主流与材质，在延续区域增加随主流迁移的相干旋度；限制局部流场的长轴尺度，以平滑重叠区域覆盖高／宽面板。触点保留边缘外距离，以有界函数影响输运；返回键维持左上、中距离。没有按素材名称加入参数分支。
- 233 组完整状态输入复查后，顺风行程中位数比值约 1.579；没有持续低速窗口或主方向倒退步。15 个代表输入中，186 个此前接近刚体运动的边缘群体扣除平移、旋转后，形变残差比值中位数约 4.231。指标用于回归，不能代替视觉审美验收。
- 118 个正式双速度视频完成渲染、编码与解码校验，新增颜色八方向及钢铁侠／附件／颜色的六格距离对照。完整既有标注视频不重导。重点入口 `videos/flow-shaping.html`；桌面预览支持点击真实位置和独立调节距离。
- 12 个模型单测、17 素材渲染性质与 30 组方向检查通过。双设备 14 组独立建材／GPU 对照通过，包含远距离输入；两台实际语言弹窗近远背景点击、返回方向和动画层清理通过。9018f404 横屏上第一次远点坐标落入状态栏，修正测试坐标为 UI 树提供的应用区域后验证通过。
- 已发布 `202609101930`，两台设备安装并核对同一 APK，返回关闭后均回到主列表。远端元数据、完整 APK 字节及九份共享资源均一致；APK SHA-256 为 `a5e6bae6d6e04ff99eb870f6a39df8f964536fc2133765221d11a452dc00ea01`。
- 代码保持未提交，其他功能的工作区改动保留。详见 [调研与实现](flow-shaping-2026-09-11.md)、[发布日志](debug-updates/update-20260911033029.md)。

## 2026-09-11 修复无观测区域停滞，完成桌面、双设备与发布

- 用户否决首个“微小速度补偿”候选，指出不应把减少视频重导理解为缩小分析范围。重新利用全部 226 组标注、853 条长线和 7 个画廊实际输入做 GPU 状态分析，并增加四个未用于本轮调参的输入；未重导整套标注视频。
- 根因是共同运动表的静态背景／无有效观测区域为零速度，材料经过后停滞。延拓邻域流动并平滑接缝，保留可靠运动采样、释放模型、粒子模型和随机起始规则。九组重点慢放检查了原边角、后段曲线和新轮廓；首个候选视频作为失败证据保留。
- 正式 110 视频按原双速度、八方向、六种子同屏设置重新导出，全部校验通过；重点对照页为 `flow-continuation.html`。桌面画廊已刷新，避免继续观看旧缓存。
- Android 共用新场，后台准备与 EGL 初始化重叠，固定资源缓存，随机时钟使用小表初值及牛顿修正；撤回法线嵌套并行。三素材双设备独立建材和 GPU 对照、两台各三次实际关闭、15 个相关 JVM 测试通过。启动采样仅有小幅改善，不夸大性能收益。
- 已发布调试更新 `202609101619`。远端元数据、APK 字节、八份模型资源一致；发布包 SHA-256 为 `162c306372b1c42dbe0badfaa2b378ef8b4fd206bd407bbece4b28a75cabd495`。两台指定设备安装同一发布包，真实语言弹窗返回关闭、粒子／背景层清理通过，均回到主列表。
- 相关改动保持未提交；未处理其他功能的未提交工作。详见[修复记录](stalled-motion-fix-2026-09-10.md)、[发布日志](debug-updates/update-20260911001932.md)。

## 2026-09-10 扩大固定边缘标注，覆盖随机种子与八方向

- 用户补充添加附件左上边缘和钢铁侠素材内屏幕左侧。保留原示例并补齐标线，另对七个主素材、四个种子和八方向的 224 个组合逐例扫描、渲染，共 226 组编号对照视频。
- 标注同时考虑成熟低速粒子和短期固定的粒群轮廓，红线定位原控件边缘，黄线定位其他外缘，青线保留人工补充。该筛选用于确认症状，不把近似固定轮廓直接认定为硬裁剪或根因。
- 原画与标注共用实际渲染帧；每组提供视频、逐帧、三档播放速度、编号局部放大及素材／种子／方向筛选。可在浏览器本地记录观察并导出统一反馈；不会自动发送内容。
- 视频及扫描结果受忽略规则管理，全部视频仍平铺在原目录，正常 110 个模型视频保留。226 视频解码和标线范围通过，HTTP 页面及范围读取通过；正式模型及渲染输入散列未变。页面实际检查覆盖补充标线、颜色弹窗其他外缘、组合筛选和放大定位。
- 等用户统一确认后再进入修复。当前未修改 Android、未操作设备、未发布、未提交。详见[扩大复核记录](stalled-edges-2026-09-10.md)。

## 2026-09-10 按要求先标注边缘停滞，等待位置确认

- 用户报告粒子在原控件边缘或后段其他边界聚集、停止、沿固定曲线淡出，要求先画线确认。保留正式模型和已发布包，当前不进入修复阶段。
- 对钢铁侠、添加附件各六个固定种子读取真实积分位置，定位已释放仍可见的低速材料；区分该证据与整条可见边缘的视觉停滞，不宣称显式裁剪是根因。
- 制作单个并排标注视频：钢铁侠原下缘／圆角用红线，添加附件后段斜弯边／小尖角用黄线；每组含原速、半速及尾段放大。视频继续平铺，诊断清单与 110 个既有模型视频分开登记。
- 等用户确认标注是否覆盖所指现象，再建立针对性修复与回归判据；本轮未操作设备、未发布、未提交。详见[标注依据](stalled-edges-2026-09-10.md)。

## 2026-09-10 先提交认可基线，再随机改变粒子化起始区域

- 按要求先提交认可模型，提交为 `fa8de16a`，标题同时使用中英文，不使用内部迭代代号；保留无损对照帧。新增随机化改动暂未提交，未处理其他功能的未提交内容。
- 用户纠正局部形变仍显相似，要求直接改变起始区域。本轮改为区域移位、镜像、轻微变形与平滑错时，释放和速度同步转换；保留既有粒子与运动规则、内容色份额和返回左上。
- 桌面可每轮生成新效果或固定种子。七个主素材各有同方向六种子同屏的 1 倍／0.5 倍视频，连同旧类型共 110 个成片，全部平铺并核验。
- 256 种子变换、17 素材、234 组实际轨迹、14 项单元测试及双设备 34 组独立建材和 GPU 对照通过。此前十个留出输入作为回归素材，没有重复计为新素材。
- 两台真实弹窗、八方向、连续开关通过；额外每台六次同一添加附件弹窗返回关闭，尺寸与方向相同、实例种子和重建释放区域不同。三星有真实系统录屏，另一台保留阶段截图。
- 调试更新 **202609101113** 已发布、远端身份核对通过，两台安装发布包复测后回到主列表。首次发布因日志绝对路径被重复拼接失败，改相对路径后通过。详见[随机起始区域记录](random-variation-2026-09-10.md)。

## 2026-09-10 沿认可控制组完成共同模型与双端发布

- 用户明确认可观测运动控制组，要求沿其形态优化并推广到其他消逝位置。本轮冻结该结果，以共同释放场和速度场保留局部释放、输运及亮弧关系，不再退回此前周期流束模型。
- 两个场共用矩形方向变换，资源共 804 KiB；人物、照片和自有弹窗没有专用运行配置。去掉额外整片分离和周期流管，细化面板色微片，保留内容色配额与返回左上。桌面和 Android 已同步。
- 冻结后新增灰度照片、横向浅暖弹窗，未按新素材回调参数。17 场景、170 组方向的 287,111,333 对可见中心、34 组双设备独立建材与 GPU 对照、12 个相关单元测试和 96 视频检查通过。钢铁侠关键进度左、上中段约 89% 未释放。
- 两台真实语言、颜色、添加附件弹窗，各八方向和各 10 次连续关闭通过。三星录屏导出 4 个双速度视频，OPD2515 保留阶段截图。调试接收器漏列两个新输入导致首次数量检查失败，补齐后通过，未因此修改生产模型。
- 发布调试更新 **202609100929**；远端完整 APK、元数据与八份资源身份一致，两台安装发布包后复测返回方向和清理，均回到主列表。保留系统倍率、语言、深色跟随和用户数据。
- 对比页增加“华为／认可控制组／共同模型”。共同模型的局部侵蚀时序仍与参考有差异，输入法恢复可能遮挡尾段；数值通过不作为完全复刻证明。详见[共同模型记录](transport-model-2026-09-10.md)。未提交 Git，未处理其他功能的未提交内容。

## 2026-09-10 纠正粒群轮廓解释与提前侵蚀范围

- 根据用户红线和 `t=0.43` 的完整区域反馈，撤回整片翻折、强涡及周边提前释放的试验。正式桌面渲染与共享规则恢复到上一发布版，保留失败代码和证据用于追溯。
- 完成两张截图的原片时刻配准、红线双向光流跟踪，以及三个参考的源纹理保留测量。旧边界只覆盖后期边界的一部分，不能将两条红线当成同一材料整体翻转。
- 增加钢铁侠左、上完整区域的实际出生时刻检查；固定释放源的输运控制组仍未充分重现亮边，不视为完成优化，不恢复人物专用运行配置。
- 本轮没有更改 Android、操作设备、更新画廊成品、发布或提交。详见[实测与否决记录](edge-roll-2026-09-10.md)。

## 2026-09-10 共用卷曲流束、内容色份额与返回左上

- 按最新四项反馈增强连续卷曲、短时汇聚成缕及末段散开；面板主色识别驱动实际内容副片分配，最多每格两片，总材料量仍限制为 16 万。桌面与 Android 共用规则，没有人物专用选择。
- 返回键固定左上 45°，清除旧抬手位置影响，保持 ComponentDialog 取消分发；按钮和外部触点继续按真实位置决定方向。
- 七个开发场景之外复核六个既有回归输入，新增咖啡照片和有色面板两个未调参输入。因语言开发样本的低透明度跨端量化修正重新冻结，保留初次冻结证据，没有按新输入回调参数。
- 12 项 JVM／着色器测试、15 场景与配置独立性、150 组方向连续轨迹、双设备 30 组独立建材和 GPU 对照通过。86 个双速度及对照视频约 426.35 MiB，全部平铺；Qt、编码解码及 HTTP 区间服务通过。
- 两台真实语言／附件／渐变颜色关闭、各八方向和各 10 次连续开关通过；紧接标题触摸后的返回仍为左上，无粒子层或暗层残留。修正验证脚本以新完成记录判断结束，避免环形日志裁剪引起误报，未因此改动应用逻辑。
- 发布 **202609100530**，远端元数据、完整 APK 与七份模型资源核对通过；两台安装发布包后复测返回方向和清理，安装字节身份一致。均返回主列表，保留语言、深色跟随及系统倍率。
- 真实录屏／阶段截图与离屏证据分开保存；保留华为局部形态差异和输入法遮挡尾段的限制。详见[本轮实现与验证](streams-content-2026-09-10.md)。未提交 Git，未处理其他功能的未提交内容。

## 2026-09-10 按用户要求发布统一规则体验版

- 使用功能目录下的独立中文发布说明，重新发布阿里云调试更新 **202609100451**，本次未调整动画代码或参数。
- 构建通过；远端元数据、发布说明、完整 APK 的大小与 SHA-256 校验通过，七份打包模型资源与冻结验收版本一致。
- 保留此前双设备验证结果，本次未重新安装或操作设备。发布说明及核对记录见 [本次发布日志](debug-updates/update-20260910125056.md)。未创建 Git 提交。

## 2026-09-10 桌面与 Android 统一规则及留出验收

- 根据用户要求，移除桌面人物专用释放／流场／寿命分支；两端共用规则文件、综合引导场和确定性随机算法。引导场保留既有参考学习来源，所有素材只使用一份参数。
- 七个开发素材确定全局规则后冻结指纹，再引入五个全新材质／尺寸输入及一个本轮未调参的真实长通知。名称／损坏专用配置独立性、颜色通道置换、13 场景起止与重播检查通过；留出验证后未追加场景参数。
- 10 项 JVM 测试通过；130 组方向、226,198,912 对相邻可见材料位置无主向倒退。双设备 26 组实际 PNG 独立建材与 GPU 对照通过，不再只依赖桌面预生成数组。半透明输入解码差异单独报告。
- 桌面 78 个双速度与对照视频约 459.04 MiB，全部平铺；Qt 预览、编码解码与 HTTP 区间服务通过。三星两段真实系统录屏另导出四个双速度版本，阶段表按 VFR 实际时间戳取样。
- 两台设备完成语言／附件／渐变颜色真实关闭、各八方向触点及各 10 次连续开关，无粒子层或暗层残留。过早返回的测试改为等待输入焦点稳定，未修改应用返回逻辑。
- 发布 **202609100418** 并安装到 `9018f404`、`R5CW20BLNKL`，实际 APK 与发布包散列一致，安装后消散复测通过，均回到主列表。语言、深色跟随和系统动画倍率未修改。
- 保留华为局部形态差异、通知背景重建、输入法恢复遮挡尾段和显示帧率证据限制；详见[统一模型验证](unified-model-2026-09-10.md)。本轮未提交 Git，未处理其他功能的工作区改动。

## 2026-09-10 本轮成果提交与目录迁移

- 用户授权提交本轮工作。将原 `tmp/particle-dismiss-tuning/tuning-0909-gpt6/` 整体迁入本目录，迁移前的 1718 个文件、约 26.38 GiB 均保留；移动后逐项核对文件身份、大小与修改时间一致。
- 本轮桌面工具入库 60 个 Python 脚本、启动脚本、README、14 份场景／拟合参数及目录规则；素材、缓存、视频、研究正文、诊断输出、实机证据和版本归档仅本地保留。旧 tmp 中的 68 个已跟踪实验文件停止跟踪，磁盘原件保留。
- 其他会话的 19 份未提交报告、重建目录与 29 份诊断文本由忽略规则排除；四份公共文档整理前的完整内容保存在本地 `local-history/20260910-before-commit/`。本轮研究、方法边界与后续统一参数验证待办保留在提交中。
- 修复迁移后的仓库定位和文档链接。审阅服务从新目录继续监听 `127.0.0.1:13070`，页面返回 200，视频区间请求返回 206；桌面离屏预览通过，60 个 Python 文件语法检查通过。
- 9 项 Android 单元测试通过，无失败或跳过；既有双设备证据汇总及 10 个实机视频解码复核通过，本次未重新操作设备。暂存字节重算桌面指纹仍为 `b52110f3260b59fbb8e1ae4026d1b87bc19772436bd36e79bd865e7483029819`，六份 Android 运行资源散列与清单一致。
- 提交按 Android 运行实现与测试、桌面工具及文档归档两组组织；相关发布记录随实现提交，不单独创建发布回填提交。空间照片与通用工具配置的其他工作区改动不纳入。

## 2026-09-10 r33：桌面收敛、Android 移植与双设备发布验证

- 用户授权自行完成桌面判断后移植消失路径，测试 `9018f404`、`R5CW20BLNKL`。比较较强收束的 r32 后选择更克制的 r33，保留钢铁侠改善，细化灭霸／科比局部聚散及白色微片明暗；共用综合流场用于自有弹窗。
- 54 个桌面视频约 418.62 MiB，模型、70 组方向轨迹与视频检查通过，r33 完整归档。局部细束仍有差异，数值颜色误差并未普遍降低，保留本轮视觉取舍说明。
- Android 新增通用释放模型和 GLES 3.1 微片渲染，固定 240 Hz 速度积分、最多 60 Hz 提交，按密度控制粒径，优化无效绘制与位图上传；修正暗层系统倍率。删除旧消失分支，四段出现着色器保持原散列。
- 9 个单元测试通过；两台七场景固定输入的可见中心与桌面最大差异小于 0.13 个逻辑像素；84 张图验证上传优化逐像素一致。真实语言／颜色／附件、各八方向、各 10 次快速开关、输入法取消及三星 0／0.5／1／2 倍系统倍率检查通过。
- 发布调试更新 **202609091858**，两台均安装发布包并完成关闭复测，设备 APK 与远端元数据散列一致。临时设置恢复、回到主列表；未清理用户数据或提交 Git。三星录屏集中保存双速度 10 视频；OPD2515 录屏受系统限制，使用真实阶段截图。详见[执行与证据记录](tuning-2026-09-10-desktop-android.md)。

## 2026-09-10 r31：白底弹窗过渡与参考局部形态

- 用户认可钢铁侠、认为科比可继续优化，指出灭霸仍有差距，更强调自有白底弹窗粒子化边界清晰。核实七场景共用模拟器，自有弹窗使用综合流场；照片有各自校准参数。
- 对照释放错时、初速、白色提亮和形状移植；采用整个控件统一的 60～140 ms 局部交接，拒绝按单个文字／颜色另设出生时间和直接移植大范围照片释放形状。
- 灭霸、科比增加少量连续区域的时序、流场与寿命校准；更新综合流场供自有弹窗使用。保留连续积分及主向前进约束。
- 七场景确定性、70 组方向的 150,134,943 对材料中心、54 视频检查通过；浏览器播放、逐帧、筛选与三组八方向图像复核完成。
- 54 视频约 423.74 MiB，新增四个自有弹窗的新旧版本比较。仍未达到参考的全部卷曲与细条带形态；未迁移 Android、使用 ADB、提交或发布。详见[r31 记录](tuning-2026-09-10-gpt6-r31.md)。

## 2026-09-10 r30：保留 r29 轨迹，改善彩色材料消逝

- 用户明确认可 r29 钢铁侠的改善。本轮归档 r29，比较释放阶段速度校准、粒径和材质方案；未采用形态收益不稳定的速度拟合及更密粒子方案。
- 以源色色度控制短时面积与寿命，减轻移动材质的白色混入，主要改善科比黄色／蓝色粒群；七场景共 3,478,594 个完整模拟状态与 r29 逐值一致。
- 七场景交接与确定性、70 组方向的材料中心轨迹、46 视频版本／编码／解码检查通过；浏览器双速度播放、逐帧、筛选与八方向图像复核完成。
- 46 视频约 292.47 MiB，三栏为华为／r29／r30。科比中段卷束及尾部位置仍有差距，未宣称视觉达标；未修改 Android、使用 ADB、提交或发布。详见[r30 记录](tuning-2026-09-09-gpt6-r30.md)。

## 2026-09-09 r29：在用户认可的 r28 上继续收敛

- 归档 r28 的代码、参数和三主序列无损渲染帧。比较随机分离、旋度、法向输运、寿命、粒径、姿态与汇聚试验，排除变厚、规则条纹等退步方案。
- r29 削弱高度顺风的分离冲量与过强转向，延后独立转动，增强尾粒细化；七场景采用共同参数，保留 r28 的释放时序和连续运动。
- 七场景像素交接与确定性、70 组轨迹检查、46 视频版本／编码／解码检查通过；已核对浏览器双速度播放、筛选、逐帧和八方向图像。
- 更新 46 视频，约 292.17 MiB；三栏对比为华为／r28／r29。科比中段黄色卷束仍不足，没有宣称视觉达标。详见[r29 记录](tuning-2026-09-09-gpt6-r29.md)。

## 2026-09-09 r28：修正粒群反弹并优化整体效果

- 复现钢铁侠左上粒群约 8 px 回程，确认由渲染额外叠加的衰减振荡平移造成；改为统一速度积分，补充真实顶点轨迹回归。
- 继续优化分界线时序、释放后的速度差、局部卷动、微片尺度、明暗与尾部寿命；撤回重新造成面部提前碎斑的科比时间前移试验。
- 七场景模型检查、70 组方向的实际可见轨迹检查通过；46 个视频全部通过版本／容器／解码验证，并完成浏览器慢放与逐帧验证。
- 原 40 个双速度与八方向视频全部更新，新增六个华为／r15／r28 三栏对比，约 307.55 MiB，仍在同一视频目录。
- 科比中段卷束等差异仍存在，未宣称视觉达标；未修改 Android、未使用 ADB、未提交或发布。详见[后续记录](tuning-2026-09-09-gpt6-followup.md)。

## 2026-09-09 GPT-6：研究、独立桌面候选和完整对照包

- 审阅 107 个有效网络来源，完成逐条用途台账及综合分析，复核九段华为参考。
- 新建独立 `tuning-0909-gpt6` 实验目录；修正灭霸前景残影、背景尾粒和三主序列输运方向。
- 完成共享 GPU 桌面模型、十五次阶段图集迭代、七场景实时预览及 40 个 60fps 视频；钢铁侠和两种添加附件均含同屏八方向，全部视频平铺。
- 七场景原位覆盖、未释放位移、末帧、重算确定性及 30 组方向检查通过；40 视频容器／解码检查通过；审阅页播放、筛选、逐帧已验证，修复 HTTP Range 定位问题。
- 仍有紧密卷束、起碎轮廓与尾部色群比例差距，未宣称视觉达标。未修改 Android、未使用 ADB、未提交或发布。详见[结果记录](results-2026-09-09-gpt6.md)。

## 2026-08-26 技术路线调研

- 目标：把鸿蒙 OS7 删除通知的粒子消散动画引入完事儿，作为所有 Dialog dismiss 动画，
  粒子颜色须反映 Dialog 内容颜色。
- 完成项目侦察：minSdk 26 / targetSdk 36；全部 28 个 DialogFragment 继承
  BaseDialogFragment，无裸 AlertDialog.Builder，onCreateDialog 统一返回
  GestureAnchoredDialog——全局接入只需改基类两处。项目已有两套 GL 基础设施
  （FableSol 自建 EGL 线程、SpatialPhotoView 的 GLSurfaceView）。
- 产出 [research-2026-08-26-technical-approach.md](research-2026-08-26-technical-approach.md)：
  推荐"DecorView 快照 → GL_POINTS 无状态点粒子（波前溶解）→ 全屏透明 TextureView
  挂 Activity DecorView"路线；不延迟真实 dismiss，动画为纯装饰层。排除 Canvas /
  AGSL / RenderNode 碎块三条备选。工程量估计中等（框架 2–3 天 + 调优多轮）。
- 未开始写代码，等待用户对路线的确认。

## 2026-08-26 样板实现（AlertDialogFragment 试点）

用户确认路线后完成首版实现，`:app:assembleDebug` 通过，待真机目验观感。

- 新增 `views/particledismiss/` 三个文件：
  - `ParticleDismissRenderer.kt`：GL 渲染线程。RGBA8888/ES 3.0 自建 EGL（照
    FableSolEglSession 的 SDR 路径，但**不调 eglTerminate**——display 为进程级
    共享，terminate 会波及并存的 FableSol/Spatial 会话）；粒子无状态，属性全由
    gl_VertexID 派生（PCG 整数 hash 出随机数，浮点 sin-hash 在 15 万粒子量级
    随机位不足会条带），每帧只更新 uTime，单 GL_POINTS draw call；varying 两侧
    显式 highp（Maleoon 规约）。
  - `ParticleDismissOverlay.kt`：全屏叠加层（dim 补偿 → TextureView → 快照
    ImageView 三层），快照层遮蔽 EGL 建链延迟并充当 GL 失败降级载体；超时兜底
    强制收尾；触摸穿透。
  - `ParticleDismissController.kt`：入口检查（动画开关/Activity 状态/主线程）、
    DecorView 软件 draw 抓图 + CPU 圆角遮罩、坐标换算（双 getLocationOnScreen
    差值，分屏安全）、粒子步长 1.5dp 自适应上限 15 万。
- `BaseDialogFragment.kt`：`useParticleDismiss()` 开关（默认 false）；
  `GestureAnchoredDialog.dismiss()` 为全路径唯一拦截点（代码 dismiss、back、
  点外部经 cancel 都汇入 Dialog.dismiss），动画接管后置空 windowExitAnimation
  防双影；dispatchTouchEvent 记录最近触点作为波前起点。
- `AlertDialogFragment.kt`：override 开关 = true（样板）。
- 已知取舍：快照无 elevation 阴影（软件 draw 特性，dim 同时淡出下几乎不可察）；
  含 SurfaceView/TextureView 的 Dialog 检测到即跳过动画（待 PixelCopy 路径）。
- 初版观感参数集中在 Renderer companion：SPREAD_TIME 0.30s / DELAY_JITTER
  0.06s / LIFETIME 0.55s / 湍流 1.3 cell / 漂移 110dp（×0.55–1.45 随机）。

## 2026-08-26 第二版：curl 流场烟云化（用户第一轮观感反馈）

用户真机反馈：基础链路认可，但"炸开方向太固定、太均匀，要华为那种遁入烟云的
轻盈优雅"。诊断：初版是逐粒子独立白噪声方向（统计均匀 = 整块上升的幕布），
烟云感的本质是**空间连贯流场**（相邻粒子被同一股气流带动、成团成缕）。

- 建立桌面观感蓝本 `tmp/particle-dismiss-tuning/render_frames.py`（moderngl
  离屏渲染 3×3 时刻网格 + 单帧，conda env everythingdone），新旧模型 A/B。
  另有 index.html WebGL 蓝本，但本会话 Browser pane 不显示、无法合成帧截图，
  弃用；序列帧蓝本是有效的观感验证通道，GLSL 与 Android 版手工同步
  （300 es ↔ 330 头部差异）。
- 新运动模型（两轮蓝本迭代定稿）：value-noise fbm 的 curl 场（无散度旋涡，
  e=0.12 差分）+ 低频升力调制 + 流场采样点随主位移移动（路径弯曲）+ 场随
  时间演化；ease 从 easeOutQuad 反转为 pow(tl,1.35) 前慢后快（运动中消失
  才轻盈）；alpha 加低频浓淡调制（静止段不参与）+ smoothstep(0.15,0.95)^1.7
  长尾；触点径向 36dp 吹散推力；主方向每次随机 ±18° 倾斜。
- 第一轮蓝本问题与第二轮修正：位移太小不够"飘走"（DRIFT 130→210dp）、矩形
  轮廓保持太久（SWIRL 0.55→0.75、NOISE_SCALE 96→120dp）、半卡半云过渡期
  太短（SPREAD 0.30→0.40s，LIFETIME 0.62→0.58）。
- 代码结构：新增 ParticleDismissSpec 承载全部动画输入，Controller 组装、
  Overlay 透传、Renderer 消费。
- 发布 `202608260337`，待用户第二轮真机反馈。

## 2026-08-26 第三版：闪黑、触点方向、宽度截断感（用户第二轮反馈）

三项反馈的诊断与修复（均先在蓝本验证再移植）：

1. **动画开始时屏幕先变黑一瞬**：根因是双 dim 叠乘——WMS 对被移除 window 的
   dim 层自带淡出过渡（并非设计时假设的"即时消失"），与 Overlay 的满值补偿
   dim 层叠加。修复：删除补偿层，背景亮度过渡完全交给系统；Overlay 回到
   快照 + TextureView 两层结构。教训：初版为"假设中的生硬瞬断"加的补偿层，
   恰是症状的制造者。
2. **触摸外部 dismiss 应体现触摸位置**：driftDir 从恒向上改为"触点指向快照
   中心"（normalize(center - touch)，点下方向上飞、点上方向下飞、点侧面向
   对侧飞），距中心 <40dp 或无触点（back 键）退回向上；随机 ±18° 倾斜叠加在
   计算方向上。waveOriginUv 不再 clamp 进快照，允许 [-0.5, 1.5]——波前从
   真实触点方向的边缘先咬入。
3. **消散云宽度被 Dialog 宽度"截断"**：蓝本（无任何窗口裁剪）可复现 → 判定
   为观感问题而非渲染裁剪。修复：气流项拆出独立缓动 flowEase=pow(tl,0.8)
   （比主位移 pow 1.35 更早起效，直边立刻失形）、SWIRL 0.75→1.0、
   DELAY_JITTER 0.06→0.10、RADIAL_PUSH 36→44dp。
- 蓝本新增 touch_below / touch_above 两个触点场景网格图，验证方向逻辑与
  波前出快照起点的形态（向下飞的形态亦自然）。
- 发布 `202608260505`，待用户第三轮真机反馈。

## 2026-08-26 第四版：方向语义反转（用户第三轮反馈）

用户裁定："触点方向和消失方向反向了"——粒子应**朝按下的位置飞**，不是被
触点推开。改动：

- Controller：driftDir = normalize(触点 - 快照中心)（原为 中心 - 触点）；
- shader：radial 从"背离触点"反转为"向触点汇聚"，与主方向同语义（像被
  指尖收走）；
- 规则统一的结果：点确认/取消按钮时粒子朝按钮方向（向下）飘散，与外部
  点击同一规则；back 键仍默认向上。已在发布日志中向用户说明。
- 蓝本第四轮验证：按钮点击场景（向下偏 12°）与 touch_below 场景，向下
  翻卷的云被流场带走、无坠重感；汇聚吸力未造成漏斗状收束。
- 发布 `202608260637`。

## 2026-08-26 第五版：逐粒子指向虚拟远触点（用户第四轮反馈）

用户反馈：点左上方时右侧粒子"也是往左上，但没有那么左上"——全局统一方向
模型的固有缺陷，用户的心理模型是每个粒子朝触点飞（方向随位置渐变）。

- 方案：逐粒子主方向 = normalize(虚拟远触点 − 粒子位置)。虚拟远触点 =
  快照中心 + (含 ±18° 随机倾角的触点方向单位向量) × 1.1 × 快照对角线。
  纯逐粒子指向真实触点会在触点贴近快照（点按钮）时两侧对冲成漏斗；推远
  到对角线尺度后方向渐变平滑、无对冲。VIRTUAL_TOUCH_FACTOR 越小汇聚感
  越强，可调。
- radial 汇聚项功能被吸收，删除（shader 少一个 uniform，运动构成简化为
  主方向 + 流场 + 抖动三项）。
- Spec：driftDirX/Y、radialPushPx → virtualTouchXPx/virtualTouchYPx。
- 蓝本第五轮：touch_upper_left 场景确认右下角后碎的粒子沿更偏水平的路径
  汇入左上主流；按钮场景无退化、无漏斗收束。
- 发布 `202608260646`。

## 2026-08-26 第六版：方向整形去混沌（用户第五轮反馈）

用户反馈：粒子"往四面八方都移动"、太混沌太随机。根因：第三轮为消宽度
截断感做的两项加强（SWIRL 1.0、flowEase pow0.8 抢跑）使粒子激活初期被
无方向偏好的旋涡场主导。修正三项：

- SWIRL 1.0 → 0.6：流场从驱动力降回扰动量级；
- flowEase pow(tl,0.8) → pow(tl,1.15)：早期运动以主方向为主，缕状摆动
  中后期才加入；
- **流场方向整形**（新增）：flowShaped = 垂直主方向的横摆分量（全保留，
  烟缕与云宽靠它）+ 顺向分量 × 0.4，逆主方向分量剔除——没有粒子往回跑，
  整团烟保持朝触点的一致流动。
- 蓝本第六轮验证：按钮与左上两场景整团流向清晰一致，边缘羽化、浓淡
  非均匀、方向随位置渐变均保留，未回退到第一版的均匀幕布感。
- 发布 `202608260655`。

## 2026-08-26 第七版：静止层原图 + 低频弯曲场（用户第六轮反馈）

两项反馈：

1. **动画瞬间内容变模糊**：原理性缺陷——"静止粒子拼回原图"本质是按
   cellPx（约 4px）网格的重采样点阵，文字笔画 2–3px 直接糊掉，GL 接管
   瞬间从 ImageView 逐像素原图突变为点阵。修复：新增**静止层 pass**——
   快照矩形 quad 以逐像素原图绘制，fragment 按与粒子完全同款的
   cell/PCG hash/波前公式逐格 discard（擦除边界与粒子激活精确对齐）；
   粒子 vertex 对未激活粒子早退移出裁剪（省 fill 与纹理采样）。动画全程
   未消散区域与真实弹窗逐像素一致——华为效果第 2 帧左半文字清晰可读的
   真实结构正是如此。
2. **太规整、像线性 warp**：虚拟远触点的方向场是线性收敛场，整团近似
   线性变形，两侧边界是向远点收敛的直线。修复：**低频弯曲场**——
   bend = (大尺度 vnoise(basePx/216dp) − 0.5) × 1.4 rad + 逐粒子 ±0.075，
   乘 smoothstep(tl) 随时间平滑增长；主方向旋转 bend（起飞朝触点、随后
   弯出螺线弧），流场方向整形改以 dirBent 为参照。空间连贯的大尺度弯
   （左右区域弯向不同）打破对称直边，不回到第五轮的混沌。
- 蓝本第七轮验证（按钮 + 左上两场景）：未消散文字全程清晰；云上下边缘
  不再平行、尾部弯出弧形垂尾；整体方向感保留。
- 发布 `202608260722`。

## 2026-08-26 第八版：Genie 漏斗收拢（用户第七轮反馈）

用户点名 macOS 窗口收进 Dock 的 Genie 动画——播放中窗口上下宽度明显不一致，
希望消散云有同类形变。

- 实现：pinch 位移项 = −(粒子相对"过触点、沿主方向 dir 的轴线"的横向偏移)
  × PINCH × ease，直接加进 drift。触点即"漏斗口"，语义与 Dock 图标位置
  对应。无需新的时序机制——既有波前天然配合：近触点侧先激活、飞行久、
  横向收得多；远侧后激活仍保持全宽，整团自成锥形。
- PINCH = 0.55（1 = 完全收拢到轴线），是收拢强度的单一旋钮。
- 蓝本第八轮验证：touch_below 上宽下窄漏斗 + 底部中央收束尾；
  touch_upper_left 斜向锥形、左上先导流收成窄尖。弧线弯曲、烟缕、浓淡
  等既有特性不受影响。
- 发布 `202608260731`。

## 2026-08-26 第九版：PINCH 0.91（用户指定）

- 用户看过 0.55 后指定调大到 0.91。蓝本确认漏斗嘴更细更尖、无挤线退化。
- 发布 `202608260738`。

## 2026-08-26 第十版：不规则锋线 + 即刻起沙（用户第九轮反馈）

用户：PINCH 调 0.96；"还没移动的粒子组成的 dialog 太规整、有点怪"，要求
对照华为原效果细节优化。重读华为帧 2 定位两处差异：

1. **锋线形态**：华为的溶解边界是弯曲参差的不规则线（纸张烧蚀的火线），
   我们是从触点扩散的光滑圆弧（逐格 jitter 只是弧线上撒盐，几何仍完美）。
   修复：delay += (vnoise(basePx / (NOISE_SCALE×0.6)) − 0.5) × WAVE_WARP
   (0.14s)。静止层 fragment 加同款函数与公式，保持擦除对齐；
   TOTAL_DURATION 补 + WARP/2。
2. **"点阵化但原地不动"的假 dialog 带**（用户主诉的直接来源）：粒子激活
   后 ease 慢启动（位移≈0）× fade 前段满值 × 方点无缝拼图 = 锋线后跟着
   一条已粒子化却静止的带子，方点网格规整而怪异。华为的粒子一旦脱离实体
   立即离位。修复三点：ease += KICK(0.02) × smoothstep(0, 0.06, tl)（几帧
   内滑出约 4dp）；fade 起点 0.15 → 0.02（激活即衰减）；vActivation
   0.12 → 0.05（几乎立即圆点化）。
- PINCH 0.91 → 0.96（用户指定）。
- 蓝本第十轮：残留实体轮廓呈咬缺状、碎化区直接是离位散沙、清晰文字区不受
  影响。蓝本删除旧模型 A/B 渲染（锋线扭曲依赖 vnoise，共享 shader 块不再
  兼容；对比使命已完成）。
- 发布 `202608260756`。

## 2026-08-26 第十一版：pinch 双层调制（用户第十轮反馈）

用户：点下方 dismiss 时粒子群两侧边缘的倾斜"很平整，像一把刀切下去"。

- 根因：pinch 是**均匀线性横向缩放**——收缩系数只依赖 tl，对全体粒子
  一致，快照左右直边的粒子收缩后仍然共线，云的侧边 = 一条随时间变陡的
  直线；PINCH 提到 0.96 后该效应主导位移，直边极明显。
- 修复：收拢量双层调制。pinchMod = (0.55 + 0.9 × vnoise(basePx / 84dp))
  ×(0.75 + 0.5 × h3)——大尺度空间波浪（区段收多收少，侧边起伏）+ 逐粒子
  毛糙羽化；均值 ≈1 保持用户指定的漏斗力度。pinchAmount 封顶 0.98 防止
  高调制值过冲穿过中轴。
- 蓝本第十一轮（touch_below）：左右轮廓起伏不对称、外缘飘稀疏粒子羽、
  中央收束尾保留。
- 发布 `202608260809`。

## 2026-08-26 第十二版：大小/寿命/疏密不均（用户第十一轮反馈）

用户："还是太规整，加大噪声和随机"；点出华为的粒子"大小不一样或不同区域
疏密/分布不一样，更灵动轻盈"。一揽子去规整化：

- 新增第 4/5 随机数（h4/h5 = 黄金比组合，PCG 30bit 已用满）；
- **尺寸不均**：sizeMod = (0.7+0.6·vnoise(60dp)) × (0.55+0.95·h4²)——区域
  粗细差 × 逐粒子平方偏斜（多数小、偶有大颗粒）。静止层接管拼图后粒子
  尺寸已无无缝约束，可自由放开；
- **寿命不均**：lifeMod ∈ [0.55, 1.25]（区域+逐粒子各半）——疏密随时间
  演化不均：有的区域早稀、有的成余缕。TOTAL_DURATION 按 ×1.25 计，尾段
  +0.15s；
- **浓淡加强**：density 0.55–1.0 → 0.4–1.0；
- 湍流 0.8→1.4 cell 且幅度 ×(0.5+h4)；SWIRL 0.6→0.75（方向整形兜底不
  混沌）；WAVE_WARP 0.14→0.18；DELAY_JITTER 0.10→0.12；bend 主项
  1.4→1.6、逐粒子 0.15→0.3；riseMod 与 speedJitter 方差均扩大。
- 蓝本第十二轮（touch_below）：起沙区颗粒大小混杂、云体浓淡斑驳（浓白/
  灰/近透明稀纱共存）、边缘破碎，t=0.3 帧质感已非常接近华为参考。
- 发布 `202608260833`。

## 2026-08-26 第十三版：整数 hash + 外扩羽流（用户第十二轮反馈）

用户报告**蓝本与真机表现不一致**（蓝本边缘不规整、真机仍规整）——关键
线索。定位根因：

- **sin-hash 在移动 GPU 退化**：hash21 = fract(sin(dot(p, ...)) × 43758)
  的 dot 输出达 1e3 弧度量级；桌面 GL 的 sin 有精确参数缩减，移动 GPU
  （Adreno/Mali）为低阶多项式近似，大参数严重失真 → vnoise 质量塌陷 →
  锋线扭曲、pinch 双层调制、尺寸/寿命/浓淡等**全部下游调制在真机上
  大打折扣**。第七至十二轮蓝本验证的"去规整"效果，真机从未完整呈现。
- 修复：hash21 换整数 hash（uvec2 乘大素数异或再乘），粒子层与静止层
  同步替换。跨平台逐位一致，蓝本从此忠实预测真机。
- 教训已有 memory（cross-language-degenerate-guards）：双端不一致时先
  比对两端数学实现；本次新变体是 GPU 内建函数精度差异。
- **外扩羽流**（用户要求放开云宽）：flare = 归一化(vnoise(basePx/96dp)
  − 0.55)，约 45% 区段 > 0；flareDrift = 横向向外 × flare × 90dp ×
  flowEase × (0.5+h5)；外扩区 pinchAmount × (1 − 0.7·flare)。主体收束
  （漏斗）+ 边缘流苏外逸并存，云宽可超原 Dialog 约 90dp。
- 蓝本第十三轮（touch_below）：云宽超出卡片轮廓、右上羽流明显越界、
  中部收束尾保留。
- 发布 `202608260845`。真机若与蓝本仍有差异需继续反馈（sin 假说的最终
  验证依赖本版真机表现）。

## 2026-08-26 铺开全部 Dialog（用户认可第十三版）

用户对第十三版观感认可（"感觉还行"——整数 hash 修复后真机首次完整呈现
噪声效果），要求扩展到颜色更丰富的其他 Dialog。

- BaseDialogFragment.useParticleDismiss() 默认 false → true；
  AlertDialogFragment 的样板 override 删除。全部 28 个 Dialog 生效。
- 已核实无子类 override onCreateDialog，拦截点唯一性成立。
- 含 SurfaceView 的 Dialog 运行时自动降级；PixelCopy 路径记入
  [followups.md](followups.md)。
- 发布 `202608260857`。sin-hash 教训已写入全局 memory
  （gpu-builtin-precision-divergence）。

## 2026-08-26 第十五版：burst 先散后聚 + 收拢封顶（用户第十四轮反馈）

铺开后用户反馈：宽 Dialog 上"宽度受限更明显"；且"有一些粒子应该先往两侧
跑一跑，而不是所有粒子从一开始就直接往最终方向运动"。

- **宽 Dialog 勒窄的根因**：pinch 按比例收缩初始横向偏移，Dialog 越宽
  边缘的绝对收拢位移越大（全宽面板边缘被一口气拉向中轴），固定 90dp 的
  flare 抵不过。修复：pinchVec 模长封顶 PINCH_MAX_DP(240dp)。
- **两段式运动（burst）**：burstDir = lateralDir（按所在侧向外）+ 0.35
  随机单位向量（中轴附近纯随机乱散）；幅度 = BURST_DP(55) × (0.25+1.1h4)
  逐粒子强随机 × (0.6+0.8·vnoise) 区域调制；burstEase =
  smoothstep(0, 0.30, tl) 前 30% 寿命完成。关键设计：pinch 作用于**初始**
  lateral（basePx 派生），burst 的额外散开不被收拢回收——先散出去的宽度
  保持到被主流带走。
- 蓝本第十四轮：新增 make_snapshot 参数化 + 560dp 宽面板场景（视口
  1400→1960）。宽面板碎化区两侧先散、云超轮廓蓬松下沉；标准卡片
  无过散退化。
- 发布 `202608260912`。

## 2026-08-26 第十六版：方向渐变替换爆散段（用户第十五轮反馈）

用户否定 burst 三点：太散、Dialog 中部形成空区、两段式结构显急；约束
**不得加大动画时长**。

- 空洞根因：burstDir 以 lateralDir 为主——左右半粒子集体背离中轴、无人
  补位。急促根因：burst 是独立快速位移段（前 30% 寿命硬跑 50–100dp），
  有速度尖峰。
- 修复：删除 burst 位移段，改**方向插值**——moveDir 从 earlyDir（随机
  为主 + 轻微偏侧）在前 55% 寿命内平滑转到 dirBent，主位移曲线（ease）
  完全不变。轨迹成"先斜出再拐向主流"的连续弧线：无速度尖峰、总时长
  不变；早期 ease 小、散开量天然温和；中部粒子早期方向纯随机、四散又
  归拢，不塌空。earlySum/moveSum 两处 normalize 零向量保护。
- BURST_DP/burstPx/uBurstPx 全链删除；PINCH_MAX_DP 封顶保留。
- 蓝本确认：云身连续密实、中部无空洞、起始温和贴实体渐散。
- 发布 `202608260924`。

## 2026-08-26 第十七版：粒子缩小约 17%（用户第十六轮反馈）

- 用户认可方向渐变版（"还可以"），反馈"粒子稍微大了点"。尺寸曲线
  mix(cell×1.2, 0.4) → mix(cell×1.0, 0.34)，sizeMod 随机分布不动。
- 发布 `202608260929`。

## 2026-08-26 第十八版：dim 与粒子同步恢复（用户第十七轮需求）

需求：背景 dim 的恢复要有与粒子动画等长的渐变——白 Dialog 消散进白背景
时粒子不可见，dim 保持期提供暗背景衬托亮色粒子。

- 与第三版闪黑的关系：当时的叠乘发生在"window 移除路径"（WMS 对 exiting
  window 的 dim 自带淡出 × 满值补偿层）。本版在真实 dismiss **之前**
  `clearFlags(FLAG_DIM_BEHIND)`——活 window 的属性修改即时生效、无 WMS
  过渡（生态中逐帧 dim 动画均依赖此行为），overlay 的 dim 接力层同帧同
  浓度接管；window 随后移除时已无 dim，叠乘源不存在。
- dim 接力层从 onGlFirstFrame 起淡出，时长 = TOTAL_DURATION × scale
  （与粒子严格同步），插值 AccelerateInterpolator(1.3)：前慢后快，粒子
  浓密的前中段背景保暗。
- GL 失败降级：整层 fallback 淡出天然包含 dim（子 View）。
- 发布 `202608260939`。

## 2026-08-26 第十九版：先就位后揭开（用户反馈仍闪）

第十八版的"同帧交接"假设不成立：接力 dim 在 activity window 里、要等
下一次 traversal + 合成才上屏；clearFlags 与 removeViewImmediate 是同步
IPC、可早一帧生效——跨窗口渲染管线不同步，出现一帧"系统 dim 已消失、
接力层未画出"的亮空窗。

- 修复：**先就位、后揭开**。Controller.start 改为回调式
  （onOverlayShown：首个 preDraw 后经 Choreographer 下一帧确认已提交合成，
  100ms 兜底幂等 fire）；GestureAnchoredDialog 在回调中才 clearFlags +
  super.dismiss()（真实 dismiss 延迟 1–2 帧 ≈ 33ms，无感知）。
- 时序不变式：画面每一刻恰有一层等浓度暗——接力层先在 dialog window 底下
  就位（被挡、无视觉变化）→ 清 flag（底层暗层以相同浓度顶上）→ 移除
  window（无 dim 可淡出）。任何一步跨帧都亮度连续。
- 延迟期间 dialog window 原样显示充当遮蔽层（fragment 路径的 view 不会被
  提前摘除——它挂在 dialog decorView 而非 fragment container）；重入时
  直接 super.dismiss()，回调侧 runCatching + Dialog.dismiss 幂等兜底。
- GL 首帧若早于揭开 ≤50ms，粒子进度提前 ≤4%，视觉可忽略，未做起播门控。
- 发布 `202608260946`。

## 2026-08-26 第二十版：dim 全程接管（用户提议，闪烁终局方案）

第十九版"先就位后揭开"仍闪——结论：部分 ROM 的 WMS 对 dim 值变化本身
带不受控过渡动画（clearFlags 的 0.6→0 跳变可能被 WMS 插值 200ms），
"系统 dim 与应用暗层并存或交接"的任何结构都存在闪烁路径。用户提议由
应用全程接管背景暗层，定案实施：

- 主题 EverythingDoneTheme.Dialog 增加 `android:backgroundDimEnabled=false`
  ——系统 dim 从源头不存在，无需运行时 clearFlags（避免 generateLayout
  时序问题）。
- 新增 [DialogDimLayer](../../app/src/main/java/com/ywwynm/everythingdone/views/particledismiss/DialogDimLayer.kt)：
  挂宿主 Activity DecorView（Dialog window 之下），show 时 220ms 淡入
  （浓度常量 0.6 = 平台默认），dismiss 时幂等 fadeOutAndDetach——粒子
  路径与动画等长 + Accelerate(1.3)（粒子浓密期保暗），普通路径 220ms。
- GestureAnchoredDialog 持 dimLayer（fragment onStart 注入）；dismiss
  拦截各分支分别触发对应淡出。Overlay 的 dim 层删除；"先就位后揭开"
  回调保留（保证内容层快照无缝，虽与 dim 已无关）。
- 副产物改进：Dialog 出现时背景从系统的瞬时变暗改为平滑淡入。
- 多 Dialog 叠放时各自一层，叠加变深与系统行为一致。
- 发布 `202608260955`。同轮用户提出"出现动画是否也做类似形式"的讨论
  （见对话），暂未实施。

## 2026-08-26 第二十一版：凝聚出现动画（用户选定 B 方案）+ dim 双保险

用户拍板 B（快速凝聚出现），并报 show 期 dim 比 dismiss 起点更黑——诊断
为系统 dim 疑似未被主题声明禁净、与接管暗层叠加（dismiss 拦截后系统 dim
随 window 消失、只剩单层所以变浅）。

- **dim 修复**：onStart 里运行时 `clearFlags(FLAG_DIM_BEHIND)` 双保险
  （主题生效时为 no-op），全程统一为 DIM_AMOUNT 0.6（平台默认）单层。
- **凝聚出现动画**（消散管线时间倒放）：
  - Spec：condenseFromT = 0.42 × TOTAL_DURATION（起点粒子距原位约 40dp、
    部分区域已擦除）、condenseDurationS = 0.32s；
  - Renderer：时钟分支 t = condenseFromT × (1 − elapsed/duration)，末帧
    t=0（静止层逐格显现的终点 = 逐像素完整原图）；
  - Overlay：凝聚模式无快照遮蔽层（从散开态入场）、onDone 回调经
    fireDone 幂等覆盖正常/降级/超时/detach 全部退出路径；
  - Controller.startCondense：decor 置 INVISIBLE → preDraw（布局完成）后
    抓图起播 → onDone 恢复 VISIBLE；150ms 兜底恢复；SurfaceView/尺寸/
    activity 状态检查与 dismiss 同款；
  - BaseDialogFragment.onStart 触发，与消散共用 useParticleDismiss()
    开关；重建恢复（savedInstanceState != null）不重播；
    condenseAttempted 保证每 dialog 一次。
- GL ready 前面板区域短暂空白（30–50ms，从无到散云属出现动画语义内）。
- 发布 `202608261005`。

## 2026-08-26 第二十二版：凝聚收尾先显后撤（用户报结束闪烁）

凝聚结束时闪：恢复面板（dialog window）与移除 overlay（activity window）
跨窗口管线不同步——overlay 先消失、面板未上屏的一帧空白。第三次同类
教训后总结出**跨窗口切换不变式：先就位、后切换**（切换前后内容逐像素
一致的层必须先上屏，确认后才移除旧层）：

- Overlay 凝聚模式 onGlFinished 只 fireDone、不自移除，保持显示末帧
  （与真实面板逐像素相同）；新增 release() 供宿主收尾。
- Controller onDone：decor VISIBLE → runAfterShown(decor)（preDraw +
  Choreographer 下帧 + 100ms 兜底）→ overlay.release()。
- runAfterShown 抽为公共函数，dismiss 端"先就位后揭开"改为复用。
- 至此三处跨窗口衔接（dismiss 揭开、dim 全程接管、凝聚收尾）全部遵循
  同一不变式。
- 发布 `202608261014`。

## 2026-08-26 "对话框粒子动画效果"设置项（用户需求 + 首次授权 adb 验证）

- 设置 → UI 组（Timer digit style 与 Spatial photo effect 之间）新增四档
  选择项：无 / 仅出现时 / 仅消失时 / 出现与消失时（默认）。
  Def.Meta.KEY_DIALOG_PARTICLE_ANIMATION，位编码 bit0=出现、bit1=消失；
  ChooserDialogFragment confirm 即时 putInt（不等设置页退出的
  storeConfiguration）；BaseDialogFragment companion 的
  particleAnimationMode() 在 show（凝聚触发）与 dismiss（拦截）时实时读，
  已打开的 Dialog 也按新档表现（chooser 自身成为即时预览）。
- 13 语言翻译（python 脚本按锚点批量插入，UTF-8 显式编码）。两个坑：
  it/fr 的撇号必须 `\'` 转义（AAPT 报 "Invalid unicode escape sequence"）；
  ChooserDialogFragment.setItems 签名是 MutableList<String?>。
- 真机验证（用户明确授权 adb）：
  - OPD2515（9018f404，**横屏 2520×1680**——先查 wm size 再定坐标，竖屏
    坐标全部无效曾白滚 40 次）：四档矩阵逐档截图——档3消散（粒子朝触点、
    静止层文字清晰、dim 同步）、档0双向普通过渡、档1凝聚粒子雾+普通关、
    档2普通开+从 CONFIRM 按钮起碎的消散；副标题实时更新；run-as 核对
    `<int name="dialog_particle_animation" value="3"/>`。
  - R5CW20BLNKL（三星）：screenrecord 两轮完整开关，486 帧 ffmpeg
    signalstats 逐帧 YAVG **零突变**（阈值 25 与 15 均零，最大帧间 8.3，
    范围 99.7–220.5）——黑屏/闪烁客观排除。
  - 设备事实：OPD2515 的 screenrecord 全路径 Permission denied（ROM 禁），
    录屏验收只能用三星；uiautomator dump 属性序 text 在 resource-id 前。
- 发布 `202608261050`。

## 2026-08-26 四项修复（用户四条反馈，双真机迭代验证）

1. **凝聚重做**（"从四周聚中间有空洞、太规律"）：根因是严格倒放下波前
   起点（中心）最后成形。Spec 参数化 spreadTime/delayJitter/waveWarp，
   凝聚专用 0.06/0.20/0.30（噪声主导凝实顺序）+ driftPx×0.55（就近
   飘入）+ fromT 0.55（起点近乎无、烟缕浮现）。蓝本 grid_condense 确认
   斑块状随机凝实、无中心空洞。
2. **show 后闪烁**：window 的主题 enter 动画（淡入+缩放）在首帧被藏时
   挂起、凝聚收尾切真面板瞬间才播——面板 alpha 0 起步渐入。修复：
   startCondense 成功即 setWindowAnimations(0)。三星录屏验证 show 结束
   时刻零跳变。
3. **凝固**（检查更新 loading + Toast 场景）：凝聚未收尾即 dismiss 时，
   window 移除令收尾的 preDraw 探测失效，且 View.postDelayed 兜底对
   detached view 进 RunQueue 永不执行——凝聚层带末帧永久残留。修复：
   runAfterShown 兜底改挂全局 mainHandler（必达）；dismiss 拦截先
   release condenseOverlay（Base 侧经 startCondense 的 onOverlayCreated
   回调持有引用）。
4. **组件变形**（圆形渐变方向按钮呈方形）：软件 draw 不执行子 View 的
   clipToOutline。第一次尝试 HardwareRenderer 离屏重渲染（API 29+）在
   三星实测返回**空 bitmap**（无异常）——消散全程不可见、消散开始出现
   dY≈31 的空窗跳变（对比修复前 486 帧零跳变，回归实锤）；已弃用并派
   联网调研代理查根因（结论待回）。改用 **PixelCopy.request(window)**：
   窗口合成结果逐像素拷贝、机制性保真；start() 改异步（PixelCopy 回调
   后建 overlay，复用"先就位后揭开"延迟链，失败路径 fire onOverlayShown
   放行真实 dismiss）。凝聚场景 window 首帧无内容不能 PixelCopy，暂回
   软件 draw（瑕疵仅凝实末帧 <100ms 可见）。
- 三星复测：688 帧逐帧亮度零跳变（max dY 10.2）；消散粒子云恢复（含
  颜色继承）；凝聚急停无残留。
- 发布 `202608261131`。

## 2026-08-26 调研结论落地：凝聚快照升级加固版硬件渲染

联网调研代理（AOSP ScrollCaptureViewSupport / androidx
CanvasBufferedRenderer / 官方 HardwareRenderer+ImageReader 样例）结论：

- 跨 renderer 引用 RenderNode 合法（AOSP 长截图同款技术），但**可见 view
  走"引用既有 displayList"分支、首帧前 INVISIBLE 走"全新录制"分支**——
  精确解释三星"消散空图、凝聚正常"的分布；空图三嫌疑：syncAndDraw 返回
  码未检查（静默不产帧）、acquireLatestImage 拿错缓冲、**无 GPU 完成
  栅栏**（可见 dialog 自身在渲染、GPU 忙时读到未写完的缓冲）。
- 消散用 PixelCopy 是正确终态（调研背书）；凝聚场景 PixelCopy 不可用
  （ERROR_SOURCE_NO_DATA），离屏硬件渲染是唯一保真路径且恰在其甜区。
- 落地：captureViaHardwareRenderer 加固版（返回码校验、acquireNextImage、
  API 33+ image.fence.await、setOpaque(false)、光源 geometry/alpha、
  官方释放顺序），失败回退软件 draw；PixelCopy 补 srcRect
  （getLocationInWindow）。凝聚的圆按钮保真补齐，全链路保真统一。
- 三星复核：257 帧零跳变（max dY 9.7）。调研报告全文见对话；方案要点
  沉淀至全局 memory（android-view-snapshot-fidelity）。
- 发布 `202608261146`。

## 2026-08-26 回归修复：不可变位图 / dismiss 重入 / PixelCopy 超时（用户报障）

用户报 1146 两回归：凝聚完全不播；按钮 dismiss 无消散（三星上弹窗甚至
卡住不关）。教训：1146 的复核只验了"亮度平滑"没验"动画在播"——凝聚
失败的兜底路径同样平滑，回归漏网。探针（Log.i + externalFilesDir 文件
双通道，OPD2515 一轮命中）：

1. **凝聚不播**：`copy(ARGB_8888, false)` 产出**不可变**位图，
   applyRoundedCornerMask 的 `Canvas(bitmap)` 抛
   "Immutable bitmap passed to Canvas constructor" → 外层 catch → 快照
   null → restore 兜底（无动画显示）。修复：isMutable = true。
2. **按钮 dismiss 无消散**：fragment 关闭链两次调 Dialog.dismiss()
   （dismissInternal 与 onDestroyView 各一次）。PixelCopy 异步化后第一次
   拦截发起抓图即 return，重入的第二次走"已尝试"分支**立即
   super.dismiss()** 移除 window——18ms 后回调 decorAttached=false，
   动画弃播。（同步抓图时代第二次重入恰好无害，异步化暴露了它。）
   修复：particleFlowPending 标志，流程接管期间重入直接 return，真实
   dismiss 统一由回调执行。
3. **三星弹窗不关**：其上 PixelCopy 回调可能不来且无第二次 dismiss 兜底
   → 永久卡住。修复：PIXEL_COPY_TIMEOUT_MS 500ms 幂等超时，放行真实
   dismiss（该次无动画但绝不卡死）。
- 双真机复核：OPD2515 探针全绿（hardware snapshot ok；PixelCopy SUCCESS
  且 decorAttached=true）+ 凝聚轻烟起点帧 + CONFIRM 起碎帧；三星消散
  恢复、2.5s 后弹窗确认关闭。探针移除后发布。
- 流程教训：动画类改动的复核必须包含"动画确实发生"的直接证据（中间帧
  目检），亮度平滑性不能替代。
- 发布 `202608261221`。

## 2026-08-26 每次动画图案随机化（用户需求"出现动画别每次一样"）

诊断：动画的全部"随机性"来自确定性来源——逐粒子 PCG hash 的输入是
gl_VertexID、噪声场的输入是 basePx，同一弹窗在相同位置尺寸下出现/消失
的斑块图案、烟缕走向、凝实顺序每次完全复现；唯一逐次变量 ±18° 倾斜在
凝聚的小位移下不可辨。

- 方案：每场动画生成双随机种子——uNoiseSeed（vec2，vnoise 内部
  `p += uNoiseSeed` 平移噪声域，锋线扭曲/pinch 调制/尺寸/寿命/浓淡/
  流场全部下游图案随之全新）+ uHashSeed（uint，异或进 PCG 输入，逐粒子
  延迟/方向/大小重洗）。**静止层与粒子层共用同一组种子**（Spec 字段
  noiseSeedX/Y、hashSeed，Controller 两处组装 Math.random 生成）——
  擦除衔接的对齐前提是两个 program 的 hash/噪声逐位一致，种子必须同源。
- 静止层 PCG 首步 `(id ^ uHashSeed) * 747796405u + 2891336453u` 与粒子层
  数学等价。蓝本 render_frames.py 同步加 uniform（固定 0 值，保持调参
  对比的可复现性）。
- 三星验证：消散两轮同触点中间帧图案明显不同（一次右上孤立消散斑、一次
  中上大片）；凝聚 320ms 撞帧不可行（0s 太早 0.4s 太晚），改 screenrecord
  两轮 + 逐帧提取 + 与末帧 MAD 曲线对齐进度，同进度帧对（f034/f042）
  凝实顺序与斑块分布完全不同、擦除衔接无错位。OPPO 冒烟：Adreno 上新
  uniform 编译正常、消散照常播放（跨 GPU 编译回归排除）。
- 发布 `202608261240`。

## 2026-08-26 凝聚收尾闪烁：alpha 隐藏替代 window INVISIBLE（用户报障）

用户报改记事颜色 bottomSheet 上"凝聚结束后面板消失一瞬再出现"。

- 根因链：凝聚以 `decor.visibility = INVISIBLE` 隐藏首帧 → 窗口从未
  显示过 → WMS 把窗口 enter 动画挂起到恢复 VISIBLE 的瞬间才播。该
  bottomSheet 的 enter 是 `bottom_panel_slide_in`（190ms、
  fromYDelta=100%p）——恢复瞬间面板从屏外滑入，而 runAfterShown 的
  preDraw + Choreographer 只能确认 app 侧帧提交、感知不到 WMS 侧的
  窗口动画，overlay 末帧撤走时面板还在屏外。第二十一版的防护
  `setWindowAnimations(0)` 防不住：子类 onStart 在 super **之后**
  `setWindowAnimations(R.style.EverythingDoneAnimationBottomPanel)`
  重设即覆盖（ThingBackgroundEditorBottomSheet:117）。
- 修复：隐藏方式改 **`decor.alpha = 0`**。窗口全程正常显示（无 WMS
  可见性事务），enter 动画在透明期内照常播完（全透明内容上屏无痕，
  190ms < 凝聚 320ms），凝聚结束恢复 alpha 只是一帧普通属性重绘——
  上屏确认因此可靠。快照不受影响：`View.draw` 渲染的是内容，自身
  alpha 由父级/RenderNode 合成时才应用（软件与离屏硬件两条路径同理）。
- Base 侧 `if (started) setWindowAnimations(0)` 删除（不再需要），顺带
  修复档位"仅出现时"下窗口 exit 动画被凝聚吃掉的隐性回归；dismiss
  粒子拦截路径的置零保留（防真实 dismiss 的窗口移除动画与粒子叠影）。
- 三星验证：bottomSheet 凝聚两轮录屏，面板区与末帧 MAD 曲线单调收敛
  至 0、无"收敛后回升"尖峰，收尾四连帧面板全程在位（修复直接证据）；
  bottomSheet 消散与设置 chooser 凝聚回归：全帧帧间 MAD 无 >25 突跳，
  且动画中间帧确认在播（消散粒子云含色相条颜色、凝聚曲线峰值后收敛）。
  OPPO：0.25s 撞中凝聚中段（色钮半凝实、快照不透明），终态完整。
- 发布 `202608261312`。

## 2026-08-26 内容色粒子增强（用户需求，grill-me 定案后实施）

用户："非 dialog 颜色的粒子显得更多一些"——白底（暗色为灰黑）面积
占绝对多数，动画里几乎全是本体色粒子，单调无趣。grill-me 逐项定案：
颜色逐位不变只重加权 / 权重 = 色距为主（黑字中档）+ 饱和度加成
（彩色最高）/ 四手段全开 / 副本仅发彩色 / 基调鲜明型。

- **权重**：`w = min(wDist·0.62 + wSat·0.6, 1)`，wDist =
  smoothstep(0.08, 0.42, 色距/√3)、wSat = smoothstep(0.18, 0.42, 饱和度)。
  参照色 uPanelColor = 快照 32×32 每通道 16 级量化直方图众数
  （Controller.dominantColor，普适自定义背景与暗色模式）。
- **尺寸**：sizeMod ×(1 + 0.7w)，副本再 ×0.8 保持主次。
- **寿命重分配**（总时长不变——用户旧裁定不得加大时长）：基础范围
  0.55–1.25 改 0.42–0.95，高权重 ×(1+0.32w) 乘回上限 1.25——白底
  先散尽、内容色成余缕，TOTAL_DURATION 分毫未动。
- **背景减密**：density ×(1 − 0.28(1−w))，白底 ×0.72 轻度。
- **副本扩倍**：REPLICAS=3（1 主 + 2 副本），draw 顶点 = cell×3
  （上限 45 万）。id 映射 `replica = id % 3, cellId = id / 3`；**主粒子
  hash 输入 = cellId 与静止层逐位一致**（擦除对齐与既有图案完全不变），
  副本 hash 输入偏移 cellId + replica×gridN（轨迹独立）；副本 delay =
  主 delay + 正偏移（绝不早于格子擦除，"凭空多一颗"不存在；凝聚倒放
  下副本先落格）。门控 wSat<0.5 的副本 vertex 阶段 degenerate（黑字
  不增量——用户裁定，彩色才是稀缺资源）。
- GLSL 蓝本与 Android 逐行同步，uContentBoost uniform 蓝本出 A/B
  （应用端恒 1）；新增 make_colorful_snapshot（色钮网格+色相条）主
  验证场景。蓝本 A/B：plain 在 t≥0.5 全白灰，boost 后彩色成簇、
  彩虹带、收尾彩色主角，t=0.9 两者同步结束。
- 真机：三星录屏逐帧——消散早段色钮碎成饱满彩色团、中段色相条化作
  彩虹粒子带、后段白烟散尽彩色余缕为主角；凝聚中段彩色烟缕先声。
  擦除对齐正常、黑字骨架不过量、无掉帧迹象。OPPO 撞帧：彩虹带与
  色钮团清晰，Adreno 正常。
- 发布 `202608261403`。

## 2026-08-26 按钮关闭闪烁：dismiss 拦截上移到 fragment 层（用户报障）

用户报消散端也闪："粒子动画触发前 dialog 已直接消失、动画又让它出现"，
且**按钮关闭 100% 复现、点外部无此问题**——路径二分直接锁定根因：

- 按钮路径：`fragment.dismiss()` → dismissInternal **先**调
  Dialog.dismiss()（被 GestureAnchoredDialog 拦截、发起 PixelCopy、
  真实 dismiss 推迟），但**接着**提交的 remove 事务只隔一条主线程
  消息就执行 onDestroyView，把内容 view 从仍在屏的窗口里摘空——
  PixelCopy 要 1–2 帧后才回调上屏，中间露出被摘空的窗口，快照随后
  "重现"。1221 版的 particleFlowPending 只拦 Dialog.dismiss() 的重入，
  **拦不住 fragment 事务摘 view**。
- 点外部路径：cancel → Dialog.dismiss（拦截）→ 真实 dismiss →
  onDismiss 回调 → dismissInternal——fragment 移除在窗口移除之后，
  无空窗。**此前 adb 验证的消散全部用点外部关闭，按钮路径漏网**
  （验证矩阵盲区，记入教训）。
- 修复：BaseDialogFragment 的 `dismiss()` 与 `dismissAllowingStateLoss()`
  统一改走 `dismissViaDialog()`——只调 Dialog.dismiss()（粒子拦截点），
  fragment 的移除由真实 dismiss 的 onDismiss 回调走 dismissInternal
  自动补全，与点外部完全同链（该链已被历轮验证）。onDestroyView 对
  Dialog.dismiss 的重入由 particleFlowPending 与 Dialog 幂等吸收；
  dialog 为 null / 未显示时退回 super.dismissAllowingStateLoss()（保持
  不抛 IllegalStateException 的既有语义，覆盖 FableSol 导出进度框等
  直接调 dismissAllowingStateLoss 的调用点）。
- 三星验证：确定 / 取消 / 点外部三条路径录屏，dialog 区域帧间 MAD
  全程 ≤22（纯动画量级）、零突跳；确定按钮消散完整在播（从按钮
  位置起碎、chooser 橙色标题碎成橙色粒子团）。OPPO 取消按钮：消散
  完整、色相条彩虹粒子清晰。
- 发布 `202608261421`。内容色增强与本修复合并以 `b643f22f` 提交。

## 2026-08-26 第二十五轮：薄纱质感四项升级（华为动画逐帧对照）

用户给华为动画 mp4（第 5 秒起）："薄纱感 vs 我们的粗糙"+"消失从
控件几个地方同时开始"。逐帧解构（单条删除段 f058–f082、清空段
f108–f146）出四个差距因子，经用户拍板（ABCD 全上；辉光为"粒子
原色的发光版本"而非固定银色）全部落地：

- **A 波前重构（多起点）**：华为的溶解顺序由噪声等值线推进（侵蚀式，
  锋线天然大波浪、卡片多处同时起碎），我们原来触点距离主导（锋线
  始终是触点为圆心的弧）。重构：锋线扭曲改**单侧** [0, WAVE_WARP]
  且成 delay 主导项（0.18 双侧 → 0.30 单侧）、波长 0.6→1.6×noiseScale
  （192dp 大波浪）、SPREAD_TIME 0.40→0.20（触点只留整体趋势）。
  时间预算从触点项转移到噪声项：delay 上限 0.61→0.62、TOTAL
  1.335→1.345s ≈ 不变。静止层同步同款公式（擦除对齐）。
- **B 细密化**：CELL_DP 1.5→1.1 + 尺寸曲线 1.0/0.34→0.9/0.32（粒径
  共约 -34%），沙粒 → 纱。MAX_PARTICLES 150k→220k（顶点 = ×3 =
  66 万，vertex 为主要开销）。
- **C 锋线辉光**：`litRgb = mix(color, min(color*2.2, 1), 0.75*glowEase)`，
  glowEase = pow(max(1-tl*2.2, 0), 1.5)（前 45% 寿命衰减完）——起碎
  瞬间为自身颜色的发光版（深色变亮色、白保持纯白），随飘散恢复真实
  色；新 varying vGlow（highp 两侧显式）控 fragment 软边内径
  0.30→0.12（光晕柔边）。凝聚倒放下恰好表现为"凝实前沿闪光"。
- **D 浓度梯度**：fade 终点/指数按内容色权重插值——白底 (0.76, 2.0)
  跟随锋线快速转净（华为式"扫过即净"），内容色 (0.92, 1.7) 保持原
  曲线、余缕飘完全程。首轮蓝本 D 全局生效时整体消失过快（t=0.4 已
  空），改为只压低权重后恢复平衡。
- 朝触点飞散的整体位移模型、总时长、每次随机、内容色增强全部不变。
- 蓝本三轮调参定稿；三星逐帧：锋线大波浪从顶部咬入多处起碎、碎化带
  白亮细纱、白纱扫过即净、尾段彩色余缕主角；凝聚彩色烟缕悬浮于发亮
  纱雾、凝实前沿闪光。消散段 138 帧 = 动画期满 60fps 产帧（66 万
  顶点无掉帧）。OPPO：波浪锋线/细纱/彩虹带正常（Adreno）。
- 发布 `202608261455`。

## 2026-08-26 第二十六轮：双尺度锋线噪声（用户指出仍非多点起碎）

用户："并没有从多个点开始动画，都聚集在某个区域；华为明显从控件的
不同区域同时开始，区域之间可能不连通。"

- 根因：第二十五轮的锋线噪声单一波长 192dp——330dp 的对话框内只有
  一两个噪声低谷，起碎必然聚集在一处。多点起碎需要噪声在面板尺度
  内有**多个互不连通的局部谷**。
- 修复：waveWarp 改双尺度叠加——低频项（1.6×noiseScale，权重 0.45）
  保留锋线大波浪形态；中频项（0.45×noiseScale ≈ 66dp，权重 0.55）
  在面板内制造 5–8 个独立谷，并做**谷整形** `midN = 1-(1-v)²`（高值
  区抬平、低谷保留突出）：多数区域较晚碎，分离的谷显著提前冒出——
  "不连通斑块先开花、周围仍完整"的华为形态。首轮 0.55/0.45 无整形
  时分离性不足（蓝本 t=0.2 仍偏聚集），第二轮反转权重 + 整形后达成。
- 粒子层与静止层同款公式；delay 上限不变（整形抬高噪声均值只让起碎
  时间重心略后移，"先完整片刻、随后多处开花"）；总时长不变。
- 三星录屏起碎早期三帧：顶部三处 + 左侧色钮区 + 右中孤立斑 5+ 处
  互不连通同时开花、扩张合并成波浪锋线。OPPO 0.3s 撞帧：6–7 处分离
  斑块、之间面板完整。凝聚（蓝本）对称呈现斑块状多区域同时凝实、
  t=0.1 起点散云无退化。
- 发布 `202608261513`。

## 2026-08-26 第二十七轮：边缘掀起 + 掀起点加厚（用户两点反馈）

用户：起碎点太多、且有些在 dialog 内部——应从边缘起碎；华为的掀起
像"掀起一层薄纱，掀起的起点更厚"（截图佐证：起碎处粒子更浓密）。

- **边缘掀起**：delay 加 `edgeDist × EDGE_DELAY`（edgeDist = 到最近
  边的归一化距离 0~0.5，EDGE_DELAY 0.32 → 中心比边缘晚 0.16s）——
  起点只出现在**边缘与噪声谷的交集**（边缘上若干互不连通段先掀起、
  向内翻卷合并），内部不再冒点、起点数自然收敛。时间预算重排：
  SPREAD_TIME 0.20→0.06（触点趋势近乎无，只留飘散方向）、WAVE_WARP
  0.30→0.28 配平——delay 上限 0.62、TOTAL 1.345 与历版**精确守恒**。
  凝聚传 uEdgeDelay=0（保持斑块状凝实，用户对凝聚无意见）。
- **掀起点加厚**：`originBoost = 1 - smoothstep(0.08, 0.30, delayMain)`
  标记起碎最早的源头区（无论因边缘还是谷），副本门控放宽为
  `wSat < 0.5 && originBoost < 0.4` 才丢弃——源头区**白色副本也激活**
  （粒子数真实 ×3，复用既有副本管线），掀起的那股烟从源头起一路
  更浓密。originBoost 用主粒子 delay（不含副本偏移）判定。
- 粒子层与静止层同款公式（擦除对齐）；蓝本一轮定稿。
- 三星起碎三帧：只从边缘掀起（顶缘/右上角/右缘/左上角四段不连通）、
  内部完整、掀起带浓厚向内翻卷。OPPO：同形态（Adreno）。
- 发布 `202608261530`。

## 2026-08-26 第二十八轮：锋线浓带——寿命两极分化（用户指出没变厚）

用户："根本没有变厚，所有地方粒子密度一样"，附华为截图红框：锋线
弧线带上的粒子密度明显更大、其余明显更小甚至非常稀疏。

- 复盘第二十七轮的两个错误：(1) 把"厚"做在**空间固定**的源头区——
  华为的浓带是**跟随锋线移动**的属性（锋线经过哪里、哪里就是浓带），
  不是起点附近固定加厚；(2) 靠 alpha 渐降区分浓稀，对比仅 ~2:1——
  华为的对比来自**数量**（浓带满数量 vs 带后骤减）。
- 重构：
  - **寿命两极分化**：约 2/3 粒子短寿（lifeMod 0.30–0.55）——只活在
    锋线带里，组成浓带后迅速消失；1/3 长寿（0.75–1.25）稀疏飘完全程。
    门控 `h6 = fract(h3 + h4·0.618)`（与尺寸随机 h4 解耦）；内容色长寿
    概率 32%→67%（`step(0.68-0.35w, h6)`），余缕仍以彩色为主。上限
    1.25 不变、总时长不变。
  - **平台式 fade**：`1 - smoothstep(0.55, 0.95, tl)`——前 55% 寿命满
    alpha（带内满密满亮），随后快速谢幕（替换原渐进衰减曲线）。
  - **density 淡入延后**：0.05–0.35 → 0.30–0.60，浓带内不被浓淡噪声
    打薄。
  - 净效果：锋线带 = 全数量 × 满 alpha 的浓亮纱带随锋线掀过；带后
    数量 3:1 骤降成稀疏尾——数量对比叠加透明度，浓稀分明。
- 上一轮的源头白色副本保留（起点额外密度，与本轮叠加）。凝聚倒放
  呈"稀纱渐浓、凝实前沿最亮"的递进（起点为长寿粒子稀纱，不空）。
- 蓝本一轮定稿；三星逐帧：浓带随锋线翻卷、已扫区明显稀疏（f055 顶部
  已扫区 vs 中下部浓带对比强烈）；OPPO 0.55s：稀疏彩色残缕 + 局部
  浓团正常。
- 发布 `202608261543`。

## 2026-08-27 第二十九~三十一轮：量化对照华为重做揭开与浓带（用户严批）

用户连续否定："根本没看到浓带，能不能不要自以为实现了"；要求**先
量化华为、逼近数字、再辅以目检**，并给出目标模型：揭开线垂直于
飞行方向、从飞行反侧边缘沿飞行方向推进（往上飞从下揭开，"像揭
一张便利贴"）；后续补充：起始只是边缘的一段、锋线是夸张弧线
（一头密一头稀）、其他边缘同步局部掀起、**浓带必须是纯粒子构成**
（不是残余的卡片内容）、并要求在截图中明确指认浓带。

- **量化华为**（block_std 纹理剖面，清空段 f106–f146）：浓带宽 ≈30%
  跨度、峰/尾（已扫区）密度比 **4–8**、单向扫过 ≈0.44s。我方旧版同
  口径实测比值 **1.0–1.2**（整面板满铺、无带）——证实用户观察。
- **三轮重构**：
  1. 单向揭开：delay 主项 = 沿飞行方向的投影 proj（uSweepDir 由
     Renderer 从虚拟触点算并归一，uSweepTime=0.35s；凝聚传 0 退回
     斑块凝实）。触点距离项废弃（SPREAD_TIME=0）。
  2. 大振幅弧线：WAVE_WARP 0.22（单侧）、波长 2.2×noiseScale——
     起始只从边缘的一段（弧线谷先头）掀起、弧线一头密一头稀；
     localEdge 项（边缘 15% 内 × 噪声谷 × 仅消散）让其他边缘同步
     局部掀起。jitter 0.05。delay 上限 0.62、总时长 1.345 守恒。
  3. **带内副本**（浓带的数量来源）：所有粒子 tl∈(0,0.30) 时副本
     激活——粒子数真实 ×3 聚在揭开线附近成密亮带，白色副本在
     tl 0.18–0.30 淡出还回单颗；叠加辉光、带内尺寸 +35%、ease
     1.35→1.6（带内滞留堆积）。
- **弯路与教训**（两次被用户打回）：(a) "整卡颗粒态"方案——把
  "同步细碎化"误解为内容颗粒化，整卡变噪点图；用户明确：浓带是
  **粒子群**形成的、未掀开区域保持清晰内容——方案删除。(b) 浓带
  检测算法把卡片本体标成浓带（band_mark 第一版红框套在完整卡上）
  ——检测必须先分割本体（与模板逐像素比对）再在粒子区找带。
- 附带修复：蓝本 `set_still` 的 spread/jitter/warp 一直读全局常量而
  非场景覆盖值——凝聚场景两层 delay 参数不一致（擦除错位、静止层
  全黑）。Android 端无此问题（uniform 均从 spec 传）。寿命两极加
  bipolar=step(0.001,uSweepTime) 门控：仅消散生效（凝聚倒放下短寿
  造成"既无粒子也无静止层"的黑洞）。
- **量化验收**（可指认证据）：真机 f038 标注图 dev_band_final2——
  红框（暗背景纯粒子密集带）亮点密度 0.608 vs 蓝框（已飘散区）
  0.104，**比值 5.9 ∈ 华为 4–8**；蓝本 band_mark3 带/已飘散数量比
  6.2–29.2。形态：起始只有边缘一段（f026 右下角）、揭开线弧线推进
  （f038 右高左低）、浓带随线移动。OPPO：方向与浓带正常（Adreno）。
- 发布 `202608262225`。

## 2026-08-27 第三十二轮：白底抽稀——浓稀对比内生（用户提议）

用户洞察："浓带之所以浓是因为别的地方稀疏"——推测华为的通知卡是
大部分透明的毛玻璃，透明区的粒子少，浓稀对比由此内生；提议对不透明
控件等效：许多区域直接少转化粒子露背景、边缘与彩色区多转化。

- **量化证实假设**：华为通知卡毛玻璃底不透明度 **α ≈ 0.26**（卡区
  亮度 136、壁纸 95、按 card = wall(1-α)+255α 反推）——透明底区的
  粒子每颗只带 1/4 强度的白、近乎不可见，可见粒子天然集中在内容与
  边缘。浓稀对比是**内容透明度分布的直接映射**，用户判断正确。
- **实现**（用户方案）：`keep = max(0.30, w, keepEdge)`——本体色格子
  仅 ~30% 转化为粒子（其余在被揭开瞬间 degenerate、直接露背景），
  内容色（w）与几何边缘带（keepEdge = smoothstep(0.10, 0.03,
  edgeDist)）全保留并叠加既有副本增密。抽稀判定用主 hash 的第二
  随机数 h2m（主粒子与副本判定一致）；仅消散生效（uSweepTime 门控），
  凝聚不抽稀（要凝实成完整面板）。静止层不涉及（未揭开区完整显示）。
- 蓝本量化：带/已飘散粒子数量比 **19.8–122.8**（上一轮 6.2–29.2 再
  上台阶）；彩色团饱满（全保留+副本）、白底稀疏、揭开边缘沿线密集
  ——浓稀对比结构性产生，不再依赖参数硬调。凝聚回归无影响。
- 三星真机逐帧：右下角一段先起碎、揭开线弧线推进、贴边缘密集碎屑带
  + 色钮彩色浓团 vs 已扫区稀疏露背景。
- 发布 `202608270010`。

## 2026-08-27 第三十三~三十四轮：悬浮粒子场 + 弯曲断续浓带（用户两轮修正）

用户修正一："稀疏露背景中的背景是指 dialog 底下的内容"、且露背景要
在**动画开始的时候**发生；并要求每轮交付都要在帧上标出浓带与稀疏带。
用户修正二：浓带太规整（直来直去，华为是弯曲的）、每条边不要同样
处理、一条边内各段也不要一样。

- **全局快速碎化**：tShatter = shatterN(噪声)×0.10 + h1m×0.05——动画
  开始 ~0.15s 内整卡零星碎化为**悬浮粒子场**：白底 keepBase =
  0.06+0.12×噪声（抽稀率随空间起伏）格子直接消失、露出 dialog 底下
  的界面；保留粒子原位噪声漂浮（floatDrift ±2.4cell 缓慢）、高权重
  副本悬浮期也显示（按自身随机错开成小簇）；静止层同步在 tShatter
  擦除。揭开线扫到才掀起飞散。凝聚 tShatter=delay（mix 门控，无此
  阶段）。
- **浓带定义最终版**：边缘与内容色转换出的**密集粒子群**（w 含
  0.85×edgeBoost；w≥0.78 的粒子副本全程 ×3——黑字 0.62 除外，用户
  裁定黑字不增量）。
- **弯曲与断续**：edgeWave（噪声 ±0.08 摆动边界线位置——带呈波浪
  而非直线）× edgeMod（沿边低频强度调制 smoothstep(0.22,0.72,噪声)
  ——同一条边有的段浓、有的段稀、有的段断开；四条边对噪声采样不同、
  彼此表现各异：真机可见左缘先有右缘后现）。
- **交付纪律**（本两轮教训沉淀）：浓带/稀疏带的标注帧是每轮验收的
  必交付物；自动检测两次把卡片本体标成浓带（先分割本体再在粒子区
  找带）、横向窗口套竖直带（带的方向要先目检）——检测口径本身要
  先验证框住的是什么。
- 真机 f033 手动指认量化：顶缘浓带亮点密度 0.376、右缘 0.327、稀疏
  悬浮场 0.042——**比值 8.9 / 7.7**（华为 4–8 区间上沿）；弯曲（顶缘
  波浪下垂）、断续（左缘段落消失）、四边各异（右缘后期才现）逐帧
  确认。蓝本 v34：边缘带断续不均、掀起弧线弯曲。
- 发布 `202608270235`（第三十三轮中间态 `202608270010` 已被覆盖）。
- 全部成果（5 个新组件、设置项与 13 语言、styles/Def/Base 改动、功能
  文档）以 `93d9948d` 提交（用户指示，spatial 相关工作区改动未纳入）。

## 2026-08-27 第三十五~四十一轮：局部帷幔 / 破碎轮廓 / 碎化即飞散

用户三轮反馈依次修正：

1. **"两段感"**（碎化 → 静止 → 消逝）：根因是第三十三轮的悬浮态——粒子
   碎化后原地漂浮，要等揭开线扫到才起飞。删除该阶段：粒子在自己的 delay
   碎化并立即飞散；"动画一开始就露出 dialog 底下的界面"改由**抽稀格子
   提前从静止层擦除**（delay × 0.15）承担，两个过程重叠。真机逐帧变化量
   曲线单峰无低谷 = 无静止分割线。
2. **"轮廓一直方方正正、总从一条边出发"**：粒子场外边界恒等于卡片边界，
   因为边缘格子 w=0.85 全保留、永不抽稀。改：边缘对 w 的贡献 0.85→0.10
   （不再豁免抽稀），并把 localEdge 换成 **edgeErode**——四周边缘 13% 内
   同时向内侵蚀，侵蚀边界受噪声 ±10% 摆动、强度沿边低频起伏（四条边各异、
   一条边内各段各异）。轮廓在动画早期即碎成参差曲线。
3. **"浓带要是一条边上的一部分形成的帷幔弧、要夸张"**：
   - **局部帷幔**：沿揭开起始边按 `curtainCenter`（0.2–0.8 随机）与
     `curtainHalf`（0.13–0.20）选出**一段**（占边长 26–40%），段内
     `keep=1`（全保留）+ 副本全程 ×3 + 尺寸 ×2；`curtainArc`（0.07）让
     段中心先掀、两端拖后，形成下垂帷幔弧。
   - **揭开线大弧**：`ARC_BOW`=0.20，中间比两侧早揭开——往上飞呈波峰、
     往下飞呈波谷；弧心 `bowCenter` 每次动画随机轻微偏移（不对称）。
   - 时间预算重排：sweep 0.24 + arc 0.20 + warp 0.14 + jitter 0.04 = 0.62，
     TOTAL_DURATION 1.345 守恒。
   - **闪现浓带**：抽稀格子不再完全丢弃，而是在揭开线扫到的瞬间以极短
     寿命（0.07）闪现——浓带是全格子密度、带外只剩保留粒子（3–9%），
     浓稀对比来自数量本身。
- **排障**：静止层 `uPanelColor` 的 uniform 声明被早前一次批量文本替换
  误删（模式因先前改动失配、且未加命中校验），shader 编译失败 → 动画整层
  降级淡出，真机表现为"面板整体变暗、完全没有粒子"。加 catch 日志一轮定位。
  教训：**GLSL 的批量文本替换必须 assert 命中数**，静默失配会退化成整层降级。
- 三星验证：f008/f009 面板下部**局部一块**不规则起碎（其余仍完整）→ 整卡
  稀疏透出底下界面 → 帷幔聚成明亮浓带飘起、轮廓不再方正。凝聚由
  `step(0.001, uSweepTime)` 门控全部新项，形态不变（斑块凝实 → 完整面板）。
- 发布 `202608270402`。

## 2026-08-27 HarmonyOS 7 参考视频与现有实现差异复盘

用户提供 HarmonyOS 7 屏摄视频，并明确目标：Dialog 从若干边缘逐步粒子化，形成多条
局部高密度帷幔；当前多轮实现仍缺少轻盈、灵动、优雅感。本轮先按
`grill-me` 流程完成证据分析，尚未改动运行时代码。

- 逐帧定位参考视频清空段约为 6.45s–7.30s，主体可见约 0.8s。观察到多条非连通、弯曲、
  会移动的高密度锋线；相邻粒子早期保持片层连续，后期才松散；终端趋势稳定向上。
- 对照当前真机录屏与 Shader，确认核心差异是运动模型而非单一参数：`curtain` 只是初始
  坐标上的保留率/副本/尺寸掩码；全角度随机早期方向、速度差、
  Curl Noise、独立抖动、`pinch`、`flare` 同时介入，破坏邻域相干；白底仅保留 3%–9%
  并让其余粒子极短闪现，形成网格针孔和白色团块。
- 复核桌面模拟器：固定单一种子、关键早期帧采样不足、只输出静态接触表、Android 与
  Python 各维护一份 Shader、场景与 density 单一；历史峰/尾密度比只验证亮度差，没有
  验证帷幔曲率、移动、邻域速度一致性和末段主方向一致性，因此指标达标不等于形态接近。
- 公开资料交叉结论：华为官方把粒子动画拆成发射器、生命周期、速度/加速度和扰动场，
  并强调“力”的秩序感；Curl Noise 适合共享的连贯流场，但不能抵消全角度独立初速度；
  Telegram Android 同类实现用持续向上加速度，验证 GLES 路线可行。华为未公开本效果
  源码，具体算法只作视频驱动的实现推断。
- 推荐保留快照、TextureView、EGL 和 GPU 粒子架构，重写消散模型：3–5 个局部边缘种子、
  沿本次触点方向的稳定终端力、按帷幔 ID 共享低频位移、前 120–200ms 保持邻域相干、个体扰动后置，
  以小粒子低 alpha 覆盖率替代大规模删点和三副本过曝；模拟器改为共享 Shader、60fps
  视频、多种子/宽高比/density 验证。
- 完整分析与公开来源记录在
  `research-2026-08-27-harmonyos7-effect-gap.md`。当前进入逐项确认产品取舍阶段；首要取舍
  是出现动画是否与消失动画解耦，建议先锁定消失效果，再另做独立凝聚模型。

用户随后明确纠正方向假设：参考视频中的上扬只代表该次效果，完事儿应继续让帷幔和尾流
朝触点方向运动——触点在下方则向下掀起，在左侧则向左掀起。已将“固定向上”从方案中
删除；方向跟随本身不是差异根因，重构重点仍是局部锋线、片层相干和渐进解体。

用户进一步指出：参考视频最终向上飞，但最早掀起点在下方，且几个掀起点近乎同时出现；
这否定了“朝向触点的近侧边缘优先起碎”。重新抽取原始 60fps 起始段确认：下方通知卡先
出现可见粒子化，其他左侧/较上方缺口在随后约 6–8 帧（100–130ms）内出现，感知上是
多起点近同时启动，而非单锋线逐卡扫描。方案修正为：首批 3–5 个种子偏向运动反向的
上游/尾侧，在 0–100ms 短窗口内错时激活，再共同沿触点方向形成帷幔和尾流。

用户确认无触点关闭默认向上，并特别强调上段规则中的“主要”：起点多数位于飞行反向一侧，
但不能全部位于该侧；侧边和少量顺向一侧也需有机会生成局部起点。该要求已同时写入决策、
偏好和调研文档，后续应实现为受约束的分布偏置，而非反向半平面的硬筛选。

用户确认允许受控随机：每次掀起点位置、帷幔弧度和横摆可变化，但主方向、起点概率偏置、
近同时激活和早期邻域相干是硬约束。后续模拟器需以种子矩阵验证随机结果，而不是只调一个
视觉上恰好成功的种子。

用户确认取消动画开场整卡全局打孔：未被任何局部帷幔触及的区域保持完整、清晰，只在
多条帷幔实际推进到的局部逐步露出 Dialog 底下内容。后续实现需删除 3%–9% 普通区域
保留率与被剔除粒子极短闪现的组合，并让静止层与粒子层共享同一多种子激活场。

## 2026-08-27 第四十二轮：多起点连贯帷幔模型

在关键产品取舍确认后，不再继续叠加旧模型参数，重写消失 Shader；出现动画与消失动画
正式拆成两条渲染路径，出现继续使用原凝聚 Shader。

- **五个受约束边缘种子**：3 个围绕运动上游侧、1 个侧边、1 个顺向侧；各自位置、段长、
  弧度受控随机，启动压在约 0–100ms。实现层面明确保证“主要但不是全部在反向侧”。
- **共享激活场**：粒子层与静止层直接插入同一个 `DISMISS_FIELD_GLSL` /
  `curtainActivationField`，静止快照只在局部锋线经过时约 32ms 交接给粒子；删除整卡提前
  擦除、3%–9% 普通区域保留率和 culled 极短闪现。
- **片层后解体**：同一 curtain ID 的粒子前段共享主方向推进、低频折叠和受整形 Curl
  流场；个体横向漂移与微抖动在 localTime 0.34–0.82 才逐渐加入。删除全角度 earlyDir、
  `pinch`、`flare` 及大速度离散，不再从第一帧变成砂砾漏斗。
- **覆盖率而非副本**：消散每格仅一个粒子，前沿由完整采样覆盖率和生命周期窗口增密；
  凝聚仍保留既有 3 副本，出现观感不随本轮变化。点尺寸、透明度和轻微沿速度拉伸共同
  形成细密帷幔，原内容颜色只做温和提亮。
- **方向与时间**：Controller 精确使用中心到触点的方向，不再随机 ±18° 旋转；无触点仍
  向上。总时长改为 1.0s，0.88–1.0s 全局收净尾粒子。
- **canonical 桌面验证**：新增 `render_curtain_model.py`，从 Kotlin 提取实际 Shader，转换
  GLSL ES 3.00 → GLSL 3.30 后编译；生成单次 60fps、12 种子矩阵、四方向矩阵和参考并排
  视频。旧 Python/HTML 模拟器标记为历史，避免双份 Shader 继续漂移。
- **验证**：`:app:compileDebugKotlin` 与 `:app:testDebugUnitTest` 通过；桌面 canonical GLSL
  编译和离屏渲染通过。12 个种子均保持多起点和受约束分布，四个方向均正确旋转整套锋线
  与尾流。按仓库规则未使用 ADB，真机材质与性能留给发布后的人工目检。
- **发布**：调试版本 `202608270727`；本地 APK、远端元数据与重新下载文件的 SHA-256
  均为 `0300e305cdc1fcc19c56e732876b82ca10201b7c0d77025fb8e89de046545afe`。

## 2026-08-27 第四十二轮修正：恢复真实内容色强调

用户检查桌面并排视频后指出粒子重新以白色为主，并提醒此前提交已专门实现内容色强调。
按 `diagnose` 流程用 canonical Shader 原始离屏粒子层建立确定性反馈：测试快照高饱和区域
占 14.79%，当前粒子阶段高饱和 alpha 占比仅 19.71%，强调倍率只有 **1.33×**，复现了
“代码保留原色但视觉上仍被本体色淹没”的问题。

- 单变量探针：关闭早期提亮仍为 1.33×；仅恢复尺寸/寿命权重为 1.40×；仅恢复高饱和
  内容色的两个副本为 2.76×；副本与尺寸/寿命合并为 **2.82×**。根因确定为重写消失
  Shader 时把旧版“彩色专属副本”误同白色带内副本一起删除，而非视频编码或取色错误。
- 修复：消失恢复 `DISMISS_REPLICAS=3`，但两个副本仅在饱和度权重 ≥0.5 时存活；白色
  面板和黑字仍只有主粒子。副本与主粒子共享激活场和前段运动，只切向错开 <1 cell，
  release 后才由独立 hash 产生轻微差异；内容色同时恢复更大的点尺寸和更高长尾概率。
- 回归检查：新增 `check_color_emphasis.py`，直接编译 Android canonical Shader，以 4 个
  种子 × 4 个时刻累计高饱和 alpha，要求强调倍率 ≥2.0×。修复前确定性失败 1.33×，
  修复后通过 2.82×。
- 验证：重新渲染单次、12 种子和四方向 60fps 视频；彩色控件形成蓝、绿、橙、紫粒子团
  与尾流，多方向帷幔结构仍正常。`:app:compileDebugKotlin` 与
  `:app:testDebugUnitTest` 通过，未使用 ADB。
- 发布调试版本 `202608270805`；本地 APK、远端元数据与重新下载文件的 SHA-256 均为
  `ee8c7b07ddf8e364fc82bc4e0d2169a677b8afa71b2c3b83bd2dfaa17499f1e0`。

## 2026-08-27 HarmonyOS 7 粒子立体感复盘（grill-me 进行中）

用户提出参考动画具有 3D/空间感，而第四十二轮仍显平，并询问透明通知卡是否为原因。
逐帧复核 6.75–6.95 秒、检查当前 Shader 并查询华为官方沉浸光感与粒子文档后，得到初步
结论：透明与背景模糊确实建立了卡片/底层界面的层级，但不能单独解释粒子带自身的亮边、
暗芯、厚度变化、前后遮挡及尺度/速度分层。

- 官方设计语言把沉浸光感描述为同时整合材质的光学行为、空间属性与交互响应，并列出
  光随指动、光线勾勒、非线性形变；这支持“透明材质 + 光照 + 形变”的组合解释，但
  HarmonyOS 未公开该通知动画 Shader，具体算法仍为视频驱动推断。
- 屏摄中的白色反射光斑、曝光 bloom、失焦、手机透视必须从目标中剔除；它们会额外放大
  参考视频的体积感。
- 当前消失 Shader 只有二维 `positionPx`、统一 alpha 混合和方向椭圆点，没有伪深度、
  稳定前后层、由片层梯度得到的法线/亮边；不透明测试 Dialog 也缺少半透明材质中间态。
- 推荐候选是受约束的 2.5D 片层：连续伪深度仅小幅影响投影尺度/速度/alpha，片层坡度
  驱动原色明暗，按后层→主体→前层稳定绘制，再用低 alpha 本体色让底层参与混合；不做
  无约束的朝镜头散射。详细证据已补充到调研文档第九节。

下一项需与用户确认：目标应是“有厚度的半透明片层在空间卷曲”，还是粒子明显向屏幕
内外飞散；建议前者，因为它更符合参考帧并能保留轻盈感。

用户选择前者：立体感采用连续的半透明 2.5D 片层；明确排除逐粒子随机 `z`、强透视和
向镜头爆散。下一步继续确认透明度只作用于已激活片层，还是提前改变未触及的 Dialog。

用户确认透明材质只作用于已经局部激活的粒子片层；未触及区域保持完整、不透明。面板
本体色粒子降低 alpha 以混合底层，内容色粒子保持更高不透明度。下一步确认伪深度的
视觉强度。

用户选择中等空间强度作为首版，并要求保留 2.5D 总开关和参数调节能力，等真机效果决定
是否增强。代码复核发现现有正式设置“对话框粒子动画效果”只控制无动画/仅出现/仅消失/
二者均启用四种播放模式，`ParticleDismissSpec` 尚无空间层次参数，也没有独立深度调节
界面；下一步需确认新开关和参数只在调试阶段开放，还是长期加入正式用户设置。

用户随即撤销开关与参数调节要求：不新增 2.5D 设置、调试入口或运行时调参能力，直接以
固定的中等强度实现；现有粒子动画播放模式设置不变。上一段关于调节能力的记录由本段
取代。

用户提出以触点作为光源，并要求系统返回操作关闭时改为向右上方消逝。技术判断是触点
可以作为光源的平面位置，但应在伪深度方向把光源抬到屏幕前方，并采用有限半径和软化
衰减，避免触点附近出现点光源奇点；明暗仍以原内容色为基色。代码检查同时发现，当前
`GestureAnchoredDialog` 只保存最近一次触点并在所有 `dismiss()` 路径复用，无法区分返回、
自动关闭和触摸关闭，返回操作甚至可能误用旧触点。实现右上规则前需要增加明确的关闭
来源或可靠地使返回路径覆盖动画锚点。

用户确认把右上规则扩展到全部无触点关闭：返回、异步自动关闭和代码主动关闭共享右上
虚拟触点，并由该点同时驱动运动方向与光照位置；只有直接造成当前关闭的真实触点才使用
真实位置。后续实现需要废除无时效、无来源的 `lastTouch` 复用方式。

用户确认无触点的右上方向以竖直上行为主：从正上方向右偏约 30°，而不是 45°。虚拟
触点与虚拟光源均沿该方向设置。

讨论真实触点接近 Dialog 几何中心的退化情形时，用户质疑设置大范围中心回退区的必要性。
代码复核发现确有少数点击中部内容后立即关闭的路径，例如 `DoingDigitStyleDialogFragment`
的样式行和 `ThreeActionsAlertDialogFragment` 的中间选项；但这些入口只说明触点可能接近
中心，不足以证明应把约 40dp 范围内的真实方向改成默认方向。建议删除宽阈值，仅在向量
长度接近数值零、无法归一化时使用右上回退，其他真实触点始终严格决定运动方向。

用户确认上述修正：删除约 40dp 中心回退区，任何可归一化的真实触点都严格决定运动方向；
仅数值零向量回退右上，光源仍留在真实触点。

用户确认空间层次的时序：伪深度和触点光照在局部激活后迅速建立，于高密度帷幔相干阶段
达到峰值，随后随个体粒子解耦而衰减，末段仅保留轻微层次。至此本轮 `grill-me` 的关键
产品取舍已收敛，可以进入实现。

实现过程中用户进一步指出当前不同区域的粒子密度近乎一致，而参考效果在揭开帷幔处
明显更密。代码核对确认 `density = 0.88 + 0.11 * noise` 只改变透明度，实际 cell 占用率
一致；用户观察成立。实现方案补充为局部年龄驱动的真实数量差：帷幔阶段全采样，尾流以
稳定 cell 哈希抽稀，面板色约保留 22%–30%、内容色约 45%–60%，三层共享抽稀掩码。

## 2026-08-27 第四十三轮：连续 2.5D 片层、触点光源与真实密度差

在 `grill-me` 取舍收敛后完成实现，未新增设置项、调试入口或运行时调参界面。

- `GestureAnchoredDialog` 不再长期保存 `lastTouch`。只有 `dispatchTouchEvent()` 调用栈内
  同步发生的关闭才携带真实触点；返回、异步完成和代码关闭不会误用历史坐标，统一使用
  从正上向右偏 30° 的右上虚拟触点。真实触点除数值零向量外始终严格决定运动方向。
- `ParticleDismissSpec` 新增本次虚拟柔光源的平面坐标。真实触摸关闭使用真实触点；无触点
  关闭使用右上虚拟目标。Shader 把该 XY 与固定的屏幕前方伪深度组合成无奇点柔光源。
- 消散既有三个顶点副本在单次 draw 内重排为后层→主体层→前层，不增加顶点总数。三层
  共享由帷幔折叠场产生的连续伪深度，深度以约 ±12% 调制速度、点尺寸、透明度和小幅
  投影视差；曲面斜率生成法线，触点光源产生原色明暗与克制中性边缘光。
- 深度和光照在局部激活后快速建立，于高密度相干阶段达到峰值，release 后降到约 16%；
  未激活区域仍由完整快照绘制。面板色材质更通透，内容色以更高 opacity、尺寸和寿命保持
  颜色身份。
- 按用户实现中途反馈增加真实密度差：局部 age 0–120ms 全采样，120–220ms 过渡；尾流
  面板色保留 22%–30%，内容色保留 52%–60%。逐 cell PCG 抽稀由三层共享，不以亮度替代
  数量，也不破坏空间厚度。
- canonical 模拟器新增三层独立输出、连续伪深度色、密度阶段色与相反光源 A/B；右上
  默认方向改为 -60°。回归结果：内容色强调 2.48×（要求 ≥2.0×），back/main=0.502、
  front/main=0.761，深度方差从帷幔阶段 0.0324 降至尾段 0.0171，帷幔/尾流视觉覆盖率
  3.34×，相反光源 RGB 差 1.966。
- 输出重新生成 `primary-up.mp4`、`seed-matrix.mp4`、`direction-matrix.mp4`、
  `spatial-diagnostics-up.png` 和最新 `reference-vs-model.mp4`。
- 验证：桌面 GLSL 3.30、原生 GLSL ES 3.00 均编译通过；`:app:testDebugUnitTest` 与
  `:app:assembleDebug` 通过。按规则未使用 ADB。
- 发布调试版本 `202608270932`。第一次发布 `202608270931` 时发现 Gradle 只嵌入日志第一个
  二级章节，远端说明缺少实现与验证；调整同一发布日志的标题层级后重新发布，最终 debug
  渠道由 `202608270932` 取代。最终本地元数据、远端元数据与重新下载 APK 的 SHA-256
  均为 `74ebe1ed22673490ce2313b5b8dca7ae37e6cc48bf644f9bc4583dfd9d72b7b9`，文件大小匹配，
  远端说明确认同时包含实现与验证章节。

## 2026-08-27 第四十四轮：增强相干回卷与运动视差

用户真机反馈第四十三轮空间感仍不明显，并提出参考效果可能主要依靠粒子群的曲折轨迹、
整体形状、密度变化与 z 轴移动共同形成近似 3D，而不只是透明材质或三层尺度差。本轮重新
逐帧和量化对照参考/当前视频，并补充华为官方资料与视觉感知研究后再增强实现。

- 逐帧复核参考 6.44–7.40 秒：最强空间线索是多条浓带持续形成拱形、半环形与斜向回卷，
  同一条带的外缘、内侧和尾部具有不同速度、密度与覆盖关系；透明卡片提供背景层级，但
  不能单独解释粒子阶段的立体感。
- 官方资料与视觉研究交叉结论：HarmonyOS 将光学材质、空间属性和交互响应统一表达；
  kinetic depth、motion parallax 与 dynamic occlusion 研究则说明连续形变、层间相对位移
  和覆盖/显露足以在单目二维画面中形成三维结构知觉。华为没有公开通知消散 Shader，
  因此本轮只按视频证据和公开原理实现，不声称复原其内部算法。
- 代码诊断发现第四十三轮三层仍近似平行：伪深度主要只改变约 ±12% 的速度/尺寸并产生
  小幅固定投影，`foldDrift` 与 z 波接近同相；有深度变量，但缺少绕锋线运动的投影轨迹。
- `curtainActivationField()` 现额外输出每个粒子所属种子段的局部锋线法向。同一帷幔把局部
  年龄映射为回卷角，屏幕位移使用 `1-cos`、伪 z 使用 `sin`；较老的浓带粒子沿反法向卷回
  已揭开区域，再由全局方向带走。三层厚度沿回卷法线旋转，不再是平行复制。
- 深度尺度/速度视差提高到约 ±24%/±20%，伪 z 的屏幕投影与层法向位移同步增强；法线
  光照仍只调制快照原色，未增加中性白色染色。完整采样浓带扩大为约 0–150ms，至 260ms
  过渡为稀疏尾流，使回卷截面有足够宽度，内容色继续获得更高尺寸与 opacity。
- canonical 回归当前结果：内容色强调 2.07×，back/main=0.425、front/main=0.869，帷幔/
  尾段深度方差 0.0296/0.0173，帷幔/尾流覆盖率 4.08×，三层联合支持区 2.51×，前后层
  质心视差 14.81px，相反触点光源平均 RGB 差 14.265。12 个种子与上/下/左/右上方向
  60fps 蓝本均已重新生成，主方向未失控。
- 新增 `ParticleDismissShaderCompileTest`，直接从 Kotlin 展开共享 GLSL，并用 Android SDK
  内的 `glslangValidator` 编译四个 GLES 3.00 Shader；单测已通过。未使用 ADB。
- `:app:testDebugUnitTest` 与 `:app:assembleDebug` 完整通过。随后按 debug 更新规则发布
  `202608271008`，本地元数据、远端 `latest.json` 与重新下载 APK 的 SHA-256 均为
  `5d29f3fa55a9a1ae1d8d6603a62fd441c8b5025f351eafa020af6f094b786cbb`，三者文件大小均为
  24,404,408 字节；远端发布说明与本地完全一致，并包含实现和验证段落。

## 2026-08-27 第四十五轮桌面迭代：方向性消融、柔边与窄密度帷幔

本轮继续以不透明控件参考视频做归一化生命周期对照，暂不发布 Android 调试版本。

- 修正左上对比中目标角停留：确认顺向种子原先落在上边中部，移动到几何角后又落入圆角
  透明像素；最终把小出口沿角平分线内移到不透明内容，使 0.10–0.16 秒已有粒子越过左上
  边界，同时限制其传播范围，不取代右下主揭开。
- 激活场加入运动轴方向性 delay 并降低局部噪声幅度。左上场景 0.16 秒右下/左上剩余
  表面 alpha 约为 0.42/0.96，0.24 秒约为 0.12/0.91；其他区域仍可由侧边和顺向种子
  近同步起碎，但宏观进度稳定从右下向左上推进。
- 静止层按真实片元位置采样连续细尺度 delay，表面与原位细粒在约 -28ms 至 +105ms
  交接；主要阶段孔洞中间 alpha 过渡宽约 5–9px，不再由整 cell 二值切出锐边。
- 区域回归发现旧传播在 1 秒结束时仍可能保留约 27% 左上表面；改为等比压缩完整传播场，
  0.96 秒剩余约 1.6%，避免 Overlay 收尾时整块跳失。
- 用户进一步指出浓带与其他粒群连续观感仍近似等密。旧 200–380ms 高密窗口使测量帧
  约 71.9% 的可见点都属于高密阶段，旧覆盖率指标因此误判。新增固定 1px、只统计主体层
  存活 cell 的真实点中心回归，并约束高密点占比。
- 高密窗口收窄为约 68–175ms；高密带保留 88%–100%，面板尾流 20%–32%，内容色尾流
  62%–74%。当前真实点局部密度比约 2.30×，高密点占比 34.7%，内容色强调 3.47×。
  深度仍在高密阶段建立并于尾流衰减，back/main=0.402、front/main=0.899，帷幔/尾流
  深度方差 0.0319/0.0240。
- 新增 `analyze_model_activation.py` 和 `make_reference_comparison.py`；重新生成左上归一化
  并排视频、接触表、种子/方向矩阵和空间诊断图。区域、颜色、空间层次回归均通过。
- 曾在用户追加密度反馈前执行一次本地 `testDebugUnitTest` 与 `assembleDebug`，只用于 GLES
  编译验证；没有提交、上传或发布 APK。密度版本按用户要求停留在桌面观感确认阶段。

用户随后授权把该版本放到 Android 实机查看。最终密度版本重新通过
`:app:testDebugUnitTest` 与 `:app:assembleDebug`，其中 `ParticleDismissShaderCompileTest`
覆盖四种展开后的 GLES 3.00 Shader。按 feature debug 更新流程发布 `202608271156`；本地
APK、远端 `latest.json` 及重新下载 APK 的 SHA-256 均为
`d433bde593143ae9ec0ba3c020b1100ac3d07abc03574310fd9f9f038a084b4f`，文件大小均为
24,404,408 字节，远端发布说明包含密度实现与验证内容。未使用 ADB，下一步由用户实机
目验窄帷幔、稀疏尾流、柔和孔洞边缘和右下向左上的整体消融时序。

## 2026-08-27 第四十六轮桌面迭代：显示尺度可读性、阶段重定时与内容色统一

用户以归一化 0.32、0.47、0.70 三张并排截图指出：模型的早期粒子群在最终显示尺度下
不够清楚，帷幔浓带缺少明显的亮度和密度峰值，且完整 Dialog 消融完成得过晚；随后又
指出彩色控件粒子像是游离在整体粒群之外。本轮按 `diagnose` 流程先建立确定性失败回归，
再修改 canonical Shader。

- 原有粒子在离屏原分辨率下数量足够，但缩到并排视频的 480×344 后接近单像素，软边、
  低 alpha 和编码共同把点群压成灰雾。激活回归因此新增最终显示尺度采样，不再用原始
  分辨率的总 alpha 代替肉眼可读性。
- 旧高密窗口虽然真实 cell 较多，却与静止表面交接重叠，且单点过软，无法形成明亮的窄
  条带。当前高密段提高原色曝光、点核心和材料不透明度，尾流仍保持稀疏；0.47 时条带/
  尾流的局部光学密度 P95 为 0.1476/0.0696，约 2.12×，非零 alpha P95 为
  0.3373/0.1725。
- 旧回归只约束 0.96 收净，而且早期分析曾把高密粒子误算成完整表面。当前独立读取静止
  表面层并压缩激活时序；0.70 时完整表面平均残留 alpha 约 0.000015，0.90 为 0，粒子
  仍由随全局时间增强的风场继续输运和统一淡出。
- 内容色不再改变寿命、局部时钟、尺寸、opacity 或深度响应，只提高尾流真实 cell 保留率。
  固定 1px 点中心回归显示，内容色源区域占 18.17%，尾流中占 41.45%，真实数量表示倍率
  为 2.28×；颜色 alpha 表示倍率为 1.70×，强化颜色存在感而不形成独立运动层。
- 重新生成 `reference-opaque-vs-model-left-up.mp4`、接触表、主输出、种子/方向矩阵和空间
  诊断图。激活、显示尺度、密度、颜色和空间层次回归均通过；
  `:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过。未使用 ADB，也未发布新的调试版本。

## 2026-08-27 第四十七轮桌面迭代：从静止白边中分离真实帷幔

用户检查第四十六轮截图后指出，所谓帷幔仍可能只是尚未粒子化的白色 Dialog 区域，并
要求继续修改。本轮再次按 `diagnose` 流程建立针对该误判的分层回归。

- 新回归单独渲染 still layer 与 band particle layer，再按并排视频最终尺度计算静止表面
  外侧的法向密度剖面。旧版本即使全局 band/tail 光学密度约为 2.12×，纯粒子峰仍位于
  表面外约 2px，6–16px 区域只有贴边粒雾的 0.80×，确定只是白边附近的模糊过渡。
- 单变量探针确认主因是高密阶段在 `activeAge≈0` 即达到峰值，此时 handoff 尚未完成。
  单纯增加局部外法向位移只能把粒云推远，最大探针把峰值移至约 30px，反而脱离锋线；
  因此移除该探针，不把额外位移当成正式修复。
- 高密权重现于局部出生约 45–85ms 建立，135–180ms 释放。现有回卷与风捕获会在此时把
  已完成交接的粒子自然带离表面，形成独立弯曲带；无需给静止层描边或增亮。
- 带内粒子使用较硬核心，单粒子材质 alpha 降回可混合范围；亮度主要来自高密真实点的
  聚集。面板尾流保留率降至约 13%–23%，强内容色最高约 85%–92%，继续以真实数量强化
  颜色而不改变动力学。
- 高密窗口内的粒子进一步按局部年龄向共同回卷截面聚拢，改变的是真实屏幕位置而非粒径。
  最终 `t=0.47` 纯粒子密度峰位于表面外约 14px，6–16px 浓带相对 0–4px 贴边粒雾为
  4.93×、相对 24–44px 外侧尾流为 4.01×；高密粒子覆盖完整表面的质量占比约 0.31%。
  固定 1px 点中心回归为帷幔/尾流 2.09×，高密点占 30.6%，内容色数量表示 2.54×。
- 桌面模拟器新增 `curtain-separation-comparison-left-up-047.png`，同页展示普通合成、仅
  未粒子化表面、仅全部粒子、仅高密帷幔粒子和仅稀疏尾流，后续不得再以合成白边冒充
  帷幔。归一化并排视频、主视频、种子/方向矩阵和空间诊断重新生成。
- 最终激活、法向浓带、颜色、真实点密度、空间层次和运动诊断通过；
  `:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，其中四种展开后的 GLES 3.00
  Shader 均编译成功。未使用 ADB，未发布新的调试版本。

## 2026-08-27 第四十八轮桌面迭代：加密独立帷幔与同群内容色

用户确认第四十七轮的修复方向后，要求帷幔稠密部分进一步加密，并让有色控件产生更多、
更突出的原色粒子。本轮继续修改 Android canonical Shader，由桌面模拟器直接解析同一份
GLSL，不增加仅供模拟器使用的视觉补丁。

- 高密窗口的基础 cell 保留率提高到约 92%，内容色在同一窗口内再增加少量真实采样；
  面板尾流基础保留率降低，内容色尾流保留斜率提高。内容色仍只改变 cell 是否存在，不
  改变粒子的速度、寿命、尺寸、透明度、光照或深度运动。
- 高密窗口的回卷截面汇聚系数由 0.55 提高到 0.68，新增粒子被压入实际的弯曲条带。
  分层图中移除静止表面后，右上和左下的高密帷幔仍独立存在，没有成为完整 Dialog 白边。
- 固定 1px 点中心回归显示帷幔/尾流真实占用密度由 2.09× 提高到 2.19×，高密点占
  31.3%；内容色源区域占 18.17%，尾流中占 51.89%，真实数量表示由 2.54× 提高到
  2.86×，颜色 alpha 表示为 2.07×。
- `t=0.47` 的纯粒子法向密度峰仍位于静止表面外约 14px；浓带相对表面 0–4px 粒雾为
  5.30×，相对 24–44px 外侧尾流为 3.94×，与静止表面重叠质量约 0.35%。归一化
  0.40、0.47、0.53 的普通合成帧和五面板分层诊断均已复核。
- 激活、颜色、真实点密度、2.5D 分层与运动回归全部通过；`:app:testDebugUnitTest` 与
  `:app:assembleDebug` 通过。未使用 ADB，未发布新的 Android 调试版本。

## 2026-08-27 第四十九轮：4.2× 窄密帷幔、4.5× 内容色与非线性卷流

用户要求停止小幅调整，直接把帷幔/尾流真实粒子密度比提高到 4.2×、内容色数量表示提高
到 4.5×，并修正近似匀速、全部粒子沿同一方向直线移动的问题。本轮继续按 `diagnose`
流程修改 Android canonical Shader，桌面模拟器只解析同一份 GLSL。

- 参考光流确认整体速度明显先慢后快：早/中/后段中位速度约为 18.21/125.25/193.73
  px/s；下边缘在归一化 0.46–0.62 的向左/向上比约 0.83，随后 0.62–0.78 降为 0.62。
- 新增固定运动队列诊断：只跟踪同一批长寿命主体层 cell，排除粒子出生、抽稀和淡出造成
  的假速度。全局风势改为 `t^3.85` 并扣除出生风势，模型中段/早段速度比为 5.77×，
  后段/中段为 3.01×，不再近似匀速。
- 每条局部帷幔按自身边缘切向先侧流，再按连续锋线坐标剪切为弯曲扇面并被主风捕获；
  左上场景下边缘的向左/向上比从 1.03 降至 0.76，形成可测的先左流、后上卷。逐粒子
  随机横向漂移已压到只保留微扰，不承担轨迹结构。
- 高密带接近满采样，释放尾流在内容色构成完成后统一抽稀至 60%。固定 1px 点中心回归
  得到帷幔/尾流 4.47×、内容色数量表示 4.61×；颜色 alpha 表示为 2.19×。高密带几何
  支撑仅占整帧 2.8%、联合粒群 19.1%，不是把半张粒云都设成高密状态。
- 2.5D 稳定层距与动态伪深度运动分离，back/main=0.509、front/main=0.896，前后层质心
  距离 15.61px，帷幔/尾流深度方差为 0.0541/0.0210。局部转向不再被深度投影覆盖。
- `curtain-separation-comparison-left-up-047.png` 复核确认高亮曲线独立存在于粒子层，
  still layer 中不存在该条带；新并排视频在 0.70 前清空完整表面，彩色粒子嵌在相同
  曲面和尾流中。
- 颜色/密度/空间层次、区域激活、运动三组桌面回归全部通过；
  `:app:testDebugUnitTest` 与 `:app:assembleDebug` 通过，未使用 ADB。
- 按 feature debug 更新流程发布 `202608271432`。本地 APK、远端 `latest.json` 指向的 APK
  和重新下载文件均为 24,404,408 字节，SHA-256 均为
  `20fcce162a06213ef1565c581486c789d505e8e764a064a2ae242ba32804e92f`；本地与远端
  `latest.json`、发布说明完全一致。未通过 ADB 自动安装。

## 2026-08-27 第五十轮：修复桌面点大小、真机过密、颜色分离与对角脊线

用户用 Android 真机截图确认第四十九轮存在三项结构性失败：真机帷幔远比桌面过密；
彩色控件粒子再次像独立系统；t=0.47 出现从左下贯穿右上的细斜带，右上端尖锐越界且
左上起始粒群不清楚。本轮继续按 `diagnose` 的复现、单变量探针和回归流程修改 canonical
Shader，没有使用 ADB。

- 根因是桌面 ModernGL core context 没有启用 `GL_PROGRAM_POINT_SIZE`，因此忽略
  `gl_PointSize` 并把所有点退化为约 1px；Android GLES 则采用真实点径。启用
  `moderngl.PROGRAM_POINT_SIZE` 后，旧 4.2×/4.5× 参数在桌面也复现成真机的白色实片，
  证明此前的倍率目标建立在错误显示状态上。
- 新增 `analyze_raster_consistency.py`。桌面默认 2.4 density 与 Android 等效 3.6 density
  的平均 alpha、强像素占比和局部密度相对差均低于 3%；1.2 density 因最小 1px 点径只
  作为降级信息。全灰压力图确认新颜色分类的误判率为 0。
- `contentWeight` 改为只取真实饱和度，移除面板色差分类；中性色尾流保留 10%–18%，
  饱和色最多额外增加 18%。静态契约限制它只能出现在两个占用率公式和诊断 varying 中，
  不能进入运动、寿命、尺寸、材质、深度或淡出。当前彩色 cell 数量表示为 2.07×、颜色
  alpha 表示为 1.58×，不再形成长时间残留的彩色矩形块。
- 删除制造斜脊的 `ribbonCompressionDrift`，高密窗口扩为约 25–285ms，点径与三层单点
  alpha 按真实 Android 尺度重新校准。后/主/前质量比为 0.427/1/0.910，帷幔/尾流真实
  占地密度为 2.74×，三层前后质心距离为 21.74px，空间感没有因降密而消失。
- 新增 `analyze_edge_sequence.py`，逐帧输出四条边缘并约束 0.32 左上粒群、0.47 带宽、
  顶部外移和右侧端点。最终 t=0.47 的高密连通区厚度为 4.67px（参考 6.15px），右侧
  越界 P95 为 0.0058w（参考 0.0065w），不再出现针状右上端点。
- 宏观方向延迟由约 110ms 降到约 50ms，顺向左上短起点扩大；五个局部种子重新主导边缘
  形态。法向高密峰位于完整表面外约 22px，相对贴边粒雾为 8.09×、相对峰外尾流为
  2.51×，与 still layer 的质量重叠低于 0.001%。0.70 时完整表面已清空。
- 修复固定 ROI 光流会在粒子移出后只测到淡出噪声的问题，新增固定同出生期、长寿命的
  下边缘 cohort。侧流/上卷的 left/up 从 1.05 降到 0.68，接近参考的 0.83→0.62；
  全局光流中段/早段为 7.30×，保持明显先慢后快。
- `.gitignore` 按现有白名单规则放行 `tmp/particle-dismiss-tuning` 的 Python/HTML/README，
  继续忽略视频、帧图和 JSON 产物，确保新增回归脚本不会在正常提交时丢失。
- Python 尺度、颜色、密度、激活、运动、逐帧边缘回归全部通过；
  `:app:testDebugUnitTest :app:assembleDebug` 通过。`ParticleDismissShaderCompileTest` 共 1 项、
  skipped=0、failures=0，四种展开后的 GLES 3.00 Shader 均真实编译成功。
- 首次发布 `202608271535` 后发现说明标题层级只让“用户反馈”进入更新对话框，随即把整份
  日志改为单一顶层 `##` 条目并重新发布最终版本 `202608271536`。本地与远端
  `latest.json` 完全一致；本地发布 APK 和重新下载 APK 均为 24,404,408 字节，SHA-256
  均为 `d39912c977add3715161fe81c0c9f4c7d1e0094b196a100f7096ccee64c894eb`，远端说明同时包含
  `PROGRAM_POINT_SIZE` 根因与回归结果。未自动安装到设备。

## 2026-08-28 第五十一轮：连续 61 帧帷幔形态、柔性交接与中后段保持

用户要求重新逐帧对照不透明参考，不能只在少数截图上调形似；验收必须同时覆盖边缘高密
分布、弯曲帷幔轮廓、Dialog/粒子柔性交接，以及 0.54 后高密形态的持续时间。本轮继续按
`diagnose` 流程直接修改 Android canonical Shader，并由桌面程序编译同一份 GLSL。

- 新增 `analyze_reference_morphology.py`，把参考和模型统一到 61 个归一化时刻、共同局部
  坐标和 4×4 区域网格。它统计普通/高密粒子分布、最大脊线质量/长度/厚度/曲率、右下
  局部曲率、表面交接孔隙与重叠，并输出八张关键帧三联图、四象限逐帧接触表和时间曲线。
- 单变量探针确认旧模型在 0.54 后过早散空并非单纯 alpha 淡出：短生命周期 band、点径
  释放和以局部年龄归一化追赶全局风程共同使高密队列快速丢失。只延长全部 band 会变成
  等密矩形，因此没有采用。
- canonical Shader 改为三个错开的持久等到达时间队列，并用低权重桥接片层连接队列；
  band 延后到局部年龄约 0.66–0.88 才释放，点径保持也与尾流分开。0.54→0.70 的总粒子
  质量保持率由约 0.586 提高到 0.660，高密质量由约 0.658 提高到 0.736。
- 静止层按片元采样连续细噪声、粒子层按 cell 中心采样同一传播场，交接期加入多孔粒子
  桥。模型边界孔隙率中位数为 3.43%，表面/粒子重叠为 16.11%，硬边比例为 6.07%；
  0.70 时完整表面已经清空，但高密曲面仍然可见。
- 弧形曲面位移改为建立后冻结的几何项；主风改为 smoothstep 速度的解析积分，不让晚出生
  粒子瞬间追赶。早/中/后段速度为 32.26/169.11/200.29px/s；固定左下队列先向左侧流，
  再转入左上抬升，方向符号回归可防止只看绝对值比造成的反向假通过。
- 内容色继续只通过同一占用概率增加真实 cell，数量表示为 1.84×；速度、寿命、点径、
  alpha、光照、材质和深度均与中性色统一。三层伪深度在帷幔/尾流阶段为 0.0273/0.0105，
  前后层质心分离 28.66px。
- 最终 61 帧回归中，普通/高密区域分布相似度中位数为 0.788/0.669；0.47 为
  0.816/0.716，0.54 为 0.814/0.741。模型密度对比 13.77、最大脊线质量占比 39.31%，
  参考为 26.03/52.07%，故记录为明显改善而非逐像素复刻。
- 重新生成归一化 60fps 并排视频、全局接触表、0.47 分层图、八个关键时刻和四象限连续
  图。Python 编译、运动、激活、颜色、栅格一致性、边缘与 61 帧形态回归全部通过。
- `:app:testDebugUnitTest :app:assembleDebug` 通过；`ParticleDismissShaderCompileTest` 共 1 项、
  failures=0、errors=0、skipped=0，四种展开后的 GLES 3.00 Shader 均真实编译成功。
- 按 feature debug 更新流程发布 `202608271713`。本地与远端 `latest.json` 的版本、URL、
  SHA-256、字节数与完整发布说明一致；本地发布 APK 与重新下载的远端 APK 均为
  24,404,408 字节，SHA-256 均为
  `3dc363b7640c84aa126dee034feaaef9868cc105709f7cd4813b033a90f1155e`。未使用 ADB，
  也未自动安装到设备。

## 2026-08-28 第五十二轮：连续布面整体运动原型

用户提出先暂停粒子细节，把参考效果视为一张被风从边角掀起、保持整体连接并同步粒子化的
薄面。本轮完成逐帧面积复核、资料调研和独立桌面原型，没有修改 Android 生产 Shader，
也没有使用 ADB。

- 参考首帧完整面积归一后，`t=0.32/0.47/0.55/0.62/0.70` 的累计离面比例约为
  `36%/67%/86%/97%/99%`；旧原型对应约 `21%/46%/60%/74%/91%`，已按参考重新拟合。
- 建立 24×15 连续三角网格，保留结构、剪切和弱弯曲约束；贴附强度连续解除，所有边永久
  保留。法向阻力大于切向阻力，刚解除前沿另有短时法向抬升，避免所有点沿二维主方向直移。
- 约束区分拉伸和压缩刚度，允许薄面局部收缩形成宽折叠，同时限制被拉成长条。
- 对比三种方案后保留多处边缘渐进解除贴附；单一卷边过于像刚性翻页，整面风压则会让
  中心和边缘同时鼓起。
- 原型位于 `tmp/particle-dismiss-tuning/cloth-motion-prototype/`，支持参考同步播放、归一化
  拖动和 A/B/C 切换。下一步是由同一曲面生成完整层与粒子层，并用投影 Jacobian 派生折叠
  处的粒子密度。

## 2026-08-28 第五十三轮：A/B 同曲面粒子化交互网站

用户确认连续布面 A、B 的整体运动都可以继续，要求两者加入粒子化，并先提供桌面交互网站
反复调试，效果确认后再迁移 Android。本轮只更新临时网页原型和功能文档，没有修改生产
Shader，也没有使用 ADB。

- A 多点解除与 B 单一卷边分别保留各自连续曲面，但共用粒子材质转换规则；两者的累计
  粒子化时序均对齐参考完整面积，`t=0.47` 约为 67%–69%，`t=0.70` 约为 99%–100%。
- 未粒子化内容改用原平面的连续像素遮罩。首版按三角网格逐片透明会出现棋盘状接缝，已
  删除该路径；当前交界使用带限连续噪声和窄交接区，不会暴露布面三角拓扑。
- 约 12,384 个固定材料采样点从同一曲面插值得到位置和法线。屏幕投影压缩与刚释放前沿
  共同提高帷幔占用率；后段微扩散对所有颜色统一。饱和色只增加同轨迹采样数量，不参与
  释放、位移、风力或寿命。
- 网站支持参考同步播放、归一化拖动、A/B URL 切换、粒子数量、帷幔增强和可选拓扑网格；
  播放改为逐动画帧读取参考视频时间，不再依赖低频 `timeupdate`。
- 浏览器逐帧复核了 A/B 的 0.32、0.47、0.55、0.70 等关键时刻；A 保留多点折叠，B 保留
  单一连续卷边并增强后段左上主风。稳定帧渲染约 3–5ms，A/B 首次切换低于 8ms。

## 2026-08-28 第五十四轮：A 改为局部折叠后整面吹走

用户指出 A 在加入粒子化后仍像右下角整体折叠到左上，最终相当于整张布翻面；期望开场
整张布就向左上缓慢牵伸，折叠只发生在局部，后段整张布保持大体朝向直接被吹走。本轮只
修改桌面交互原型和功能文档，没有修改 Android，也没有使用 ADB。

- 新增 A 的共享开场基准姿态：完整像素遮罩和动态网格都执行低速左上平移，并让左上顺风
  侧获得更大的纵向牵伸；平均顺风位移在 `t=0.15/0.25` 从接近零提高到 `0.04/0.08`。
- 动态网格的中段受风和连续约束继续保留；后段改为朝保持正面方向的低频弯曲薄面渐进
  匹配，只留一个局部褶皱、宽尺度弯曲及顺风拉伸，不再让右下区域卷过整张布。
- A 的反面比例在 `t=0.47/0.55/0.70/0.78` 为 `6%/5%/0%/0%`；旧 A 在
  `t=0.70/0.78` 约为 `48%/54%`。B 未改变，`t=0.65` 仍约有 `38%` 的反面，继续作为
  完整翻卷对照。
- 逐帧抽查 `t=0.34–0.86`，顺风位移从 `0.09` 单调增长到 `0.91`，没有在中后段约束
  交接时回跳。连续播放和 A/B 按钮均正常，稳定渲染约 `3–5ms`，浏览器控制台无错误。

## 2026-08-28 第五十五轮：开场左上同步粒子化与后段弹道输运

用户指出 A 的左上粒子化仍晚于开场牵伸，后段所有粒子又像同一张被拉伸的 Dialog 一样
机械移动。本轮按诊断闭环为网页原型加入左上粒子数、材料坐标锁形相关度和基础粒子数，
逐项验证释放场、输运模型和寿命函数；没有修改 Android，也没有使用 ADB。

- 旧版 `t=0.03/0.06/0.10/0.15` 的左上可见粒子均为 0。左上出口改为局部高次距离场，
  刚释放前沿使用同材料点密度增强；新版 `t=0.03/0.06/0.10` 为 `10/73/152`，顺风
  位移也已同步开始。累计离面比例仍保持约 `41%/70%/87%/98%/100%`。
- 旧版每帧把全部粒子重新采样到当前布面，`t=0.68–0.90` 的锁形相关度始终约
  `0.996–0.997`。新版为每颗粒子预计算脱离时刻的位置、速度和法线，短时贴随形成帷幔，
  随后进入带速度记忆、顺风响应差、连续涡度和法向抬升的独立轨迹。
- 晚释放区域缩短贴随时间；顺风速度差在 `t>0.52` 后增强，横向随机量保持受限，使云团
  沿左上主风展开而不是各向同性铺满画布。`t=0.70/0.78/0.90` 的锁形相关度降为
  `0.67/0.59/0.50`，按钮排列和 Dialog 外轮廓不再稳定保存。
- A 的寿命在 `t≈0.72–0.99` 间错峰结束，`t=0.90` 的基础粒子由 `4,288` 降为 `597`，
  `t=0.96` 只剩 `17`。连续播放稳定帧约 `3–6ms`，浏览器控制台无错误。
- B 未使用新的弹道输运或寿命；`t=0.65` 仍为 `97%` 粒子化、`0.42` 顺风位移和
  `38%` 反面比例，继续作为完整翻卷对照。

## 2026-08-28 第五十六轮：开场改为局部脱附拉伸，贴附主体不动

用户否决了“左上伸出、右下等量反向补偿”的中心固定仿射方案，指出两处运动没有物理上的
必然联系。本轮撤销完整 Dialog 的开场仿射姿态，改为只依赖已有连续薄面求解：未脱附区域
固定在原平面，已脱附区域受风并通过完整网格向邻域传递张力；后续主风阶段才允许整面位移。
没有修改 Android，也没有使用 ADB。

- `basePoseA` 恢复为原始材料坐标，不再包含开场平移、定向缩放或背风端补偿。
- 未粒子化像素遮罩在开场保持原位；左上粒子仍从 `t=0.03` 开始出现，并沿物理曲面局部
  延展。`t=0.03/0.06/0.10/0.15/0.25` 的左上基础粒子为 `10/73/152/254/309`。
- 网格逐帧检查中，`t=0.15/0.25` 的平均顺风位移仅为 `-0.00008/0.0018`，贴附主体与
  原平面重合；`t=0.55/0.70` 再进入 `0.12/0.53` 的整体吹离阶段。
- 连续播放从 `t=0` 正常推进，抽查帧耗时约 `2.2–5.4ms`；B 未改动。

## 2026-08-28 第五十七轮：收束末段粒子流并建立窄折叠帷幔

用户指出末段粒子存在不必要的各向扩散，同时帷幔或折叠区域仍没有形成明显的高密亮带。
本轮先建立可重复诊断指标，再分别修改自由飞行和折叠采样；没有修改 Android，也没有使用
ADB。

- 基线复现中，`t=0.82` 的横向飞行量/顺风飞行量约为 `0.25`。原因是随机横移随
  `age × breakup` 持续增大，渲染阶段还重复加入随机散布。现改为有界、低频且材料邻域
  相干的横向场，只让顺风速度差继续增长；该比例降至约 `0.067`，反向飞行占比为零。
- `t=0.55` 的普通投影压缩覆盖约 `48%` 网格，证实旧密度信号过宽。新增脱离时刻的邻域
  法线曲率和视线投影压缩联合判定，仅约 `5%` 材料点成为折叠脊。
- 折叠脊不使用独立轨迹，只保留更高的真实采样数量和局部相干顺风响应。其相对普通区域
  的密度在 `t=0.55/0.70/0.78` 约为 `4.1×/4.7×/4.1×`，中段形成连续高亮曲线，
  脱离后仍能保留一段时间。
- 顺风响应差继续破坏 Dialog 轮廓，`t=0.70/0.78/0.82` 的锁形相关度约为
  `0.76/0.69/0.66`。A 的 `t=0.03/0.10/0.15/0.25` 平均顺风位移仍接近零，B 的
  `t=0.65` 离面比例、位移和反面比例保持约 `97%/0.42/38%`。
- 连续播放正常；抽查帧耗时约 `2.3–7.2ms`，浏览器控制台无错误。

## 2026-08-28 第五十八轮：将 A 的约束迁移到 Android canonical Shader

用户认可桌面 A 的当前方向，要求迁移 Android 实机查看。本轮直接修改
`ParticleDismissRenderer.kt` 的生产消失 Shader，网页原型仍作为历史对照，没有增加运行时开关。

- 用连续定向释放场替代旧五个持久局部场，并让 still/particle 两层共用公式。
- 中段密度身份收敛为一条连续弯曲脊，局部出口只保留开场作用；固定累计风程和全局曲面
  漂移减少中段分块。
- 末段不再使用随时间增长的随机横移；新材料相干流只做有界曲折，顺风速度保留非线性建立和个体差。
- 普通尾流前后层抽稀到 `0.12`，高密脊保留三层；删除旧 curl 有限差分，减少真机顶点和片元压力。
- canonical GLSL 内容色回归为 `2.16×` alpha 占比、`1.39×` 真实点占比；折叠脊/尾流
  点占地密度为 `2.44×`，三层联合支撑为主体层的 `1.93×`，前后层质心距为 `33.44px`。
- `check_color_emphasis.py`、`analyze_raster_consistency.py` 和真实 `glslangValidator` 编译回归已通过；
  `:app:testDebugUnitTest :app:assembleDebug` 整包成功。Debug 更新 `202608280944` 已发布，远端
  `latest.json`、本地 APK 和回读 APK 哈希一致。本轮未使用 ADB。

## 2026-08-28 第五十九轮：用真机截图隔离 Android 与桌面模型差异

用户提供同一页面的无 Dialog 背景截图和带 Dialog 截图，要求在桌面模拟器中使用真实
素材，并解释真机与模拟器仍不一致的原因。本轮没有修改 Android 生产代码，也没有使用
ADB。

- 新增 `prepare_device_fixture.py`，从两张 `1440×3168` 截图中提取 `1120×1099` 的
  圆角 Dialog RGBA 纹理、无 Dialog 背景和夹具元数据；检测圆角约 `64 px`，背景暗化
  实测 `0.6016`。
- 新增 `render_device_fixture.py`，直接读取 Android canonical GLSL，以真机一半线性尺度
  渲染 `720×1584` 视频；density、cell、drift 和 noise scale 同步减半，因此仍保持
  真机约 `255×250×3=191250` 个顶点的网格规模。输出左上 `-128°` 与无触点右上
  `-60°` 两条确定性视频和关键帧接触表。
- 网页新增 `fixture=device` 模式：左右两侧均使用真实背景、真实 Dialog、左上方向和相同
  归一化时间；左侧为 Android 同源 GLSL，右侧为旧 JS/PBD。真实 Dialog 纵横比不再被
  旧 `960×570` 合成卡片比例拉伸。服务器对所有 mp4 增加 Range 支持，时间滑杆可以准确
  seek。
- 用户澄清真机关闭来自左上真实触点，不是返回键，因此右上回退方向不参与本次差异解释。
  同方向对照显示 `t=0.32/0.47` 的消融进度已经接近，但 Android 粒子占用在中段快速
  降低，JS/PBD 仍维持连续大块褶面。真实 Dialog 有效区域约 `96.5%` 为近白色；生产
  Shader 的中性普通尾流基础占用率约为 `7%–10%`，高密带占用率约为 `43%–55%`；它没有
  JS/PBD 的拓扑连续布面，因此真机更容易显得稀疏、分块、帷幔保持不足。此前误把凝聚
  Shader 的 `0.16–0.28` 短寿命段当作消失路径；消失 Shader 实际寿命约为
  `0.80–0.995`，本轮记录在继续实现前已经纠正。
- 浏览器复核 `t=0/0.32/0.47/0.70`，初始 Dialog 位置和尺寸左右一致，视频时间与滑杆
  一致，控制台无错误；稳定 JS 帧耗时约 `6–15ms`。对照入口为
  `http://127.0.0.1:8765/?fixture=device&variant=A&t=0.470`。

## 2026-08-28 第六十轮：Android 与桌面 A 共用有状态连续布面

用户确认真实关闭来自左上触摸控件，要求继续迁移并保持一致。本轮把桌面 A 的状态模型
直接移入 Android 生产路径，不再继续调旧解析近似；没有使用 ADB。用户随后明确要求发布
阿里云 Debug 更新，发布号为 `202608281340`。

- 新增 `ParticleDismissClothModel`，逐项移植桌面 A 的 `24×15` PBD 网格、结构/剪切/弱
  弯曲约束、渐进贴附、法向与切向风、下边缘侧流及后段姿态约束。左上 `-128°` 的
  `t=0.15/0.25/0.55/0.70` 平均顺风位移与桌面 A 一致，约为
  `-0.00008/0.0018/0.12/0.53`。
- 渲染初始化时生成 `241×25×16` 个 `(x,y,z,released)` 状态并上传为 `RGBA16F`
  `sampler2DArray`；顶点 Shader 对空间和时间双线性采样，从同一曲面状态取得当前位置、
  脱离位置、速度、法线、曲率和投影压缩。未粒子化像素与粒子层使用同一个释放量，避免
  表面—粒子边界成为截断函数。
- 彩色内容只增加同一轨迹上的采样数，不再拥有独立速度、寿命或尺寸路径；触点继续作为
  光源，但只沿原始 RGB 调整明暗。折叠高密区由脱离时的真实曲率和投影压缩识别，仍与
  连续薄面连接，不创建多个独立 emitter。
- 消失网格统一为约 `144` 列，副本预算收敛为 `3`，真机截图夹具对应
  `144×142×3=61,344` 次最坏顶点调用，较旧约 `191,250` 次降低约 `68%`。强折叠脊用
  点径和亮度补足可读性，避免继续增加全局副本。按同一真机素材的最终显示尺度比较后，
  基础点径由 `0.60×cell` 校准为 `0.66×cell`，补偿 Android 软粒子轮廓相对桌面 A
  方形采样约两成的有效覆盖损失，而不增加顶点数。
- `GestureAnchoredDialog` 只在当前触摸事件分发栈内暴露 dismiss 触点：点击左上关闭控件
  会把真实触点传入方向和光源；返回键、异步完成和代码关闭不会复用历史触点，统一走既有
  无触点右上回退。
- 审查中修正一处仅在非正方形 Dialog 上明显的残余差异：静止层/粒子层曾按快照像素
  宽高计算释放投影，而 PBD 和桌面 A 使用归一化材料坐标。现三者统一为同一投影，释放
  边界的低频弯曲也随方向镜像，避免宽扁 Dialog 的表面边界与布面状态错位。
- 新增 2 个连续薄面数值测试和 3 个 canonical Shader 结构/真实 `glslangValidator` 编译
  测试，5 项均通过；`:app:assembleDebug` 完整成功。最终 APK 位于
  `app/build/outputs/apk/debug/app-debug.apk`。`:app:publishDebugUpdate` 成功；本地 APK、
  远端元数据记录值和远端 APK 回读文件的 SHA-256 均为
  `3c7cfac62953dfabcea1c40ddc2843f5d16c9487c4b0054b2e9087d459d7318f`。
- 重新生成真机截图夹具，并在浏览器逐帧目验 `t=0.32/0.47/0.65/0.70`：Android 与
  桌面 A 的开场局部脱附、连续曲面边界、单一折叠高密区和尾段左上离场已保持同一整体
  形态，控制台无错误。离屏验证不替代真实手机 GPU 帧率，仍保留手动实机验收待办。

## 2026-08-28 第六十一轮：修复 dim 先于粒子播放并完成 R5 实机闭环

用户报告 Android 上 dim 动画结束后粒子才开始，并授权连接 `R5CW20BLNKL` 录制完整动画。
本轮用同一台三星 SM-S9180、同一个“添加附件”Dialog 和左上外部触点复现、插桩、修复、
冷暖启动复测；没有把另一台设备上的现象归因于设备型号或 PixelCopy。

- 基线时间戳为：消失请求 `0 ms`、PixelCopy `14 ms`、PBD 完成约 `959 ms`、粒子首帧
  约 `975 ms`；dim 在 `0 ms` 已启动。根因是 GL 线程同步生成 `24×15×240×9` 的 PBD
  轨迹，而 dim 在控制器返回时就开始，不是 PixelCopy 延迟。
- 新增 `onAnimationStarted` 首帧握手：只有第一次 GL buffer swap 已完成，overlay 才隐藏
  快照并启动 dim；失败、超时和降级路径也恰好一次放行。回归测试明确禁止在 dismiss 请求
  阶段启动 dim。
- PBD 热路径由对象/List 改为 primitive SoA 数组；空间控制网格收敛为 `16×10`，每步
  9 轮约束按累计刚度换算为 1 轮，时间仍为 240 步。现有宏观位移、释放比例和 Shader
  结构测试保持通过，粒子采样网格及可见粒子数量不变。
- 插桩版实测：冷启动 PixelCopy `11 ms`、PBD `216 ms`、触摸到首帧 `248 ms`；同进程
  第二次为 PBD `70 ms`、触摸到首帧 `99 ms`。两次 dim 与粒子首帧的偏差均为 `0–1 ms`。
- 先后保存基线、修复插桩冷启动和暖启动完整录屏及接触表，逐帧检查未发现 dim 抢跑、
  快照闪烁、曲面折线化、末段停帧或错误方向。临时 `[DEBUG-dim-sync]` 日志在最终构建前
  全部移除。
- 首版干净录屏进一步发现真实 Dialog 仍只等待 overlay 上屏，PBD 准备期间输入法会先升起。
  新增 `ParticleDismissStartGate`，真实退出同时等待 overlay 与 GL 首帧；最终干净录屏中，
  触摸后的静止准备期内 Dialog、dim、输入法和背景都保持不变，粒子首帧出现时才共同衔接。
- 最终干净录屏为 `final-clean-synchronized-r5cw20blnkl.mp4`。动画有效区间编码了 121 帧，
  平均帧间隔 `8.34 ms`（约 `119.93 fps`），P95 `9.39 ms`、最大 `17.40 ms`；gfxinfo 的
  UI 帧 P50/P95/P99 为 `8/12/14 ms`，GPU 为 `8/10/11 ms`。完整粒子测试集和
  `:app:assembleDebug` 均通过。
- 用户要求发布后，已通过 `:app:publishDebugUpdate` 将 Debug 更新 `202608281424` 发布至
  阿里云。发布任务携带本轮功能日志；本地版本化 APK、远端 `latest.json` 记录值和远端
  APK 回读文件的大小均为 `24,404,408` 字节，SHA-256 均为
  `2ce984633dafb214211760f1db44af30ffa137dd7d6f98161ebcd74303bfb1a0`，发布号、日志与哈希
  三方一致。

## 2026-08-28 第六十二轮：细粒子增密与连续弧形消融边界

用户以“添加附件”Dialog 的真机截图指出：折叠区像单粒子变粗，剩余白色表面形成尖角，
表面与粒子在四条外边缘处衔接生硬，而且彩色区域强调不足。本轮按诊断流程先用同一真机
截图夹具复现，再将释放场、粒径和采样数量分别隔离；未连接 Android 设备。

- 找到两项独立根因：折叠／锋线权重进入点径和亮度公式，造成“粗粒子”错觉；右侧、
  下侧两个独立反向点源在交汇处形成鞍点，导致剩余表面出现尖锐半岛。
- 折叠、锋线与帷幔身份已从点径公式删除，单粒子改为窄范围统一细点；高密区改由最多
  4 个同轨迹真实副本和折叠保留率形成。彩色区域的保留增益与副本期望值同步提高，但不
  进入运动、寿命、深度或尺寸。
- 两个反向点源合并为连接右、下边缘的连续四分之一圆弧，仍与主体场和顺向角小出口做
  软并集。离线逐帧中，旧版 t=0.32 的尖锐白色舌已变为圆滑连续曲线；释放比例在
  t=0.32/0.47/0.55/0.62 为约 `43%/68%/87%/99%`。
- 同步修复了 Android PBD 已是 `16×10`、但 Shader 与桌面仍残留 `24×15` 的隐藏分叉；
  三端现在使用相同网格尺寸、释放弧和一轮等效约束。结构测试同时禁止恢复独立反向点源、
  折叠放大粒径或颜色进入动力学。

## 2026-08-29 第六十三轮：物理释放场、同源桌面夹具与 R5 真机闭环

用户要求停止逐帧硬拟合参考视频，改为分析并实现可泛化的布面受风、折叠和粒子脱离规律，
同时交付桌面模拟器、截图／视频对比和 `R5CW20BLNKL` 真机实测。本轮完成：

- 删除生产运行时的参考释放／密度 profile；新增 `49×33` 物理释放场。边界种子由风压、
  切向剪切、少量顺风角吸力和低频材料韧性共同决定，再经八邻域各向异性传播。左上场景
  早期节点约 `84%` 位于主风反向侧、`16%` 位于其它边缘，符合“主要但并非全部”。
- `16×10×241` PBD 轨迹保存位置、离面高度和 CPU 计算的折叠密度；折叠密度来自曲率、
  压缩应变、投影面积压缩和剥离前沿张力。GPU 不再为每个粒子重复做四邻点曲率采样。
- 移除后段逐粒子各向随机扩散、特殊彩色运动和残留外框寿命。彩色区域仅增加同轨迹真实
  副本；高密帷幔也只增加同尺寸细粒，普通尾流、折叠粒子和内容色共用释放、速度、寿命、
  深度和点径。四个种子、四个中段时刻的离屏回归中，内容色真实 cell 数量赔率为源图的
  `5.58×`，alpha 占比为 `3.18×`；普通合成中仍嵌在同一粒群内，没有独立颜色轨迹。
- 残留表面与粒子改用同一物理释放时钟和宽互补过渡。桌面关键帧显示 t=0.32、0.47 的
  残留面保持单一连通且边界圆滑；t=0.70 表面已经清空，粒子群仍保持连续形状。
- 桌面“添加附件”夹具按原始 `1440×3168`、density `4.0` 渲染，再编码到
  `720×1584`，避免直接半分辨率渲染丢失细粒覆盖。交互入口为
  `http://127.0.0.1:8765/physical.html?t=0.470`，滑杆已验证会把参考 3.35–8.05 秒与
  canonical 0–1 同步到相同归一化进度；页面同时提供慢速三列视频、真机原速片段、关键帧
  接触表和帷幔分层图。
- 在三星 SM-S9180 `R5CW20BLNKL` 安装当前 Debug APK，以界面树定位“添加附件”入口，
  使用 Dialog 左上外部真实触点 `(60,700)` 关闭并录制。真机与桌面在 t=0.32、0.47 的
  残留轮廓与粒子包络一致；dim 与粒子从同一可见首帧开始，没有再次出现 dim 先播完。
- 真机活动 1 秒区间记录 121 帧，帧间隔中位数 `8.36 ms`（约 `119.61 Hz`）、P95
  `9.35 ms`、最大 `10.38 ms`。单次重置后的 UI gfxinfo 为 166 帧、jank `3.01%`，
  P50/P95/P99 `5/25/48 ms`；GL 活动区间以录屏时间戳为准。日志未发现 AndroidRuntime、
  EGL 或 GL 错误。
- `ParticleDismissClothModelTest`、`ParticleDismissShaderCompileTest` 和
  `:app:assembleDebug` 均通过。没有发布阿里云、没有创建 Git 提交。

## 2026-08-29 第六十四轮：单值相位场消除尖嘴并完成 R5 柔边复验

用户指出上一轮虽然改善了部分衔接，但仍可能在两个局部消融区之间留下尖锐残片，要求
粒子化边缘始终柔和，不能在调整粒群轮廓、帷幔密度或颜色后再次回退。本轮继续使用
`diagnose` 流程，在真实“添加附件”Dialog、桌面 canonical 与参考动画之间逐帧检查。

- 根因不是抗锯齿不足，而是旧释放场即使不再使用显式圆形点源，仍叠加多组横向频率；
  局部等时线会形成多个凹口，在相邻区域相遇时留下尖嘴或狭长白片。宏观释放改为严格的
  单值方向相位场：沿主风单调推进，仅保留低于一个周期的宽弯曲和一处宽反向压差；其它
  边缘的早期粒子只做表层预脱落，不再改写完整面的释放时钟。
- 静止层与粒子层继续读取同一 `49×33` 到达纹理。静止 alpha 先做 `3×3` 材料尺度
  规则化，再以 `120 ms smoothstep` 过渡；表层孔隙最多降低 `72%`，不能把完整面打穿。
  数值回归确认左上场逐行、逐列均无反向折返，离散二阶变化分别不超过约
  `0.0047/0.0058`；可见留存面始终只有一个显著连通块。
- t=0.24/0.32/0.47 的柔边宽度约为 `4.77/5.34/6.29 px`。锋线高密窗口恢复到
  `0.095`，因为按 Dialog 宽度归一化后模型条带约 `2.01%`、参考约 `1.34%`，仍属于同一
  细带量级；单粒径不随帷幔、折叠或颜色变化，亮带来自更多同尺寸真实副本。
- 彩色数量回归改为直接表示倍数，避免占比赔率产生假高值。当前彩色 cell 在粒子群中的
  表示约为源图的 `4.39×`，彩色 alpha 占比约为 `3.33×`；颜色仍不参与轨迹、寿命、
  深度或粒径。
- 桌面模拟器重新生成并通过浏览器验证：滑杆可同步到 t=0.32/0.47，完整播放可到 t=1.0，
  控制台无错误。拓扑、激活、运动、连通性、边缘、颜色、跨分辨率和真实 GLSL 编译回归
  均通过；`:app:assembleDebug` 成功。
- 在授权三星 SM-S9180 `R5CW20BLNKL` 安装当前 APK，使用界面树定位“添加附件”，再以
  左上外部真实触点 `(60,700)` 关闭并完整录制。放大检查 t=0.10/0.16/0.24/0.32/
  0.40/0.47/0.54/0.62：未出现内部孤岛、两波相夹尖嘴或外边缘直线截断，完整面与粒子
  之间保持连续渐变，粒群外包络仍有低频弯曲而非拉伸矩形。
- 真机有效动画为 121 帧，帧间隔中位数 `8.33 ms`（约 `120.08 Hz`）、P95 `9.59 ms`、
  最大 `12.87 ms`；UI jank `1.61%`，UI P50/P95/P99 为 `5/8/10 ms`，GPU 为
  `4/7/8 ms`。日志没有 AndroidRuntime、EGL 或 ParticleDismiss 错误；dim 与粒子仍从
  同一可见起点开始。本轮没有发布阿里云、没有创建 Git 提交。

## 2026-08-29 第六十五轮：开场多边缘牵伸与严格一秒闭环

用户指出左上消逝时，当前开场几乎只有右下反向侧在粒子化，而参考动画的左上顺风角和
其它若干边缘也会从最早阶段同期起粒；同时桌面模拟器在真实一秒后仍未结束。按诊断流程
分别检查 Shader 激活、粒子越界位移、网页时钟和编码帧数，确认是两个相互独立的问题：

- 原有顺风角预脱落虽然数值上已经激活，但使用普通粒子的 `0.014–0.224 s` 脱离等待，
  又被接缝回拉到原图；缩放后只剩贴在白色 Dialog 上的亚像素点，视觉上等同于没有。
- `physical.html` 错把归一化进度除以参考窗口的 `4700 ms`；两个离线渲染脚本还编码
  `FPS + 1` 帧，造成 60 fps 媒体实际为 `1.0167 s`。

canonical GLSL 现使用随 `uSweepDir` 旋转的三个宽边缘瓣：顺风角最强，两个侧角采用不同
权重，整圈只有很低的表层底噪；反向侧继续由单一宏观相位场承担主要消融。预脱落样本从
同一 PBD 薄面提前离面，沿统一主风做最大约 `0.046` 布面单位的有限牵伸，仍贴附的
Dialog 不移动。为防止这些最早纤维在中段先于整体飞成孤立条带，其自由输运年龄受相邻
布面张力限制，并在 `t=0.46–0.62` 平滑退场；主前沿粒子的时钟、输运和寿命不变。

离屏回归在 `t=0.10` 测得左上、上侧和左侧越过原 Dialog 外缘的粒子 alpha 质量约为
`287/13.6/6.7`，形成明确的主次关系；`t=0.47` 顶部外移约 `0.233h`，仍处于参考约束
范围内，没有留下远端独立条带。`t=1.000` 的完整层和粒子层均严格为零。方向拓扑、四边
时序、单群连贯性、运动、跨分辨率光栅和内容色回归全部通过；内容色真实 cell 表示保持
约 `4.40×`，没有因本轮边缘调节回退。

桌面夹具改为恰好 `60 fps × 60 帧 = 1.000000 s`，首末帧分别采样逻辑 `t=0/1`；关键
接触表另按精确归一化时刻渲染。网页参考 3.35–8.05 秒窗口和 canonical 模型现在都由
`1000 ms` 时钟驱动，浏览器内部完整播放实测 `1015 ms`（60 Hz 下一帧取整），终点
`model.currentTime=1.0`。另生成三列 `reference-desktop-device-normalized-1s.mp4`，参考、
桌面和真机均在同一个真实秒内播放；4.70 秒版本只保留作逐帧慢放。

在授权三星 SM-S9180 `R5CW20BLNKL` 安装当前 Debug APK，以界面树定位添加附件按钮，
打开真实“添加附件”Dialog 后用左上外部触点 `(60,700)` 关闭并完整录制。真机
`t=0.10` 可同时看到右下主释放、左上牵伸粒群和其它边缘弱起粒；`t=1.00` 已完全清空。
录屏识别出的活动起点为 `0.689389 s`，其后一个逻辑秒包含 120 帧，帧间隔中位数
`8.367 ms`（约 `119.52 Hz`）、P95 `9.523 ms`、最大 `12.789 ms`；应用进程没有 error
级日志。粒子单测和 `:app:assembleDebug` 均成功。随后按用户要求发布阿里云 Debug
版本 `202608291526`；本地 APK、远端 `latest.json` 与远端 APK 回读文件的 SHA-256 均为
`fc0aefd5f0de25489a83f1140f0dab5268cb9bca9c7ea118a0a4e49cb7a46383`。本轮没有创建 Git
提交。

## 2026-08-30 第六十六轮：开场自由边持续粒子化与 R5 放大复验

用户指出上一版左上角、上边缘和左边缘只在开场象征性起粒，随后变化极轻且近似停住，
左上拉伸也不足。本轮重新检查参考归一化早期帧、桌面 canonical 和 R5 真机原始录屏，
把“真实表面损失”“粒子质量增长”“外移距离增长”拆开诊断。

- 根因是旧预脱落只提前少量表层粒子，完整层仍等待主体释放；早发粒子的输运年龄又被
  近似封顶。现把顺风角、上自由边和左自由边以有限边界层的平滑并集纳入同一连续到达场，
  完整层与粒子层同步推进；边缘影响进入内部后连续衰减，宏观仍只有一个主前沿。
- 早发纤维越过 `0.20 s` 后保留正的飞行年龄斜率，持续进入同一主风；左上牵伸不再在
  中段回缩或停住。新增回归分别约束 `t=0.24/0.32/0.40` 的区域留存变化、
  `0.16→0.32` 的粒子质量及上、左外缘 P90 距离增长。
- 同时给唯一剥离前沿增加切向单峰弓形，使右下折叠曲线更连贯；高张力过渡区增加细尺度
  软孔，避免通过锐利 alpha 截断制造变化。锋线亮带继续由更多同尺寸粒子形成，彩色内容
  的直接数量表示为 `4.49×`，没有独立颜色轨迹。
- 形态、激活、运动、连贯性、边缘时序、方向拓扑、跨分辨率及颜色回归全部通过；
  `:app:testDebugUnitTest :app:assembleDebug` 成功。桌面 60 帧夹具严格为 `1.000 s`，
  `t=1.000` 的完整层和粒子层均为零。
- 在授权三星 SM-S9180 `R5CW20BLNKL` 安装最终 APK，用界面树定位“添加附件”并以左上
  外部真实触点 `(60,700)` 关闭。原始帧显示：`t=0.10` 左上已有真实缺口和外移粒子；
  `t=0.16` 左边缘继续向下扩展；`t=0.216–0.32` 上边缘持续向右推进，同时右下主前沿
  保持主导。最终发布 APK 的活动一秒记录 120 帧，帧间隔中位数 `8.334 ms`、P95
  `9.611 ms`、最大 `12.933 ms`，没有出现此前的卡顿回退。
- 新增开场放大三列接触表
  `final-physical-validation/early-edge-reference-desktop-device-contact.png`，并更新桌面
  `physical.html` 的真机视频、完整关键帧和开场边缘入口。
- 按用户要求通过 `:app:publishDebugUpdate` 发布阿里云 Debug 更新 `202608291731`。本地
  `latest.json` 与远端文件逐字节一致；本地版本化 APK、远端元数据记录值和远端 APK
  回读文件均为 `24,404,408` 字节，SHA-256 均为
  `4cd8edbf93cc4728f97f602a6dff8d6d09b9d1ad7db5ae5eaffeeac53b78ec12`。本轮没有创建 Git 提交。

## 2026-08-30 第六十七轮：消除四周向中心拓扑并完成桌面／R5 复验

用户指出现有粒子化仍呈现四周向中心收缩，要求先重新分析参考动画的粒子化规律，再按
背后的运动和材料解除规律复现。本轮把“首次失去完整表面的时刻”映射回 Dialog 材料坐标，
分别检查最近边缘相关性、主风相关性、四条边向内领先范围、留存质心和连通分支，而不是
只比较若干截图。

- 参考在三个 alpha 阈值下的最近边缘相关性约为 `0.30–0.37`；四条边中只有 `1–2` 条
  出现大范围持续向内领先。留存质心的主风投影随进度由约 `0.053` 迁移到 `0.156`，说明
  它不是环形侵蚀，也不会把最后材料固定在几何中心。
- 删除主体方向场与多条边缘距离场的软最小值。新增 `ParticleDismissReleaseField`，用
  方向风压、低频材料阻力、内部附着脊和若干有限宽度压力／剪切区形成单张连续相位；
  Python 桌面实现使用同一公式。平滑前不截断原始相位，避免后段大量节点同刻消失。
- 完整层与粒子层继续共享 release phase，但进一步分开贴附接缝 `seamFront`、连续输运
  `front` 和高密折叠脊 `densityFront`。高密区仍由更多同尺寸真实细粒形成，彩色内容只
  增加同轨迹副本，不进入运动、寿命、深度或粒径。
- 拓扑、早期多边缘、方向迁移、形态、柔边、连贯性／性能、折叠激活、非线性运动、跨
  分辨率光栅和颜色表示回归全部通过。模型的最近边缘相关性约为 `0.33`，四边中只有一条
  大范围领先；t=0.47 高密脊厚度约 `0.0116w`，参考约 `0.0134w`；彩色 cell 直接表示
  `3.86×`。Android 单元测试、真实 Shader 编译测试和 `:app:assembleDebug` 均成功。
- 桌面模拟器用浏览器逐一验证 `t=0.10/0.47/0.70/1.00`：两路媒体均完全加载，模型媒体
  严格为 `1.000 s`，控制台没有 warning 或 error；页面保留在 `t=0.47` 供交互检查。
- 在唯一授权的三星 SM-S9180 `R5CW20BLNKL` 安装当前 APK，以界面树定位“添加附件”，
  使用左上外部触点 `(60,700)` 关闭并完整录制。有效一秒包含 `120` 帧，帧间隔中位数
  `8.30 ms`、P95 `9.95 ms`、最大 `13.53 ms`；gfxinfo 为 `124` 帧、jank `2.42%`，
  UI P50/P95/P99 为 `9/11/12 ms`，GPU 为 `9/10/11 ms`。应用进程 error 日志为空。
- 已重建参考／桌面／真机三列归一化视频、关键帧接触表和开场边缘放大表，并让本地网页
  指向本轮真机录制。本轮没有发布阿里云，也没有创建 Git 提交。

## 2026-08-30 第六十八轮：消除下凸包曲率拼接并完成桌面／R5 复验

用户指出中段右下粒群的下凸包像多段曲线拼接，局部转折磕绊且过度突出。本轮将高密前沿
单独提取成二维密度包络，在 `t=0.40/0.47/0.54` 同时检查突出深度、低频残差、曲率步进
和曲率总变差，并用渲染分层排除把未粒子化 Dialog 白边误当成帷幔的情况。

- 根因不是 PBD 布面折叠，而是 Shader 在已经连续的释放／布面轨迹上又叠加了一条有限
  区间抛物线。该函数在区间端点只有位置连续、斜率不连续，和相邻的 capture 位移叠加后
  形成多个肉眼可见的小凸包；较长的脱离时间队列又把不同年龄层堆成过深的外包络。
- 删除横向 `frontArc` 及其截断端点，不再人为塑造局部弓形。高密前沿整体沿主方向平行
  离开仍贴附的表面 `0.030w`，因此直接继承同一释放相位和 PBD 薄面的连续曲率；脱离队列
  缩为 `0.100→0.065 s`，capture 脉冲同步降为 `2.85`，避免旧粒层堆叠和顶部过冲。
- 桌面固定场景在 `t=0.47` 的下凸深度由 `0.1355w` 降至 `0.1092w`，低频粗糙度由
  `0.0191w` 降至 `0.0157w`，曲率总变差由 `0.8533` 降至 `0.7959`。独立高密帷幔相对
  贴边 `0–4 px` 粒雾的真实密度仍为 `1.43×`，没有通过把粒子压回白边换取平滑。
- 全部形态、激活、拓扑、连贯性、运动、早期边缘、跨分辨率和内容色回归通过；Android
  945 项单元测试及 `:app:assembleDebug` 成功。桌面 simulator 已重建严格一秒的 canonical
  视频，并新增下边缘三列放大接触表。
- 在授权三星 SM-S9180 `R5CW20BLNKL` 安装当前 APK，打开真实“添加附件”Dialog，继续
  使用左上外部触点 `(60,700)` 关闭并录制。逐帧放大显示桌面和真机均为同一条连续下缘，
  不再出现多个拼接凸包。活动一秒包含 `120` 帧，帧间隔中位数 `8.289 ms`、P95
  `9.967 ms`、最大 `12.589 ms`；gfxinfo 为 `124` 帧、jank `1.61%`，UI P50/P95/P99
  为 `9/11/13 ms`，GPU 为 `8/10/11 ms`，应用日志无渲染异常。
- 按用户要求通过 `:app:publishDebugUpdate` 发布阿里云 Debug 更新 `202608300612`。本地与
  远端 `latest.json` 逐字节一致；本地版本化 APK、远端元数据和远端 APK 回读文件均为
  `24,404,408` 字节，SHA-256 均为
  `76a42bf47b446ab4cc77170b7624229ecf0f980dedf0bf150df6241f848061c1`。本轮没有创建
  Git 提交。

## 2026-08-30 第六十九轮：抛开既有结论重新测量参考，定位方向相干性为主要差距

用户反馈"改着改着越来越怪"，要求重新分析参考动画的运动模型、物理模型和粒子模型，
并做网络调研。本轮不复用任何既有测量，直接从视频重算，结论写入
[research-2026-08-30-reference-remeasure.md](research-2026-08-30-reference-remeasure.md)。

- 先纠正三个前提：视频在效果段被慢放 **3.42×**（由状态栏计时器秒进位实测），
  参考真实时长约 **1.29 s** 而非 4.4 s；参考里 Dialog 自身关闭是 150 ms 纯淡出、
  无粒子，粒子效果来自桌面卡片移除；源插画本身带碎裂笔触，已用首帧差分排除。
- **参考的完整表面全程静止**：相位相关位移 < 0.12 px，像素值与原图逐位相同。
  没有掀起、卷曲、缩放、整体淡出或半透明。PBD 薄面、2.5D 前后层、透明材质在
  参考里没有对应物。
- 释放场是**极低频连续场 + 逐格白噪声**：90% 方差在 ≥0.49 卡宽的尺度，
  抖动 sd 为平滑项的 23.9%，形成 17 px（3.6% 卡宽）的点状过渡带。拓扑为
  1 个内部主核 + 2 个角/边次核，在前 12% 时间内先后出现。
- 粒子运动可由 `v_i = W(t)·d·s_i + k_i` 完全解释：方向恒定 119–134°，
  速度线性升到 n=0.6 后走平，逐粒子方向 sd 从 29° 单调收窄到 15°，速度变异
  系数恒为 0.4。横向固定踢约 0.21·W_max。质心总位移仅卡片尺寸的 13%，
  P90 外扩 16% 卡宽。
- 浓淡 8× 的对比来自寿命衰减（半衰期 0.17、年龄 0.70 熄灭），前沿亮度只比
  源色高 10–20%。完整表面在 n≈0.70 消耗完，末 30% 为纯尘埃。
- 用 `render_curtain_model.py` 输出跑同一套脚本得到对照。最大差距是
  **逐粒子方向 sd：参考 20.7° vs 当前 90.3°（n=0.45）**，根因是
  `SWIRL=0.75` + `FLOW_EVOLVE=0.5` 打散、`PINCH=0.96` 再拽回轴。其次是
  `ARC_BOW=0.20`/`WAVE_WARP=0.14` 造出参考并不存在的正弦揭开边界、
  `DISMISS_REPLICAS=5` 糊掉颗粒感、`LIFETIME=0.58` 尾段过早清空、
  外扩距离 32% 卡宽（参考 16%）。
- 已列出与 2026-08-27 若干定案（2.5D 片层、透明材质、3–5 个近同时起点、
  窄帷幔、`ARC_BOW` 的"华为截图弓形"依据）冲突的部分，等待用户裁定，
  本轮未改动任何实现代码，未构建、未发布、未提交。

## 2026-08-30 第七十轮：按复测结论重建消散模型，桌面与真机一致

用户确认按 [research-2026-08-30-reference-remeasure.md](research-2026-08-30-reference-remeasure.md)
的方案实现，要求先桌面、再 Android、两端一致，并在 `R5CW20BLNKL` 上验证、
产出对照视频和可播放的桌面模拟器。

- **先纠正了自己上一轮的窗口错误**：参考真正收干净在 `t=8.38 s` 而非 7.70，
  窗口应为 `3.28→8.38 s`（5.10 视频秒，真实约 **1.49 s**）。据此重算全部归一化
  数值：表面消耗完 `n≈0.60`、占位率半衰期 0.145、浓淡比 12×、抖动/平滑 0.39、
  过渡带宽 4.9% 卡宽、锋线速度 0.83 卡宽/单位 n。研究文档与报告已同步修正。
- **补测寿命律**：把释放时刻分成五组分别看归一化衰减，五条曲线几乎重合，
  说明**寿命与出生时刻无关、参考没有全局收尾包络**；曲线有闭式
  `存活率 = (1 − age/0.62)²`，因此逐粒子寿命取 `lifeMax·(1 − √u)`。
- **否决布褶重叠假设**（用户提出的浓带成因）：覆盖率上限只能贡献 1.18×、
  静止表面位移 < 0.12 px、年龄回归后的残差各向同性 1.00–1.33。
- **实现**：重写 `DISMISS_FIELD_GLSL` / `DISMISS_VERTEX_SHADER` /
  `DISMISS_FRAGMENT_SHADER` / `DISMISS_STILL_FRAGMENT_SHADER`；删除
  `ParticleDismissClothModel.kt`、`ParticleDismissReleaseField.kt`、薄面与释放场
  纹理、`ARC_BOW`/`SWEEP_TIME`，`DISMISS_REPLICAS` 由 5 降为 1。渲染器由
  1961 行降到 1325 行，另删两个文件 743 行；顶点数由约 6.9 万降到 1.2 万。
- **两端一致**：除几何、时间、种子、方向外全部参数改为 GLSL `const float`，
  释放核也在 Shader 内由种子与方向生成，桌面 canonical 直接抽取同一段源码。
- **迭代中发现并修掉三个真问题**：释放相位的最大值随种子在 0.84–1.14 漂移
  （改为自归一化构造后恒为 0.993）；桌面探针用了 1280×900 的 viewport 却渲染到
  720×420 的 buffer，导致释放图大面积为 0；粒径 0.5 格距使覆盖率只有参考的
  一半（改为 1.15 格距）。
- **桌面验收**（四个方向／种子全部通过）：maxPhase 0.993、表面消耗完 0.57–0.58、
  过渡带宽 3.5–3.9% 卡宽、外扩 P90 16.9–20.1% 卡宽、粒子亮像素峰值 27–29%、
  衰减律最大偏差 0.081–0.088。
- **同口径对照**（用测参考的同一套脚本量模型）：抖动/平滑 0.397 vs 0.412、
  过渡带宽 0.050 vs 0.063、外扩 P90 0.173 vs 0.177、速度变异 0.43–0.48 vs
  0.41–0.49、速度包络均单调上升。仍存在两项二阶差异，见 followups。
- **测试**：`ParticleDismissClothModelTest` 删除，`ParticleDismissShaderCompileTest`
  重写为新模型的契约（逐项点名允许的用法，不做宽泛关键字搜索）。
  `:app:testDebugUnitTest` 本轮真实执行 **932 项、跳过 1、失败 0**，其中
  glslangValidator 对四段 GLES3 着色器的真实编译 skipped=0。
- **真机**：`R5CW20BLNKL` 安装后打开真实「添加附件」Dialog，以左上外部触点
  `(60,700)` 关闭并录制。逐帧确认与桌面同构：内部起核 → 有机扩张 → 逐格硬切
  交接 → 单方向漂散 → 稀疏尾粒。有效窗口 `4.15–5.10 s` 共 114 帧，帧间隔
  中位数 **8.40 ms**、P95 **9.47 ms**、最大 **10.15 ms**（约 119 fps，无掉帧）；
  gfxinfo 173 帧、jank 3.47%，GPU P50/P90 = 2/12 ms。
- **产物**：`tmp/particle-dismiss-tuning/cloth-motion-prototype/comparison.mp4`
  为参考／桌面／真机三列同步对照（各 1.000 s）；同目录 `physical.html` 为可
  播放、可逐帧拖动的桌面模拟器（改用 fetch + Blob URL，绕开
  `python -m http.server` 不支持 Range 的限制）。
- 本轮没有发布阿里云，也没有创建 Git 提交。

## 2026-08-30（晚）折叠压缩与寿命上限

- 验证了用户"华为的粒子化在三维空间内做"的判断：回归掉年龄与源亮度后，参考的密度
  残差是我们的 1.6–1.7 倍，且沿稳定主轴拉长。
- 以**投影压缩**（顶点着色器里的材料位移）实现"叠在一起的布"，不引入布料求解器。
  新增常量 `CURTAIN_FOLD_COUNT/AMPLITUDE/BASE/TILT`，并在契约测试中锁死。
- 分清了两件被混为一谈的事：折叠决定密度的**结构**（各向异性 1.11→1.31），寿命上限
  决定同一瞬间活着的**总量**（litPeak 40%→60%）。`CURTAIN_LIFE_MAX` 0.55 → 1.05。
- 11 个 particledismiss 单测通过（skipped=0，glslangValidator 真实编译），
  `:app:assembleDebug` 成功。
- **真机验证未完成**：R5CW20BLNKL 本轮已断开，只剩未授权的另一台设备，未上机。
  对照视频本轮只含「参考 / 桌面蓝本」两列，未拿上一版录像充当当前版本。

## 2026-08-31 侵蚀边界与折叠走向

- 定位并修掉三个构造性缺陷：中心度的矩形等值线（横贯亮线 + 对角折痕导致的尖角）、
  顺风角平顶的干净圆弧（角内分形被刻意压到 15%）、折叠相位沿脊方向不变（笔直斜带）。
- 新增 `tmp/particle-dismiss-tuning/analyze_edge_and_folds.py`（边界卷曲度/过渡带宽、
  折叠取向弥散/沿脊与跨脊相关长度）与 `diagnose_edge_corners.py`（释放场等值线图）。
- 新增 `tmp/particle-dismiss-tuning/detect_device_window.py`：真机录屏窗口自动检测。
  **screenrecord 在 R5CW20BLNKL 上输出约 20fps**，按 60 算窗口会错 3 倍。
- 契约测试新增去注释断言辅助 `stripComments`：解释某构造为何被废弃的注释里必然写着
  那个构造本身，字面搜索会把正确代码判成失败（本次实际发生）。
- 11 个 particledismiss 单测通过（skipped=0），真机 124 帧、jank 0.81%、90 分位 12ms。

## 2026-08-31（续）粒子云的运动与前沿

- 删除了「横向发散」项。它的依据是我此前对参考的误读：把「沿风/横风伸展比下降」
  当成横向在膨胀，实际是纵向在收缩。拆成分量后横向 sd 斜率是 −0.036。
- 认清密度与前沿的取舍：`CURTAIN_LIFE_MAX` 长则画面是历次释放的累积，没有前沿。
  1.05 → 0.70，平移速率 0.678 → 0.919（参考 0.895），膨胀/平移 0.24 → 0.10。
- 新增 `analyze_spread_and_front.py`（膨胀/平移、前沿锐度、轮廓曲率半径）。
- `detect_device_window.py` 改用每帧真实时间戳：screenrecord 是变帧率，静止期几乎
  不出帧、动画期跑到约 117fps，按平均帧率换算会把窗口算错一倍以上。
- 11 个单测通过（skipped=0），真机 124 帧、jank 2.42%、90 分位 12ms。

## 2026-08-31（三）预变暗、角块、越界、粒度

- 四条逐条量化并落实：静止层释放前压暗到 0.78 且新生粒子从 0.86 起步；角块取消
  分形抑制、改为错开释放并按错开系数重新定标提前量；折叠在轮廓处收敛，右/下越界
  归零；网格 512 → 256 列并新增逐粒子粒径倍率。
- 两个测量陷阱记入研究文档：GLSL 的 `smoothstep` 在 edge0 > edge1 时未定义（整条
  变暗静默失效）；用合成帧求硬释放时刻求不出来（粒子停在原位挡住了释放瞬间）。
- 11 个单测通过（skipped=0），真机 124 帧、jank 1.61%、90 分位 14ms。

## 2026-08-31（四）前沿形状的生成规律

- 用梯度签名区分了三种候选物理：参考既不是平面扫掠（方向只占 25% 方差），也不是
  常速法向传播（到点距离 R² 仅 0.37、|∇T| 离散度 0.82）。
- 关键结论：**前沿形状由到达时间场里非方向的那部分决定，且它集中在 0.11-0.28 卡宽
  这一档**。方向项占到六成时等值线必然是直线。
- 据此新增 `swell` 项补该档、抬高 fbm 幅度，方向项不动。方向占比 0.61 → 0.28
  （参考 0.25），|∇T| 离散度 0.61 → 0.83（参考 0.82）。
- 连带调整：`CURTAIN_CORNER_ADVANCE` 1.24 → 1.00、`CURTAIN_DITHER` 0.187 → 0.28。
- 新增 `tmp/particle-dismiss-tuning/analyze_front_law.py`。11 个单测通过（skipped=0），
  真机 124 帧、jank 2.42%、90 分位 18ms。

## 2026-08-31（五）开场的同时成核

- 病因是 clamp 饱和：第 14 节把加性幅度加到 3.2 而场的值域只有 1.0，6.89% 的格子
  释放时刻被压到恰好 0（参考 0.18%）；逐格抖动提到 0.28 后又压掉一批。
- 证伪了坐标形变这条思路：T 只依赖 dot(q+warp, direction)，形变的横向分量无效、
  沿向分量与加性噪声等价，方向性占比只从 0.632 动到 0.611。
- 改为把起伏限制在 ramp·(1-ramp) 的带宽内（atan 归一化），相位数学上不可能越界；
  抖动同样两端收敛；另加一条单调的消耗速率重映射。
- 结果：t≈0 的格子占比 0.0689 → 0.0013，开场收敛成单一贴边小块（1 个连通块、
  空间散布 0.093）。代价是方向性回升到 0.430。
- 新增 `analyze_front_topology.py`。11 个单测通过（skipped=0），真机 123 帧、
  jank 4.07%、90 分位 17ms。

## 2026-08-31（六）前沿之前的灰斑

- 用户圈出的区域不是提前粒子化：该处释放时刻 0.484，与纯斜坡预测 0.490 一致；
  「离前沿主体超过 0.06 卡宽的侵蚀面积占比」也比参考更低。
- 真正的病因是预变暗：范围过大（n=0.35 时 40.4% 的完整表面被压暗，参考 24.2%），
  且变暗量用含逐格抖动的释放时刻算，相邻格差一大截，成了散布全卡的斑驳灰。
- 改为用不含抖动的平滑释放时刻算变暗，并收窄提前量、补回深度。
- 新增 `analyze_detached_patches.py`。11 个单测通过（skipped=0），真机 123 帧、
  jank 3.25%、90 分位 17ms。

## 2026-08-31（七）建模自由度的纠正

- 先验证目标：参考释放场的测量噪声约占 9% 方差，方向性真值约 0.33-0.39（我一直
  用的阈值给出 0.254，偏低），而模型当时已是 0.430——量早就接近了。
- 真正的问题是用二维噪声去塑造只有一个自由度的前沿：二维场里任何地方都能出现
  低值坑，必然产生脱离前沿的独立区域；为把幅度压回值域而加的 atan 增益又让场
  退化成两级台地，前沿出现尖点。
- 改为扰动前沿的位置，并把扰动沿飞行轴的变化率约束在斜坡斜率以内，使「单一连通
  前沿」成为数学保证而非调参目标。
- 新增 `validate_reference_field.py`、`analyze_front_geometry.py`。11 个单测通过
  （skipped=0），真机 123 帧、jank 4.07%、90 分位 18ms。

## 2026-08-31（八）运动模型重建

- 定位到「不可能像纱」的结构性原因：非均匀运动只有逐粒子白噪声，空间上不相关。
- 调研后改为 curl noise 相干湍流 + 纱面高度场 + 透视投影 + 法向明暗，删除全部
  `CURTAIN_FOLD_*` 静态折叠构造。
- 放大 4 倍才看清的缺陷：粒径 3-9px 而网格步长只有 2.81px，粒子互相重叠糊成一片。
  `CURTAIN_POINT_RATIO` 2.15 → 1.15。
- 11 个单测通过（skipped=0），APK 构建成功。真机已拔出，本轮只在桌面端验证。

## 2026-08-31（九）明暗带与添加附件场景

- 把「翻滚」化成可测量：云内部局部亮度的变异系数。参考 0.236-0.308、相关长度
  127-156‰卡宽；模型改前只有 0.094-0.097，尺度对而强度差三倍。
- 据此放大法向坡度增益与明暗区间（并把透视从形同虚设的 1.09 倍改到真正起作用），
  改后 0.241-0.271 / 128-136‰。
- 新增 `analyze_rolling.py` 与 `render_attachment_scene.py`（按真机截图重建的
  「添加附件」桌面场景，手机不在手边时用它判断真机观感）。
- 11 个单测通过（skipped=0），APK 构建成功。真机未参与本轮验证。

## 2026-08-31（十）补上风与形状的耦合

- 查明模型里没有风：`curtainWind` 只是标量速度乘固定方向、对所有粒子相同；
  `height` 只进 perspective 与 normal，从不进入位置。风与形状零耦合。
- 补上 `F = k (U·n) n` 的法向压力：z 分量放大已有的鼓起（颤振失稳的一步显式
  迭代），xy 分量搬运材料；明暗改用同一个面法向。
- 凸起、折叠、翻滚由此成为同一个反馈的三种表现，不再各自捏。
- 11 个单测通过（skipped=0），APK 构建成功。真机未参与。

## 2026-09-01（二）拆掉假光照，补齐产物

- 用废止实验证明断层完全来自明暗：关掉明暗后云是连续的一片，黑带里粒子都还在，
  只是被压到与背景等亮。第 22 节按「透视撕裂」的诊断是错的。
- 更根本的纠正：「云内部明暗带」这个目标测错了。参考**源图**的局部亮度变异系数是
  0.537，高于云内部的 0.24-0.31——那些带是照片自身的明暗被粒子带着走，不是光照。
  我们的源图只有 0.102，这个指标追不得。
- 删除 `CURTAIN_NORMAL_GAIN`，明暗改用真实几何法向、区间收到 0.84-1.14；折叠同步
  收轻（wave 0.16→0.075、height noise 0.16→0.085、press 0.30→0.16）。
- 新增 `render_timeline_strip.py`（十个时刻的并排长条）。用它推翻了自己的两个目测
  结论：后段清空并不慢、参考也没有飞出可见区域，两者都是裁剪尺度造成的假象。
- 11 个单测通过（skipped=0），APK 构建成功；全部产物已重新生成。真机仍未接入。

## 2026-09-01（三）删掉中心钉

- 粒子化中途卡片正中出现的白圆来自「中心钉」`mix(low, 1.0, pow(centreness, 16.0))`：
  它把正中一小块的相位硬顶到上界，纯粹是归一化手段，却让那块成了最后消失的完整
  表面；而且它连归一化都没做到（maxPhase 实测 0.928）。已删除。
- 残留区的形状改为在被 sway 扭过的坐标上算 centreness，不再是规则椭圆。
- 11 个单测通过（skipped=0），APK 构建成功，全部产物重新生成。真机仍未接入。

## 2026-09-01（四）四列对照与真机复测

- 对照视频、轮廓对照、逐时刻长条统一扩到四列：参考 / 桌面 canonical /
  桌面「添加附件」/ 真机。逐时刻长条改为读归一化分片，并标注每行的裁剪范围。
- 「桌面添加附件」与「真机」两列走势高度一致，验证了桌面重建可用于替代上机判断。
- 四列摊开后最突出的差异：参考在 n=0.50 之后材料明显飞离卡片，我们三列都是原地
  变淡。与场景无关，是模型问题，列为下一轮主要目标。
- 真机 R5CW20BLNKL：122 帧、jank 4.92%、90 分位 23ms。

## 2026-09-01（五）同内容对照、帷幔与性能

- 新增「参考内容 + 我们的动画」列与 `analyze_same_content.py`，内容变量被隔离后
  才第一次有了真正同口径的测量。
- 帷幔差的是团的个数不是浓度：新增中尺度成团项；并把细尺度项的包络从
  `ramp·(1-ramp)` 改成骑在最终相位上的 `min(phase, 1-phase)`——此前顺风角处包络
  恰好为零，缺口边缘完全没有细纹理。高密块 2/4/3/6 → 3/6/12/7（参考 4/6/9/12）。
- `CURTAIN_FRESH_BOOST` 方向搞反了（0.86 比源色暗）：参考是「先变暗 → 释放出明亮
  粒子」，这个跳变才是前沿那条亮带。改成 1.45。
- 性能：噪声调用数是瓶颈，不是重复求值。合并角块噪声、clump 降为两 octave 后
  jank 11.5% → 1.61%、90 分位 26ms → 17ms；但 grain 的最细 octave 不能砍，砍了
  缺口过渡带宽掉一半且提幅度补不回来。
- 11 个单测通过（skipped=0），真机 124 帧、jank 3.23%、90 分位 17ms。

## 2026-09-01（六）消耗曲线的形状

- 「我们整体滞后」是目测误判。同内容下量出来是：开场慢 2.5 倍、中段反超，时间比值
  1.57 → 0.90，是 S 形不是平移。
- 根因是线性斜坡在卡片上呈三角分布，中间那一档格子扎堆，消耗曲线必然 S 形；参考的
  分布接近均匀（它的场里非方向结构占主导）。速率指数只能调快慢、调不了形状。
- 用三角分布自己的 CDF 对 ramp 做直方图均衡（单调重映射，等值线不变），配指数 0.60。
  与参考的最大偏差从 0.112 降到 0.055。
- 11 个单测通过（skipped=0），真机 124 帧、jank 3.23%、90 分位 16ms。

## 2026-09-01（七）角的起爆点与羽流的证伪

- 左上角「从洞开始」的根因：减法式提前量在鼓包外沿留下一圈局部极大（释放场探针上
  直接可见）。改为把顺风角当作第二个起爆点、与斜坡取 min——两个单调波前取 min 天然
  无局部极大。核半径必须覆盖整个圆角区域。
- 与第 17、25 节同类：为局部目的引入的加减法构造会在场里留下不该有的极值。
- 羽流那条目测不成立：同内容下我们的聚拢度（0.527 对 0.450）与粒子平均亮度
  （138.8 对 118.1）都高于参考，印象来自参考照片自身的亮部。
- 11 个单测通过（skipped=0），真机 124 帧、jank 2.42%、90 分位 18ms。

## 2026-09-01（八）羽流 = 前沿的宽度与亮度

- 证伪了两个假设：参考的前沿褶皱既不横向行进（漂移 0.0000），形状更新率也比我们低
  （0.0211 对 0.0400）。新增 `analyze_front_flow.py`。
- 放大前沿才看清：参考是一条很宽的过渡带、碎成细密明亮的尘；我们是一条窄而硬的边
  加一薄条粗颗粒。
- 带宽 ≈ 主导细尺度项的相关长度，加幅度会饱和。把幅度从 grain（0.05 卡宽）挪到
  clump（0.12 卡宽）；带宽 78/64/52/48 → 71/64/68/88（参考 103/75/86/70）。
- 「亮尘 vs 黑洞」：新生粒子必须更大（CURTAIN_FRESH_SIZE = 2.00，随年龄回落），
  推翻了此前「粒径不得随年龄变化」的契约。
- 11 个单测通过（skipped=0），真机 124 帧、jank 4.03%、90 分位 18ms。

## 2026-09-01（九）带宽口径纠错与 Stokes 数

- 第 31 节的带宽统计作废：距离变换把「孤岛多」读成「带宽大」。改用沿扫掠轴的一维
  剖面后，我们的带宽后段比参考还宽（242/255 对 219/211），参考真正多的是孤岛
  （n=0.30 时 22 个对 5 个）。
- 调研得到轻盈感的两个来源：Stokes 数（τ ∝ 直径²，细尘缠绕 + 粗粒穿行两层）与
  涡丝（细长涡而非圆 churn）。
- 实现：tau 与湍流耦合都按逐粒子 Stokes 数变化；湍流势沿飞行轴拉长；新生粒径
  2.00 → 1.55。
- 11 个单测通过（skipped=0），真机 124 帧、jank 4.03%、90 分位 18ms。

## 2026-09-01（十）取消全部加性相位项

- 孤立暗区与凹凸不平的边界同源：第 27 节为做帷幔引入的加性相位项会造出局部极小
  （孤岛），并破坏相位沿飞行轴的单调性（前沿不再单值）。
- 与前沿单值性、中心钉、减法式角提前量属同一类：**任何直接加减到相位上的局部构造
  都会留下不该有的极值**。
- 处方：删除全部加性细尺度项，改为给 sway 增加两档更细的octave——位置扰动受不折叠
  约束，前沿永远单值光滑，孤岛构造上不可能出现，团块由前沿局部快慢自然形成。
- 孤岛数 4-10（参考 8-22），消耗曲线仍对齐。11 个单测通过（skipped=0），
  真机 123 帧、jank 3.25%、90 分位 16ms。

## 2026-09-01（十一）删掉亮度压暗，前沿蜿蜒、交接带宽窄起伏

- 用户禁止用数值判断观感，本轮全程按放大帧目视比对。
- 白底上的「莫名其妙的暗区」＝ 释放前的亮度压暗。用平滑时刻算是一片脏影，用逐格
  时刻算是一片灰阶椒盐；参考根本没有亮度压暗，那里的变暗全部来自散格露出深色背景。
  整段删除。
- 羽流平直＝ 起伏最粗档在整卡上不足一个周期，等同常数偏移；频率 1.15→1.70、
  幅度 0.62→0.95、沿轴变化率 0.28→0.16。
- 羽流又粗又匀＝ 交接带处处等宽 + 粒子铺满一格。加低频噪声调制抖动宽度，粒径改成
  亚格（FRESH_SIZE 1.55→1.28、POINT_RATIO 1.15→1.02）。
- 顺带修两个构造性缺陷：抖动越界（改为对余量取 min）、斜坡量程未给起伏留余量
  （加 CURTAIN_SWAY_ROOM，clamped 归零）。
- 桌面门限里记录旧观感的四条重新取值、occupancy 一条移除，理由写在脚本里；
  四个方向全过。11 个单测通过，真机 125 帧、jank 3.20%、90 分位 18ms。

## 2026-09-01（十一）静止层改可形变网格，压暗由几何产生

- 核实参考并未掀起控件：轮廓从不越出首帧外形、边缘内容不平移不压缩、边上的
  「亮边」是分离的粒子而非连续高光。
- 但参考确有前沿之前的平滑压暗；它在纯白对话框上读作脏影，因此改由真实几何产生：
  静止层从平四边形改成 40×40 可形变网格，前沿附近卷起并走同一套透视投影。
- 三个坑：斜向光在白卡上被截到白（改用法向 z 分量）；暗带必须宽过抖动的侵蚀区
  （深度与宽度必须拆成两个常数）；第 0 帧必须与原窗口逐像素相同（释放起步偏置
  ＋角核缓升＋卷起时间包络）。
- 网格分辨率直接换耗时：96 时 90 分位 31ms、jank 17.4%；40 时回到 16ms / 3.36%。
- 工作预览按用户要求改用「参考控件内容 + 我们的动画」，产物
  `gap-analysis-20260830/32-参考内容对照.jpg` 与 `frames-refcontent/refcontent-slow.mp4`。

## 2026-09-01（十二）掀起动作去机械化

- `lift = f(lead)` 让每一点走同一条曲线、只差时间平移，观感是一整片一起匀速鼓起。
- 三处一起改：逐区域的最终高度与快慢（两档低频噪声）；掀起本身加缓入缓出；叠一层
  局部上下摆动，相位里带低频噪声所以各片各摆，而不是整块控件一起上下。
- 摆动必须一起进斜率，否则有起伏而明暗是平的，仍旧是死的。
- 11 个单测通过，四个方向的桌面门限全过。真机未验：R5CW20BLNKL 已断开。

## 2026-09-01（十三）掀起归粒子群，羽流有正反两面

- 纠正方向：参考掀起的只有粒子群，控件本体不动。静止层的透视位移删除，只留明暗，
  并改述为「被掀起的纱面挡在控件与光之间」。
- 羽流按用户给出的空间关系重做：卷角随年龄单调增大，超过 90° 的部分法向 z 变负，
  自然分出折脊两侧的亮面与暗面；明暗下限 0.84 -> 0.46（不得低于 0.38）。
- 11 个单测通过，四个方向桌面门限全过。真机未验。

## 2026-09-02（十四）逐帧逐像素对齐参考

- 建了三件工具：逐帧差异分解、释放时刻场对照、从着色器探针直接做参数网格搜索。
- 先修掉三个测量缺陷（裁剪错位 5 像素、判据被内容带偏、桌面渲染静默失败），
  否则所有结论都是错的。
- 参考的场 = 线性扫描 + 中心推后 + 起伏，起伏能量 98% 在整卡 1-3 个周期。按此把
  扫描方向、时间跨度、中心推后、起伏频谱逐项对齐。
- 两个假设被实验推翻：均衡不是起伏的瓶颈（减弱反而更差）；加大幅度会饱和。解法是
  让起伏幅度随行程增长。
- 粒子层：翻面判据随年龄退场（后段亮度偏差 −44 → −29）、粒径 1.02 → 1.26。
  粒子 IoU 全段提升约 20-70%。
- 11 个单测通过，四个方向桌面门限全过。真机未验：R5CW20BLNKL 未连接。

## 2026-09-02（十五）Fable 接手：六项设计定案，桌面草稿先行

- 用 /grill-with-docs 逐题定案（决策见 decisions.md 当日六条、ADR 0006、CONTEXT.md 术语）：
  释放场删中心项改速度剖面；慢段随机不贴边；粒子出生继承纱布速度；变暗窄而浅逐格；
  控件本体不动；不加 Z 轴几何只复现痕迹。
- 用户要求"先改桌面模拟器，OK 再改 Android"：新增 `tmp/particle-dismiss-tuning/shader-draft/`
  草稿着色器，模拟器与调参器优先读草稿；Android 源码里本轮的释放场改动已撤回。
- 草稿四轮迭代（每轮 306 帧逐帧对照）。终态：分界线慢/快车道比 0.44（参考 0.37）、
  释放分位 0.14/0.30/0.57（参考 0.12/0.36/0.56）、最后消失的那片长宽比 2.0 落在左偏中、
  粒子从出生就在动（在动占比 0.99）、速度 120→433（参考 162→425）、方向散布 36°→14°
  （参考 51°→12°）、粒子峰值 0.21（参考 0.27）、逐帧 MAE 均值 18.3（起点 19.9）。
- 关键改动：每格 3 个粒子（DISMISS_REPLICAS=3，消散顶点着色器按副本拆 gl_VertexID）；
  风的拐点 0.42、风速比 0.60、随机踢 0.35、继承 0.65；寿命上限 0.80、末尾按粒子随机
  错开在 0.90 前清空；折脊亮面 0.90、暗面 0.62、翻面判据 0.16 内退场。
- 已知剩余差距：参考底边中部化得更早（我们从右边缘起）；n=0.4 附近云偏灰；
  n=0.62 我们略快。待用户看图裁定后再移植 Android。

## 2026-09-02（十六）底边先化、单向前沿、羽流边界行

- 用户指出两处：参考开场底部"被风从下面掀起"形成亮雾，我们没有；收尾时最后那片被
  粒子从四周合围。看图确认：参考 n=0.18 底边一整条已化成向上升的亮雾；我们的脸在
  收尾时被左上角核和顶边反向吃过来。
- 草稿改动：删除顺风角核与三个局部核（`curtainNucleus` 整段删除）；加**迎风两条边
  的早化带**（`CURTAIN_EDGE_*`，带深 0.50、带内相位 ≤0.10、出带迅速抬到 1 以上——
  不封顶时线性到达时刻会一直涨到卡片对面，把整张卡压早，p90 从 0.56 掉到 0.37）。
  看图确认底部升起、收尾单向。
- 对比视频改为 2x2：下排叠羽流边界（亮于背景 55 级的粒子、15px 密度 >0.10 的外
  轮廓，两列同判据）。`make_draft_comparison.py`。
- "两条羽流"：分区域光流实测参考各区域方向均为 217–263°（左上），无区域向下——
  两条是两个来源中间隔着已飞光的快段，不是翻折成两层。
- 老粒子加随年龄变暗（`CURTAIN_OLD_DIM`）。逐帧扫描取 RATE_EXPONENT 0.80、
  RELEASE_END 0.92。终态：羽流边界内面积 n=0.25/0.37/0.50/0.62 为 69/85/87/72（参考
  64/72/94/101）千像素；表面中段仍比参考快约 0.04–0.14；后段老粒子仍偏亮 +50。

## 2026-09-02（十七）叠加图定位形状差异，收尾不再是实心块

- 对比视频加第三行：叠加（参考→红通道、草稿→青通道）与逐像素差异图，形状差一眼
  可见。`make_draft_comparison.py`，两列三行。
- 叠加图指出 n=0.586 的青色大块 = 我们左上还完整；据此把顺风角改回"点起火"（无核
  无平顶无亮边），并去掉新生粒子亮边、抖动分方向钳位 + 粗孔。
- 终态（逐帧）：表面曲线 0.873/0.761/0.644/0.533/0.414/0.320/0.271 对参考
  0.859/0.780/0.719/0.637/0.482/0.319/0.264；粒子占比与羽流面积全程对齐（峰值 0.244
  对 0.270；羽流面积 23/57/70/104/100/64 对 23/64/72/94/101/56 千像素）；MAE 均值
  19.2、粒子 IoU 0.236、表面 IoU 0.646，均为历轮最好。
- 仍差：n≈0.4 中段我们略快；右侧羽流轮廓偏方；左下第二条羽流比参考小。

## 2026-09-02 第十八轮：点源扩张模型落地（桌面草稿）

按用户"分析华为动画为什么是那样的形状"的要求，对参考释放时间图做了源点/距离/方向三项统计，
得出"胸口一块起火、向右与右上快、向左慢、左上角独立起火"的法则，写进桌面草稿的
`curtainReleasePhase`（源点、各向异性慢度、同期起火的头部半径、角落独立起火），删掉了斜坡、
车道慢段、上下边带。用户"做吧，试试"后跑了三版常量，取 SLOPE 0.30 / ANISO 1.8 /
CORNER_START 0.34 / HEAD 0.18 这一版交付。指标见 decisions。已交付 3 行对比视频
`tmp/particle-dismiss-tuning/frames-refcontent/compare-2col-slow.mp4`。剩余差异：0.16–0.25
略慢（早期表面多 6%）、0.41–0.49 略快（少 8–12%）、右羽流不如参考高。Android 仍未改。

## 2026-09-02 第十九轮：扇区快慢、远处平台、出生照白（桌面草稿）

用户指出分界线凸向相反、羽流边缘不够亮。新增 `measure_sector_band.py`（按方向统计释放时刻、
按到完好区距离统计粒子亮度）和 `metrics_perframe.py`（逐帧汇总）。扇区朝向两次推导都错，
改为 numpy 穷举符号/轴向与实测相关后标定旋转量。中途试过"按出生时刻开闸的照白"，早期粒子
暗到与纱面分不开，已撤回。结果与剩余差异见 decisions。视频仍是
`frames-refcontent/compare-2col-slow.mp4`。Android 未改。

## 2026-09-02 第二十轮：点源/扇区作废，回到"垂直于风向的前沿沿风向平推"；经验总结

用户看过扇区版后否定：开场从右下角向四周扩散、整个粒子区域明暗乱动。改为逐帧目视参考
（八个时刻的对照表 `perframe/sheet-ref-vs-ours.png`）后重定几何：前沿是一条与风向垂直的斜线，
从最迎风的角（右下）出发沿风向（光流 -125°，左上）平推，粒子沿同一方向、比前沿慢地跟着走，
前沿身后很快就空；顺风角另起一团；前沿中段滞后。SCENARIO_ANGLE 改为 235。去掉湍流、随机踢、
折面明暗、随年龄的亮度变化。指标 MAE 16.9、表面 IoU 0.684，但用户判定粒子蓬松、羽流没有边界。

经验（本轮之后的判断）：
1. 对参考做统计拟合（按方向的释放时刻、IoU、MAE）连续两次把结构猜错（点源、扇区），数字
   好看而画面不对；最后是把八个时刻并排目视十分钟定下的几何。指标只能做回归检查，不能定结构。
2. 坐标约定推了三次都错，见记忆 calibrate-conventions-by-measurement。
3. 宏观几何已经清楚且简单（ADR 0006 的家族是对的，方向错了）。剩下的差距不在释放场，在粒子
   模型本身：逐粒子的随机踢、速度散布、粒径散布、逐格抖动把释放出去的薄片打成雾；参考的羽流
   边界其实是**卡片自己的边被气流搬走后的形状**，只有让释放出去的材料作为一张连续薄片整体
   运动才会有这条边。
4. 逐粒子随时间变的亮度（照白褪去、折面翻转、老化变暗）读作闪烁；亮度要么来自平滑曲面的光照，
   要么是常量。

## 2026-09-02 第二十一轮：连续薄片原型落地（桌面，`tmp/particle-dismiss-tuning/sheet/sheet_model.py`）

grill 定案后新写的原型：三角网格 + numpy 逐帧平流（风 + 两尺度 curl noise，幅度随离开时间
增长）+ 前沿处按年龄的抬起鼓包 + 逐像素噪声阈值溶解（粗斑块拉开到 0-1 + 细颗粒，硬切）+
法向光照双面异色。静止层是一张平四边形，年龄从释放场纹理逐像素取；飞行层只画含已释放顶点的
三角形，年龄 < 0 的片元丢弃，前沿按像素平滑。输出 `frames-sheet/compare-sheet-slow.mp4`、
`sheet-contact.png`。

踩过的坑（都已修）：
- `gl_FrontFacing` 被 y 翻转的投影搞反，整张薄片按背面画成灰色 → 改按法向 z 判正反。
- 溶解切边宽度按 `fwidth(pattern)` 取，被细颗粒撑到 0.3，整张薄片半透明 → 固定 0.01。
- fbm 取值集中在 0.35-0.65，全部像素几乎同时过阈值 → 粗斑块 smoothstep(0.3, 0.7) 拉开。
- 抬起按速度积分会让老材料更高、法向背光，前沿反而暗 → 改为按年龄的鼓包直接给 z。
- 风 700 px/单位时间时材料以接近前沿的速度追着前沿走，网格压缩五倍以上、翻折成一条细带
  （三角形面积 1.6-4.4 px²）→ 风 460，约两倍密的带。
- 静止层与飞行层按整格切换，前沿留下一格宽的黑锯齿 → 飞行层画含任一已释放顶点的三角形。

## 2026-09-02 第二十二轮：用户三问之后的修正（薄片原型）

用户问：左上角那团是什么、内容为何被放大；是否是往 Z 轴上方卷；为何从控件中间开始碎。
答与改：
- 那团是"顺风角另起一片"。放大主要来自我让飞行层画"任一顶点已释放"的三角形，释放区边缘
  的三角形被拉在飞走与未动的顶点之间，内容拉大一两倍 → 改回只画整片释放的三角形，静止层
  多保留 0.015 盖住接缝。
- 材料朝观者抬起并被风带走，没有翻卷；红框处是最老的材料在溶解，不是背面。
- "前 40% 寿命保持完整"是我自己加的、加错了，破碎点落在带子尾巴上 → 溶解从脱离前沿的那
  一刻开始，粒子化发生在前沿那条边上。
- 光照改为以平放薄片为基准（平的面亮度 1、不提白），否则静止层与刚释放的格子亮度不同、前沿
  出格子边，整片抬起的平台被均匀提白。透视 Z_CAMERA 700→1400，唇高 45→35，LIFE 0.30→0.24，
  褶皱增长 3000/900→2000/700（0.60 时最后一片被拉成斜条）。

## 2026-09-02 第二十三轮：从翻板到"弯起脱离飞走"，用户首次认可方向

薄片原型这一轮经过：圆柱整卷（太尖）→ 浅卷封顶（超出部分成平放复制品）→ 倾斜平板（像翻面）
→ 弯到 46° 脱离、沿风向飞走并碎成尘（用户认可，"接近我想要的效果"）。期间去掉了受光提白、
释放前变暗、粗斑块的洞；左上角从独立翻板改为柔和隆起 + 从角尖向内的粒子化。静止层改为同一张
网格。所有决定见 decisions.md 当日各条。Android 仍未改。
