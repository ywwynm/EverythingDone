# Debug 更新 20260731215711

## 空间照片效果：界面配色跟随记事强调色，设置文案全面梳理

空间效果的全部界面元素改为跟随当前记事的强调色渐变：全屏查看器中激活状态的空间图标、强度调节条、生成进度圈，以及取消与模式切换按钮的触摸波纹，不再出现固定黄色或系统默认色；详情页图片附件角上的空间标识改为与实况、HDR 标识一致的偏白样式。双层/单层切换按钮的文字由「P0 单层 / P1 双层」改为更直观的「稳定 / 立体」。补全模型不可用时弹出的三项选择对话框，选项触摸波纹改为整行矩形，与应用其它多选项对话框一致。设置方面：主设置里空间照片效果入口不再带右侧图标；空间照片设置页中已安装组件的「删除」改为占位更小的圆形波纹图标按钮，「下载／取消／清除」等文字操作统一为胶囊形渐变波纹；文案更专业易懂——「本机空间计算」改为「推理运行环境」，分组改名「深度估计模型」「背景补全模型」，模型条目只保留名称与大小状态，不再展示协议与耗时等技术细节；AOT-GAN 工作分辨率选项只在选中 AOT-GAN 时显示。
