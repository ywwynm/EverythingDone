# 空间照片效果：立体模式视差幅度修复

## 修复立体模式倾斜幅度过小的问题

- 修复「立体」模式下倾斜手机视角变化幅度远小于「稳定」模式的问题：上一版把
  主体拼回连续曲面时，网格深度沿用了旧架构里只负责"残差"的压缩处理，场景
  内部的深度层次被压平，倾斜时只剩很小的位移。
- 现在立体模式使用完整深度行程：前景、主体、背景按各自远近反向分离运动，
  实测主体与背景的相对视差约为稳定模式的 4 倍，近景到远景的总跨度更大，
  空间纵深感明显增强。
- 纯渲染端修复，已生成的空间效果无需重建，更新后立即生效；两种模式的取景
  与倾斜手感（上一版的恒定取景、软饱和）保持不变。
