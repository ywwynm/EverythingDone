# 空间照片效果：内部清理与显露带修正

## 移除旧渲染残留，修正背景显露带宽度计算

- 「分割作为先验」改造收尾：删除旧架构遗留的残差深度压缩器、主体保护层等
  已无职责的内部路径，避免此前"旧机件残留导致视差塌缩"一类问题再次发生。
- 修正背景补全显露带的宽度计算：此前代表深度取自被压缩的深度场，会低估倾斜
  时的最大位移、把补全范围算窄，极端视角下可能露出未补全边缘；现按真实深度
  计算。
- 对已有效果无强制影响；重新生成后生效。观感与上一版一致，无新增功能。
