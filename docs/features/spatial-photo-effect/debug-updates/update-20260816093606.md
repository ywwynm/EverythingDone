# 空间照片华为设备渲染崩溃修复

## 修复华为平板打开空间照片即闪退

修复空间照片在部分华为设备（如 MatePad Pro 13.2，Maleoon GPU）上生成完成后与再次查看时的必现闪退。原因是 LDI 着色器中 `uViewpoint` 在顶点与片元两侧声明精度不一致（highp 与 mediump），华为 GPU 驱动按 GLSL 规范拒绝链接程序并报 L0001 错误；高通设备驱动宽容，故此前未暴露。现将片元侧该变量显式声明为 highp，与顶点侧对齐，不引入新的设备兼容性要求，渲染效果不变。此前生成并保存的空间照片数据完好，更新后可直接正常查看，无需重新生成。
