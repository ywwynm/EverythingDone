# 空间照片效果：人物形变与发缘伪影修复

## 重构立体渲染，修复脸型变化、白色发缘和锯齿

- 参考视点现在直接显示全分辨率原图；倾斜后的取景缩放改为严格等比，修复横向移动时
  人脸被单独拉宽、脸型和五官比例变化的问题。
- 「立体」模式改用连续区域共享网格、仅在真实遮挡边界分层的渲染结构。人物脸部、头发
  和身体不会再被拆成大量各自移动的小块；旧的实验性 MPI 选择会自动迁移到新版「立体」。
- 发丝 Alpha 边缘改为连续覆盖率，消除 matting 活性区域边缘的透明度硬跳；同时避免
  边界补片重复叠加，并在低透明度处从人物内部恢复前景颜色，明显减少黑发亮背景上的
  白色碎点和亮边。
- 增加只作用于透明边缘的四点亚像素采样，减弱斜向发丝的锯齿，不会对脸部或画面内部
  施加全屏模糊。
- 已在 OnePlus 真机上对两张单人/双人人像进行左右满强度端点和放大检查：人物形状保持
  稳定，白色碎屑基本消失，未发现黑边增厚或细发成片丢失。极细半透明发丝仍受原图与
  MODNet 分辨率限制。

已有空间效果如被提示不兼容，请重新生成一次；深度、补图、MODNet 和运行组件仍按需
下载，本次更新不会把模型重新打进 APK。

## 发布验证

- Debug 更新号：`202608011349`（versionCode 43，versionName 2.0.0）。
- APK：24,119,221 字节，SHA-256
  `3501a2f312cb036a0022f8a1931ac1cfaa8ae074fc263d38dac455f63642064f`。
- 已从阿里云回读 `latest.json` 与版本化 APK；元数据逐字节一致，APK 与本地发布产物
  SHA-256 一致。
