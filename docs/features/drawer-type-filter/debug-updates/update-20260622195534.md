# 状态分段胶囊三处修复

1. 修复刚打开 Drawer 时选中段（正在进行）icon 和文字挤在一起：宽度分配改在 onMeasure 中静态完成，首帧即正确展开，不再依赖布局期 requestLayout。
2. 增高到 46dp 并让未选中段为正方形，触摸 ripple 正好是圆形。
3. 轮廓与选中实心填充之间不再留缝：填充在垂直方向贴到描边内沿，水平在选中段位于两端时贴到左右描边内沿。
