# Phase E：记事列表 Widget 支持状态（含数据库迁移）

包含数据库迁移（版本 18→19，给 app_widget 表新增 status 列，仅新增、非破坏性）。

- 记事列表 Widget 配置界面新增“记事状态”行：正在进行 / 已完成（沿用显示模式那行的样式）。
- Widget 内容按所选状态显示（不含回收站）；文件夹卡片计数也按所选状态统计。
- 编辑已有 Widget 时回显其状态。

验证重点：① 配置一个“已完成”的列表 Widget，桌面应显示已完成内容；② 切回“正在进行”应显示进行中内容；③ 旧 Widget 升级后默认仍是正在进行、显示正常。
