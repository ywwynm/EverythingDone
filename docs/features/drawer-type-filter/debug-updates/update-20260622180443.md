# Drawer 筛选区改为上下布局

调整 Drawer 筛选区布局：
- 状态与类型改为上下两行。
- 上行：两个胶囊表示"正在进行 / 已完成"（单选）。
- 下行：恢复为 5 个圆形图标按钮（全部类型 / 记录 / 提醒 / 习惯 / 目标），去掉提示性文本。

选中适配：
- 状态胶囊和类型圆形按钮选中时都使用 accent→accent2 渐变（在某个文件夹下则为该文件夹的纯色/渐变）实心填充，图标/文字颜色按填充亮度自适应黑/白。
- 状态胶囊未选中为透明加轮廓；类型圆形按钮未选中无轮廓，仅显示自适应黑/白图标。
- 触摸均有 ripple。
