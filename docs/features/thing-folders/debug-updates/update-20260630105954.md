# 撤回 folder-drop 收尾重绑的滚动保护（验证是否为无意义改动）

## 主要改动

- **撤回 `ThingsActivity.rebindHomeListAfterFolderDropModeExit` 里的滚动 save/restore**，恢复为 HEAD 的
  简单 `notifyDataSetChanged`。这处改动此前在工作区里未提交，思路与已被否掉的"拖拽闪动→SGLM gap 修正"
  方案相同（重绑前后用 LayoutManager 锚点 `onSaveInstanceState`/`onRestoreInstanceState` 保住滚动位置），
  怀疑同属误判、无实际意义，故撤回后发版验证。

## 验证重点

- 把记事拖到另一条记事/文件夹上做"合并成文件夹 / 移入文件夹"，**收尾时列表是否会上下跳一下**：
  - 若不跳 → 这处改动确为无意义，可以放心丢弃；
  - 若会跳 → 这处改动有真实作用，需要另查 folder-drop 收尾跳动的真因。
- 本版仅动这一处；之前三处大文件夹缩略图修复（预览数量、侧图高度闪动、媒体背景置底）均保留。

## 备注

本次会话已提交的修复（commit `4e93303c`）不含本撤回；这处 folder-drop 改动原本就是会话前遗留在工作区的
未提交改动，撤回后工作区该文件与 HEAD 一致。
