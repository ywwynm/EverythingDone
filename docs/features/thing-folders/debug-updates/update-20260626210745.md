## 2026-06-26 - 拖拽命中阈值改为直接 dp 资源

本次 debug update 继续调整首页拖拽建夹/移入文件夹的 hover 触发阈值。

用户指出上一版用 `folder_drop_target_inset_card_gap_multiplier` 不够直观，询问为什么不直接使用 dp 数值。此前使用 multiplier 的考虑是延续“跟随首页卡片间距”的表达，但这个值本质上是交互手感参数，后续调试时直接使用 dp 更清楚。

本次调整：

- 移除 `folder_drop_target_inset_card_gap_multiplier`。
- 新增直接 dp 资源 `app/src/main/res/values/dimens.xml`：`folder_drop_target_inset`，当前为 `16dp`。
- 旧 `ThingsActivity` / `ItemTouchHelper` 路径和当前更常用的 `ThingListOverlayDragController` overlay 路径都读取 `folder_drop_target_inset`。
- 保留“按被拖拽卡片放大后的可见左上角判断”的修正。

验证状态：

- `git diff --check` 通过，仅有仓库既有 LF/CRLF 提示。
- `:app:assembleDebug --console=plain --no-configuration-cache` 已通过。
- 拖拽 hover 手感需要在真机上通过 debug update 验证。
