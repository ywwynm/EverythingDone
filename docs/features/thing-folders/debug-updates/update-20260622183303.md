# Phase C：文件夹"完成/恢复当前筛选内容"

文件夹动作菜单按当前状态新增内容态操作：
- 正在进行投影下：完成当前筛选下的内容（递归把子树内、命中当前类型筛选的正在进行记事标记为已完成；文件夹本身不标记完成）。
- 已完成投影下：恢复当前筛选下的内容为正在进行（递归把命中的已完成记事恢复为正在进行）。
- 回收站下不提供这两个操作。

两个操作都有确认对话框，文案显示会影响的记事数量。内容态操作只作用于当前类型筛选命中的记事，并保留各记事的文件夹归属。
