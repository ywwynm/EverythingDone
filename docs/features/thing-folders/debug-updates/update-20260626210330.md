## 2026-06-26 - 拖拽建夹/移入文件夹命中阈值改回 16dp

本次 debug update 用于对比首页拖拽建夹/移入文件夹的 hover 触发手感。

用户要求将上一版约 32dp 的目标卡片内缩阈值改回 16dp 试试，并询问这个数值在哪里调整。

本次调整：

- 保留上一版对两条拖拽路径的修正：旧 `ThingsActivity` / `ItemTouchHelper` 路径和当前更常用的 `ThingListOverlayDragController` overlay 路径都会按被拖拽卡片放大后的可见左上角判断。
- 将目标卡片内缩阈值从 2 倍首页相邻卡片可视间距改回 1 倍，当前等价于约 16dp。
- 新增统一调整入口：`app/src/main/res/values/dimens.xml` 中的 `folder_drop_target_inset`，当前为 `16dp`。两条拖拽路径都读取同一 dp 资源。

验证状态：

- `git diff --check` 通过，仅有仓库既有 LF/CRLF 提示。
- `:app:assembleDebug --console=plain --no-configuration-cache` 已通过。
- 拖拽 hover 手感需要在真机上通过 debug update 验证。
