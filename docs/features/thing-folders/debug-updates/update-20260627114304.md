## 还原旧拖拽提交动画的目标计算

本次是上一版“移动到文件夹 Dialog 飞入动画目标坐标修正”的边界修正：

- 用户提醒：旧拖拽提交动画有自己的拖拽落点语义，不应该套用 Dialog 移动的“飞入 F 可见区域中心”规则。
- 已还原 `ThingsActivity.captureFolderDropCommitVisual` 的旧目标矩形计算，拖拽提交路径继续按原有目标处理。
- “移动到文件夹” Dialog 触发的动画仍保留上一版修正：目标文件夹 F 不完整可见时，飞入终点取 F 在当前列表可见区域内的中心，并扣除 selecting mode contextual actionbar 的遮挡。
- 文档和上一条本地 debug update 说明已同步改成 Dialog 专属规则，避免把拖拽路径描述进去。

验证：`E:\projects\EverythingDone\gradlew.bat :app:assembleDebug` 已通过。未使用 adb。
