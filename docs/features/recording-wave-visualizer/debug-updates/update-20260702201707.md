# 录音 dialog 高度调整为宽度四比三

本次 debug update 调整记事详情页录音 dialog 的整体高度。

- 用户询问当前录音 dialog 高度，并要求把高度调高为宽度的 `4/3`。
- 当前布局宽度为 `280dp`，此前高度为 `320dp`；按 `280 × 4 / 3` 计算，新高度为约 `373.33dp`。
- 修改 `fragment_record_audio.xml`：将根布局 `android:layout_height` 和 `android:minHeight` 同步改为 `373.33dp`。由于 `BaseDialogFragment` 以 `null parent` 充气，`minHeight` 是保证实际固有高度的关键。
- 本次未改录音按钮、波浪响应、水位、录音采样或状态切换逻辑。

验证：`:app:assembleDebug` 已通过；已使用 `:app:publishDebugUpdate` 发布到阿里云 debug 通道，debug update code 为 `202607021217`。远端 APK 为 `http://120.25.194.207/everythingdone-updates/debug/apk/app-debug-202607021217.apk`，SHA-256 为 `474c2c85e667c1e4108446ac701748b8761318bde40ce67658f0b8e854422130`。
