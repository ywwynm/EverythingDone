# 录音波形可视化 debug 更新

## 本次修复

- 优化部分波峰过窄、过高、显得太尖锐的问题。
- 只限制正向波峰的额外尖化曲率，不降低整体浪幅、频段驱动或瞬态浪幅加成。
- 为 `crestFactor` 增加上限，并在高正峰处加入 taper 和圆角软限制，让极端峰顶更圆润。
- 保留上一版的随机错峰、灵动感和声音大小反馈。

## 验证

- `:app:assembleDebug` 已通过。
- `:app:publishDebugUpdate` 已发布，debug code 为 `202607020636`。
- 未使用 adb，未安装到物理设备或模拟器。
