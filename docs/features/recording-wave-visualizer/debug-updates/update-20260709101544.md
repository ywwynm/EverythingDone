# 清理已废弃录音波浪旁路

## 本次更新

- 按要求删除录音功能中已废弃的旧波浪旁路，不再保留对应的 recorder 接入口、receiver 列表、analyzer 分发和 silence 分发。
- 删除 `views/recording/` 下已废弃的旧旁路 Kotlin 类，录音对话框当前只接入 Opus 波浪可视化链路。
- 清理 `docs/features` 中对应的旧方案文档目录、发布记录和历史引用，使录音波浪与 Opus 文档反映当前状态。

## 验证

- 已执行 `:app:assembleDebug --console=plain --no-configuration-cache`，编译通过。
- 已检索 `docs/features` 与录音功能代码路径，确认不再残留旧旁路名称、类名或接入口。
- 本轮未使用 adb，未安装到物理设备或模拟器。
