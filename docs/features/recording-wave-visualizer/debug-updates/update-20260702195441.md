# D37 录音波浪水位范围和潮位速度微调

用户指定：水位从 `0.24` 到 `0.49`，上升 / 回落时间为 `0.32s / 0.64s`。

本次调整：
- `VoiceVisualizer.REST_FRAC` 从 `0.31` 调整为 `0.24`，让小声或安静时水位更低。
- `VoiceVisualizer.MAX_FRAC` 保持 `0.49`，大声时仍能达到 D36 的高位。
- `LEVEL_ATTACK_TAU` 从 `0.30` 调整为 `0.32`。
- `LEVEL_RELEASE_TAU` 从 `0.62` 调整为 `0.64`。

验证与发布：`:app:assembleDebug` 已通过；`:app:publishDebugUpdate` 已发布到阿里云 debug 渠道，code **202607021154**。未使用 adb，未安装到物理设备或模拟器，未提交 Git。
