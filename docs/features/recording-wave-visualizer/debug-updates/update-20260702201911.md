# 录音 dialog 高度改为 360dp

本次 debug update 继续微调记事详情页录音 dialog 的整体高度。

- 用户要求将上一版 `373.33dp` 的 dialog 高度改为 `360dp`。
- 修改 `fragment_record_audio.xml`：将根布局 `android:layout_height` 和 `android:minHeight` 同步改为 `360dp`。
- 本次未改录音按钮、波浪响应、水位、录音采样或状态切换逻辑。

验证：`:app:assembleDebug` 已通过；已使用 `:app:publishDebugUpdate` 发布到阿里云 debug 通道，debug update code 为 `202607021219`。远端 APK 为 `http://120.25.194.207/everythingdone-updates/debug/apk/app-debug-202607021219.apk`，SHA-256 为 `f14fc0cfcca867bd6e8c7901ecf0c1681583493d02560d4ebf4a538e618d2982`。
