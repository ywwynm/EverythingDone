# 录音确认 FAB 跟随记事颜色

本次 debug update 调整记事详情页录音 dialog 的确认/保存 FAB 外观。

- 用户反馈：录音完成后，保存录音的 FAB 不应继续使用固定绿色，而应变为当前记事的颜色；如果记事是渐变色，FAB 也应显示渐变；图标颜色需要根据记事颜色自适应。普通录音状态仍保持白色。
- 修改 `fragment_record_audio.xml`：在主 FAB 后增加同尺寸圆形色层，用于确认态显示纯色或渐变背景；FAB 本体仍保留点击、阴影、涟漪和图标能力。
- 修改 `AudioRecordDialogFragment.kt`：保存态不再硬编码 `#4CAF50`，改为读取当前 `ThingBackground`；确认态让 FAB 本体透明并显示背后的纯色/渐变色层；开始录音和重新录音时恢复白色 FAB；确认图标通过 `BackgroundUtil.onColor(...)` 根据纯色/渐变背景自动选择偏白或偏黑。
- 本次未改录音采样、波浪响应、水位或动画参数。

验证：`:app:assembleDebug` 已通过；已使用 `:app:publishDebugUpdate` 发布到阿里云 debug 通道，debug update code 为 `202607021207`。远端 APK 为 `http://120.25.194.207/everythingdone-updates/debug/apk/app-debug-202607021207.apk`，SHA-256 为 `cc71bfa3c167b1826930cfdcbb54222446e2ae8c4ec55be32b26750310168900`。
