# 待办 — 录音波形可视化改造

## 数值微调（等用户真机反馈）

当前可测版本 code **202607021154**（D37）：在 D36 基础上把水位范围调整为 `0.24..0.49`，上升 / 回落时间调整为 `0.32s / 0.64s`；保留采样数 216。

**companion 常量**（全局固定，直接改）：
- `LAYERS / COMPONENTS`：层数 / 每层波分量数。
- `INPUT_HISTORY_FRAMES / LAYER_SURGE_MAX_DELAY`：个性化错峰响应的历史帧长度与每层浪涌最大延迟。
- `mResponseDelayFrames / mResponseAttack / mResponseRelease`：每层 / 每分量的延迟、上升和回落惯性。
- `mLayerSurgeDelay / mLayerSurgeAmp / mLayerSurgeCrest / mLayerSurgeTau`：每层局部浪涌的延迟、强度、尖峰加成和衰减。
- `MIN_DB / MAX_DB`：音量→归一化区间（灵敏度）。
- `CURVE_GAMMA`：音量非线性幂曲线（>1 压低中小音量、突出大声；越大对比越极端）。
- `INTENSITY_* / AMP_INTENSITY_*`：声强对比曲线与浪高放大范围，用于拉开正常音量和大音量。
- `BAND_GAMMA / GLOBAL_LOUDNESS_MIX / BAND_MIX`：频段能量曲线，以及整体响度与频段驱动的混合比例。
- `AMP_JITTER / BODY_JITTER_SCALE / DETAIL_JITTER_SCALE / JITTER_QUIET_SCALE`：每个波分量的慢速随机包络深度，以及主体 / 细节 / 安静态缩放。
- `AMP_BOOST_*`：整层峰高放大的死区和 attack/release 平滑，用于吸收小幅 rhythm / surge envelope 变化。
- `LEVEL_* / REST_FRAC / MAX_FRAC`：水位随音量 / 声强变化的曲线、上升 / 回落速度和最低 / 最高水位。
- `TAU_SURGE / SURGE_AMP_BOOST / SURGE_CREST_BOOST`：瞬态浪涌的衰减、浪幅加成和波峰尖化加成。
- `PACE_* / FLOW_PACE_*`：事件密度 / tempo 对横向流速和波形变化速度的放大。
- `IDLE_AMP`：静音微动幅度。
- `REST_FRAC / MAX_FRAC`：静息 / 最高水位（二者接近＝水位近固定；跨度变大＝音量导致的水位高低更明显）。
- `MAX_AMP_DP`：波浪最大振幅。
- `TOP_LIMIT_DP / SOFT_DP`：顶部软限的上界与软化尺度（浪尖最高约 `TOP_LIMIT_DP+SOFT_DP*ln2`，防糊住计时）。
- `TROUGH_FACTOR / TROUGH_SOFT`：波谷不对称压浅（越小谷越浅；1=对称正弦）。
- `CREST_FACTOR / CREST_SOFT / CREST_*_TAU / CREST_*_DEADBAND`：波峰尖化强度、过渡尺度，以及 crest 形变的死区和平滑。
- `WANDER_DP`：基线微漂幅度。

**`AudioRecorder.VoiceAudioAnalyzer` 常量**（调音频响应）：
- `FFT_SIZE`：当前 2048；越大频率分辨率越高、响应越慢。
- `VISUAL_MIN_DB / VISUAL_MAX_DB`：RMS/分贝到 `loudness` 的归一化范围。
- `ADAPTIVE_*_DB`：自适应 loudness contrast 的环境底噪 / 近期峰值范围，用于拉开声强差异。
- `BAND_MIN_DB / BAND_MAX_DB`：FFT 频段能量到 0..1 的归一化范围。
- `BAND_ATTACK / BAND_RELEASE`：频段能量平滑的上升 / 回落速度。
- `BAND_RANGES`：`low`、`lowMid`、`mid`、`high`、`air` 的频段边界。

**构造函数 `init` 里的生成区间公式**（改这些区间可调"野度"，每实例随机生成）：
- `mAmpFactor / mLighten / mAlpha`：各层振幅尺度 / 颜色变亮量 / 透明度（线性、无随机）。
- `mK`：各分量空间频率区间（波峰疏密与参差）。
- `mTau`：各分量缓动时间常数（高频快 / 低频慢的错落）。
- `mDrift`：各分量漂移速度（横向流动快慢、正负）。
- `mPhi / mWanderPhase`：随机相位。
- `mCompWeight`：低频大浪 vs 高频细纹的占比分布。
- `mWanderSpeed`：基线微漂速度。
- `SIDE_CONTROL_SCRIM_ALPHA`（AudioRecordDialogFragment）：侧边图标衬底浓淡。

## 可能的后续

- 若用户确认效果，再考虑是否 git commit。
- 词汇表 **Voice Waveform** 若将来扩展到"已保存音频的播放波形"，需再区分范围。

### 歌曲节拍/节奏响应优化（已实现，待真机确认）

用户反馈：播放激情或节奏较快的歌曲时，当前动画和节拍、演唱速度不够贴合，观感偏慢。已按以下方向实现：

- 音频端补充更适合节奏的特征，例如 onset / beat energy / 高频瞬态密度，而不是只靠当前 `transient` 标量；
- 视觉端分离“水波主体层”和“节奏强调层”：主体继续保持 D21-D23 的随机灵动，节奏层负责更快的浪花、亮度、细纹或短促浪涌；
- 缩短或旁路部分 100ms 采样、2048 FFT 窗口、per-layer delay、attack/release 和 surge smoothing 带来的延迟，但只作用于节奏层，避免整体回到机械同步。

当前实现：`AudioRecorder` 约每 40ms 生成 `VoiceAudioFrame`，新增 `onset` / `beatPulse` / `beatPhase` / `tempoBpm` / `tempoConfidence` 等节奏字段；`VoiceVisualizer` 新增每层独立延迟和相位的 rhythm accent。后续若仍觉得“慢”，优先调 rhythm 层的 `RHYTHM_*` 常量和 onset/tempo 阈值，而不是直接加快主水波。

#### D24 反馈：细纹过多 + 高潮响应仍滞后

用户反馈：D24 容易出现整片水面都是细纹、像锯齿；同时音乐进入高潮后视觉没有及时反应。下一轮建议改为 D25：

- 从几何轮廓中移除或大幅压低高频 rhythm surface displacement，避免把节奏感画成密集锯齿；
- 节奏响应改成双通道：低延迟 onset 快通道负责立即触发较宽的局部浪涌，tempo/beat 慢通道只在置信度足够后负责相位和周期性增强；
- 新增 onset backfill / lead compensation：检测到 onset 后按事件时间反推一次短促 accent，视觉上补偿 40ms hop + 2048 FFT window + 平滑链路带来的滞后；
- 对 `highPulse` / `air` 做 novelty gate，不再直接驱动大面积细纹，只允许在强 onset 附近短时间参与；
- 主体水波继续保留 D21-D23 的随机、灵动和个性化错峰。

已按 D25 实现并发布 debug code `202607020742`：`AudioRecorder` 增加 512-sample fast RMS/rise，采样间隔降至约 25ms；`VoiceVisualizer` 移除高频 rhythm surface displacement，改为宽轮廓脉冲、细节预算和 rhythm event backfill。后续以真机歌曲反馈继续微调阈值。

### D20 回退与灵动感重做（已实现，待真机确认）

用户反馈：D20 版本野性不如上一版，整体太平稳，每个波浪都差不多；希望回到上一版，再重新优化，核心是保留随机、灵动、每个波浪像有自己的个性，并且不要所有波浪在同一时刻做出同样的上升、下降、变化。

新的调研结论：

- D20 的“主形/细纹分离 + 质量门控 + 空间平滑”解决了形状收束，但也压掉了 D19 的个性差异和不确定性。
- 更适合本需求的方向不是统一谱形，而是“群体运动里的错峰响应”：每层 / 每分量有独立 envelope follower、attack/release、延迟、overshoot 与噪声调制。
- 动画设计里的 stagger / offset / overlap 原理可以直接映射到波浪：同一个声音事件不应让所有层同时响应，而应按每层个性产生随机先后、惯性和余波。
- 有机随机不应是每帧跳变，也不应被过度质量门控；应使用连续 noise / LFO / envelope，让随机性在时间上连贯。

已按推荐方向实现：

- 回退 D20 的质量门控、严格谱式权重、主形/细纹强分离、空间平滑和 headroom 前置限幅，恢复更自由的多分量基底。
- 保留 D19 的小颤动修复：即时 `AMP_JITTER` 仍保持为连续慢速包络，`transient` 门限保留。
- 新增 `incoming → delayed target → current` 响应链；每层 / 每分量按自己的 delay / attack / release 追随音频目标。
- 不同层生成“个性参数”：响应延迟、attack/release 时间、局部浪涌强度、尖峰加成、衰减速度、漂移速度和 envelope 相位。
- 瞬态改为 layer-local surge，让某些层先起、某些层后涌，形成随机先后顺序。
- D19 的高频细纹和多分量干涉回归；只保留顶部 soft limit 和波谷压浅等局部保护，不做统一质量门控。

### 波形美感优化（已实现，待真机确认）

用户反馈：偶尔感觉波浪形状不够好看。进一步调研后，初步判断当前方案的问题不在“是否使用 Gerstner 式不对称”，而在多分量生成还偏自由随机：

- 真实/图形学水面通常围绕一个主峰频率和能量谱分布组织波，而不是让各分量频率、权重、尖峰和高频细纹独立随机。
- `mK` 当前跨度较宽，`mCompWeight` 递减较缓，多个高频分量叠加后有时会形成过密、破碎或不协调的局部轮廓。
- `CREST_FACTOR`、`MAX_AMP_DP`、顶部 soft limit 和多层 alpha 叠加会共同改变轮廓观感：浪尖过多或过尖时，水面容易显得“乱”而不是“自然”。

已按优先路线实现：

- 低频 3 个分量决定主轮廓，高频 / air 分量只按小幅 `DETAIL_AMP_SCALE` 叠加为表面细纹。
- 分量权重改为更严格的谱式递减，主形分量占绝大部分能量。
- 实例随机参数加入质量门控：过密、过碎、细纹占比过高或曲率过高会重抽。
- 波峰尖化加入 `CREST_MIN_INPUT / CREST_FULL_INPUT` 和 `STEEPNESS_CAP`，小碎峰不再都被尖化。
- 最终水面采样点做轻量空间平滑，并根据顶部 headroom 预先限制振幅。

### 小颤动问题（已修，待真机确认）

用户反馈：波浪已经上升后，随着音量和频段参数变化，浪体内部偶尔出现小颤动，观感奇怪。

初步判断不是帧循环卡顿，而是音频目标值与随机项共同造成的视觉抖动：

- `VoiceVisualizer.receive(VoiceAudioFrame)` 每 100ms 为每个波分量重新乘一次 `AMP_JITTER=0.7`，即同一声音能量下目标振幅仍可跳动 ±70%。
- `AudioRecorder.VoiceAudioAnalyzer` 的 FFT 窗口为 2048 样本（约 46ms），每 100ms 取最近一窗；高频 / air 频段和 `transient` 容易受短时噪声影响。
- `BAND_ATTACK=0.62`、高频分量 `mTau` 较短，使细纹对这些跳变反应偏快；波浪幅度已经较大时，细小目标变化会被 `MAX_AMP_DP=60` 放大。

已按优先路线修复：

- 随机抖动从“每次采样即时换目标”改成“每分量连续慢速随机包络”。
- `transient` 加 0.20 门限和 180ms 冷却，避免微小频谱 flux 反复触发浪涌。
- 高频 / air 频段权重略降，`BAND_MIX` 0.78→0.70。
- `AMP_JITTER` 0.70→0.32，把“野度”更多交给相位/频率干涉，而不是实时振幅随机。

### FFT 频段驱动（已实现，待真机调参）

`AudioRecorder` 已对 PCM 帧做轻量 FFT，并输出 5 个频段；`VoiceVisualizer` 已把低频映射到大浪、
中频映射到主体浪、高频映射到细纹。后续重点是真机判断频段归一化是否过敏或过钝。

- 若细纹过吵：降低 `air` 频段增益、提高 `BAND_GAMMA` 或降低 `BAND_MIX`。
- 若声音内容不明显：降低 `BAND_GAMMA`、提高 `BAND_MIX` 或调整 `BAND_MIN_DB/BAND_MAX_DB`。
- 若人声低频大浪太弱：提高 `low` / `lowMid` 在 `bandDrive` 中的权重。

### 物理感深化（已实现，待真机调参）

已在 `VoiceVisualizer` 中增加：

- 波峰尖化（`CREST_FACTOR / CREST_SOFT`）；
- 分量漂移速度与空间频率的近似频散关系；
- 谱式递减权重 + 少量随机；
- 瞬态浪涌时的短时额外浪幅和尖峰。

### 完整音频特征帧（已实现）

`VoiceAudioFrame` 已新增：

- `loudness`：RMS/分贝，继续控制整体浪幅和很小的水位变化；
- `low` / `lowMid` / `mid` / `high` / `air`：FFT 分频段能量，分别映射大浪、中浪、细纹与表层扰动。
- `transient`：由响度快速上升 + 频谱正向变化合成，触发短时额外浪涌。
- 兼容策略：`receive(Int)` 仍保留，会包装成只有 `loudness` 的全频段特征帧。
## 2026-07-02 — D26 待真机反馈

当前可测版本 code **202607020804**（D26：去除 rhythm backfill 的当前值跳变、beat phase 改为限速吸附、渐变方向跟随 `ThingBackground.orientation`、锁屏返回恢复动画循环、音频读取块缩小并把可视化采样降到约 20ms）。

如果用户仍觉得响应速度不够，可再评估“双速音频特征”：RMS/onset 以约 16ms 轻量更新，FFT/tempo 继续约 32ms 或 40ms 更新。这个方案比每 16ms 跑完整 2048 FFT 更稳，但需要进一步拆分 `VoiceAudioFrame` 的节奏快通道和频域慢通道。
## 2026-07-02 - D27 待真机反馈

当前可测版本 code **202607020823**（D27）：加入视觉输入死区以减轻果冻感，并重建 `AudioRecorder` 的重新开始链路。真机重点验证：
- 多次重复“开始 -> 结束 -> 重新开始”后，波峰波谷是否始终能继续响应麦克风输入；
- 普通人声连续音节下，已经成形的波峰是否还会小幅上下抖动；
- 死区是否过强导致轻声、短促音节或音乐节拍反馈变钝。
## 2026-07-02 - D28 待真机反馈

当前可测版本 code **202607020832**（D28）：停止 / 重新开始 / 关闭 dialog 的录音收束与文件 IO 都已移出 UI 点击路径。真机重点验证：
- 停止按钮点击后 UI 是否立即响应，不再卡住约一秒；
- 重新开始按钮点击后 UI 是否立即回到准备状态；
- 保存按钮是否会在 wav 文件写完前短暂不可点，并在后台完成后恢复；
- 多次重复“开始 -> 停止 -> 重新开始”后，D27 修复过的波峰波谷响应是否仍然稳定。

## 2026-07-02 - D29 待真机反馈

当前可测版本 code **202607020854**：加入 `activity` 有效活动度状态，安静时门控 rhythm/beat/onset/pulse，降低细节预算、漂移 boost 和 rhythm contour，并把静音微动改成低频慢漂移。真机重点验证：

- 相对安静、环境底噪或轻声停顿时，水面是否只保留低频轻微水感，不再频繁高频翻动；
- 人声突然开始、短促音节或音乐强拍到来时，是否仍能快速唤醒波浪和节奏层；
- 音乐高潮段是否仍能体现能量与节奏变化，而不是被安静门控压得过钝；
- 细节减少后，整体水感是否更自然，但不会变成过于平、过于慢的静态水面。

## 2026-07-02 - D30 待真机反馈

当前可测版本 code **202607020915**：高位波峰变化小于阈值时沿用上一帧 y 值；主相位、慢速随机包络、idle 波和基线微漂都随 `activity` 放慢，tempo confidence 足够时按 BPM 调整快慢。真机重点验证：

- 高位波峰是否不再出现细小抖动或“峰顶发抖”；
- 强音节或明显节拍到来时，波峰仍能越过阈值及时变化，不会显得被冻住；
- 安静场景下横向流动是否足够慢、足够稳；
- 快节奏音乐和慢节奏音乐下，横向速度是否能体现该快则快、该慢则慢。

### D30 反馈：峰顶出现锯齿

用户反馈：D30 后波峰处能看到锯齿。初步判断这是 D30 的峰顶稳定器回归：当前实现按固定采样点逐点冻结 y 值，某些相邻采样点被沿用上一帧、某些采样点更新到当前帧，会在峰顶形成空间不连续。下一版应替换逐点硬冻结方案，改为更连续的峰顶稳定方式，例如对峰顶位移做连续低通/滞回、对相邻采样点做一致化，或只削弱峰顶细节分量而不直接冻结最终 y。

已按 D31 修复：删除逐点冻结最终 y 的方案，改为在波峰区连续衰减 `detailS`，并把水面采样数提升到 180。待发布后真机确认。

## 2026-07-02 - D31 待真机反馈

当前可测版本 code **202607020921**：不再冻结最终 y；高位波峰通过 smooth step 连续衰减细节分量，横向流速的活动度控制保留。真机重点验证：

- D30 引入的峰顶锯齿是否消失；
- 高位波峰的小变化是否仍比 D29 更少；
- 强音节或音乐节拍下，峰顶是否仍能自然变化，不会僵住；
- 采样点提升到 180 后是否有可接受的性能表现。

## 2026-07-02 - D32 待真机反馈

当前可测版本 code **202607020942**：安静态 `activity` 门槛收紧；低活动浪高、细节、idle 和基线微漂底线降低；横向流速改为低活动 smooth step 门控，安静底速明显降低。真机重点验证：

- 工位安静场景下，水面是否比 D31 更平；
- 没有快速声音时，横向流动是否不再显得快；
- 说话、敲击或播放音乐时，波浪是否仍能及时变高并恢复合适速度；
- 过渡是否自然，没有从平静到活跃的突兀跳变。

## 2026-07-02 - D33 待真机反馈

当前可测版本 code **202607020958**：activity/onset/fastImpact 改为 low/mid/body flux 主导，high/air flux 降权；视觉侧低活动 body/detail 映射改为 smooth step 门控，横向底速和 rhythm phase 进一步降低。真机重点验证：

- 中央空调持续声下，水面是否明显更平、更慢；
- 键盘声是否只产生轻微、短促反应，不会让水面持续活跃；
- 人声或音乐出现时，是否仍能及时唤醒主体浪和合适流速；
- 高频音乐细节是否仍有表现，但不会把细纹画成持续噪声。

## 2026-07-02 - D34 待真机反馈

当前可测版本 code **202607021009**：峰高放大、crest 形变和主体随机包络都加入连续死区/平滑，目标是减少波峰高度的抖动式上下变化，同时保留强声音事件的响应。真机重点验证：

- 已经升起的波峰是否不再被小幅 envelope 变化推着上下抖动；
- 安静工位状态下，主体峰高是否比 D33 更稳定；
- 人声、敲击或音乐强拍出现时，峰高变化是否仍然及时，不会显得迟钝；
- 峰顶是否保持圆润连续，没有回到 D30 的锯齿感。

## 2026-07-02 - D35 待真机反馈

当前可测版本 code **202607021024**：`activity`、`intensity`、`pace` 已拆分，声强决定浪高，事件密度 / tempo 决定横向流速和波形变化速度。真机重点验证：

- 正常音量说话的波峰是否明显低于大声说话；
- 普通音量音乐是否明显低于大声音乐；
- 正常语速说话的横向流动和波形变化是否明显慢于快速说话；
- 古典等慢节奏音乐是否明显慢于嘻哈等快节奏音乐；
- 安静工位和空调风噪下是否仍保持 D33/D34 的平缓稳定。

## 2026-07-02 - D36 待真机反馈

当前可测版本 code **202607021137**：水位重新随音量 / 声强适度涨落，范围为 `REST_FRAC=0.31` 到 `MAX_FRAC=0.49`，并使用慢速上升 / 回落。真机重点验证：

- 安静或小声时水位是否明显处于低位；
- 大声说话或大声音乐时水位是否明显升高；
- 水位变化是否是柔和潮位感，而不是跟每个音节机械跳动；
- 水位升高后是否仍不糊住计时文本，且不破坏 D34 的峰高稳定。

## 2026-07-02 - D37 待真机反馈

当前可测版本 code **202607021154**：水位范围为 `REST_FRAC=0.24` 到 `MAX_FRAC=0.49`，上升 / 回落时间为 `0.32s / 0.64s`。真机重点验证：

- 小声或安静时水位是否足够低；
- 大声时水位是否仍能到达合适高位；
- 低位到高位的跨度是否明显但不突兀；
- 回落是否足够柔和。
