# 修复渐变触摸波纹起点

- 修复 `GradientRippleDrawable` 在 pressed 状态早于 hotspot 到达时，波纹从控件中心而不是触摸点扩散的问题。
- 影响所有使用渐变 ripple 的入口，包括记事详情调整颜色面板、首页长按后的调整颜色面板、计时数字风格 dialog，以及其它按钮、列表行和选择项。
- 避免快速连续点击时复用上一轮触点；移除计时数字风格 dialog 的局部触点转发，统一走 drawable 内部逻辑。
