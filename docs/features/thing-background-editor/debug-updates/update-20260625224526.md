# 从世界取色相机图标换更清晰的轮廓

- 上一版「相机 + 中间滴管」的相机轮廓看不出是相机。改用 Material `camera_alt` 那种标准相机机身轮廓
  （带顶部居中梯形凸起），中间仍放缩放居中的滴管（Material colorize），整体由当前颜色 tint。

验证状态：

- `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug` 已通过，结果为 `BUILD SUCCESSFUL`。
