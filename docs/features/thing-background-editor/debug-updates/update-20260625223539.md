# 从世界取色图标改为「相机 + 中间滴管」+ 滚动分割线铺满全宽

- **从世界取色图标**：改为「相机 + 中间一个滴管」——相机外框代表相机/眼前取景，中间的滴管代表取色
  （滴管复用 Material colorize 形状，缩放居中）。整体由当前颜色 tint。
- **滚动分割线铺满全宽**：标题下/操作上的滚动提示分割线之前左右有边距，现在改为铺满整个面板/对话框宽度
  （通过负横向边距抵消容器内边距 + clipToPadding=false），与许可证/语言选择 dialog 一致。详情对话框与
  首页颜色页都已修正。

验证状态：

- `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug` 已通过，结果为 `BUILD SUCCESSFUL`。
