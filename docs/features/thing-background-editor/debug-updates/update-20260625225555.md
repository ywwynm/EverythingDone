# 从世界取色图标改为「照片 + 取色」+ 详情页面板不再压暗背后

- **从世界取色图标**：改为「照片(相框 + 太阳 + 山峰) + 滴管取色」——照片是通用易识别的图像图标，
  右上滴管表示从照片/画面取色（复用 Material colorize）。整体由当前颜色 tint。
- **详情页颜色面板不再压暗背后**：之前面板打开会让上方变暗，导致记事颜色看起来偏暗、不准确；
  现在清除了对话框的 dim，背后记事颜色显示准确。

验证状态：

- `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug` 已通过，结果为 `BUILD SUCCESSFUL`。
