# 从世界取色图标退回相机版

- 图标经过多轮（地球/取景框/眼睛/照片等）后，按用户要求退回到相机版：
  Material `camera_alt` 标准相机机身轮廓 + 中间滴管（Material colorize），整体由当前颜色 tint。

验证状态：

- `E:\projects\EverythingDone\gradlew.bat :app:assembleDebug` 已通过，结果为 `BUILD SUCCESSFUL`。
